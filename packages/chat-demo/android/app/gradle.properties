android.useAndroidX=true

# This app is always New Arch, always Hermes, and always edge to edge — the last
# is the reason it exists, since a window that fits system windows cannot show
# whether the safe-area reservation does anything.
newArchEnabled=true
hermesEnabled=true
edgeToEdgeEnabled=true
