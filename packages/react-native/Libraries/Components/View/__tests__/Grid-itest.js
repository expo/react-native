/**
 * Copyright (c) Meta Platforms, Inc. and affiliates.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 *
 * @flow strict-local
 * @format
 * @generated by grid-lanes-conformance/gen-fantom-test.js
 */

/**
 * CSS Grid layout, checked end to end through React Native.
 *
 * Every expected number here was measured in real Safari, which implements
 * CSS Grid natively — see grid-lanes-conformance/oracle.js. The only
 * exception is the stacking-axis position in a handful of grid-lanes cases,
 * where Safari does not implement css-grid-3 §6.4 and mis-distributes §6.3;
 * those are derived from the spec in stacking-alignment-test.cpp and marked
 * in cases.js with the reason.
 *
 * The C++ harness (grid-lanes-conformance/replay.cpp) proves the same cases
 * against Yoga directly. This suite proves the rest of the path: the style
 * prop, the track-list parser, the props wiring, and layout as the app sees
 * it through getBoundingClientRect().
 *
 * 341 cases; 7 corpus cases are not expressible as RN styles
 * (min-content as a maximum, inline-level containers, order, and the
 * percentage flow-tolerance Safari cannot adjudicate).
 */

import '@react-native/fantom/src/setUpDefaultReactNativeEnvironment';

import type {HostInstance} from 'react-native';

import ensureInstance from '../../../../src/private/__tests__/utilities/ensureInstance';
import * as Fantom from '@react-native/fantom';
import * as React from 'react';
import {createRef} from 'react';
import {View} from 'react-native';
import ReactNativeElement from 'react-native/src/private/webapis/dom/nodes/ReactNativeElement';

// Sub-pixel rounding differs between engines; everything else must match.
const TOLERANCE = 0.75;

// The browser measured every case inside a 900px-wide wrapper.
const VIEWPORT_WIDTH = 900;

function rectOf(ref: {current: HostInstance | null}) {
  return ensureInstance(ref.current, ReactNativeElement).getBoundingClientRect();
}

function expectClose(actual: number, expected: number, what: string) {
  if (Math.abs(actual - expected) > TOLERANCE) {
    throw new Error(
      `${what}: got ${actual}, Safari measured ${expected}`,
    );
  }
}

describe("tracks-px", () => {
  it("tracks-px-0001: three identical px tracks", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid","width":600,"gridTemplateColumns":"120px 120px 120px"}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":40,"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":40,"height":30}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":40,"height":25}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'tracks-px-0001 container.width');
    expectClose(container.height, 30, 'tracks-px-0001 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'tracks-px-0001 item[0].x');
      expectClose(r.y - container.y, 0, 'tracks-px-0001 item[0].y');
      expectClose(r.width, 40, 'tracks-px-0001 item[0].w');
      expectClose(r.height, 20, 'tracks-px-0001 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 120, 'tracks-px-0001 item[1].x');
      expectClose(r.y - container.y, 0, 'tracks-px-0001 item[1].y');
      expectClose(r.width, 40, 'tracks-px-0001 item[1].w');
      expectClose(r.height, 30, 'tracks-px-0001 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 240, 'tracks-px-0001 item[2].x');
      expectClose(r.y - container.y, 0, 'tracks-px-0001 item[2].y');
      expectClose(r.width, 40, 'tracks-px-0001 item[2].w');
      expectClose(r.height, 25, 'tracks-px-0001 item[2].h');
    }
  });
});

describe("tracks-pct", () => {
  it("tracks-pct-0002: three identical pct tracks", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid","width":600,"gridTemplateColumns":"25% 25% 25%"}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":40,"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":40,"height":30}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":40,"height":25}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'tracks-pct-0002 container.width');
    expectClose(container.height, 30, 'tracks-pct-0002 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'tracks-pct-0002 item[0].x');
      expectClose(r.y - container.y, 0, 'tracks-pct-0002 item[0].y');
      expectClose(r.width, 40, 'tracks-pct-0002 item[0].w');
      expectClose(r.height, 20, 'tracks-pct-0002 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 150, 'tracks-pct-0002 item[1].x');
      expectClose(r.y - container.y, 0, 'tracks-pct-0002 item[1].y');
      expectClose(r.width, 40, 'tracks-pct-0002 item[1].w');
      expectClose(r.height, 30, 'tracks-pct-0002 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 300, 'tracks-pct-0002 item[2].x');
      expectClose(r.y - container.y, 0, 'tracks-pct-0002 item[2].y');
      expectClose(r.width, 40, 'tracks-pct-0002 item[2].w');
      expectClose(r.height, 25, 'tracks-pct-0002 item[2].h');
    }
  });
});

describe("tracks-fr", () => {
  it("tracks-fr-0003: three identical fr tracks", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid","width":600,"gridTemplateColumns":"1fr 1fr 1fr"}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":40,"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":40,"height":30}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":40,"height":25}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'tracks-fr-0003 container.width');
    expectClose(container.height, 30, 'tracks-fr-0003 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'tracks-fr-0003 item[0].x');
      expectClose(r.y - container.y, 0, 'tracks-fr-0003 item[0].y');
      expectClose(r.width, 40, 'tracks-fr-0003 item[0].w');
      expectClose(r.height, 20, 'tracks-fr-0003 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 200, 'tracks-fr-0003 item[1].x');
      expectClose(r.y - container.y, 0, 'tracks-fr-0003 item[1].y');
      expectClose(r.width, 40, 'tracks-fr-0003 item[1].w');
      expectClose(r.height, 30, 'tracks-fr-0003 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 400, 'tracks-fr-0003 item[2].x');
      expectClose(r.y - container.y, 0, 'tracks-fr-0003 item[2].y');
      expectClose(r.width, 40, 'tracks-fr-0003 item[2].w');
      expectClose(r.height, 25, 'tracks-fr-0003 item[2].h');
    }
  });
});

describe("tracks-auto", () => {
  it("tracks-auto-0004: three identical auto tracks", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid","width":600,"gridTemplateColumns":"auto auto auto"}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":40,"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":40,"height":30}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":40,"height":25}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'tracks-auto-0004 container.width');
    expectClose(container.height, 30, 'tracks-auto-0004 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'tracks-auto-0004 item[0].x');
      expectClose(r.y - container.y, 0, 'tracks-auto-0004 item[0].y');
      expectClose(r.width, 40, 'tracks-auto-0004 item[0].w');
      expectClose(r.height, 20, 'tracks-auto-0004 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 200, 'tracks-auto-0004 item[1].x');
      expectClose(r.y - container.y, 0, 'tracks-auto-0004 item[1].y');
      expectClose(r.width, 40, 'tracks-auto-0004 item[1].w');
      expectClose(r.height, 30, 'tracks-auto-0004 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 400, 'tracks-auto-0004 item[2].x');
      expectClose(r.y - container.y, 0, 'tracks-auto-0004 item[2].y');
      expectClose(r.width, 40, 'tracks-auto-0004 item[2].w');
      expectClose(r.height, 25, 'tracks-auto-0004 item[2].h');
    }
  });
});

describe("tracks-minmax-px-fr", () => {
  it("tracks-minmax-px-fr-0005: three identical minmax-px-fr tracks", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid","width":600,"gridTemplateColumns":"minmax(80px, 1fr) minmax(80px, 1fr) minmax(80px, 1fr)"}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":40,"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":40,"height":30}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":40,"height":25}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'tracks-minmax-px-fr-0005 container.width');
    expectClose(container.height, 30, 'tracks-minmax-px-fr-0005 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'tracks-minmax-px-fr-0005 item[0].x');
      expectClose(r.y - container.y, 0, 'tracks-minmax-px-fr-0005 item[0].y');
      expectClose(r.width, 40, 'tracks-minmax-px-fr-0005 item[0].w');
      expectClose(r.height, 20, 'tracks-minmax-px-fr-0005 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 200, 'tracks-minmax-px-fr-0005 item[1].x');
      expectClose(r.y - container.y, 0, 'tracks-minmax-px-fr-0005 item[1].y');
      expectClose(r.width, 40, 'tracks-minmax-px-fr-0005 item[1].w');
      expectClose(r.height, 30, 'tracks-minmax-px-fr-0005 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 400, 'tracks-minmax-px-fr-0005 item[2].x');
      expectClose(r.y - container.y, 0, 'tracks-minmax-px-fr-0005 item[2].y');
      expectClose(r.width, 40, 'tracks-minmax-px-fr-0005 item[2].w');
      expectClose(r.height, 25, 'tracks-minmax-px-fr-0005 item[2].h');
    }
  });
});

describe("tracks-minmax-px-px", () => {
  it("tracks-minmax-px-px-0006: three identical minmax-px-px tracks", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid","width":600,"gridTemplateColumns":"minmax(80px, 150px) minmax(80px, 150px) minmax(80px, 150px)"}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":40,"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":40,"height":30}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":40,"height":25}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'tracks-minmax-px-px-0006 container.width');
    expectClose(container.height, 30, 'tracks-minmax-px-px-0006 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'tracks-minmax-px-px-0006 item[0].x');
      expectClose(r.y - container.y, 0, 'tracks-minmax-px-px-0006 item[0].y');
      expectClose(r.width, 40, 'tracks-minmax-px-px-0006 item[0].w');
      expectClose(r.height, 20, 'tracks-minmax-px-px-0006 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 150, 'tracks-minmax-px-px-0006 item[1].x');
      expectClose(r.y - container.y, 0, 'tracks-minmax-px-px-0006 item[1].y');
      expectClose(r.width, 40, 'tracks-minmax-px-px-0006 item[1].w');
      expectClose(r.height, 30, 'tracks-minmax-px-px-0006 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 300, 'tracks-minmax-px-px-0006 item[2].x');
      expectClose(r.y - container.y, 0, 'tracks-minmax-px-px-0006 item[2].y');
      expectClose(r.width, 40, 'tracks-minmax-px-px-0006 item[2].w');
      expectClose(r.height, 25, 'tracks-minmax-px-px-0006 item[2].h');
    }
  });
});

describe("tracks-minmax-auto-fr", () => {
  it("tracks-minmax-auto-fr-0007: three identical minmax-auto-fr tracks", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid","width":600,"gridTemplateColumns":"minmax(auto, 1fr) minmax(auto, 1fr) minmax(auto, 1fr)"}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":40,"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":40,"height":30}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":40,"height":25}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'tracks-minmax-auto-fr-0007 container.width');
    expectClose(container.height, 30, 'tracks-minmax-auto-fr-0007 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'tracks-minmax-auto-fr-0007 item[0].x');
      expectClose(r.y - container.y, 0, 'tracks-minmax-auto-fr-0007 item[0].y');
      expectClose(r.width, 40, 'tracks-minmax-auto-fr-0007 item[0].w');
      expectClose(r.height, 20, 'tracks-minmax-auto-fr-0007 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 200, 'tracks-minmax-auto-fr-0007 item[1].x');
      expectClose(r.y - container.y, 0, 'tracks-minmax-auto-fr-0007 item[1].y');
      expectClose(r.width, 40, 'tracks-minmax-auto-fr-0007 item[1].w');
      expectClose(r.height, 30, 'tracks-minmax-auto-fr-0007 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 400, 'tracks-minmax-auto-fr-0007 item[2].x');
      expectClose(r.y - container.y, 0, 'tracks-minmax-auto-fr-0007 item[2].y');
      expectClose(r.width, 40, 'tracks-minmax-auto-fr-0007 item[2].w');
      expectClose(r.height, 25, 'tracks-minmax-auto-fr-0007 item[2].h');
    }
  });
});

describe("tracks-minmax-pct-fr", () => {
  it("tracks-minmax-pct-fr-0008: three identical minmax-pct-fr tracks", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid","width":600,"gridTemplateColumns":"minmax(10%, 1fr) minmax(10%, 1fr) minmax(10%, 1fr)"}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":40,"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":40,"height":30}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":40,"height":25}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'tracks-minmax-pct-fr-0008 container.width');
    expectClose(container.height, 30, 'tracks-minmax-pct-fr-0008 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'tracks-minmax-pct-fr-0008 item[0].x');
      expectClose(r.y - container.y, 0, 'tracks-minmax-pct-fr-0008 item[0].y');
      expectClose(r.width, 40, 'tracks-minmax-pct-fr-0008 item[0].w');
      expectClose(r.height, 20, 'tracks-minmax-pct-fr-0008 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 200, 'tracks-minmax-pct-fr-0008 item[1].x');
      expectClose(r.y - container.y, 0, 'tracks-minmax-pct-fr-0008 item[1].y');
      expectClose(r.width, 40, 'tracks-minmax-pct-fr-0008 item[1].w');
      expectClose(r.height, 30, 'tracks-minmax-pct-fr-0008 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 400, 'tracks-minmax-pct-fr-0008 item[2].x');
      expectClose(r.y - container.y, 0, 'tracks-minmax-pct-fr-0008 item[2].y');
      expectClose(r.width, 40, 'tracks-minmax-pct-fr-0008 item[2].w');
      expectClose(r.height, 25, 'tracks-minmax-pct-fr-0008 item[2].h');
    }
  });
});

describe("fr-distribution", () => {
  it("fr-distribution-0009: fr free-space distribution", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid","width":600,"gridTemplateColumns":"1fr 1fr 1fr"}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":30,"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":30,"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":30,"height":20}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'fr-distribution-0009 container.width');
    expectClose(container.height, 20, 'fr-distribution-0009 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'fr-distribution-0009 item[0].x');
      expectClose(r.y - container.y, 0, 'fr-distribution-0009 item[0].y');
      expectClose(r.width, 30, 'fr-distribution-0009 item[0].w');
      expectClose(r.height, 20, 'fr-distribution-0009 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 200, 'fr-distribution-0009 item[1].x');
      expectClose(r.y - container.y, 0, 'fr-distribution-0009 item[1].y');
      expectClose(r.width, 30, 'fr-distribution-0009 item[1].w');
      expectClose(r.height, 20, 'fr-distribution-0009 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 400, 'fr-distribution-0009 item[2].x');
      expectClose(r.y - container.y, 0, 'fr-distribution-0009 item[2].y');
      expectClose(r.width, 30, 'fr-distribution-0009 item[2].w');
      expectClose(r.height, 20, 'fr-distribution-0009 item[2].h');
    }
  });
  it("fr-distribution-0010: fr free-space distribution", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid","width":600,"gridTemplateColumns":"1fr 2fr 3fr"}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":30,"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":30,"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":30,"height":20}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'fr-distribution-0010 container.width');
    expectClose(container.height, 20, 'fr-distribution-0010 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'fr-distribution-0010 item[0].x');
      expectClose(r.y - container.y, 0, 'fr-distribution-0010 item[0].y');
      expectClose(r.width, 30, 'fr-distribution-0010 item[0].w');
      expectClose(r.height, 20, 'fr-distribution-0010 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 100, 'fr-distribution-0010 item[1].x');
      expectClose(r.y - container.y, 0, 'fr-distribution-0010 item[1].y');
      expectClose(r.width, 30, 'fr-distribution-0010 item[1].w');
      expectClose(r.height, 20, 'fr-distribution-0010 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 300, 'fr-distribution-0010 item[2].x');
      expectClose(r.y - container.y, 0, 'fr-distribution-0010 item[2].y');
      expectClose(r.width, 30, 'fr-distribution-0010 item[2].w');
      expectClose(r.height, 20, 'fr-distribution-0010 item[2].h');
    }
  });
  it("fr-distribution-0011: fr free-space distribution", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid","width":600,"gridTemplateColumns":"100px 1fr 2fr"}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":30,"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":30,"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":30,"height":20}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'fr-distribution-0011 container.width');
    expectClose(container.height, 20, 'fr-distribution-0011 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'fr-distribution-0011 item[0].x');
      expectClose(r.y - container.y, 0, 'fr-distribution-0011 item[0].y');
      expectClose(r.width, 30, 'fr-distribution-0011 item[0].w');
      expectClose(r.height, 20, 'fr-distribution-0011 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 100, 'fr-distribution-0011 item[1].x');
      expectClose(r.y - container.y, 0, 'fr-distribution-0011 item[1].y');
      expectClose(r.width, 30, 'fr-distribution-0011 item[1].w');
      expectClose(r.height, 20, 'fr-distribution-0011 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 266.66, 'fr-distribution-0011 item[2].x');
      expectClose(r.y - container.y, 0, 'fr-distribution-0011 item[2].y');
      expectClose(r.width, 30, 'fr-distribution-0011 item[2].w');
      expectClose(r.height, 20, 'fr-distribution-0011 item[2].h');
    }
  });
  it("fr-distribution-0012: fr free-space distribution", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid","width":600,"gridTemplateColumns":"0.5fr 0.5fr"}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":30,"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":30,"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":30,"height":20}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'fr-distribution-0012 container.width');
    expectClose(container.height, 40, 'fr-distribution-0012 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'fr-distribution-0012 item[0].x');
      expectClose(r.y - container.y, 0, 'fr-distribution-0012 item[0].y');
      expectClose(r.width, 30, 'fr-distribution-0012 item[0].w');
      expectClose(r.height, 20, 'fr-distribution-0012 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 300, 'fr-distribution-0012 item[1].x');
      expectClose(r.y - container.y, 0, 'fr-distribution-0012 item[1].y');
      expectClose(r.width, 30, 'fr-distribution-0012 item[1].w');
      expectClose(r.height, 20, 'fr-distribution-0012 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 0, 'fr-distribution-0012 item[2].x');
      expectClose(r.y - container.y, 20, 'fr-distribution-0012 item[2].y');
      expectClose(r.width, 30, 'fr-distribution-0012 item[2].w');
      expectClose(r.height, 20, 'fr-distribution-0012 item[2].h');
    }
  });
  it("fr-distribution-0013: fr free-space distribution", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid","width":600,"gridTemplateColumns":"0.25fr 0.25fr 0.25fr"}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":30,"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":30,"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":30,"height":20}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'fr-distribution-0013 container.width');
    expectClose(container.height, 20, 'fr-distribution-0013 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'fr-distribution-0013 item[0].x');
      expectClose(r.y - container.y, 0, 'fr-distribution-0013 item[0].y');
      expectClose(r.width, 30, 'fr-distribution-0013 item[0].w');
      expectClose(r.height, 20, 'fr-distribution-0013 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 150, 'fr-distribution-0013 item[1].x');
      expectClose(r.y - container.y, 0, 'fr-distribution-0013 item[1].y');
      expectClose(r.width, 30, 'fr-distribution-0013 item[1].w');
      expectClose(r.height, 20, 'fr-distribution-0013 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 300, 'fr-distribution-0013 item[2].x');
      expectClose(r.y - container.y, 0, 'fr-distribution-0013 item[2].y');
      expectClose(r.width, 30, 'fr-distribution-0013 item[2].w');
      expectClose(r.height, 20, 'fr-distribution-0013 item[2].h');
    }
  });
  it("fr-distribution-0014: fr free-space distribution", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid","width":600,"gridTemplateColumns":"3fr 200px 1fr"}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":30,"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":30,"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":30,"height":20}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'fr-distribution-0014 container.width');
    expectClose(container.height, 20, 'fr-distribution-0014 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'fr-distribution-0014 item[0].x');
      expectClose(r.y - container.y, 0, 'fr-distribution-0014 item[0].y');
      expectClose(r.width, 30, 'fr-distribution-0014 item[0].w');
      expectClose(r.height, 20, 'fr-distribution-0014 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 300, 'fr-distribution-0014 item[1].x');
      expectClose(r.y - container.y, 0, 'fr-distribution-0014 item[1].y');
      expectClose(r.width, 30, 'fr-distribution-0014 item[1].w');
      expectClose(r.height, 20, 'fr-distribution-0014 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 500, 'fr-distribution-0014 item[2].x');
      expectClose(r.y - container.y, 0, 'fr-distribution-0014 item[2].y');
      expectClose(r.width, 30, 'fr-distribution-0014 item[2].w');
      expectClose(r.height, 20, 'fr-distribution-0014 item[2].h');
    }
  });
  it("fr-distribution-0015: fr free-space distribution", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid","width":600,"gridTemplateColumns":"20% 1fr"}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":30,"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":30,"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":30,"height":20}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'fr-distribution-0015 container.width');
    expectClose(container.height, 40, 'fr-distribution-0015 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'fr-distribution-0015 item[0].x');
      expectClose(r.y - container.y, 0, 'fr-distribution-0015 item[0].y');
      expectClose(r.width, 30, 'fr-distribution-0015 item[0].w');
      expectClose(r.height, 20, 'fr-distribution-0015 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 120, 'fr-distribution-0015 item[1].x');
      expectClose(r.y - container.y, 0, 'fr-distribution-0015 item[1].y');
      expectClose(r.width, 30, 'fr-distribution-0015 item[1].w');
      expectClose(r.height, 20, 'fr-distribution-0015 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 0, 'fr-distribution-0015 item[2].x');
      expectClose(r.y - container.y, 20, 'fr-distribution-0015 item[2].y');
      expectClose(r.width, 30, 'fr-distribution-0015 item[2].w');
      expectClose(r.height, 20, 'fr-distribution-0015 item[2].h');
    }
  });
  it("fr-distribution-0016: fr free-space distribution", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid","width":600,"gridTemplateColumns":"minmax(150px, 1fr) 1fr"}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":30,"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":30,"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":30,"height":20}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'fr-distribution-0016 container.width');
    expectClose(container.height, 40, 'fr-distribution-0016 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'fr-distribution-0016 item[0].x');
      expectClose(r.y - container.y, 0, 'fr-distribution-0016 item[0].y');
      expectClose(r.width, 30, 'fr-distribution-0016 item[0].w');
      expectClose(r.height, 20, 'fr-distribution-0016 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 300, 'fr-distribution-0016 item[1].x');
      expectClose(r.y - container.y, 0, 'fr-distribution-0016 item[1].y');
      expectClose(r.width, 30, 'fr-distribution-0016 item[1].w');
      expectClose(r.height, 20, 'fr-distribution-0016 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 0, 'fr-distribution-0016 item[2].x');
      expectClose(r.y - container.y, 20, 'fr-distribution-0016 item[2].y');
      expectClose(r.width, 30, 'fr-distribution-0016 item[2].w');
      expectClose(r.height, 20, 'fr-distribution-0016 item[2].h');
    }
  });
  it("fr-distribution-0017: fr free-space distribution", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid","width":600,"gridTemplateColumns":"minmax(400px, 1fr) 1fr"}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":30,"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":30,"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":30,"height":20}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'fr-distribution-0017 container.width');
    expectClose(container.height, 40, 'fr-distribution-0017 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'fr-distribution-0017 item[0].x');
      expectClose(r.y - container.y, 0, 'fr-distribution-0017 item[0].y');
      expectClose(r.width, 30, 'fr-distribution-0017 item[0].w');
      expectClose(r.height, 20, 'fr-distribution-0017 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 400, 'fr-distribution-0017 item[1].x');
      expectClose(r.y - container.y, 0, 'fr-distribution-0017 item[1].y');
      expectClose(r.width, 30, 'fr-distribution-0017 item[1].w');
      expectClose(r.height, 20, 'fr-distribution-0017 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 0, 'fr-distribution-0017 item[2].x');
      expectClose(r.y - container.y, 20, 'fr-distribution-0017 item[2].y');
      expectClose(r.width, 30, 'fr-distribution-0017 item[2].w');
      expectClose(r.height, 20, 'fr-distribution-0017 item[2].h');
    }
  });
});

describe("fr-x-gap", () => {
  it("fr-x-gap-0018: fr distribution with gap 0", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid","width":600,"gridTemplateColumns":"1fr 1fr 2fr","gap":0}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":30,"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":30,"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":30,"height":20}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'fr-x-gap-0018 container.width');
    expectClose(container.height, 20, 'fr-x-gap-0018 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'fr-x-gap-0018 item[0].x');
      expectClose(r.y - container.y, 0, 'fr-x-gap-0018 item[0].y');
      expectClose(r.width, 30, 'fr-x-gap-0018 item[0].w');
      expectClose(r.height, 20, 'fr-x-gap-0018 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 150, 'fr-x-gap-0018 item[1].x');
      expectClose(r.y - container.y, 0, 'fr-x-gap-0018 item[1].y');
      expectClose(r.width, 30, 'fr-x-gap-0018 item[1].w');
      expectClose(r.height, 20, 'fr-x-gap-0018 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 300, 'fr-x-gap-0018 item[2].x');
      expectClose(r.y - container.y, 0, 'fr-x-gap-0018 item[2].y');
      expectClose(r.width, 30, 'fr-x-gap-0018 item[2].w');
      expectClose(r.height, 20, 'fr-x-gap-0018 item[2].h');
    }
  });
  it("fr-x-gap-0019: fr distribution with gap 5", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid","width":600,"gridTemplateColumns":"1fr 1fr 2fr","gap":5}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":30,"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":30,"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":30,"height":20}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'fr-x-gap-0019 container.width');
    expectClose(container.height, 20, 'fr-x-gap-0019 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'fr-x-gap-0019 item[0].x');
      expectClose(r.y - container.y, 0, 'fr-x-gap-0019 item[0].y');
      expectClose(r.width, 30, 'fr-x-gap-0019 item[0].w');
      expectClose(r.height, 20, 'fr-x-gap-0019 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 152.5, 'fr-x-gap-0019 item[1].x');
      expectClose(r.y - container.y, 0, 'fr-x-gap-0019 item[1].y');
      expectClose(r.width, 30, 'fr-x-gap-0019 item[1].w');
      expectClose(r.height, 20, 'fr-x-gap-0019 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 305, 'fr-x-gap-0019 item[2].x');
      expectClose(r.y - container.y, 0, 'fr-x-gap-0019 item[2].y');
      expectClose(r.width, 30, 'fr-x-gap-0019 item[2].w');
      expectClose(r.height, 20, 'fr-x-gap-0019 item[2].h');
    }
  });
  it("fr-x-gap-0020: fr distribution with gap 10", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid","width":600,"gridTemplateColumns":"1fr 1fr 2fr","gap":10}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":30,"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":30,"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":30,"height":20}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'fr-x-gap-0020 container.width');
    expectClose(container.height, 20, 'fr-x-gap-0020 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'fr-x-gap-0020 item[0].x');
      expectClose(r.y - container.y, 0, 'fr-x-gap-0020 item[0].y');
      expectClose(r.width, 30, 'fr-x-gap-0020 item[0].w');
      expectClose(r.height, 20, 'fr-x-gap-0020 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 155, 'fr-x-gap-0020 item[1].x');
      expectClose(r.y - container.y, 0, 'fr-x-gap-0020 item[1].y');
      expectClose(r.width, 30, 'fr-x-gap-0020 item[1].w');
      expectClose(r.height, 20, 'fr-x-gap-0020 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 310, 'fr-x-gap-0020 item[2].x');
      expectClose(r.y - container.y, 0, 'fr-x-gap-0020 item[2].y');
      expectClose(r.width, 30, 'fr-x-gap-0020 item[2].w');
      expectClose(r.height, 20, 'fr-x-gap-0020 item[2].h');
    }
  });
  it("fr-x-gap-0021: fr distribution with gap 37", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid","width":600,"gridTemplateColumns":"1fr 1fr 2fr","gap":37}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":30,"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":30,"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":30,"height":20}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'fr-x-gap-0021 container.width');
    expectClose(container.height, 20, 'fr-x-gap-0021 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'fr-x-gap-0021 item[0].x');
      expectClose(r.y - container.y, 0, 'fr-x-gap-0021 item[0].y');
      expectClose(r.width, 30, 'fr-x-gap-0021 item[0].w');
      expectClose(r.height, 20, 'fr-x-gap-0021 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 168.5, 'fr-x-gap-0021 item[1].x');
      expectClose(r.y - container.y, 0, 'fr-x-gap-0021 item[1].y');
      expectClose(r.width, 30, 'fr-x-gap-0021 item[1].w');
      expectClose(r.height, 20, 'fr-x-gap-0021 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 337, 'fr-x-gap-0021 item[2].x');
      expectClose(r.y - container.y, 0, 'fr-x-gap-0021 item[2].y');
      expectClose(r.width, 30, 'fr-x-gap-0021 item[2].w');
      expectClose(r.height, 20, 'fr-x-gap-0021 item[2].h');
    }
  });
});

describe("container-box-x-fr", () => {
  it("container-box-x-fr-0022: padding 0 border 0 reduce the fr space", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid","width":600,"gridTemplateColumns":"1fr 1fr","gap":10}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":30,"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":30,"height":20}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'container-box-x-fr-0022 container.width');
    expectClose(container.height, 20, 'container-box-x-fr-0022 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'container-box-x-fr-0022 item[0].x');
      expectClose(r.y - container.y, 0, 'container-box-x-fr-0022 item[0].y');
      expectClose(r.width, 30, 'container-box-x-fr-0022 item[0].w');
      expectClose(r.height, 20, 'container-box-x-fr-0022 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 305, 'container-box-x-fr-0022 item[1].x');
      expectClose(r.y - container.y, 0, 'container-box-x-fr-0022 item[1].y');
      expectClose(r.width, 30, 'container-box-x-fr-0022 item[1].w');
      expectClose(r.height, 20, 'container-box-x-fr-0022 item[1].h');
    }
  });
  it("container-box-x-fr-0023: padding 20 border 0 reduce the fr space", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid","width":600,"gridTemplateColumns":"1fr 1fr","gap":10,"padding":20}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":30,"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":30,"height":20}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'container-box-x-fr-0023 container.width');
    expectClose(container.height, 60, 'container-box-x-fr-0023 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 20, 'container-box-x-fr-0023 item[0].x');
      expectClose(r.y - container.y, 20, 'container-box-x-fr-0023 item[0].y');
      expectClose(r.width, 30, 'container-box-x-fr-0023 item[0].w');
      expectClose(r.height, 20, 'container-box-x-fr-0023 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 305, 'container-box-x-fr-0023 item[1].x');
      expectClose(r.y - container.y, 20, 'container-box-x-fr-0023 item[1].y');
      expectClose(r.width, 30, 'container-box-x-fr-0023 item[1].w');
      expectClose(r.height, 20, 'container-box-x-fr-0023 item[1].h');
    }
  });
  it("container-box-x-fr-0024: padding 0 border 5 reduce the fr space", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid","width":600,"gridTemplateColumns":"1fr 1fr","gap":10,"borderWidth":5,"borderColor":"transparent"}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":30,"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":30,"height":20}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'container-box-x-fr-0024 container.width');
    expectClose(container.height, 30, 'container-box-x-fr-0024 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 5, 'container-box-x-fr-0024 item[0].x');
      expectClose(r.y - container.y, 5, 'container-box-x-fr-0024 item[0].y');
      expectClose(r.width, 30, 'container-box-x-fr-0024 item[0].w');
      expectClose(r.height, 20, 'container-box-x-fr-0024 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 305, 'container-box-x-fr-0024 item[1].x');
      expectClose(r.y - container.y, 5, 'container-box-x-fr-0024 item[1].y');
      expectClose(r.width, 30, 'container-box-x-fr-0024 item[1].w');
      expectClose(r.height, 20, 'container-box-x-fr-0024 item[1].h');
    }
  });
  it("container-box-x-fr-0025: padding 12 border 3 reduce the fr space", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid","width":600,"gridTemplateColumns":"1fr 1fr","gap":10,"padding":12,"borderWidth":3,"borderColor":"transparent"}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":30,"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":30,"height":20}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'container-box-x-fr-0025 container.width');
    expectClose(container.height, 50, 'container-box-x-fr-0025 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 15, 'container-box-x-fr-0025 item[0].x');
      expectClose(r.y - container.y, 15, 'container-box-x-fr-0025 item[0].y');
      expectClose(r.width, 30, 'container-box-x-fr-0025 item[0].w');
      expectClose(r.height, 20, 'container-box-x-fr-0025 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 305, 'container-box-x-fr-0025 item[1].x');
      expectClose(r.y - container.y, 15, 'container-box-x-fr-0025 item[1].y');
      expectClose(r.width, 30, 'container-box-x-fr-0025 item[1].w');
      expectClose(r.height, 20, 'container-box-x-fr-0025 item[1].h');
    }
  });
});

describe("auto-track-x-margin", () => {
  it("auto-track-x-margin-0026: auto track absorbs item margin 0", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid","width":600,"gridTemplateColumns":"auto auto","gap":10}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":50,"height":20,"margin":0}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":80,"height":20,"margin":0}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'auto-track-x-margin-0026 container.width');
    expectClose(container.height, 20, 'auto-track-x-margin-0026 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'auto-track-x-margin-0026 item[0].x');
      expectClose(r.y - container.y, 0, 'auto-track-x-margin-0026 item[0].y');
      expectClose(r.width, 50, 'auto-track-x-margin-0026 item[0].w');
      expectClose(r.height, 20, 'auto-track-x-margin-0026 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 290, 'auto-track-x-margin-0026 item[1].x');
      expectClose(r.y - container.y, 0, 'auto-track-x-margin-0026 item[1].y');
      expectClose(r.width, 80, 'auto-track-x-margin-0026 item[1].w');
      expectClose(r.height, 20, 'auto-track-x-margin-0026 item[1].h');
    }
  });
  it("auto-track-x-margin-0027: auto track absorbs item margin 8", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid","width":600,"gridTemplateColumns":"auto auto","gap":10}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":50,"height":20,"margin":8}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":80,"height":20,"margin":8}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'auto-track-x-margin-0027 container.width');
    expectClose(container.height, 36, 'auto-track-x-margin-0027 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 8, 'auto-track-x-margin-0027 item[0].x');
      expectClose(r.y - container.y, 8, 'auto-track-x-margin-0027 item[0].y');
      expectClose(r.width, 50, 'auto-track-x-margin-0027 item[0].w');
      expectClose(r.height, 20, 'auto-track-x-margin-0027 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 298, 'auto-track-x-margin-0027 item[1].x');
      expectClose(r.y - container.y, 8, 'auto-track-x-margin-0027 item[1].y');
      expectClose(r.width, 80, 'auto-track-x-margin-0027 item[1].w');
      expectClose(r.height, 20, 'auto-track-x-margin-0027 item[1].h');
    }
  });
  it("auto-track-x-margin-0028: auto track absorbs item margin -6", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid","width":600,"gridTemplateColumns":"auto auto","gap":10}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":50,"height":20,"margin":-6}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":80,"height":20,"margin":-6}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'auto-track-x-margin-0028 container.width');
    expectClose(container.height, 8, 'auto-track-x-margin-0028 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, -6, 'auto-track-x-margin-0028 item[0].x');
      expectClose(r.y - container.y, -6, 'auto-track-x-margin-0028 item[0].y');
      expectClose(r.width, 50, 'auto-track-x-margin-0028 item[0].w');
      expectClose(r.height, 20, 'auto-track-x-margin-0028 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 284, 'auto-track-x-margin-0028 item[1].x');
      expectClose(r.y - container.y, -6, 'auto-track-x-margin-0028 item[1].y');
      expectClose(r.width, 80, 'auto-track-x-margin-0028 item[1].w');
      expectClose(r.height, 20, 'auto-track-x-margin-0028 item[1].h');
    }
  });
});

describe("pct-track-x-width", () => {
  it("pct-track-x-width-0029: percentage tracks at container width 400", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid","width":400,"gridTemplateColumns":"25% 50% 25%"}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":30,"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":30,"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":30,"height":20}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 400, 'pct-track-x-width-0029 container.width');
    expectClose(container.height, 20, 'pct-track-x-width-0029 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'pct-track-x-width-0029 item[0].x');
      expectClose(r.y - container.y, 0, 'pct-track-x-width-0029 item[0].y');
      expectClose(r.width, 30, 'pct-track-x-width-0029 item[0].w');
      expectClose(r.height, 20, 'pct-track-x-width-0029 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 100, 'pct-track-x-width-0029 item[1].x');
      expectClose(r.y - container.y, 0, 'pct-track-x-width-0029 item[1].y');
      expectClose(r.width, 30, 'pct-track-x-width-0029 item[1].w');
      expectClose(r.height, 20, 'pct-track-x-width-0029 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 300, 'pct-track-x-width-0029 item[2].x');
      expectClose(r.y - container.y, 0, 'pct-track-x-width-0029 item[2].y');
      expectClose(r.width, 30, 'pct-track-x-width-0029 item[2].w');
      expectClose(r.height, 20, 'pct-track-x-width-0029 item[2].h');
    }
  });
  it("pct-track-x-width-0030: percentage tracks at container width 600", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid","width":600,"gridTemplateColumns":"25% 50% 25%"}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":30,"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":30,"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":30,"height":20}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'pct-track-x-width-0030 container.width');
    expectClose(container.height, 20, 'pct-track-x-width-0030 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'pct-track-x-width-0030 item[0].x');
      expectClose(r.y - container.y, 0, 'pct-track-x-width-0030 item[0].y');
      expectClose(r.width, 30, 'pct-track-x-width-0030 item[0].w');
      expectClose(r.height, 20, 'pct-track-x-width-0030 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 150, 'pct-track-x-width-0030 item[1].x');
      expectClose(r.y - container.y, 0, 'pct-track-x-width-0030 item[1].y');
      expectClose(r.width, 30, 'pct-track-x-width-0030 item[1].w');
      expectClose(r.height, 20, 'pct-track-x-width-0030 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 450, 'pct-track-x-width-0030 item[2].x');
      expectClose(r.y - container.y, 0, 'pct-track-x-width-0030 item[2].y');
      expectClose(r.width, 30, 'pct-track-x-width-0030 item[2].w');
      expectClose(r.height, 20, 'pct-track-x-width-0030 item[2].h');
    }
  });
  it("pct-track-x-width-0031: percentage tracks at container width 777", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid","width":777,"gridTemplateColumns":"25% 50% 25%"}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":30,"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":30,"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":30,"height":20}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 777, 'pct-track-x-width-0031 container.width');
    expectClose(container.height, 20, 'pct-track-x-width-0031 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'pct-track-x-width-0031 item[0].x');
      expectClose(r.y - container.y, 0, 'pct-track-x-width-0031 item[0].y');
      expectClose(r.width, 30, 'pct-track-x-width-0031 item[0].w');
      expectClose(r.height, 20, 'pct-track-x-width-0031 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 194.25, 'pct-track-x-width-0031 item[1].x');
      expectClose(r.y - container.y, 0, 'pct-track-x-width-0031 item[1].y');
      expectClose(r.width, 30, 'pct-track-x-width-0031 item[1].w');
      expectClose(r.height, 20, 'pct-track-x-width-0031 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 582.75, 'pct-track-x-width-0031 item[2].x');
      expectClose(r.y - container.y, 0, 'pct-track-x-width-0031 item[2].y');
      expectClose(r.width, 30, 'pct-track-x-width-0031 item[2].w');
      expectClose(r.height, 20, 'pct-track-x-width-0031 item[2].h');
    }
  });
});

describe("span-basic", () => {
  it("span-basic-0032: item spanning 2 fixed tracks", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid","width":600,"gridTemplateColumns":"100px 100px 100px","gap":10}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":40,"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":40,"height":20,"gridColumnEnd":"span 2"}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":40,"height":20}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'span-basic-0032 container.width');
    expectClose(container.height, 50, 'span-basic-0032 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'span-basic-0032 item[0].x');
      expectClose(r.y - container.y, 0, 'span-basic-0032 item[0].y');
      expectClose(r.width, 40, 'span-basic-0032 item[0].w');
      expectClose(r.height, 20, 'span-basic-0032 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 110, 'span-basic-0032 item[1].x');
      expectClose(r.y - container.y, 0, 'span-basic-0032 item[1].y');
      expectClose(r.width, 40, 'span-basic-0032 item[1].w');
      expectClose(r.height, 20, 'span-basic-0032 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 0, 'span-basic-0032 item[2].x');
      expectClose(r.y - container.y, 30, 'span-basic-0032 item[2].y');
      expectClose(r.width, 40, 'span-basic-0032 item[2].w');
      expectClose(r.height, 20, 'span-basic-0032 item[2].h');
    }
  });
  it("span-basic-0035: item spanning 3 fixed tracks", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid","width":600,"gridTemplateColumns":"100px 100px 100px","gap":10}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":40,"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":40,"height":20,"gridColumnEnd":"span 3"}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":40,"height":20}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'span-basic-0035 container.width');
    expectClose(container.height, 80, 'span-basic-0035 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'span-basic-0035 item[0].x');
      expectClose(r.y - container.y, 0, 'span-basic-0035 item[0].y');
      expectClose(r.width, 40, 'span-basic-0035 item[0].w');
      expectClose(r.height, 20, 'span-basic-0035 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 0, 'span-basic-0035 item[1].x');
      expectClose(r.y - container.y, 30, 'span-basic-0035 item[1].y');
      expectClose(r.width, 40, 'span-basic-0035 item[1].w');
      expectClose(r.height, 20, 'span-basic-0035 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 0, 'span-basic-0035 item[2].x');
      expectClose(r.y - container.y, 60, 'span-basic-0035 item[2].y');
      expectClose(r.width, 40, 'span-basic-0035 item[2].w');
      expectClose(r.height, 20, 'span-basic-0035 item[2].h');
    }
  });
});

describe("span-x-fr", () => {
  it("span-x-fr-0033: item spanning 2 fr tracks", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid","width":600,"gridTemplateColumns":"1fr 1fr 1fr","gap":10}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":40,"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":40,"height":20,"gridColumnEnd":"span 2"}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":40,"height":20}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'span-x-fr-0033 container.width');
    expectClose(container.height, 50, 'span-x-fr-0033 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'span-x-fr-0033 item[0].x');
      expectClose(r.y - container.y, 0, 'span-x-fr-0033 item[0].y');
      expectClose(r.width, 40, 'span-x-fr-0033 item[0].w');
      expectClose(r.height, 20, 'span-x-fr-0033 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 203.33, 'span-x-fr-0033 item[1].x');
      expectClose(r.y - container.y, 0, 'span-x-fr-0033 item[1].y');
      expectClose(r.width, 40, 'span-x-fr-0033 item[1].w');
      expectClose(r.height, 20, 'span-x-fr-0033 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 0, 'span-x-fr-0033 item[2].x');
      expectClose(r.y - container.y, 30, 'span-x-fr-0033 item[2].y');
      expectClose(r.width, 40, 'span-x-fr-0033 item[2].w');
      expectClose(r.height, 20, 'span-x-fr-0033 item[2].h');
    }
  });
  it("span-x-fr-0036: item spanning 3 fr tracks", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid","width":600,"gridTemplateColumns":"1fr 1fr 1fr","gap":10}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":40,"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":40,"height":20,"gridColumnEnd":"span 3"}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":40,"height":20}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'span-x-fr-0036 container.width');
    expectClose(container.height, 80, 'span-x-fr-0036 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'span-x-fr-0036 item[0].x');
      expectClose(r.y - container.y, 0, 'span-x-fr-0036 item[0].y');
      expectClose(r.width, 40, 'span-x-fr-0036 item[0].w');
      expectClose(r.height, 20, 'span-x-fr-0036 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 0, 'span-x-fr-0036 item[1].x');
      expectClose(r.y - container.y, 30, 'span-x-fr-0036 item[1].y');
      expectClose(r.width, 40, 'span-x-fr-0036 item[1].w');
      expectClose(r.height, 20, 'span-x-fr-0036 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 0, 'span-x-fr-0036 item[2].x');
      expectClose(r.y - container.y, 60, 'span-x-fr-0036 item[2].y');
      expectClose(r.width, 40, 'span-x-fr-0036 item[2].w');
      expectClose(r.height, 20, 'span-x-fr-0036 item[2].h');
    }
  });
});

describe("span-x-auto", () => {
  it("span-x-auto-0034: spanning item distributes its size across 2 auto tracks", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid","width":600,"gridTemplateColumns":"auto auto auto","gap":10}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":40,"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":250,"height":20,"gridColumnEnd":"span 2"}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":40,"height":20}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'span-x-auto-0034 container.width');
    expectClose(container.height, 50, 'span-x-auto-0034 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'span-x-auto-0034 item[0].x');
      expectClose(r.y - container.y, 0, 'span-x-auto-0034 item[0].y');
      expectClose(r.width, 40, 'span-x-auto-0034 item[0].w');
      expectClose(r.height, 20, 'span-x-auto-0034 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 150, 'span-x-auto-0034 item[1].x');
      expectClose(r.y - container.y, 0, 'span-x-auto-0034 item[1].y');
      expectClose(r.width, 250, 'span-x-auto-0034 item[1].w');
      expectClose(r.height, 20, 'span-x-auto-0034 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 0, 'span-x-auto-0034 item[2].x');
      expectClose(r.y - container.y, 30, 'span-x-auto-0034 item[2].y');
      expectClose(r.width, 40, 'span-x-auto-0034 item[2].w');
      expectClose(r.height, 20, 'span-x-auto-0034 item[2].h');
    }
  });
  it("span-x-auto-0037: spanning item distributes its size across 3 auto tracks", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid","width":600,"gridTemplateColumns":"auto auto auto","gap":10}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":40,"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":250,"height":20,"gridColumnEnd":"span 3"}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":40,"height":20}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'span-x-auto-0037 container.width');
    expectClose(container.height, 80, 'span-x-auto-0037 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'span-x-auto-0037 item[0].x');
      expectClose(r.y - container.y, 0, 'span-x-auto-0037 item[0].y');
      expectClose(r.width, 40, 'span-x-auto-0037 item[0].w');
      expectClose(r.height, 20, 'span-x-auto-0037 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 0, 'span-x-auto-0037 item[1].x');
      expectClose(r.y - container.y, 30, 'span-x-auto-0037 item[1].y');
      expectClose(r.width, 250, 'span-x-auto-0037 item[1].w');
      expectClose(r.height, 20, 'span-x-auto-0037 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 0, 'span-x-auto-0037 item[2].x');
      expectClose(r.y - container.y, 60, 'span-x-auto-0037 item[2].y');
      expectClose(r.width, 40, 'span-x-auto-0037 item[2].w');
      expectClose(r.height, 20, 'span-x-auto-0037 item[2].h');
    }
  });
});

describe("explicit-placement", () => {
  it("explicit-placement-0038: first item explicitly at column line 1", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid","width":600,"gridTemplateColumns":"100px 100px 100px","gap":10}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":40,"height":20,"gridColumnStart":1}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":40,"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":40,"height":20}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'explicit-placement-0038 container.width');
    expectClose(container.height, 20, 'explicit-placement-0038 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'explicit-placement-0038 item[0].x');
      expectClose(r.y - container.y, 0, 'explicit-placement-0038 item[0].y');
      expectClose(r.width, 40, 'explicit-placement-0038 item[0].w');
      expectClose(r.height, 20, 'explicit-placement-0038 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 110, 'explicit-placement-0038 item[1].x');
      expectClose(r.y - container.y, 0, 'explicit-placement-0038 item[1].y');
      expectClose(r.width, 40, 'explicit-placement-0038 item[1].w');
      expectClose(r.height, 20, 'explicit-placement-0038 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 220, 'explicit-placement-0038 item[2].x');
      expectClose(r.y - container.y, 0, 'explicit-placement-0038 item[2].y');
      expectClose(r.width, 40, 'explicit-placement-0038 item[2].w');
      expectClose(r.height, 20, 'explicit-placement-0038 item[2].h');
    }
  });
  it("explicit-placement-0039: first item explicitly at column line 2", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid","width":600,"gridTemplateColumns":"100px 100px 100px","gap":10}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":40,"height":20,"gridColumnStart":2}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":40,"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":40,"height":20}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'explicit-placement-0039 container.width');
    expectClose(container.height, 50, 'explicit-placement-0039 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 110, 'explicit-placement-0039 item[0].x');
      expectClose(r.y - container.y, 0, 'explicit-placement-0039 item[0].y');
      expectClose(r.width, 40, 'explicit-placement-0039 item[0].w');
      expectClose(r.height, 20, 'explicit-placement-0039 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 220, 'explicit-placement-0039 item[1].x');
      expectClose(r.y - container.y, 0, 'explicit-placement-0039 item[1].y');
      expectClose(r.width, 40, 'explicit-placement-0039 item[1].w');
      expectClose(r.height, 20, 'explicit-placement-0039 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 0, 'explicit-placement-0039 item[2].x');
      expectClose(r.y - container.y, 30, 'explicit-placement-0039 item[2].y');
      expectClose(r.width, 40, 'explicit-placement-0039 item[2].w');
      expectClose(r.height, 20, 'explicit-placement-0039 item[2].h');
    }
  });
  it("explicit-placement-0040: first item explicitly at column line 3", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid","width":600,"gridTemplateColumns":"100px 100px 100px","gap":10}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":40,"height":20,"gridColumnStart":3}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":40,"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":40,"height":20}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'explicit-placement-0040 container.width');
    expectClose(container.height, 50, 'explicit-placement-0040 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 220, 'explicit-placement-0040 item[0].x');
      expectClose(r.y - container.y, 0, 'explicit-placement-0040 item[0].y');
      expectClose(r.width, 40, 'explicit-placement-0040 item[0].w');
      expectClose(r.height, 20, 'explicit-placement-0040 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 0, 'explicit-placement-0040 item[1].x');
      expectClose(r.y - container.y, 30, 'explicit-placement-0040 item[1].y');
      expectClose(r.width, 40, 'explicit-placement-0040 item[1].w');
      expectClose(r.height, 20, 'explicit-placement-0040 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 110, 'explicit-placement-0040 item[2].x');
      expectClose(r.y - container.y, 30, 'explicit-placement-0040 item[2].y');
      expectClose(r.width, 40, 'explicit-placement-0040 item[2].w');
      expectClose(r.height, 20, 'explicit-placement-0040 item[2].h');
    }
  });
  it("explicit-placement-0041: first item explicitly at column line -1", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid","width":600,"gridTemplateColumns":"100px 100px 100px","gap":10}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":40,"height":20,"gridColumnStart":-1}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":40,"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":40,"height":20}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'explicit-placement-0041 container.width');
    expectClose(container.height, 50, 'explicit-placement-0041 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 330, 'explicit-placement-0041 item[0].x');
      expectClose(r.y - container.y, 0, 'explicit-placement-0041 item[0].y');
      expectClose(r.width, 40, 'explicit-placement-0041 item[0].w');
      expectClose(r.height, 20, 'explicit-placement-0041 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 0, 'explicit-placement-0041 item[1].x');
      expectClose(r.y - container.y, 30, 'explicit-placement-0041 item[1].y');
      expectClose(r.width, 40, 'explicit-placement-0041 item[1].w');
      expectClose(r.height, 20, 'explicit-placement-0041 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 110, 'explicit-placement-0041 item[2].x');
      expectClose(r.y - container.y, 30, 'explicit-placement-0041 item[2].y');
      expectClose(r.width, 40, 'explicit-placement-0041 item[2].w');
      expectClose(r.height, 20, 'explicit-placement-0041 item[2].h');
    }
  });
  it("explicit-placement-0042: first item explicitly at column line -2", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid","width":600,"gridTemplateColumns":"100px 100px 100px","gap":10}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":40,"height":20,"gridColumnStart":-2}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":40,"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":40,"height":20}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'explicit-placement-0042 container.width');
    expectClose(container.height, 50, 'explicit-placement-0042 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 220, 'explicit-placement-0042 item[0].x');
      expectClose(r.y - container.y, 0, 'explicit-placement-0042 item[0].y');
      expectClose(r.width, 40, 'explicit-placement-0042 item[0].w');
      expectClose(r.height, 20, 'explicit-placement-0042 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 0, 'explicit-placement-0042 item[1].x');
      expectClose(r.y - container.y, 30, 'explicit-placement-0042 item[1].y');
      expectClose(r.width, 40, 'explicit-placement-0042 item[1].w');
      expectClose(r.height, 20, 'explicit-placement-0042 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 110, 'explicit-placement-0042 item[2].x');
      expectClose(r.y - container.y, 30, 'explicit-placement-0042 item[2].y');
      expectClose(r.width, 40, 'explicit-placement-0042 item[2].w');
      expectClose(r.height, 20, 'explicit-placement-0042 item[2].h');
    }
  });
});

describe("gap-asymmetric", () => {
  it("gap-asymmetric-0043: row-gap 0 column-gap 0", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid","width":600,"gridTemplateColumns":"100px 100px","gridTemplateRows":"50px 50px","rowGap":0,"columnGap":0}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":30,"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":30,"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":30,"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":30,"height":20}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'gap-asymmetric-0043 container.width');
    expectClose(container.height, 100, 'gap-asymmetric-0043 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'gap-asymmetric-0043 item[0].x');
      expectClose(r.y - container.y, 0, 'gap-asymmetric-0043 item[0].y');
      expectClose(r.width, 30, 'gap-asymmetric-0043 item[0].w');
      expectClose(r.height, 20, 'gap-asymmetric-0043 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 100, 'gap-asymmetric-0043 item[1].x');
      expectClose(r.y - container.y, 0, 'gap-asymmetric-0043 item[1].y');
      expectClose(r.width, 30, 'gap-asymmetric-0043 item[1].w');
      expectClose(r.height, 20, 'gap-asymmetric-0043 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 0, 'gap-asymmetric-0043 item[2].x');
      expectClose(r.y - container.y, 50, 'gap-asymmetric-0043 item[2].y');
      expectClose(r.width, 30, 'gap-asymmetric-0043 item[2].w');
      expectClose(r.height, 20, 'gap-asymmetric-0043 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 100, 'gap-asymmetric-0043 item[3].x');
      expectClose(r.y - container.y, 50, 'gap-asymmetric-0043 item[3].y');
      expectClose(r.width, 30, 'gap-asymmetric-0043 item[3].w');
      expectClose(r.height, 20, 'gap-asymmetric-0043 item[3].h');
    }
  });
  it("gap-asymmetric-0044: row-gap 10 column-gap 0", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid","width":600,"gridTemplateColumns":"100px 100px","gridTemplateRows":"50px 50px","rowGap":10,"columnGap":0}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":30,"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":30,"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":30,"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":30,"height":20}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'gap-asymmetric-0044 container.width');
    expectClose(container.height, 110, 'gap-asymmetric-0044 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'gap-asymmetric-0044 item[0].x');
      expectClose(r.y - container.y, 0, 'gap-asymmetric-0044 item[0].y');
      expectClose(r.width, 30, 'gap-asymmetric-0044 item[0].w');
      expectClose(r.height, 20, 'gap-asymmetric-0044 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 100, 'gap-asymmetric-0044 item[1].x');
      expectClose(r.y - container.y, 0, 'gap-asymmetric-0044 item[1].y');
      expectClose(r.width, 30, 'gap-asymmetric-0044 item[1].w');
      expectClose(r.height, 20, 'gap-asymmetric-0044 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 0, 'gap-asymmetric-0044 item[2].x');
      expectClose(r.y - container.y, 60, 'gap-asymmetric-0044 item[2].y');
      expectClose(r.width, 30, 'gap-asymmetric-0044 item[2].w');
      expectClose(r.height, 20, 'gap-asymmetric-0044 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 100, 'gap-asymmetric-0044 item[3].x');
      expectClose(r.y - container.y, 60, 'gap-asymmetric-0044 item[3].y');
      expectClose(r.width, 30, 'gap-asymmetric-0044 item[3].w');
      expectClose(r.height, 20, 'gap-asymmetric-0044 item[3].h');
    }
  });
  it("gap-asymmetric-0045: row-gap 0 column-gap 10", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid","width":600,"gridTemplateColumns":"100px 100px","gridTemplateRows":"50px 50px","rowGap":0,"columnGap":10}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":30,"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":30,"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":30,"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":30,"height":20}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'gap-asymmetric-0045 container.width');
    expectClose(container.height, 100, 'gap-asymmetric-0045 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'gap-asymmetric-0045 item[0].x');
      expectClose(r.y - container.y, 0, 'gap-asymmetric-0045 item[0].y');
      expectClose(r.width, 30, 'gap-asymmetric-0045 item[0].w');
      expectClose(r.height, 20, 'gap-asymmetric-0045 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 110, 'gap-asymmetric-0045 item[1].x');
      expectClose(r.y - container.y, 0, 'gap-asymmetric-0045 item[1].y');
      expectClose(r.width, 30, 'gap-asymmetric-0045 item[1].w');
      expectClose(r.height, 20, 'gap-asymmetric-0045 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 0, 'gap-asymmetric-0045 item[2].x');
      expectClose(r.y - container.y, 50, 'gap-asymmetric-0045 item[2].y');
      expectClose(r.width, 30, 'gap-asymmetric-0045 item[2].w');
      expectClose(r.height, 20, 'gap-asymmetric-0045 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 110, 'gap-asymmetric-0045 item[3].x');
      expectClose(r.y - container.y, 50, 'gap-asymmetric-0045 item[3].y');
      expectClose(r.width, 30, 'gap-asymmetric-0045 item[3].w');
      expectClose(r.height, 20, 'gap-asymmetric-0045 item[3].h');
    }
  });
  it("gap-asymmetric-0046: row-gap 4 column-gap 21", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid","width":600,"gridTemplateColumns":"100px 100px","gridTemplateRows":"50px 50px","rowGap":4,"columnGap":21}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":30,"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":30,"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":30,"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":30,"height":20}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'gap-asymmetric-0046 container.width');
    expectClose(container.height, 104, 'gap-asymmetric-0046 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'gap-asymmetric-0046 item[0].x');
      expectClose(r.y - container.y, 0, 'gap-asymmetric-0046 item[0].y');
      expectClose(r.width, 30, 'gap-asymmetric-0046 item[0].w');
      expectClose(r.height, 20, 'gap-asymmetric-0046 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 121, 'gap-asymmetric-0046 item[1].x');
      expectClose(r.y - container.y, 0, 'gap-asymmetric-0046 item[1].y');
      expectClose(r.width, 30, 'gap-asymmetric-0046 item[1].w');
      expectClose(r.height, 20, 'gap-asymmetric-0046 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 0, 'gap-asymmetric-0046 item[2].x');
      expectClose(r.y - container.y, 54, 'gap-asymmetric-0046 item[2].y');
      expectClose(r.width, 30, 'gap-asymmetric-0046 item[2].w');
      expectClose(r.height, 20, 'gap-asymmetric-0046 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 121, 'gap-asymmetric-0046 item[3].x');
      expectClose(r.y - container.y, 54, 'gap-asymmetric-0046 item[3].y');
      expectClose(r.width, 30, 'gap-asymmetric-0046 item[3].w');
      expectClose(r.height, 20, 'gap-asymmetric-0046 item[3].h');
    }
  });
});

describe("align-items-matrix", () => {
  it("align-items-matrix-0047: justify-items:start align-items:start", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid","width":600,"gridTemplateColumns":"150px 150px","gridTemplateRows":"80px 80px","gap":10,"justifyItems":"start","alignItems":"start"}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":40,"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":40,"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'align-items-matrix-0047 container.width');
    expectClose(container.height, 170, 'align-items-matrix-0047 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'align-items-matrix-0047 item[0].x');
      expectClose(r.y - container.y, 0, 'align-items-matrix-0047 item[0].y');
      expectClose(r.width, 40, 'align-items-matrix-0047 item[0].w');
      expectClose(r.height, 20, 'align-items-matrix-0047 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 160, 'align-items-matrix-0047 item[1].x');
      expectClose(r.y - container.y, 0, 'align-items-matrix-0047 item[1].y');
      expectClose(r.width, 0, 'align-items-matrix-0047 item[1].w');
      expectClose(r.height, 0, 'align-items-matrix-0047 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 0, 'align-items-matrix-0047 item[2].x');
      expectClose(r.y - container.y, 90, 'align-items-matrix-0047 item[2].y');
      expectClose(r.width, 40, 'align-items-matrix-0047 item[2].w');
      expectClose(r.height, 20, 'align-items-matrix-0047 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 160, 'align-items-matrix-0047 item[3].x');
      expectClose(r.y - container.y, 90, 'align-items-matrix-0047 item[3].y');
      expectClose(r.width, 0, 'align-items-matrix-0047 item[3].w');
      expectClose(r.height, 0, 'align-items-matrix-0047 item[3].h');
    }
  });
  it("align-items-matrix-0048: justify-items:start align-items:center", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid","width":600,"gridTemplateColumns":"150px 150px","gridTemplateRows":"80px 80px","gap":10,"justifyItems":"start","alignItems":"center"}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":40,"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":40,"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'align-items-matrix-0048 container.width');
    expectClose(container.height, 170, 'align-items-matrix-0048 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'align-items-matrix-0048 item[0].x');
      expectClose(r.y - container.y, 30, 'align-items-matrix-0048 item[0].y');
      expectClose(r.width, 40, 'align-items-matrix-0048 item[0].w');
      expectClose(r.height, 20, 'align-items-matrix-0048 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 160, 'align-items-matrix-0048 item[1].x');
      expectClose(r.y - container.y, 40, 'align-items-matrix-0048 item[1].y');
      expectClose(r.width, 0, 'align-items-matrix-0048 item[1].w');
      expectClose(r.height, 0, 'align-items-matrix-0048 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 0, 'align-items-matrix-0048 item[2].x');
      expectClose(r.y - container.y, 120, 'align-items-matrix-0048 item[2].y');
      expectClose(r.width, 40, 'align-items-matrix-0048 item[2].w');
      expectClose(r.height, 20, 'align-items-matrix-0048 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 160, 'align-items-matrix-0048 item[3].x');
      expectClose(r.y - container.y, 130, 'align-items-matrix-0048 item[3].y');
      expectClose(r.width, 0, 'align-items-matrix-0048 item[3].w');
      expectClose(r.height, 0, 'align-items-matrix-0048 item[3].h');
    }
  });
  it("align-items-matrix-0049: justify-items:start align-items:end", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid","width":600,"gridTemplateColumns":"150px 150px","gridTemplateRows":"80px 80px","gap":10,"justifyItems":"start","alignItems":"end"}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":40,"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":40,"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'align-items-matrix-0049 container.width');
    expectClose(container.height, 170, 'align-items-matrix-0049 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'align-items-matrix-0049 item[0].x');
      expectClose(r.y - container.y, 60, 'align-items-matrix-0049 item[0].y');
      expectClose(r.width, 40, 'align-items-matrix-0049 item[0].w');
      expectClose(r.height, 20, 'align-items-matrix-0049 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 160, 'align-items-matrix-0049 item[1].x');
      expectClose(r.y - container.y, 80, 'align-items-matrix-0049 item[1].y');
      expectClose(r.width, 0, 'align-items-matrix-0049 item[1].w');
      expectClose(r.height, 0, 'align-items-matrix-0049 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 0, 'align-items-matrix-0049 item[2].x');
      expectClose(r.y - container.y, 150, 'align-items-matrix-0049 item[2].y');
      expectClose(r.width, 40, 'align-items-matrix-0049 item[2].w');
      expectClose(r.height, 20, 'align-items-matrix-0049 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 160, 'align-items-matrix-0049 item[3].x');
      expectClose(r.y - container.y, 170, 'align-items-matrix-0049 item[3].y');
      expectClose(r.width, 0, 'align-items-matrix-0049 item[3].w');
      expectClose(r.height, 0, 'align-items-matrix-0049 item[3].h');
    }
  });
  it("align-items-matrix-0050: justify-items:start align-items:stretch", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid","width":600,"gridTemplateColumns":"150px 150px","gridTemplateRows":"80px 80px","gap":10,"justifyItems":"start","alignItems":"stretch"}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":40,"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":40,"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'align-items-matrix-0050 container.width');
    expectClose(container.height, 170, 'align-items-matrix-0050 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'align-items-matrix-0050 item[0].x');
      expectClose(r.y - container.y, 0, 'align-items-matrix-0050 item[0].y');
      expectClose(r.width, 40, 'align-items-matrix-0050 item[0].w');
      expectClose(r.height, 20, 'align-items-matrix-0050 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 160, 'align-items-matrix-0050 item[1].x');
      expectClose(r.y - container.y, 0, 'align-items-matrix-0050 item[1].y');
      expectClose(r.width, 0, 'align-items-matrix-0050 item[1].w');
      expectClose(r.height, 80, 'align-items-matrix-0050 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 0, 'align-items-matrix-0050 item[2].x');
      expectClose(r.y - container.y, 90, 'align-items-matrix-0050 item[2].y');
      expectClose(r.width, 40, 'align-items-matrix-0050 item[2].w');
      expectClose(r.height, 20, 'align-items-matrix-0050 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 160, 'align-items-matrix-0050 item[3].x');
      expectClose(r.y - container.y, 90, 'align-items-matrix-0050 item[3].y');
      expectClose(r.width, 0, 'align-items-matrix-0050 item[3].w');
      expectClose(r.height, 80, 'align-items-matrix-0050 item[3].h');
    }
  });
  it("align-items-matrix-0051: justify-items:center align-items:start", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid","width":600,"gridTemplateColumns":"150px 150px","gridTemplateRows":"80px 80px","gap":10,"justifyItems":"center","alignItems":"start"}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":40,"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":40,"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'align-items-matrix-0051 container.width');
    expectClose(container.height, 170, 'align-items-matrix-0051 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 55, 'align-items-matrix-0051 item[0].x');
      expectClose(r.y - container.y, 0, 'align-items-matrix-0051 item[0].y');
      expectClose(r.width, 40, 'align-items-matrix-0051 item[0].w');
      expectClose(r.height, 20, 'align-items-matrix-0051 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 235, 'align-items-matrix-0051 item[1].x');
      expectClose(r.y - container.y, 0, 'align-items-matrix-0051 item[1].y');
      expectClose(r.width, 0, 'align-items-matrix-0051 item[1].w');
      expectClose(r.height, 0, 'align-items-matrix-0051 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 55, 'align-items-matrix-0051 item[2].x');
      expectClose(r.y - container.y, 90, 'align-items-matrix-0051 item[2].y');
      expectClose(r.width, 40, 'align-items-matrix-0051 item[2].w');
      expectClose(r.height, 20, 'align-items-matrix-0051 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 235, 'align-items-matrix-0051 item[3].x');
      expectClose(r.y - container.y, 90, 'align-items-matrix-0051 item[3].y');
      expectClose(r.width, 0, 'align-items-matrix-0051 item[3].w');
      expectClose(r.height, 0, 'align-items-matrix-0051 item[3].h');
    }
  });
  it("align-items-matrix-0052: justify-items:center align-items:center", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid","width":600,"gridTemplateColumns":"150px 150px","gridTemplateRows":"80px 80px","gap":10,"justifyItems":"center","alignItems":"center"}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":40,"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":40,"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'align-items-matrix-0052 container.width');
    expectClose(container.height, 170, 'align-items-matrix-0052 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 55, 'align-items-matrix-0052 item[0].x');
      expectClose(r.y - container.y, 30, 'align-items-matrix-0052 item[0].y');
      expectClose(r.width, 40, 'align-items-matrix-0052 item[0].w');
      expectClose(r.height, 20, 'align-items-matrix-0052 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 235, 'align-items-matrix-0052 item[1].x');
      expectClose(r.y - container.y, 40, 'align-items-matrix-0052 item[1].y');
      expectClose(r.width, 0, 'align-items-matrix-0052 item[1].w');
      expectClose(r.height, 0, 'align-items-matrix-0052 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 55, 'align-items-matrix-0052 item[2].x');
      expectClose(r.y - container.y, 120, 'align-items-matrix-0052 item[2].y');
      expectClose(r.width, 40, 'align-items-matrix-0052 item[2].w');
      expectClose(r.height, 20, 'align-items-matrix-0052 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 235, 'align-items-matrix-0052 item[3].x');
      expectClose(r.y - container.y, 130, 'align-items-matrix-0052 item[3].y');
      expectClose(r.width, 0, 'align-items-matrix-0052 item[3].w');
      expectClose(r.height, 0, 'align-items-matrix-0052 item[3].h');
    }
  });
  it("align-items-matrix-0053: justify-items:center align-items:end", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid","width":600,"gridTemplateColumns":"150px 150px","gridTemplateRows":"80px 80px","gap":10,"justifyItems":"center","alignItems":"end"}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":40,"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":40,"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'align-items-matrix-0053 container.width');
    expectClose(container.height, 170, 'align-items-matrix-0053 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 55, 'align-items-matrix-0053 item[0].x');
      expectClose(r.y - container.y, 60, 'align-items-matrix-0053 item[0].y');
      expectClose(r.width, 40, 'align-items-matrix-0053 item[0].w');
      expectClose(r.height, 20, 'align-items-matrix-0053 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 235, 'align-items-matrix-0053 item[1].x');
      expectClose(r.y - container.y, 80, 'align-items-matrix-0053 item[1].y');
      expectClose(r.width, 0, 'align-items-matrix-0053 item[1].w');
      expectClose(r.height, 0, 'align-items-matrix-0053 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 55, 'align-items-matrix-0053 item[2].x');
      expectClose(r.y - container.y, 150, 'align-items-matrix-0053 item[2].y');
      expectClose(r.width, 40, 'align-items-matrix-0053 item[2].w');
      expectClose(r.height, 20, 'align-items-matrix-0053 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 235, 'align-items-matrix-0053 item[3].x');
      expectClose(r.y - container.y, 170, 'align-items-matrix-0053 item[3].y');
      expectClose(r.width, 0, 'align-items-matrix-0053 item[3].w');
      expectClose(r.height, 0, 'align-items-matrix-0053 item[3].h');
    }
  });
  it("align-items-matrix-0054: justify-items:center align-items:stretch", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid","width":600,"gridTemplateColumns":"150px 150px","gridTemplateRows":"80px 80px","gap":10,"justifyItems":"center","alignItems":"stretch"}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":40,"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":40,"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'align-items-matrix-0054 container.width');
    expectClose(container.height, 170, 'align-items-matrix-0054 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 55, 'align-items-matrix-0054 item[0].x');
      expectClose(r.y - container.y, 0, 'align-items-matrix-0054 item[0].y');
      expectClose(r.width, 40, 'align-items-matrix-0054 item[0].w');
      expectClose(r.height, 20, 'align-items-matrix-0054 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 235, 'align-items-matrix-0054 item[1].x');
      expectClose(r.y - container.y, 0, 'align-items-matrix-0054 item[1].y');
      expectClose(r.width, 0, 'align-items-matrix-0054 item[1].w');
      expectClose(r.height, 80, 'align-items-matrix-0054 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 55, 'align-items-matrix-0054 item[2].x');
      expectClose(r.y - container.y, 90, 'align-items-matrix-0054 item[2].y');
      expectClose(r.width, 40, 'align-items-matrix-0054 item[2].w');
      expectClose(r.height, 20, 'align-items-matrix-0054 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 235, 'align-items-matrix-0054 item[3].x');
      expectClose(r.y - container.y, 90, 'align-items-matrix-0054 item[3].y');
      expectClose(r.width, 0, 'align-items-matrix-0054 item[3].w');
      expectClose(r.height, 80, 'align-items-matrix-0054 item[3].h');
    }
  });
  it("align-items-matrix-0055: justify-items:end align-items:start", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid","width":600,"gridTemplateColumns":"150px 150px","gridTemplateRows":"80px 80px","gap":10,"justifyItems":"end","alignItems":"start"}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":40,"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":40,"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'align-items-matrix-0055 container.width');
    expectClose(container.height, 170, 'align-items-matrix-0055 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 110, 'align-items-matrix-0055 item[0].x');
      expectClose(r.y - container.y, 0, 'align-items-matrix-0055 item[0].y');
      expectClose(r.width, 40, 'align-items-matrix-0055 item[0].w');
      expectClose(r.height, 20, 'align-items-matrix-0055 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 310, 'align-items-matrix-0055 item[1].x');
      expectClose(r.y - container.y, 0, 'align-items-matrix-0055 item[1].y');
      expectClose(r.width, 0, 'align-items-matrix-0055 item[1].w');
      expectClose(r.height, 0, 'align-items-matrix-0055 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 110, 'align-items-matrix-0055 item[2].x');
      expectClose(r.y - container.y, 90, 'align-items-matrix-0055 item[2].y');
      expectClose(r.width, 40, 'align-items-matrix-0055 item[2].w');
      expectClose(r.height, 20, 'align-items-matrix-0055 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 310, 'align-items-matrix-0055 item[3].x');
      expectClose(r.y - container.y, 90, 'align-items-matrix-0055 item[3].y');
      expectClose(r.width, 0, 'align-items-matrix-0055 item[3].w');
      expectClose(r.height, 0, 'align-items-matrix-0055 item[3].h');
    }
  });
  it("align-items-matrix-0056: justify-items:end align-items:center", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid","width":600,"gridTemplateColumns":"150px 150px","gridTemplateRows":"80px 80px","gap":10,"justifyItems":"end","alignItems":"center"}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":40,"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":40,"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'align-items-matrix-0056 container.width');
    expectClose(container.height, 170, 'align-items-matrix-0056 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 110, 'align-items-matrix-0056 item[0].x');
      expectClose(r.y - container.y, 30, 'align-items-matrix-0056 item[0].y');
      expectClose(r.width, 40, 'align-items-matrix-0056 item[0].w');
      expectClose(r.height, 20, 'align-items-matrix-0056 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 310, 'align-items-matrix-0056 item[1].x');
      expectClose(r.y - container.y, 40, 'align-items-matrix-0056 item[1].y');
      expectClose(r.width, 0, 'align-items-matrix-0056 item[1].w');
      expectClose(r.height, 0, 'align-items-matrix-0056 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 110, 'align-items-matrix-0056 item[2].x');
      expectClose(r.y - container.y, 120, 'align-items-matrix-0056 item[2].y');
      expectClose(r.width, 40, 'align-items-matrix-0056 item[2].w');
      expectClose(r.height, 20, 'align-items-matrix-0056 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 310, 'align-items-matrix-0056 item[3].x');
      expectClose(r.y - container.y, 130, 'align-items-matrix-0056 item[3].y');
      expectClose(r.width, 0, 'align-items-matrix-0056 item[3].w');
      expectClose(r.height, 0, 'align-items-matrix-0056 item[3].h');
    }
  });
  it("align-items-matrix-0057: justify-items:end align-items:end", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid","width":600,"gridTemplateColumns":"150px 150px","gridTemplateRows":"80px 80px","gap":10,"justifyItems":"end","alignItems":"end"}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":40,"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":40,"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'align-items-matrix-0057 container.width');
    expectClose(container.height, 170, 'align-items-matrix-0057 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 110, 'align-items-matrix-0057 item[0].x');
      expectClose(r.y - container.y, 60, 'align-items-matrix-0057 item[0].y');
      expectClose(r.width, 40, 'align-items-matrix-0057 item[0].w');
      expectClose(r.height, 20, 'align-items-matrix-0057 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 310, 'align-items-matrix-0057 item[1].x');
      expectClose(r.y - container.y, 80, 'align-items-matrix-0057 item[1].y');
      expectClose(r.width, 0, 'align-items-matrix-0057 item[1].w');
      expectClose(r.height, 0, 'align-items-matrix-0057 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 110, 'align-items-matrix-0057 item[2].x');
      expectClose(r.y - container.y, 150, 'align-items-matrix-0057 item[2].y');
      expectClose(r.width, 40, 'align-items-matrix-0057 item[2].w');
      expectClose(r.height, 20, 'align-items-matrix-0057 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 310, 'align-items-matrix-0057 item[3].x');
      expectClose(r.y - container.y, 170, 'align-items-matrix-0057 item[3].y');
      expectClose(r.width, 0, 'align-items-matrix-0057 item[3].w');
      expectClose(r.height, 0, 'align-items-matrix-0057 item[3].h');
    }
  });
  it("align-items-matrix-0058: justify-items:end align-items:stretch", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid","width":600,"gridTemplateColumns":"150px 150px","gridTemplateRows":"80px 80px","gap":10,"justifyItems":"end","alignItems":"stretch"}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":40,"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":40,"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'align-items-matrix-0058 container.width');
    expectClose(container.height, 170, 'align-items-matrix-0058 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 110, 'align-items-matrix-0058 item[0].x');
      expectClose(r.y - container.y, 0, 'align-items-matrix-0058 item[0].y');
      expectClose(r.width, 40, 'align-items-matrix-0058 item[0].w');
      expectClose(r.height, 20, 'align-items-matrix-0058 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 310, 'align-items-matrix-0058 item[1].x');
      expectClose(r.y - container.y, 0, 'align-items-matrix-0058 item[1].y');
      expectClose(r.width, 0, 'align-items-matrix-0058 item[1].w');
      expectClose(r.height, 80, 'align-items-matrix-0058 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 110, 'align-items-matrix-0058 item[2].x');
      expectClose(r.y - container.y, 90, 'align-items-matrix-0058 item[2].y');
      expectClose(r.width, 40, 'align-items-matrix-0058 item[2].w');
      expectClose(r.height, 20, 'align-items-matrix-0058 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 310, 'align-items-matrix-0058 item[3].x');
      expectClose(r.y - container.y, 90, 'align-items-matrix-0058 item[3].y');
      expectClose(r.width, 0, 'align-items-matrix-0058 item[3].w');
      expectClose(r.height, 80, 'align-items-matrix-0058 item[3].h');
    }
  });
  it("align-items-matrix-0059: justify-items:stretch align-items:start", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid","width":600,"gridTemplateColumns":"150px 150px","gridTemplateRows":"80px 80px","gap":10,"justifyItems":"stretch","alignItems":"start"}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":40,"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":40,"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'align-items-matrix-0059 container.width');
    expectClose(container.height, 170, 'align-items-matrix-0059 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'align-items-matrix-0059 item[0].x');
      expectClose(r.y - container.y, 0, 'align-items-matrix-0059 item[0].y');
      expectClose(r.width, 40, 'align-items-matrix-0059 item[0].w');
      expectClose(r.height, 20, 'align-items-matrix-0059 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 160, 'align-items-matrix-0059 item[1].x');
      expectClose(r.y - container.y, 0, 'align-items-matrix-0059 item[1].y');
      expectClose(r.width, 150, 'align-items-matrix-0059 item[1].w');
      expectClose(r.height, 0, 'align-items-matrix-0059 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 0, 'align-items-matrix-0059 item[2].x');
      expectClose(r.y - container.y, 90, 'align-items-matrix-0059 item[2].y');
      expectClose(r.width, 40, 'align-items-matrix-0059 item[2].w');
      expectClose(r.height, 20, 'align-items-matrix-0059 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 160, 'align-items-matrix-0059 item[3].x');
      expectClose(r.y - container.y, 90, 'align-items-matrix-0059 item[3].y');
      expectClose(r.width, 150, 'align-items-matrix-0059 item[3].w');
      expectClose(r.height, 0, 'align-items-matrix-0059 item[3].h');
    }
  });
  it("align-items-matrix-0060: justify-items:stretch align-items:center", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid","width":600,"gridTemplateColumns":"150px 150px","gridTemplateRows":"80px 80px","gap":10,"justifyItems":"stretch","alignItems":"center"}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":40,"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":40,"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'align-items-matrix-0060 container.width');
    expectClose(container.height, 170, 'align-items-matrix-0060 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'align-items-matrix-0060 item[0].x');
      expectClose(r.y - container.y, 30, 'align-items-matrix-0060 item[0].y');
      expectClose(r.width, 40, 'align-items-matrix-0060 item[0].w');
      expectClose(r.height, 20, 'align-items-matrix-0060 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 160, 'align-items-matrix-0060 item[1].x');
      expectClose(r.y - container.y, 40, 'align-items-matrix-0060 item[1].y');
      expectClose(r.width, 150, 'align-items-matrix-0060 item[1].w');
      expectClose(r.height, 0, 'align-items-matrix-0060 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 0, 'align-items-matrix-0060 item[2].x');
      expectClose(r.y - container.y, 120, 'align-items-matrix-0060 item[2].y');
      expectClose(r.width, 40, 'align-items-matrix-0060 item[2].w');
      expectClose(r.height, 20, 'align-items-matrix-0060 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 160, 'align-items-matrix-0060 item[3].x');
      expectClose(r.y - container.y, 130, 'align-items-matrix-0060 item[3].y');
      expectClose(r.width, 150, 'align-items-matrix-0060 item[3].w');
      expectClose(r.height, 0, 'align-items-matrix-0060 item[3].h');
    }
  });
  it("align-items-matrix-0061: justify-items:stretch align-items:end", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid","width":600,"gridTemplateColumns":"150px 150px","gridTemplateRows":"80px 80px","gap":10,"justifyItems":"stretch","alignItems":"end"}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":40,"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":40,"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'align-items-matrix-0061 container.width');
    expectClose(container.height, 170, 'align-items-matrix-0061 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'align-items-matrix-0061 item[0].x');
      expectClose(r.y - container.y, 60, 'align-items-matrix-0061 item[0].y');
      expectClose(r.width, 40, 'align-items-matrix-0061 item[0].w');
      expectClose(r.height, 20, 'align-items-matrix-0061 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 160, 'align-items-matrix-0061 item[1].x');
      expectClose(r.y - container.y, 80, 'align-items-matrix-0061 item[1].y');
      expectClose(r.width, 150, 'align-items-matrix-0061 item[1].w');
      expectClose(r.height, 0, 'align-items-matrix-0061 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 0, 'align-items-matrix-0061 item[2].x');
      expectClose(r.y - container.y, 150, 'align-items-matrix-0061 item[2].y');
      expectClose(r.width, 40, 'align-items-matrix-0061 item[2].w');
      expectClose(r.height, 20, 'align-items-matrix-0061 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 160, 'align-items-matrix-0061 item[3].x');
      expectClose(r.y - container.y, 170, 'align-items-matrix-0061 item[3].y');
      expectClose(r.width, 150, 'align-items-matrix-0061 item[3].w');
      expectClose(r.height, 0, 'align-items-matrix-0061 item[3].h');
    }
  });
  it("align-items-matrix-0062: justify-items:stretch align-items:stretch", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid","width":600,"gridTemplateColumns":"150px 150px","gridTemplateRows":"80px 80px","gap":10,"justifyItems":"stretch","alignItems":"stretch"}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":40,"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":40,"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'align-items-matrix-0062 container.width');
    expectClose(container.height, 170, 'align-items-matrix-0062 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'align-items-matrix-0062 item[0].x');
      expectClose(r.y - container.y, 0, 'align-items-matrix-0062 item[0].y');
      expectClose(r.width, 40, 'align-items-matrix-0062 item[0].w');
      expectClose(r.height, 20, 'align-items-matrix-0062 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 160, 'align-items-matrix-0062 item[1].x');
      expectClose(r.y - container.y, 0, 'align-items-matrix-0062 item[1].y');
      expectClose(r.width, 150, 'align-items-matrix-0062 item[1].w');
      expectClose(r.height, 80, 'align-items-matrix-0062 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 0, 'align-items-matrix-0062 item[2].x');
      expectClose(r.y - container.y, 90, 'align-items-matrix-0062 item[2].y');
      expectClose(r.width, 40, 'align-items-matrix-0062 item[2].w');
      expectClose(r.height, 20, 'align-items-matrix-0062 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 160, 'align-items-matrix-0062 item[3].x');
      expectClose(r.y - container.y, 90, 'align-items-matrix-0062 item[3].y');
      expectClose(r.width, 150, 'align-items-matrix-0062 item[3].w');
      expectClose(r.height, 80, 'align-items-matrix-0062 item[3].h');
    }
  });
});

describe("justify-content", () => {
  it("justify-content-0063: justify-content:start with 300px of slack", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid","width":500,"gridTemplateColumns":"100px 100px","justifyContent":"start"}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":40,"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":40,"height":20}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 500, 'justify-content-0063 container.width');
    expectClose(container.height, 20, 'justify-content-0063 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'justify-content-0063 item[0].x');
      expectClose(r.y - container.y, 0, 'justify-content-0063 item[0].y');
      expectClose(r.width, 40, 'justify-content-0063 item[0].w');
      expectClose(r.height, 20, 'justify-content-0063 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 100, 'justify-content-0063 item[1].x');
      expectClose(r.y - container.y, 0, 'justify-content-0063 item[1].y');
      expectClose(r.width, 40, 'justify-content-0063 item[1].w');
      expectClose(r.height, 20, 'justify-content-0063 item[1].h');
    }
  });
  it("justify-content-0064: justify-content:center with 300px of slack", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid","width":500,"gridTemplateColumns":"100px 100px","justifyContent":"center"}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":40,"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":40,"height":20}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 500, 'justify-content-0064 container.width');
    expectClose(container.height, 20, 'justify-content-0064 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 150, 'justify-content-0064 item[0].x');
      expectClose(r.y - container.y, 0, 'justify-content-0064 item[0].y');
      expectClose(r.width, 40, 'justify-content-0064 item[0].w');
      expectClose(r.height, 20, 'justify-content-0064 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 250, 'justify-content-0064 item[1].x');
      expectClose(r.y - container.y, 0, 'justify-content-0064 item[1].y');
      expectClose(r.width, 40, 'justify-content-0064 item[1].w');
      expectClose(r.height, 20, 'justify-content-0064 item[1].h');
    }
  });
  it("justify-content-0065: justify-content:end with 300px of slack", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid","width":500,"gridTemplateColumns":"100px 100px","justifyContent":"end"}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":40,"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":40,"height":20}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 500, 'justify-content-0065 container.width');
    expectClose(container.height, 20, 'justify-content-0065 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 300, 'justify-content-0065 item[0].x');
      expectClose(r.y - container.y, 0, 'justify-content-0065 item[0].y');
      expectClose(r.width, 40, 'justify-content-0065 item[0].w');
      expectClose(r.height, 20, 'justify-content-0065 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 400, 'justify-content-0065 item[1].x');
      expectClose(r.y - container.y, 0, 'justify-content-0065 item[1].y');
      expectClose(r.width, 40, 'justify-content-0065 item[1].w');
      expectClose(r.height, 20, 'justify-content-0065 item[1].h');
    }
  });
  it("justify-content-0066: justify-content:space-between with 300px of slack", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid","width":500,"gridTemplateColumns":"100px 100px","justifyContent":"space-between"}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":40,"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":40,"height":20}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 500, 'justify-content-0066 container.width');
    expectClose(container.height, 20, 'justify-content-0066 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'justify-content-0066 item[0].x');
      expectClose(r.y - container.y, 0, 'justify-content-0066 item[0].y');
      expectClose(r.width, 40, 'justify-content-0066 item[0].w');
      expectClose(r.height, 20, 'justify-content-0066 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 400, 'justify-content-0066 item[1].x');
      expectClose(r.y - container.y, 0, 'justify-content-0066 item[1].y');
      expectClose(r.width, 40, 'justify-content-0066 item[1].w');
      expectClose(r.height, 20, 'justify-content-0066 item[1].h');
    }
  });
  it("justify-content-0067: justify-content:space-around with 300px of slack", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid","width":500,"gridTemplateColumns":"100px 100px","justifyContent":"space-around"}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":40,"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":40,"height":20}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 500, 'justify-content-0067 container.width');
    expectClose(container.height, 20, 'justify-content-0067 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 75, 'justify-content-0067 item[0].x');
      expectClose(r.y - container.y, 0, 'justify-content-0067 item[0].y');
      expectClose(r.width, 40, 'justify-content-0067 item[0].w');
      expectClose(r.height, 20, 'justify-content-0067 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 325, 'justify-content-0067 item[1].x');
      expectClose(r.y - container.y, 0, 'justify-content-0067 item[1].y');
      expectClose(r.width, 40, 'justify-content-0067 item[1].w');
      expectClose(r.height, 20, 'justify-content-0067 item[1].h');
    }
  });
  it("justify-content-0068: justify-content:space-evenly with 300px of slack", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid","width":500,"gridTemplateColumns":"100px 100px","justifyContent":"space-evenly"}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":40,"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":40,"height":20}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 500, 'justify-content-0068 container.width');
    expectClose(container.height, 20, 'justify-content-0068 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 100, 'justify-content-0068 item[0].x');
      expectClose(r.y - container.y, 0, 'justify-content-0068 item[0].y');
      expectClose(r.width, 40, 'justify-content-0068 item[0].w');
      expectClose(r.height, 20, 'justify-content-0068 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 300, 'justify-content-0068 item[1].x');
      expectClose(r.y - container.y, 0, 'justify-content-0068 item[1].y');
      expectClose(r.width, 40, 'justify-content-0068 item[1].w');
      expectClose(r.height, 20, 'justify-content-0068 item[1].h');
    }
  });
});

describe("justify-self", () => {
  it("justify-self-0069: justify-self:start overrides justify-items:start", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid","width":600,"gridTemplateColumns":"150px 150px","gap":10,"justifyItems":"start"}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":40,"height":20,"justifySelf":"start"}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":40,"height":20}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'justify-self-0069 container.width');
    expectClose(container.height, 20, 'justify-self-0069 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'justify-self-0069 item[0].x');
      expectClose(r.y - container.y, 0, 'justify-self-0069 item[0].y');
      expectClose(r.width, 40, 'justify-self-0069 item[0].w');
      expectClose(r.height, 20, 'justify-self-0069 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 160, 'justify-self-0069 item[1].x');
      expectClose(r.y - container.y, 0, 'justify-self-0069 item[1].y');
      expectClose(r.width, 40, 'justify-self-0069 item[1].w');
      expectClose(r.height, 20, 'justify-self-0069 item[1].h');
    }
  });
  it("justify-self-0070: justify-self:center overrides justify-items:start", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid","width":600,"gridTemplateColumns":"150px 150px","gap":10,"justifyItems":"start"}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":40,"height":20,"justifySelf":"center"}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":40,"height":20}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'justify-self-0070 container.width');
    expectClose(container.height, 20, 'justify-self-0070 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 55, 'justify-self-0070 item[0].x');
      expectClose(r.y - container.y, 0, 'justify-self-0070 item[0].y');
      expectClose(r.width, 40, 'justify-self-0070 item[0].w');
      expectClose(r.height, 20, 'justify-self-0070 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 160, 'justify-self-0070 item[1].x');
      expectClose(r.y - container.y, 0, 'justify-self-0070 item[1].y');
      expectClose(r.width, 40, 'justify-self-0070 item[1].w');
      expectClose(r.height, 20, 'justify-self-0070 item[1].h');
    }
  });
  it("justify-self-0071: justify-self:end overrides justify-items:start", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid","width":600,"gridTemplateColumns":"150px 150px","gap":10,"justifyItems":"start"}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":40,"height":20,"justifySelf":"end"}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":40,"height":20}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'justify-self-0071 container.width');
    expectClose(container.height, 20, 'justify-self-0071 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 110, 'justify-self-0071 item[0].x');
      expectClose(r.y - container.y, 0, 'justify-self-0071 item[0].y');
      expectClose(r.width, 40, 'justify-self-0071 item[0].w');
      expectClose(r.height, 20, 'justify-self-0071 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 160, 'justify-self-0071 item[1].x');
      expectClose(r.y - container.y, 0, 'justify-self-0071 item[1].y');
      expectClose(r.width, 40, 'justify-self-0071 item[1].w');
      expectClose(r.height, 20, 'justify-self-0071 item[1].h');
    }
  });
});

describe("lanes-basic", () => {
  it("lanes-basic-0072: canonical waterfall: shortest-lane placement", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid-lanes","width":600,"gridTemplateColumns":"1fr 1fr 1fr","gap":10,"flowTolerance":0}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":50}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":70}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":40}}
          />
          <View
            collapsable={false}
            ref={itemRefs[4]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":60}}
          />
          <View
            collapsable={false}
            ref={itemRefs[5]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":20}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'lanes-basic-0072 container.width');
    expectClose(container.height, 120, 'lanes-basic-0072 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'lanes-basic-0072 item[0].x');
      expectClose(r.y - container.y, 0, 'lanes-basic-0072 item[0].y');
      expectClose(r.width, 193.33, 'lanes-basic-0072 item[0].w');
      expectClose(r.height, 50, 'lanes-basic-0072 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 203.33, 'lanes-basic-0072 item[1].x');
      expectClose(r.y - container.y, 0, 'lanes-basic-0072 item[1].y');
      expectClose(r.width, 193.33, 'lanes-basic-0072 item[1].w');
      expectClose(r.height, 30, 'lanes-basic-0072 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 406.66, 'lanes-basic-0072 item[2].x');
      expectClose(r.y - container.y, 0, 'lanes-basic-0072 item[2].y');
      expectClose(r.width, 193.34, 'lanes-basic-0072 item[2].w');
      expectClose(r.height, 70, 'lanes-basic-0072 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 203.33, 'lanes-basic-0072 item[3].x');
      expectClose(r.y - container.y, 40, 'lanes-basic-0072 item[3].y');
      expectClose(r.width, 193.33, 'lanes-basic-0072 item[3].w');
      expectClose(r.height, 40, 'lanes-basic-0072 item[3].h');
    }
    {
      const r = rectOf(itemRefs[4]);
      expectClose(r.x - container.x, 0, 'lanes-basic-0072 item[4].x');
      expectClose(r.y - container.y, 60, 'lanes-basic-0072 item[4].y');
      expectClose(r.width, 193.33, 'lanes-basic-0072 item[4].w');
      expectClose(r.height, 60, 'lanes-basic-0072 item[4].h');
    }
    {
      const r = rectOf(itemRefs[5]);
      expectClose(r.x - container.x, 406.66, 'lanes-basic-0072 item[5].x');
      expectClose(r.y - container.y, 80, 'lanes-basic-0072 item[5].y');
      expectClose(r.width, 193.34, 'lanes-basic-0072 item[5].w');
      expectClose(r.height, 20, 'lanes-basic-0072 item[5].h');
    }
  });
});

describe("lanes-count", () => {
  it("lanes-count-0073: 1 lanes", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid-lanes","width":600,"gridTemplateColumns":"1fr","gap":10,"flowTolerance":0}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":40}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":55}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":25}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":70}}
          />
          <View
            collapsable={false}
            ref={itemRefs[4]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":35}}
          />
          <View
            collapsable={false}
            ref={itemRefs[5]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":50}}
          />
          <View
            collapsable={false}
            ref={itemRefs[6]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":45}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'lanes-count-0073 container.width');
    expectClose(container.height, 380, 'lanes-count-0073 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'lanes-count-0073 item[0].x');
      expectClose(r.y - container.y, 0, 'lanes-count-0073 item[0].y');
      expectClose(r.width, 600, 'lanes-count-0073 item[0].w');
      expectClose(r.height, 40, 'lanes-count-0073 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 0, 'lanes-count-0073 item[1].x');
      expectClose(r.y - container.y, 50, 'lanes-count-0073 item[1].y');
      expectClose(r.width, 600, 'lanes-count-0073 item[1].w');
      expectClose(r.height, 55, 'lanes-count-0073 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 0, 'lanes-count-0073 item[2].x');
      expectClose(r.y - container.y, 115, 'lanes-count-0073 item[2].y');
      expectClose(r.width, 600, 'lanes-count-0073 item[2].w');
      expectClose(r.height, 25, 'lanes-count-0073 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 0, 'lanes-count-0073 item[3].x');
      expectClose(r.y - container.y, 150, 'lanes-count-0073 item[3].y');
      expectClose(r.width, 600, 'lanes-count-0073 item[3].w');
      expectClose(r.height, 70, 'lanes-count-0073 item[3].h');
    }
    {
      const r = rectOf(itemRefs[4]);
      expectClose(r.x - container.x, 0, 'lanes-count-0073 item[4].x');
      expectClose(r.y - container.y, 230, 'lanes-count-0073 item[4].y');
      expectClose(r.width, 600, 'lanes-count-0073 item[4].w');
      expectClose(r.height, 35, 'lanes-count-0073 item[4].h');
    }
    {
      const r = rectOf(itemRefs[5]);
      expectClose(r.x - container.x, 0, 'lanes-count-0073 item[5].x');
      expectClose(r.y - container.y, 275, 'lanes-count-0073 item[5].y');
      expectClose(r.width, 600, 'lanes-count-0073 item[5].w');
      expectClose(r.height, 50, 'lanes-count-0073 item[5].h');
    }
    {
      const r = rectOf(itemRefs[6]);
      expectClose(r.x - container.x, 0, 'lanes-count-0073 item[6].x');
      expectClose(r.y - container.y, 335, 'lanes-count-0073 item[6].y');
      expectClose(r.width, 600, 'lanes-count-0073 item[6].w');
      expectClose(r.height, 45, 'lanes-count-0073 item[6].h');
    }
  });
  it("lanes-count-0074: 2 lanes", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid-lanes","width":600,"gridTemplateColumns":"1fr 1fr","gap":10,"flowTolerance":0}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":40}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":55}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":25}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":70}}
          />
          <View
            collapsable={false}
            ref={itemRefs[4]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":35}}
          />
          <View
            collapsable={false}
            ref={itemRefs[5]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":50}}
          />
          <View
            collapsable={false}
            ref={itemRefs[6]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":45}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'lanes-count-0074 container.width');
    expectClose(container.height, 190, 'lanes-count-0074 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'lanes-count-0074 item[0].x');
      expectClose(r.y - container.y, 0, 'lanes-count-0074 item[0].y');
      expectClose(r.width, 295, 'lanes-count-0074 item[0].w');
      expectClose(r.height, 40, 'lanes-count-0074 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 305, 'lanes-count-0074 item[1].x');
      expectClose(r.y - container.y, 0, 'lanes-count-0074 item[1].y');
      expectClose(r.width, 295, 'lanes-count-0074 item[1].w');
      expectClose(r.height, 55, 'lanes-count-0074 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 0, 'lanes-count-0074 item[2].x');
      expectClose(r.y - container.y, 50, 'lanes-count-0074 item[2].y');
      expectClose(r.width, 295, 'lanes-count-0074 item[2].w');
      expectClose(r.height, 25, 'lanes-count-0074 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 305, 'lanes-count-0074 item[3].x');
      expectClose(r.y - container.y, 65, 'lanes-count-0074 item[3].y');
      expectClose(r.width, 295, 'lanes-count-0074 item[3].w');
      expectClose(r.height, 70, 'lanes-count-0074 item[3].h');
    }
    {
      const r = rectOf(itemRefs[4]);
      expectClose(r.x - container.x, 0, 'lanes-count-0074 item[4].x');
      expectClose(r.y - container.y, 85, 'lanes-count-0074 item[4].y');
      expectClose(r.width, 295, 'lanes-count-0074 item[4].w');
      expectClose(r.height, 35, 'lanes-count-0074 item[4].h');
    }
    {
      const r = rectOf(itemRefs[5]);
      expectClose(r.x - container.x, 0, 'lanes-count-0074 item[5].x');
      expectClose(r.y - container.y, 130, 'lanes-count-0074 item[5].y');
      expectClose(r.width, 295, 'lanes-count-0074 item[5].w');
      expectClose(r.height, 50, 'lanes-count-0074 item[5].h');
    }
    {
      const r = rectOf(itemRefs[6]);
      expectClose(r.x - container.x, 305, 'lanes-count-0074 item[6].x');
      expectClose(r.y - container.y, 145, 'lanes-count-0074 item[6].y');
      expectClose(r.width, 295, 'lanes-count-0074 item[6].w');
      expectClose(r.height, 45, 'lanes-count-0074 item[6].h');
    }
  });
  it("lanes-count-0075: 3 lanes", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid-lanes","width":600,"gridTemplateColumns":"1fr 1fr 1fr","gap":10,"flowTolerance":0}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":40}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":55}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":25}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":70}}
          />
          <View
            collapsable={false}
            ref={itemRefs[4]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":35}}
          />
          <View
            collapsable={false}
            ref={itemRefs[5]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":50}}
          />
          <View
            collapsable={false}
            ref={itemRefs[6]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":45}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'lanes-count-0075 container.width');
    expectClose(container.height, 140, 'lanes-count-0075 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'lanes-count-0075 item[0].x');
      expectClose(r.y - container.y, 0, 'lanes-count-0075 item[0].y');
      expectClose(r.width, 193.33, 'lanes-count-0075 item[0].w');
      expectClose(r.height, 40, 'lanes-count-0075 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 203.33, 'lanes-count-0075 item[1].x');
      expectClose(r.y - container.y, 0, 'lanes-count-0075 item[1].y');
      expectClose(r.width, 193.33, 'lanes-count-0075 item[1].w');
      expectClose(r.height, 55, 'lanes-count-0075 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 406.66, 'lanes-count-0075 item[2].x');
      expectClose(r.y - container.y, 0, 'lanes-count-0075 item[2].y');
      expectClose(r.width, 193.34, 'lanes-count-0075 item[2].w');
      expectClose(r.height, 25, 'lanes-count-0075 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 406.66, 'lanes-count-0075 item[3].x');
      expectClose(r.y - container.y, 35, 'lanes-count-0075 item[3].y');
      expectClose(r.width, 193.34, 'lanes-count-0075 item[3].w');
      expectClose(r.height, 70, 'lanes-count-0075 item[3].h');
    }
    {
      const r = rectOf(itemRefs[4]);
      expectClose(r.x - container.x, 0, 'lanes-count-0075 item[4].x');
      expectClose(r.y - container.y, 50, 'lanes-count-0075 item[4].y');
      expectClose(r.width, 193.33, 'lanes-count-0075 item[4].w');
      expectClose(r.height, 35, 'lanes-count-0075 item[4].h');
    }
    {
      const r = rectOf(itemRefs[5]);
      expectClose(r.x - container.x, 203.33, 'lanes-count-0075 item[5].x');
      expectClose(r.y - container.y, 65, 'lanes-count-0075 item[5].y');
      expectClose(r.width, 193.33, 'lanes-count-0075 item[5].w');
      expectClose(r.height, 50, 'lanes-count-0075 item[5].h');
    }
    {
      const r = rectOf(itemRefs[6]);
      expectClose(r.x - container.x, 0, 'lanes-count-0075 item[6].x');
      expectClose(r.y - container.y, 95, 'lanes-count-0075 item[6].y');
      expectClose(r.width, 193.33, 'lanes-count-0075 item[6].w');
      expectClose(r.height, 45, 'lanes-count-0075 item[6].h');
    }
  });
  it("lanes-count-0076: 4 lanes", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid-lanes","width":600,"gridTemplateColumns":"1fr 1fr 1fr 1fr","gap":10,"flowTolerance":0}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":40}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":55}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":25}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":70}}
          />
          <View
            collapsable={false}
            ref={itemRefs[4]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":35}}
          />
          <View
            collapsable={false}
            ref={itemRefs[5]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":50}}
          />
          <View
            collapsable={false}
            ref={itemRefs[6]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":45}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'lanes-count-0076 container.width');
    expectClose(container.height, 110, 'lanes-count-0076 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'lanes-count-0076 item[0].x');
      expectClose(r.y - container.y, 0, 'lanes-count-0076 item[0].y');
      expectClose(r.width, 142.5, 'lanes-count-0076 item[0].w');
      expectClose(r.height, 40, 'lanes-count-0076 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 152.5, 'lanes-count-0076 item[1].x');
      expectClose(r.y - container.y, 0, 'lanes-count-0076 item[1].y');
      expectClose(r.width, 142.5, 'lanes-count-0076 item[1].w');
      expectClose(r.height, 55, 'lanes-count-0076 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 305, 'lanes-count-0076 item[2].x');
      expectClose(r.y - container.y, 0, 'lanes-count-0076 item[2].y');
      expectClose(r.width, 142.5, 'lanes-count-0076 item[2].w');
      expectClose(r.height, 25, 'lanes-count-0076 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 457.5, 'lanes-count-0076 item[3].x');
      expectClose(r.y - container.y, 0, 'lanes-count-0076 item[3].y');
      expectClose(r.width, 142.5, 'lanes-count-0076 item[3].w');
      expectClose(r.height, 70, 'lanes-count-0076 item[3].h');
    }
    {
      const r = rectOf(itemRefs[4]);
      expectClose(r.x - container.x, 305, 'lanes-count-0076 item[4].x');
      expectClose(r.y - container.y, 35, 'lanes-count-0076 item[4].y');
      expectClose(r.width, 142.5, 'lanes-count-0076 item[4].w');
      expectClose(r.height, 35, 'lanes-count-0076 item[4].h');
    }
    {
      const r = rectOf(itemRefs[5]);
      expectClose(r.x - container.x, 0, 'lanes-count-0076 item[5].x');
      expectClose(r.y - container.y, 50, 'lanes-count-0076 item[5].y');
      expectClose(r.width, 142.5, 'lanes-count-0076 item[5].w');
      expectClose(r.height, 50, 'lanes-count-0076 item[5].h');
    }
    {
      const r = rectOf(itemRefs[6]);
      expectClose(r.x - container.x, 152.5, 'lanes-count-0076 item[6].x');
      expectClose(r.y - container.y, 65, 'lanes-count-0076 item[6].y');
      expectClose(r.width, 142.5, 'lanes-count-0076 item[6].w');
      expectClose(r.height, 45, 'lanes-count-0076 item[6].h');
    }
  });
  it("lanes-count-0077: 5 lanes", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid-lanes","width":600,"gridTemplateColumns":"1fr 1fr 1fr 1fr 1fr","gap":10,"flowTolerance":0}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":40}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":55}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":25}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":70}}
          />
          <View
            collapsable={false}
            ref={itemRefs[4]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":35}}
          />
          <View
            collapsable={false}
            ref={itemRefs[5]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":50}}
          />
          <View
            collapsable={false}
            ref={itemRefs[6]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":45}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'lanes-count-0077 container.width');
    expectClose(container.height, 90, 'lanes-count-0077 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'lanes-count-0077 item[0].x');
      expectClose(r.y - container.y, 0, 'lanes-count-0077 item[0].y');
      expectClose(r.width, 112, 'lanes-count-0077 item[0].w');
      expectClose(r.height, 40, 'lanes-count-0077 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 122, 'lanes-count-0077 item[1].x');
      expectClose(r.y - container.y, 0, 'lanes-count-0077 item[1].y');
      expectClose(r.width, 112, 'lanes-count-0077 item[1].w');
      expectClose(r.height, 55, 'lanes-count-0077 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 244, 'lanes-count-0077 item[2].x');
      expectClose(r.y - container.y, 0, 'lanes-count-0077 item[2].y');
      expectClose(r.width, 112, 'lanes-count-0077 item[2].w');
      expectClose(r.height, 25, 'lanes-count-0077 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 366, 'lanes-count-0077 item[3].x');
      expectClose(r.y - container.y, 0, 'lanes-count-0077 item[3].y');
      expectClose(r.width, 112, 'lanes-count-0077 item[3].w');
      expectClose(r.height, 70, 'lanes-count-0077 item[3].h');
    }
    {
      const r = rectOf(itemRefs[4]);
      expectClose(r.x - container.x, 488, 'lanes-count-0077 item[4].x');
      expectClose(r.y - container.y, 0, 'lanes-count-0077 item[4].y');
      expectClose(r.width, 112, 'lanes-count-0077 item[4].w');
      expectClose(r.height, 35, 'lanes-count-0077 item[4].h');
    }
    {
      const r = rectOf(itemRefs[5]);
      expectClose(r.x - container.x, 244, 'lanes-count-0077 item[5].x');
      expectClose(r.y - container.y, 35, 'lanes-count-0077 item[5].y');
      expectClose(r.width, 112, 'lanes-count-0077 item[5].w');
      expectClose(r.height, 50, 'lanes-count-0077 item[5].h');
    }
    {
      const r = rectOf(itemRefs[6]);
      expectClose(r.x - container.x, 488, 'lanes-count-0077 item[6].x');
      expectClose(r.y - container.y, 45, 'lanes-count-0077 item[6].y');
      expectClose(r.width, 112, 'lanes-count-0077 item[6].w');
      expectClose(r.height, 45, 'lanes-count-0077 item[6].h');
    }
  });
});

describe("lanes-brick", () => {
  it("lanes-brick-0078: brick layout: grid-template-rows defines the lanes", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid-lanes","width":600,"height":300,"gridTemplateRows":"1fr 1fr 1fr","gap":10,"flowTolerance":0}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":50}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":30}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":70}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":40}}
          />
          <View
            collapsable={false}
            ref={itemRefs[4]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":60}}
          />
          <View
            collapsable={false}
            ref={itemRefs[5]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":20}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'lanes-brick-0078 container.width');
    expectClose(container.height, 300, 'lanes-brick-0078 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'lanes-brick-0078 item[0].x');
      expectClose(r.y - container.y, 0, 'lanes-brick-0078 item[0].y');
      expectClose(r.width, 50, 'lanes-brick-0078 item[0].w');
      expectClose(r.height, 93.33, 'lanes-brick-0078 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 0, 'lanes-brick-0078 item[1].x');
      expectClose(r.y - container.y, 103.33, 'lanes-brick-0078 item[1].y');
      expectClose(r.width, 30, 'lanes-brick-0078 item[1].w');
      expectClose(r.height, 93.33, 'lanes-brick-0078 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 0, 'lanes-brick-0078 item[2].x');
      expectClose(r.y - container.y, 206.66, 'lanes-brick-0078 item[2].y');
      expectClose(r.width, 70, 'lanes-brick-0078 item[2].w');
      expectClose(r.height, 93.33, 'lanes-brick-0078 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 40, 'lanes-brick-0078 item[3].x');
      expectClose(r.y - container.y, 103.33, 'lanes-brick-0078 item[3].y');
      expectClose(r.width, 40, 'lanes-brick-0078 item[3].w');
      expectClose(r.height, 93.33, 'lanes-brick-0078 item[3].h');
    }
    {
      const r = rectOf(itemRefs[4]);
      expectClose(r.x - container.x, 60, 'lanes-brick-0078 item[4].x');
      expectClose(r.y - container.y, 0, 'lanes-brick-0078 item[4].y');
      expectClose(r.width, 60, 'lanes-brick-0078 item[4].w');
      expectClose(r.height, 93.33, 'lanes-brick-0078 item[4].h');
    }
    {
      const r = rectOf(itemRefs[5]);
      expectClose(r.x - container.x, 80, 'lanes-brick-0078 item[5].x');
      expectClose(r.y - container.y, 206.66, 'lanes-brick-0078 item[5].y');
      expectClose(r.width, 20, 'lanes-brick-0078 item[5].w');
      expectClose(r.height, 93.33, 'lanes-brick-0078 item[5].h');
    }
  });
});

describe("lanes-tracks-fixed", () => {
  it("lanes-tracks-fixed-0079: lanes over fixed tracks", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid-lanes","width":600,"gridTemplateColumns":"150px 150px 150px","gap":10,"flowTolerance":0}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":40}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":60}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":50}}
          />
          <View
            collapsable={false}
            ref={itemRefs[4]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":45}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'lanes-tracks-fixed-0079 container.width');
    expectClose(container.height, 95, 'lanes-tracks-fixed-0079 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'lanes-tracks-fixed-0079 item[0].x');
      expectClose(r.y - container.y, 0, 'lanes-tracks-fixed-0079 item[0].y');
      expectClose(r.width, 150, 'lanes-tracks-fixed-0079 item[0].w');
      expectClose(r.height, 40, 'lanes-tracks-fixed-0079 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 160, 'lanes-tracks-fixed-0079 item[1].x');
      expectClose(r.y - container.y, 0, 'lanes-tracks-fixed-0079 item[1].y');
      expectClose(r.width, 150, 'lanes-tracks-fixed-0079 item[1].w');
      expectClose(r.height, 60, 'lanes-tracks-fixed-0079 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 320, 'lanes-tracks-fixed-0079 item[2].x');
      expectClose(r.y - container.y, 0, 'lanes-tracks-fixed-0079 item[2].y');
      expectClose(r.width, 150, 'lanes-tracks-fixed-0079 item[2].w');
      expectClose(r.height, 30, 'lanes-tracks-fixed-0079 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 320, 'lanes-tracks-fixed-0079 item[3].x');
      expectClose(r.y - container.y, 40, 'lanes-tracks-fixed-0079 item[3].y');
      expectClose(r.width, 150, 'lanes-tracks-fixed-0079 item[3].w');
      expectClose(r.height, 50, 'lanes-tracks-fixed-0079 item[3].h');
    }
    {
      const r = rectOf(itemRefs[4]);
      expectClose(r.x - container.x, 0, 'lanes-tracks-fixed-0079 item[4].x');
      expectClose(r.y - container.y, 50, 'lanes-tracks-fixed-0079 item[4].y');
      expectClose(r.width, 150, 'lanes-tracks-fixed-0079 item[4].w');
      expectClose(r.height, 45, 'lanes-tracks-fixed-0079 item[4].h');
    }
  });
});

describe("lanes-tracks-fr", () => {
  it("lanes-tracks-fr-0080: lanes over fr tracks", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid-lanes","width":600,"gridTemplateColumns":"1fr 1fr 1fr","gap":10,"flowTolerance":0}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":40}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":60}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":50}}
          />
          <View
            collapsable={false}
            ref={itemRefs[4]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":45}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'lanes-tracks-fr-0080 container.width');
    expectClose(container.height, 95, 'lanes-tracks-fr-0080 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'lanes-tracks-fr-0080 item[0].x');
      expectClose(r.y - container.y, 0, 'lanes-tracks-fr-0080 item[0].y');
      expectClose(r.width, 193.33, 'lanes-tracks-fr-0080 item[0].w');
      expectClose(r.height, 40, 'lanes-tracks-fr-0080 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 203.33, 'lanes-tracks-fr-0080 item[1].x');
      expectClose(r.y - container.y, 0, 'lanes-tracks-fr-0080 item[1].y');
      expectClose(r.width, 193.33, 'lanes-tracks-fr-0080 item[1].w');
      expectClose(r.height, 60, 'lanes-tracks-fr-0080 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 406.66, 'lanes-tracks-fr-0080 item[2].x');
      expectClose(r.y - container.y, 0, 'lanes-tracks-fr-0080 item[2].y');
      expectClose(r.width, 193.34, 'lanes-tracks-fr-0080 item[2].w');
      expectClose(r.height, 30, 'lanes-tracks-fr-0080 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 406.66, 'lanes-tracks-fr-0080 item[3].x');
      expectClose(r.y - container.y, 40, 'lanes-tracks-fr-0080 item[3].y');
      expectClose(r.width, 193.34, 'lanes-tracks-fr-0080 item[3].w');
      expectClose(r.height, 50, 'lanes-tracks-fr-0080 item[3].h');
    }
    {
      const r = rectOf(itemRefs[4]);
      expectClose(r.x - container.x, 0, 'lanes-tracks-fr-0080 item[4].x');
      expectClose(r.y - container.y, 50, 'lanes-tracks-fr-0080 item[4].y');
      expectClose(r.width, 193.33, 'lanes-tracks-fr-0080 item[4].w');
      expectClose(r.height, 45, 'lanes-tracks-fr-0080 item[4].h');
    }
  });
});

describe("lanes-tracks-mixed", () => {
  it("lanes-tracks-mixed-0081: lanes over mixed tracks", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid-lanes","width":600,"gridTemplateColumns":"120px 1fr 2fr","gap":10,"flowTolerance":0}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":40}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":60}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":50}}
          />
          <View
            collapsable={false}
            ref={itemRefs[4]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":45}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'lanes-tracks-mixed-0081 container.width');
    expectClose(container.height, 95, 'lanes-tracks-mixed-0081 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'lanes-tracks-mixed-0081 item[0].x');
      expectClose(r.y - container.y, 0, 'lanes-tracks-mixed-0081 item[0].y');
      expectClose(r.width, 120, 'lanes-tracks-mixed-0081 item[0].w');
      expectClose(r.height, 40, 'lanes-tracks-mixed-0081 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 130, 'lanes-tracks-mixed-0081 item[1].x');
      expectClose(r.y - container.y, 0, 'lanes-tracks-mixed-0081 item[1].y');
      expectClose(r.width, 153.33, 'lanes-tracks-mixed-0081 item[1].w');
      expectClose(r.height, 60, 'lanes-tracks-mixed-0081 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 293.33, 'lanes-tracks-mixed-0081 item[2].x');
      expectClose(r.y - container.y, 0, 'lanes-tracks-mixed-0081 item[2].y');
      expectClose(r.width, 306.67, 'lanes-tracks-mixed-0081 item[2].w');
      expectClose(r.height, 30, 'lanes-tracks-mixed-0081 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 293.33, 'lanes-tracks-mixed-0081 item[3].x');
      expectClose(r.y - container.y, 40, 'lanes-tracks-mixed-0081 item[3].y');
      expectClose(r.width, 306.67, 'lanes-tracks-mixed-0081 item[3].w');
      expectClose(r.height, 50, 'lanes-tracks-mixed-0081 item[3].h');
    }
    {
      const r = rectOf(itemRefs[4]);
      expectClose(r.x - container.x, 0, 'lanes-tracks-mixed-0081 item[4].x');
      expectClose(r.y - container.y, 50, 'lanes-tracks-mixed-0081 item[4].y');
      expectClose(r.width, 120, 'lanes-tracks-mixed-0081 item[4].w');
      expectClose(r.height, 45, 'lanes-tracks-mixed-0081 item[4].h');
    }
  });
});

describe("lanes-tracks-minmax", () => {
  it("lanes-tracks-minmax-0082: lanes over minmax tracks", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid-lanes","width":600,"gridTemplateColumns":"minmax(100px, 1fr) minmax(100px, 1fr)","gap":10,"flowTolerance":0}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":40}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":60}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":50}}
          />
          <View
            collapsable={false}
            ref={itemRefs[4]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":45}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'lanes-tracks-minmax-0082 container.width');
    expectClose(container.height, 135, 'lanes-tracks-minmax-0082 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'lanes-tracks-minmax-0082 item[0].x');
      expectClose(r.y - container.y, 0, 'lanes-tracks-minmax-0082 item[0].y');
      expectClose(r.width, 295, 'lanes-tracks-minmax-0082 item[0].w');
      expectClose(r.height, 40, 'lanes-tracks-minmax-0082 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 305, 'lanes-tracks-minmax-0082 item[1].x');
      expectClose(r.y - container.y, 0, 'lanes-tracks-minmax-0082 item[1].y');
      expectClose(r.width, 295, 'lanes-tracks-minmax-0082 item[1].w');
      expectClose(r.height, 60, 'lanes-tracks-minmax-0082 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 0, 'lanes-tracks-minmax-0082 item[2].x');
      expectClose(r.y - container.y, 50, 'lanes-tracks-minmax-0082 item[2].y');
      expectClose(r.width, 295, 'lanes-tracks-minmax-0082 item[2].w');
      expectClose(r.height, 30, 'lanes-tracks-minmax-0082 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 305, 'lanes-tracks-minmax-0082 item[3].x');
      expectClose(r.y - container.y, 70, 'lanes-tracks-minmax-0082 item[3].y');
      expectClose(r.width, 295, 'lanes-tracks-minmax-0082 item[3].w');
      expectClose(r.height, 50, 'lanes-tracks-minmax-0082 item[3].h');
    }
    {
      const r = rectOf(itemRefs[4]);
      expectClose(r.x - container.x, 0, 'lanes-tracks-minmax-0082 item[4].x');
      expectClose(r.y - container.y, 90, 'lanes-tracks-minmax-0082 item[4].y');
      expectClose(r.width, 295, 'lanes-tracks-minmax-0082 item[4].w');
      expectClose(r.height, 45, 'lanes-tracks-minmax-0082 item[4].h');
    }
  });
});

describe("lanes-tracks-pct", () => {
  it("lanes-tracks-pct-0083: lanes over pct tracks", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid-lanes","width":600,"gridTemplateColumns":"30% 30% 40%","gap":10,"flowTolerance":0}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":40}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":60}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":50}}
          />
          <View
            collapsable={false}
            ref={itemRefs[4]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":45}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'lanes-tracks-pct-0083 container.width');
    expectClose(container.height, 95, 'lanes-tracks-pct-0083 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'lanes-tracks-pct-0083 item[0].x');
      expectClose(r.y - container.y, 0, 'lanes-tracks-pct-0083 item[0].y');
      expectClose(r.width, 180, 'lanes-tracks-pct-0083 item[0].w');
      expectClose(r.height, 40, 'lanes-tracks-pct-0083 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 190, 'lanes-tracks-pct-0083 item[1].x');
      expectClose(r.y - container.y, 0, 'lanes-tracks-pct-0083 item[1].y');
      expectClose(r.width, 180, 'lanes-tracks-pct-0083 item[1].w');
      expectClose(r.height, 60, 'lanes-tracks-pct-0083 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 380, 'lanes-tracks-pct-0083 item[2].x');
      expectClose(r.y - container.y, 0, 'lanes-tracks-pct-0083 item[2].y');
      expectClose(r.width, 240, 'lanes-tracks-pct-0083 item[2].w');
      expectClose(r.height, 30, 'lanes-tracks-pct-0083 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 380, 'lanes-tracks-pct-0083 item[3].x');
      expectClose(r.y - container.y, 40, 'lanes-tracks-pct-0083 item[3].y');
      expectClose(r.width, 240, 'lanes-tracks-pct-0083 item[3].w');
      expectClose(r.height, 50, 'lanes-tracks-pct-0083 item[3].h');
    }
    {
      const r = rectOf(itemRefs[4]);
      expectClose(r.x - container.x, 0, 'lanes-tracks-pct-0083 item[4].x');
      expectClose(r.y - container.y, 50, 'lanes-tracks-pct-0083 item[4].y');
      expectClose(r.width, 180, 'lanes-tracks-pct-0083 item[4].w');
      expectClose(r.height, 45, 'lanes-tracks-pct-0083 item[4].h');
    }
  });
});

describe("lanes-tracks-auto", () => {
  it("lanes-tracks-auto-0084: lanes over auto tracks", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid-lanes","width":600,"gridTemplateColumns":"auto auto auto","gap":10,"flowTolerance":0}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":40}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":60}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":50}}
          />
          <View
            collapsable={false}
            ref={itemRefs[4]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":45}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'lanes-tracks-auto-0084 container.width');
    expectClose(container.height, 95, 'lanes-tracks-auto-0084 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'lanes-tracks-auto-0084 item[0].x');
      expectClose(r.y - container.y, 0, 'lanes-tracks-auto-0084 item[0].y');
      expectClose(r.width, 193.33, 'lanes-tracks-auto-0084 item[0].w');
      expectClose(r.height, 40, 'lanes-tracks-auto-0084 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 203.33, 'lanes-tracks-auto-0084 item[1].x');
      expectClose(r.y - container.y, 0, 'lanes-tracks-auto-0084 item[1].y');
      expectClose(r.width, 193.33, 'lanes-tracks-auto-0084 item[1].w');
      expectClose(r.height, 60, 'lanes-tracks-auto-0084 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 406.66, 'lanes-tracks-auto-0084 item[2].x');
      expectClose(r.y - container.y, 0, 'lanes-tracks-auto-0084 item[2].y');
      expectClose(r.width, 193.33, 'lanes-tracks-auto-0084 item[2].w');
      expectClose(r.height, 30, 'lanes-tracks-auto-0084 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 406.66, 'lanes-tracks-auto-0084 item[3].x');
      expectClose(r.y - container.y, 40, 'lanes-tracks-auto-0084 item[3].y');
      expectClose(r.width, 193.33, 'lanes-tracks-auto-0084 item[3].w');
      expectClose(r.height, 50, 'lanes-tracks-auto-0084 item[3].h');
    }
    {
      const r = rectOf(itemRefs[4]);
      expectClose(r.x - container.x, 0, 'lanes-tracks-auto-0084 item[4].x');
      expectClose(r.y - container.y, 50, 'lanes-tracks-auto-0084 item[4].y');
      expectClose(r.width, 193.33, 'lanes-tracks-auto-0084 item[4].w');
      expectClose(r.height, 45, 'lanes-tracks-auto-0084 item[4].h');
    }
  });
});

describe("lanes-flow-tolerance-sweep", () => {
  it("lanes-flow-tolerance-sweep-0085: flow-tolerance:0 against a 60/40/20 staircase", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid-lanes","width":600,"gridTemplateColumns":"1fr 1fr 1fr","gap":10,"flowTolerance":0}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":60}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":40}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30}}
          />
          <View
            collapsable={false}
            ref={itemRefs[4]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30}}
          />
          <View
            collapsable={false}
            ref={itemRefs[5]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'lanes-flow-tolerance-sweep-0085 container.width');
    expectClose(container.height, 100, 'lanes-flow-tolerance-sweep-0085 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'lanes-flow-tolerance-sweep-0085 item[0].x');
      expectClose(r.y - container.y, 0, 'lanes-flow-tolerance-sweep-0085 item[0].y');
      expectClose(r.width, 193.33, 'lanes-flow-tolerance-sweep-0085 item[0].w');
      expectClose(r.height, 60, 'lanes-flow-tolerance-sweep-0085 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 203.33, 'lanes-flow-tolerance-sweep-0085 item[1].x');
      expectClose(r.y - container.y, 0, 'lanes-flow-tolerance-sweep-0085 item[1].y');
      expectClose(r.width, 193.33, 'lanes-flow-tolerance-sweep-0085 item[1].w');
      expectClose(r.height, 40, 'lanes-flow-tolerance-sweep-0085 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 406.66, 'lanes-flow-tolerance-sweep-0085 item[2].x');
      expectClose(r.y - container.y, 0, 'lanes-flow-tolerance-sweep-0085 item[2].y');
      expectClose(r.width, 193.34, 'lanes-flow-tolerance-sweep-0085 item[2].w');
      expectClose(r.height, 20, 'lanes-flow-tolerance-sweep-0085 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 406.66, 'lanes-flow-tolerance-sweep-0085 item[3].x');
      expectClose(r.y - container.y, 30, 'lanes-flow-tolerance-sweep-0085 item[3].y');
      expectClose(r.width, 193.34, 'lanes-flow-tolerance-sweep-0085 item[3].w');
      expectClose(r.height, 30, 'lanes-flow-tolerance-sweep-0085 item[3].h');
    }
    {
      const r = rectOf(itemRefs[4]);
      expectClose(r.x - container.x, 203.33, 'lanes-flow-tolerance-sweep-0085 item[4].x');
      expectClose(r.y - container.y, 50, 'lanes-flow-tolerance-sweep-0085 item[4].y');
      expectClose(r.width, 193.33, 'lanes-flow-tolerance-sweep-0085 item[4].w');
      expectClose(r.height, 30, 'lanes-flow-tolerance-sweep-0085 item[4].h');
    }
    {
      const r = rectOf(itemRefs[5]);
      expectClose(r.x - container.x, 406.66, 'lanes-flow-tolerance-sweep-0085 item[5].x');
      expectClose(r.y - container.y, 70, 'lanes-flow-tolerance-sweep-0085 item[5].y');
      expectClose(r.width, 193.34, 'lanes-flow-tolerance-sweep-0085 item[5].w');
      expectClose(r.height, 30, 'lanes-flow-tolerance-sweep-0085 item[5].h');
    }
  });
  it("lanes-flow-tolerance-sweep-0086: flow-tolerance:5 against a 60/40/20 staircase", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid-lanes","width":600,"gridTemplateColumns":"1fr 1fr 1fr","gap":10,"flowTolerance":5}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":60}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":40}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30}}
          />
          <View
            collapsable={false}
            ref={itemRefs[4]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30}}
          />
          <View
            collapsable={false}
            ref={itemRefs[5]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'lanes-flow-tolerance-sweep-0086 container.width');
    expectClose(container.height, 100, 'lanes-flow-tolerance-sweep-0086 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'lanes-flow-tolerance-sweep-0086 item[0].x');
      expectClose(r.y - container.y, 0, 'lanes-flow-tolerance-sweep-0086 item[0].y');
      expectClose(r.width, 193.33, 'lanes-flow-tolerance-sweep-0086 item[0].w');
      expectClose(r.height, 60, 'lanes-flow-tolerance-sweep-0086 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 203.33, 'lanes-flow-tolerance-sweep-0086 item[1].x');
      expectClose(r.y - container.y, 0, 'lanes-flow-tolerance-sweep-0086 item[1].y');
      expectClose(r.width, 193.33, 'lanes-flow-tolerance-sweep-0086 item[1].w');
      expectClose(r.height, 40, 'lanes-flow-tolerance-sweep-0086 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 406.66, 'lanes-flow-tolerance-sweep-0086 item[2].x');
      expectClose(r.y - container.y, 0, 'lanes-flow-tolerance-sweep-0086 item[2].y');
      expectClose(r.width, 193.34, 'lanes-flow-tolerance-sweep-0086 item[2].w');
      expectClose(r.height, 20, 'lanes-flow-tolerance-sweep-0086 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 406.66, 'lanes-flow-tolerance-sweep-0086 item[3].x');
      expectClose(r.y - container.y, 30, 'lanes-flow-tolerance-sweep-0086 item[3].y');
      expectClose(r.width, 193.34, 'lanes-flow-tolerance-sweep-0086 item[3].w');
      expectClose(r.height, 30, 'lanes-flow-tolerance-sweep-0086 item[3].h');
    }
    {
      const r = rectOf(itemRefs[4]);
      expectClose(r.x - container.x, 203.33, 'lanes-flow-tolerance-sweep-0086 item[4].x');
      expectClose(r.y - container.y, 50, 'lanes-flow-tolerance-sweep-0086 item[4].y');
      expectClose(r.width, 193.33, 'lanes-flow-tolerance-sweep-0086 item[4].w');
      expectClose(r.height, 30, 'lanes-flow-tolerance-sweep-0086 item[4].h');
    }
    {
      const r = rectOf(itemRefs[5]);
      expectClose(r.x - container.x, 406.66, 'lanes-flow-tolerance-sweep-0086 item[5].x');
      expectClose(r.y - container.y, 70, 'lanes-flow-tolerance-sweep-0086 item[5].y');
      expectClose(r.width, 193.34, 'lanes-flow-tolerance-sweep-0086 item[5].w');
      expectClose(r.height, 30, 'lanes-flow-tolerance-sweep-0086 item[5].h');
    }
  });
  it("lanes-flow-tolerance-sweep-0087: flow-tolerance:10 against a 60/40/20 staircase", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid-lanes","width":600,"gridTemplateColumns":"1fr 1fr 1fr","gap":10,"flowTolerance":10}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":60}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":40}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30}}
          />
          <View
            collapsable={false}
            ref={itemRefs[4]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30}}
          />
          <View
            collapsable={false}
            ref={itemRefs[5]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'lanes-flow-tolerance-sweep-0087 container.width');
    expectClose(container.height, 100, 'lanes-flow-tolerance-sweep-0087 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'lanes-flow-tolerance-sweep-0087 item[0].x');
      expectClose(r.y - container.y, 0, 'lanes-flow-tolerance-sweep-0087 item[0].y');
      expectClose(r.width, 193.33, 'lanes-flow-tolerance-sweep-0087 item[0].w');
      expectClose(r.height, 60, 'lanes-flow-tolerance-sweep-0087 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 203.33, 'lanes-flow-tolerance-sweep-0087 item[1].x');
      expectClose(r.y - container.y, 0, 'lanes-flow-tolerance-sweep-0087 item[1].y');
      expectClose(r.width, 193.33, 'lanes-flow-tolerance-sweep-0087 item[1].w');
      expectClose(r.height, 40, 'lanes-flow-tolerance-sweep-0087 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 406.66, 'lanes-flow-tolerance-sweep-0087 item[2].x');
      expectClose(r.y - container.y, 0, 'lanes-flow-tolerance-sweep-0087 item[2].y');
      expectClose(r.width, 193.34, 'lanes-flow-tolerance-sweep-0087 item[2].w');
      expectClose(r.height, 20, 'lanes-flow-tolerance-sweep-0087 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 406.66, 'lanes-flow-tolerance-sweep-0087 item[3].x');
      expectClose(r.y - container.y, 30, 'lanes-flow-tolerance-sweep-0087 item[3].y');
      expectClose(r.width, 193.34, 'lanes-flow-tolerance-sweep-0087 item[3].w');
      expectClose(r.height, 30, 'lanes-flow-tolerance-sweep-0087 item[3].h');
    }
    {
      const r = rectOf(itemRefs[4]);
      expectClose(r.x - container.x, 203.33, 'lanes-flow-tolerance-sweep-0087 item[4].x');
      expectClose(r.y - container.y, 50, 'lanes-flow-tolerance-sweep-0087 item[4].y');
      expectClose(r.width, 193.33, 'lanes-flow-tolerance-sweep-0087 item[4].w');
      expectClose(r.height, 30, 'lanes-flow-tolerance-sweep-0087 item[4].h');
    }
    {
      const r = rectOf(itemRefs[5]);
      expectClose(r.x - container.x, 406.66, 'lanes-flow-tolerance-sweep-0087 item[5].x');
      expectClose(r.y - container.y, 70, 'lanes-flow-tolerance-sweep-0087 item[5].y');
      expectClose(r.width, 193.34, 'lanes-flow-tolerance-sweep-0087 item[5].w');
      expectClose(r.height, 30, 'lanes-flow-tolerance-sweep-0087 item[5].h');
    }
  });
  it("lanes-flow-tolerance-sweep-0088: flow-tolerance:15 against a 60/40/20 staircase", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid-lanes","width":600,"gridTemplateColumns":"1fr 1fr 1fr","gap":10,"flowTolerance":15}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":60}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":40}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30}}
          />
          <View
            collapsable={false}
            ref={itemRefs[4]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30}}
          />
          <View
            collapsable={false}
            ref={itemRefs[5]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'lanes-flow-tolerance-sweep-0088 container.width');
    expectClose(container.height, 100, 'lanes-flow-tolerance-sweep-0088 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'lanes-flow-tolerance-sweep-0088 item[0].x');
      expectClose(r.y - container.y, 0, 'lanes-flow-tolerance-sweep-0088 item[0].y');
      expectClose(r.width, 193.33, 'lanes-flow-tolerance-sweep-0088 item[0].w');
      expectClose(r.height, 60, 'lanes-flow-tolerance-sweep-0088 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 203.33, 'lanes-flow-tolerance-sweep-0088 item[1].x');
      expectClose(r.y - container.y, 0, 'lanes-flow-tolerance-sweep-0088 item[1].y');
      expectClose(r.width, 193.33, 'lanes-flow-tolerance-sweep-0088 item[1].w');
      expectClose(r.height, 40, 'lanes-flow-tolerance-sweep-0088 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 406.66, 'lanes-flow-tolerance-sweep-0088 item[2].x');
      expectClose(r.y - container.y, 0, 'lanes-flow-tolerance-sweep-0088 item[2].y');
      expectClose(r.width, 193.34, 'lanes-flow-tolerance-sweep-0088 item[2].w');
      expectClose(r.height, 20, 'lanes-flow-tolerance-sweep-0088 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 406.66, 'lanes-flow-tolerance-sweep-0088 item[3].x');
      expectClose(r.y - container.y, 30, 'lanes-flow-tolerance-sweep-0088 item[3].y');
      expectClose(r.width, 193.34, 'lanes-flow-tolerance-sweep-0088 item[3].w');
      expectClose(r.height, 30, 'lanes-flow-tolerance-sweep-0088 item[3].h');
    }
    {
      const r = rectOf(itemRefs[4]);
      expectClose(r.x - container.x, 203.33, 'lanes-flow-tolerance-sweep-0088 item[4].x');
      expectClose(r.y - container.y, 50, 'lanes-flow-tolerance-sweep-0088 item[4].y');
      expectClose(r.width, 193.33, 'lanes-flow-tolerance-sweep-0088 item[4].w');
      expectClose(r.height, 30, 'lanes-flow-tolerance-sweep-0088 item[4].h');
    }
    {
      const r = rectOf(itemRefs[5]);
      expectClose(r.x - container.x, 406.66, 'lanes-flow-tolerance-sweep-0088 item[5].x');
      expectClose(r.y - container.y, 70, 'lanes-flow-tolerance-sweep-0088 item[5].y');
      expectClose(r.width, 193.34, 'lanes-flow-tolerance-sweep-0088 item[5].w');
      expectClose(r.height, 30, 'lanes-flow-tolerance-sweep-0088 item[5].h');
    }
  });
  it("lanes-flow-tolerance-sweep-0089: flow-tolerance:16 against a 60/40/20 staircase", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid-lanes","width":600,"gridTemplateColumns":"1fr 1fr 1fr","gap":10,"flowTolerance":16}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":60}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":40}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30}}
          />
          <View
            collapsable={false}
            ref={itemRefs[4]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30}}
          />
          <View
            collapsable={false}
            ref={itemRefs[5]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'lanes-flow-tolerance-sweep-0089 container.width');
    expectClose(container.height, 100, 'lanes-flow-tolerance-sweep-0089 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'lanes-flow-tolerance-sweep-0089 item[0].x');
      expectClose(r.y - container.y, 0, 'lanes-flow-tolerance-sweep-0089 item[0].y');
      expectClose(r.width, 193.33, 'lanes-flow-tolerance-sweep-0089 item[0].w');
      expectClose(r.height, 60, 'lanes-flow-tolerance-sweep-0089 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 203.33, 'lanes-flow-tolerance-sweep-0089 item[1].x');
      expectClose(r.y - container.y, 0, 'lanes-flow-tolerance-sweep-0089 item[1].y');
      expectClose(r.width, 193.33, 'lanes-flow-tolerance-sweep-0089 item[1].w');
      expectClose(r.height, 40, 'lanes-flow-tolerance-sweep-0089 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 406.66, 'lanes-flow-tolerance-sweep-0089 item[2].x');
      expectClose(r.y - container.y, 0, 'lanes-flow-tolerance-sweep-0089 item[2].y');
      expectClose(r.width, 193.34, 'lanes-flow-tolerance-sweep-0089 item[2].w');
      expectClose(r.height, 20, 'lanes-flow-tolerance-sweep-0089 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 406.66, 'lanes-flow-tolerance-sweep-0089 item[3].x');
      expectClose(r.y - container.y, 30, 'lanes-flow-tolerance-sweep-0089 item[3].y');
      expectClose(r.width, 193.34, 'lanes-flow-tolerance-sweep-0089 item[3].w');
      expectClose(r.height, 30, 'lanes-flow-tolerance-sweep-0089 item[3].h');
    }
    {
      const r = rectOf(itemRefs[4]);
      expectClose(r.x - container.x, 203.33, 'lanes-flow-tolerance-sweep-0089 item[4].x');
      expectClose(r.y - container.y, 50, 'lanes-flow-tolerance-sweep-0089 item[4].y');
      expectClose(r.width, 193.33, 'lanes-flow-tolerance-sweep-0089 item[4].w');
      expectClose(r.height, 30, 'lanes-flow-tolerance-sweep-0089 item[4].h');
    }
    {
      const r = rectOf(itemRefs[5]);
      expectClose(r.x - container.x, 406.66, 'lanes-flow-tolerance-sweep-0089 item[5].x');
      expectClose(r.y - container.y, 70, 'lanes-flow-tolerance-sweep-0089 item[5].y');
      expectClose(r.width, 193.34, 'lanes-flow-tolerance-sweep-0089 item[5].w');
      expectClose(r.height, 30, 'lanes-flow-tolerance-sweep-0089 item[5].h');
    }
  });
  it("lanes-flow-tolerance-sweep-0090: flow-tolerance:17 against a 60/40/20 staircase", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid-lanes","width":600,"gridTemplateColumns":"1fr 1fr 1fr","gap":10,"flowTolerance":17}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":60}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":40}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30}}
          />
          <View
            collapsable={false}
            ref={itemRefs[4]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30}}
          />
          <View
            collapsable={false}
            ref={itemRefs[5]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'lanes-flow-tolerance-sweep-0090 container.width');
    expectClose(container.height, 100, 'lanes-flow-tolerance-sweep-0090 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'lanes-flow-tolerance-sweep-0090 item[0].x');
      expectClose(r.y - container.y, 0, 'lanes-flow-tolerance-sweep-0090 item[0].y');
      expectClose(r.width, 193.33, 'lanes-flow-tolerance-sweep-0090 item[0].w');
      expectClose(r.height, 60, 'lanes-flow-tolerance-sweep-0090 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 203.33, 'lanes-flow-tolerance-sweep-0090 item[1].x');
      expectClose(r.y - container.y, 0, 'lanes-flow-tolerance-sweep-0090 item[1].y');
      expectClose(r.width, 193.33, 'lanes-flow-tolerance-sweep-0090 item[1].w');
      expectClose(r.height, 40, 'lanes-flow-tolerance-sweep-0090 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 406.66, 'lanes-flow-tolerance-sweep-0090 item[2].x');
      expectClose(r.y - container.y, 0, 'lanes-flow-tolerance-sweep-0090 item[2].y');
      expectClose(r.width, 193.34, 'lanes-flow-tolerance-sweep-0090 item[2].w');
      expectClose(r.height, 20, 'lanes-flow-tolerance-sweep-0090 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 406.66, 'lanes-flow-tolerance-sweep-0090 item[3].x');
      expectClose(r.y - container.y, 30, 'lanes-flow-tolerance-sweep-0090 item[3].y');
      expectClose(r.width, 193.34, 'lanes-flow-tolerance-sweep-0090 item[3].w');
      expectClose(r.height, 30, 'lanes-flow-tolerance-sweep-0090 item[3].h');
    }
    {
      const r = rectOf(itemRefs[4]);
      expectClose(r.x - container.x, 203.33, 'lanes-flow-tolerance-sweep-0090 item[4].x');
      expectClose(r.y - container.y, 50, 'lanes-flow-tolerance-sweep-0090 item[4].y');
      expectClose(r.width, 193.33, 'lanes-flow-tolerance-sweep-0090 item[4].w');
      expectClose(r.height, 30, 'lanes-flow-tolerance-sweep-0090 item[4].h');
    }
    {
      const r = rectOf(itemRefs[5]);
      expectClose(r.x - container.x, 406.66, 'lanes-flow-tolerance-sweep-0090 item[5].x');
      expectClose(r.y - container.y, 70, 'lanes-flow-tolerance-sweep-0090 item[5].y');
      expectClose(r.width, 193.34, 'lanes-flow-tolerance-sweep-0090 item[5].w');
      expectClose(r.height, 30, 'lanes-flow-tolerance-sweep-0090 item[5].h');
    }
  });
  it("lanes-flow-tolerance-sweep-0091: flow-tolerance:20 against a 60/40/20 staircase", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid-lanes","width":600,"gridTemplateColumns":"1fr 1fr 1fr","gap":10,"flowTolerance":20}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":60}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":40}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30}}
          />
          <View
            collapsable={false}
            ref={itemRefs[4]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30}}
          />
          <View
            collapsable={false}
            ref={itemRefs[5]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'lanes-flow-tolerance-sweep-0091 container.width');
    expectClose(container.height, 100, 'lanes-flow-tolerance-sweep-0091 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'lanes-flow-tolerance-sweep-0091 item[0].x');
      expectClose(r.y - container.y, 0, 'lanes-flow-tolerance-sweep-0091 item[0].y');
      expectClose(r.width, 193.33, 'lanes-flow-tolerance-sweep-0091 item[0].w');
      expectClose(r.height, 60, 'lanes-flow-tolerance-sweep-0091 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 203.33, 'lanes-flow-tolerance-sweep-0091 item[1].x');
      expectClose(r.y - container.y, 0, 'lanes-flow-tolerance-sweep-0091 item[1].y');
      expectClose(r.width, 193.33, 'lanes-flow-tolerance-sweep-0091 item[1].w');
      expectClose(r.height, 40, 'lanes-flow-tolerance-sweep-0091 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 406.66, 'lanes-flow-tolerance-sweep-0091 item[2].x');
      expectClose(r.y - container.y, 0, 'lanes-flow-tolerance-sweep-0091 item[2].y');
      expectClose(r.width, 193.34, 'lanes-flow-tolerance-sweep-0091 item[2].w');
      expectClose(r.height, 20, 'lanes-flow-tolerance-sweep-0091 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 203.33, 'lanes-flow-tolerance-sweep-0091 item[3].x');
      expectClose(r.y - container.y, 50, 'lanes-flow-tolerance-sweep-0091 item[3].y');
      expectClose(r.width, 193.33, 'lanes-flow-tolerance-sweep-0091 item[3].w');
      expectClose(r.height, 30, 'lanes-flow-tolerance-sweep-0091 item[3].h');
    }
    {
      const r = rectOf(itemRefs[4]);
      expectClose(r.x - container.x, 406.66, 'lanes-flow-tolerance-sweep-0091 item[4].x');
      expectClose(r.y - container.y, 30, 'lanes-flow-tolerance-sweep-0091 item[4].y');
      expectClose(r.width, 193.34, 'lanes-flow-tolerance-sweep-0091 item[4].w');
      expectClose(r.height, 30, 'lanes-flow-tolerance-sweep-0091 item[4].h');
    }
    {
      const r = rectOf(itemRefs[5]);
      expectClose(r.x - container.x, 0, 'lanes-flow-tolerance-sweep-0091 item[5].x');
      expectClose(r.y - container.y, 70, 'lanes-flow-tolerance-sweep-0091 item[5].y');
      expectClose(r.width, 193.33, 'lanes-flow-tolerance-sweep-0091 item[5].w');
      expectClose(r.height, 30, 'lanes-flow-tolerance-sweep-0091 item[5].h');
    }
  });
  it("lanes-flow-tolerance-sweep-0092: flow-tolerance:25 against a 60/40/20 staircase", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid-lanes","width":600,"gridTemplateColumns":"1fr 1fr 1fr","gap":10,"flowTolerance":25}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":60}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":40}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30}}
          />
          <View
            collapsable={false}
            ref={itemRefs[4]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30}}
          />
          <View
            collapsable={false}
            ref={itemRefs[5]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'lanes-flow-tolerance-sweep-0092 container.width');
    expectClose(container.height, 100, 'lanes-flow-tolerance-sweep-0092 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'lanes-flow-tolerance-sweep-0092 item[0].x');
      expectClose(r.y - container.y, 0, 'lanes-flow-tolerance-sweep-0092 item[0].y');
      expectClose(r.width, 193.33, 'lanes-flow-tolerance-sweep-0092 item[0].w');
      expectClose(r.height, 60, 'lanes-flow-tolerance-sweep-0092 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 203.33, 'lanes-flow-tolerance-sweep-0092 item[1].x');
      expectClose(r.y - container.y, 0, 'lanes-flow-tolerance-sweep-0092 item[1].y');
      expectClose(r.width, 193.33, 'lanes-flow-tolerance-sweep-0092 item[1].w');
      expectClose(r.height, 40, 'lanes-flow-tolerance-sweep-0092 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 406.66, 'lanes-flow-tolerance-sweep-0092 item[2].x');
      expectClose(r.y - container.y, 0, 'lanes-flow-tolerance-sweep-0092 item[2].y');
      expectClose(r.width, 193.34, 'lanes-flow-tolerance-sweep-0092 item[2].w');
      expectClose(r.height, 20, 'lanes-flow-tolerance-sweep-0092 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 203.33, 'lanes-flow-tolerance-sweep-0092 item[3].x');
      expectClose(r.y - container.y, 50, 'lanes-flow-tolerance-sweep-0092 item[3].y');
      expectClose(r.width, 193.33, 'lanes-flow-tolerance-sweep-0092 item[3].w');
      expectClose(r.height, 30, 'lanes-flow-tolerance-sweep-0092 item[3].h');
    }
    {
      const r = rectOf(itemRefs[4]);
      expectClose(r.x - container.x, 406.66, 'lanes-flow-tolerance-sweep-0092 item[4].x');
      expectClose(r.y - container.y, 30, 'lanes-flow-tolerance-sweep-0092 item[4].y');
      expectClose(r.width, 193.34, 'lanes-flow-tolerance-sweep-0092 item[4].w');
      expectClose(r.height, 30, 'lanes-flow-tolerance-sweep-0092 item[4].h');
    }
    {
      const r = rectOf(itemRefs[5]);
      expectClose(r.x - container.x, 0, 'lanes-flow-tolerance-sweep-0092 item[5].x');
      expectClose(r.y - container.y, 70, 'lanes-flow-tolerance-sweep-0092 item[5].y');
      expectClose(r.width, 193.33, 'lanes-flow-tolerance-sweep-0092 item[5].w');
      expectClose(r.height, 30, 'lanes-flow-tolerance-sweep-0092 item[5].h');
    }
  });
  it("lanes-flow-tolerance-sweep-0093: flow-tolerance:40 against a 60/40/20 staircase", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid-lanes","width":600,"gridTemplateColumns":"1fr 1fr 1fr","gap":10,"flowTolerance":40}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":60}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":40}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30}}
          />
          <View
            collapsable={false}
            ref={itemRefs[4]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30}}
          />
          <View
            collapsable={false}
            ref={itemRefs[5]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'lanes-flow-tolerance-sweep-0093 container.width');
    expectClose(container.height, 100, 'lanes-flow-tolerance-sweep-0093 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'lanes-flow-tolerance-sweep-0093 item[0].x');
      expectClose(r.y - container.y, 0, 'lanes-flow-tolerance-sweep-0093 item[0].y');
      expectClose(r.width, 193.33, 'lanes-flow-tolerance-sweep-0093 item[0].w');
      expectClose(r.height, 60, 'lanes-flow-tolerance-sweep-0093 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 203.33, 'lanes-flow-tolerance-sweep-0093 item[1].x');
      expectClose(r.y - container.y, 0, 'lanes-flow-tolerance-sweep-0093 item[1].y');
      expectClose(r.width, 193.33, 'lanes-flow-tolerance-sweep-0093 item[1].w');
      expectClose(r.height, 40, 'lanes-flow-tolerance-sweep-0093 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 406.66, 'lanes-flow-tolerance-sweep-0093 item[2].x');
      expectClose(r.y - container.y, 0, 'lanes-flow-tolerance-sweep-0093 item[2].y');
      expectClose(r.width, 193.34, 'lanes-flow-tolerance-sweep-0093 item[2].w');
      expectClose(r.height, 20, 'lanes-flow-tolerance-sweep-0093 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 0, 'lanes-flow-tolerance-sweep-0093 item[3].x');
      expectClose(r.y - container.y, 70, 'lanes-flow-tolerance-sweep-0093 item[3].y');
      expectClose(r.width, 193.33, 'lanes-flow-tolerance-sweep-0093 item[3].w');
      expectClose(r.height, 30, 'lanes-flow-tolerance-sweep-0093 item[3].h');
    }
    {
      const r = rectOf(itemRefs[4]);
      expectClose(r.x - container.x, 203.33, 'lanes-flow-tolerance-sweep-0093 item[4].x');
      expectClose(r.y - container.y, 50, 'lanes-flow-tolerance-sweep-0093 item[4].y');
      expectClose(r.width, 193.33, 'lanes-flow-tolerance-sweep-0093 item[4].w');
      expectClose(r.height, 30, 'lanes-flow-tolerance-sweep-0093 item[4].h');
    }
    {
      const r = rectOf(itemRefs[5]);
      expectClose(r.x - container.x, 406.66, 'lanes-flow-tolerance-sweep-0093 item[5].x');
      expectClose(r.y - container.y, 30, 'lanes-flow-tolerance-sweep-0093 item[5].y');
      expectClose(r.width, 193.34, 'lanes-flow-tolerance-sweep-0093 item[5].w');
      expectClose(r.height, 30, 'lanes-flow-tolerance-sweep-0093 item[5].h');
    }
  });
  it("lanes-flow-tolerance-sweep-0094: flow-tolerance:100 against a 60/40/20 staircase", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid-lanes","width":600,"gridTemplateColumns":"1fr 1fr 1fr","gap":10,"flowTolerance":100}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":60}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":40}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30}}
          />
          <View
            collapsable={false}
            ref={itemRefs[4]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30}}
          />
          <View
            collapsable={false}
            ref={itemRefs[5]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'lanes-flow-tolerance-sweep-0094 container.width');
    expectClose(container.height, 100, 'lanes-flow-tolerance-sweep-0094 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'lanes-flow-tolerance-sweep-0094 item[0].x');
      expectClose(r.y - container.y, 0, 'lanes-flow-tolerance-sweep-0094 item[0].y');
      expectClose(r.width, 193.33, 'lanes-flow-tolerance-sweep-0094 item[0].w');
      expectClose(r.height, 60, 'lanes-flow-tolerance-sweep-0094 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 203.33, 'lanes-flow-tolerance-sweep-0094 item[1].x');
      expectClose(r.y - container.y, 0, 'lanes-flow-tolerance-sweep-0094 item[1].y');
      expectClose(r.width, 193.33, 'lanes-flow-tolerance-sweep-0094 item[1].w');
      expectClose(r.height, 40, 'lanes-flow-tolerance-sweep-0094 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 406.66, 'lanes-flow-tolerance-sweep-0094 item[2].x');
      expectClose(r.y - container.y, 0, 'lanes-flow-tolerance-sweep-0094 item[2].y');
      expectClose(r.width, 193.34, 'lanes-flow-tolerance-sweep-0094 item[2].w');
      expectClose(r.height, 20, 'lanes-flow-tolerance-sweep-0094 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 0, 'lanes-flow-tolerance-sweep-0094 item[3].x');
      expectClose(r.y - container.y, 70, 'lanes-flow-tolerance-sweep-0094 item[3].y');
      expectClose(r.width, 193.33, 'lanes-flow-tolerance-sweep-0094 item[3].w');
      expectClose(r.height, 30, 'lanes-flow-tolerance-sweep-0094 item[3].h');
    }
    {
      const r = rectOf(itemRefs[4]);
      expectClose(r.x - container.x, 203.33, 'lanes-flow-tolerance-sweep-0094 item[4].x');
      expectClose(r.y - container.y, 50, 'lanes-flow-tolerance-sweep-0094 item[4].y');
      expectClose(r.width, 193.33, 'lanes-flow-tolerance-sweep-0094 item[4].w');
      expectClose(r.height, 30, 'lanes-flow-tolerance-sweep-0094 item[4].h');
    }
    {
      const r = rectOf(itemRefs[5]);
      expectClose(r.x - container.x, 406.66, 'lanes-flow-tolerance-sweep-0094 item[5].x');
      expectClose(r.y - container.y, 30, 'lanes-flow-tolerance-sweep-0094 item[5].y');
      expectClose(r.width, 193.34, 'lanes-flow-tolerance-sweep-0094 item[5].w');
      expectClose(r.height, 30, 'lanes-flow-tolerance-sweep-0094 item[5].h');
    }
  });
  it("lanes-flow-tolerance-sweep-0095: flow-tolerance:normal against a 60/40/20 staircase", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid-lanes","width":600,"gridTemplateColumns":"1fr 1fr 1fr","gap":10,"flowTolerance":"normal"}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":60}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":40}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30}}
          />
          <View
            collapsable={false}
            ref={itemRefs[4]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30}}
          />
          <View
            collapsable={false}
            ref={itemRefs[5]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'lanes-flow-tolerance-sweep-0095 container.width');
    expectClose(container.height, 100, 'lanes-flow-tolerance-sweep-0095 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'lanes-flow-tolerance-sweep-0095 item[0].x');
      expectClose(r.y - container.y, 0, 'lanes-flow-tolerance-sweep-0095 item[0].y');
      expectClose(r.width, 193.33, 'lanes-flow-tolerance-sweep-0095 item[0].w');
      expectClose(r.height, 60, 'lanes-flow-tolerance-sweep-0095 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 203.33, 'lanes-flow-tolerance-sweep-0095 item[1].x');
      expectClose(r.y - container.y, 0, 'lanes-flow-tolerance-sweep-0095 item[1].y');
      expectClose(r.width, 193.33, 'lanes-flow-tolerance-sweep-0095 item[1].w');
      expectClose(r.height, 40, 'lanes-flow-tolerance-sweep-0095 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 406.66, 'lanes-flow-tolerance-sweep-0095 item[2].x');
      expectClose(r.y - container.y, 0, 'lanes-flow-tolerance-sweep-0095 item[2].y');
      expectClose(r.width, 193.34, 'lanes-flow-tolerance-sweep-0095 item[2].w');
      expectClose(r.height, 20, 'lanes-flow-tolerance-sweep-0095 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 406.66, 'lanes-flow-tolerance-sweep-0095 item[3].x');
      expectClose(r.y - container.y, 30, 'lanes-flow-tolerance-sweep-0095 item[3].y');
      expectClose(r.width, 193.34, 'lanes-flow-tolerance-sweep-0095 item[3].w');
      expectClose(r.height, 30, 'lanes-flow-tolerance-sweep-0095 item[3].h');
    }
    {
      const r = rectOf(itemRefs[4]);
      expectClose(r.x - container.x, 203.33, 'lanes-flow-tolerance-sweep-0095 item[4].x');
      expectClose(r.y - container.y, 50, 'lanes-flow-tolerance-sweep-0095 item[4].y');
      expectClose(r.width, 193.33, 'lanes-flow-tolerance-sweep-0095 item[4].w');
      expectClose(r.height, 30, 'lanes-flow-tolerance-sweep-0095 item[4].h');
    }
    {
      const r = rectOf(itemRefs[5]);
      expectClose(r.x - container.x, 406.66, 'lanes-flow-tolerance-sweep-0095 item[5].x');
      expectClose(r.y - container.y, 70, 'lanes-flow-tolerance-sweep-0095 item[5].y');
      expectClose(r.width, 193.34, 'lanes-flow-tolerance-sweep-0095 item[5].w');
      expectClose(r.height, 30, 'lanes-flow-tolerance-sweep-0095 item[5].h');
    }
  });
  it("lanes-flow-tolerance-sweep-0096: flow-tolerance:infinite against a 60/40/20 staircase", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid-lanes","width":600,"gridTemplateColumns":"1fr 1fr 1fr","gap":10,"flowTolerance":"infinite"}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":60}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":40}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30}}
          />
          <View
            collapsable={false}
            ref={itemRefs[4]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30}}
          />
          <View
            collapsable={false}
            ref={itemRefs[5]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'lanes-flow-tolerance-sweep-0096 container.width');
    expectClose(container.height, 100, 'lanes-flow-tolerance-sweep-0096 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'lanes-flow-tolerance-sweep-0096 item[0].x');
      expectClose(r.y - container.y, 0, 'lanes-flow-tolerance-sweep-0096 item[0].y');
      expectClose(r.width, 193.33, 'lanes-flow-tolerance-sweep-0096 item[0].w');
      expectClose(r.height, 60, 'lanes-flow-tolerance-sweep-0096 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 203.33, 'lanes-flow-tolerance-sweep-0096 item[1].x');
      expectClose(r.y - container.y, 0, 'lanes-flow-tolerance-sweep-0096 item[1].y');
      expectClose(r.width, 193.33, 'lanes-flow-tolerance-sweep-0096 item[1].w');
      expectClose(r.height, 40, 'lanes-flow-tolerance-sweep-0096 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 406.66, 'lanes-flow-tolerance-sweep-0096 item[2].x');
      expectClose(r.y - container.y, 0, 'lanes-flow-tolerance-sweep-0096 item[2].y');
      expectClose(r.width, 193.34, 'lanes-flow-tolerance-sweep-0096 item[2].w');
      expectClose(r.height, 20, 'lanes-flow-tolerance-sweep-0096 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 0, 'lanes-flow-tolerance-sweep-0096 item[3].x');
      expectClose(r.y - container.y, 70, 'lanes-flow-tolerance-sweep-0096 item[3].y');
      expectClose(r.width, 193.33, 'lanes-flow-tolerance-sweep-0096 item[3].w');
      expectClose(r.height, 30, 'lanes-flow-tolerance-sweep-0096 item[3].h');
    }
    {
      const r = rectOf(itemRefs[4]);
      expectClose(r.x - container.x, 203.33, 'lanes-flow-tolerance-sweep-0096 item[4].x');
      expectClose(r.y - container.y, 50, 'lanes-flow-tolerance-sweep-0096 item[4].y');
      expectClose(r.width, 193.33, 'lanes-flow-tolerance-sweep-0096 item[4].w');
      expectClose(r.height, 30, 'lanes-flow-tolerance-sweep-0096 item[4].h');
    }
    {
      const r = rectOf(itemRefs[5]);
      expectClose(r.x - container.x, 406.66, 'lanes-flow-tolerance-sweep-0096 item[5].x');
      expectClose(r.y - container.y, 30, 'lanes-flow-tolerance-sweep-0096 item[5].y');
      expectClose(r.width, 193.34, 'lanes-flow-tolerance-sweep-0096 item[5].w');
      expectClose(r.height, 30, 'lanes-flow-tolerance-sweep-0096 item[5].h');
    }
  });
});

describe("lanes-flow-tolerance-scale", () => {
  it("lanes-flow-tolerance-scale-0097: lane offsets scaled 1x against a fixed 16px tolerance", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid-lanes","width":600,"gridTemplateColumns":"1fr 1fr 1fr","gap":10,"flowTolerance":16}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":60}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":40}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30}}
          />
          <View
            collapsable={false}
            ref={itemRefs[4]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'lanes-flow-tolerance-scale-0097 container.width');
    expectClose(container.height, 80, 'lanes-flow-tolerance-scale-0097 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'lanes-flow-tolerance-scale-0097 item[0].x');
      expectClose(r.y - container.y, 0, 'lanes-flow-tolerance-scale-0097 item[0].y');
      expectClose(r.width, 193.33, 'lanes-flow-tolerance-scale-0097 item[0].w');
      expectClose(r.height, 60, 'lanes-flow-tolerance-scale-0097 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 203.33, 'lanes-flow-tolerance-scale-0097 item[1].x');
      expectClose(r.y - container.y, 0, 'lanes-flow-tolerance-scale-0097 item[1].y');
      expectClose(r.width, 193.33, 'lanes-flow-tolerance-scale-0097 item[1].w');
      expectClose(r.height, 40, 'lanes-flow-tolerance-scale-0097 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 406.66, 'lanes-flow-tolerance-scale-0097 item[2].x');
      expectClose(r.y - container.y, 0, 'lanes-flow-tolerance-scale-0097 item[2].y');
      expectClose(r.width, 193.34, 'lanes-flow-tolerance-scale-0097 item[2].w');
      expectClose(r.height, 20, 'lanes-flow-tolerance-scale-0097 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 406.66, 'lanes-flow-tolerance-scale-0097 item[3].x');
      expectClose(r.y - container.y, 30, 'lanes-flow-tolerance-scale-0097 item[3].y');
      expectClose(r.width, 193.34, 'lanes-flow-tolerance-scale-0097 item[3].w');
      expectClose(r.height, 30, 'lanes-flow-tolerance-scale-0097 item[3].h');
    }
    {
      const r = rectOf(itemRefs[4]);
      expectClose(r.x - container.x, 203.33, 'lanes-flow-tolerance-scale-0097 item[4].x');
      expectClose(r.y - container.y, 50, 'lanes-flow-tolerance-scale-0097 item[4].y');
      expectClose(r.width, 193.33, 'lanes-flow-tolerance-scale-0097 item[4].w');
      expectClose(r.height, 30, 'lanes-flow-tolerance-scale-0097 item[4].h');
    }
  });
  it("lanes-flow-tolerance-scale-0098: lane offsets scaled 2x against a fixed 16px tolerance", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid-lanes","width":600,"gridTemplateColumns":"1fr 1fr 1fr","gap":10,"flowTolerance":16}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":120}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":80}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":40}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30}}
          />
          <View
            collapsable={false}
            ref={itemRefs[4]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'lanes-flow-tolerance-scale-0098 container.width');
    expectClose(container.height, 120, 'lanes-flow-tolerance-scale-0098 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'lanes-flow-tolerance-scale-0098 item[0].x');
      expectClose(r.y - container.y, 0, 'lanes-flow-tolerance-scale-0098 item[0].y');
      expectClose(r.width, 193.33, 'lanes-flow-tolerance-scale-0098 item[0].w');
      expectClose(r.height, 120, 'lanes-flow-tolerance-scale-0098 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 203.33, 'lanes-flow-tolerance-scale-0098 item[1].x');
      expectClose(r.y - container.y, 0, 'lanes-flow-tolerance-scale-0098 item[1].y');
      expectClose(r.width, 193.33, 'lanes-flow-tolerance-scale-0098 item[1].w');
      expectClose(r.height, 80, 'lanes-flow-tolerance-scale-0098 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 406.66, 'lanes-flow-tolerance-scale-0098 item[2].x');
      expectClose(r.y - container.y, 0, 'lanes-flow-tolerance-scale-0098 item[2].y');
      expectClose(r.width, 193.34, 'lanes-flow-tolerance-scale-0098 item[2].w');
      expectClose(r.height, 40, 'lanes-flow-tolerance-scale-0098 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 406.66, 'lanes-flow-tolerance-scale-0098 item[3].x');
      expectClose(r.y - container.y, 50, 'lanes-flow-tolerance-scale-0098 item[3].y');
      expectClose(r.width, 193.34, 'lanes-flow-tolerance-scale-0098 item[3].w');
      expectClose(r.height, 30, 'lanes-flow-tolerance-scale-0098 item[3].h');
    }
    {
      const r = rectOf(itemRefs[4]);
      expectClose(r.x - container.x, 203.33, 'lanes-flow-tolerance-scale-0098 item[4].x');
      expectClose(r.y - container.y, 90, 'lanes-flow-tolerance-scale-0098 item[4].y');
      expectClose(r.width, 193.33, 'lanes-flow-tolerance-scale-0098 item[4].w');
      expectClose(r.height, 30, 'lanes-flow-tolerance-scale-0098 item[4].h');
    }
  });
  it("lanes-flow-tolerance-scale-0099: lane offsets scaled 4x against a fixed 16px tolerance", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid-lanes","width":600,"gridTemplateColumns":"1fr 1fr 1fr","gap":10,"flowTolerance":16}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":240}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":160}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":80}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30}}
          />
          <View
            collapsable={false}
            ref={itemRefs[4]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'lanes-flow-tolerance-scale-0099 container.width');
    expectClose(container.height, 240, 'lanes-flow-tolerance-scale-0099 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'lanes-flow-tolerance-scale-0099 item[0].x');
      expectClose(r.y - container.y, 0, 'lanes-flow-tolerance-scale-0099 item[0].y');
      expectClose(r.width, 193.33, 'lanes-flow-tolerance-scale-0099 item[0].w');
      expectClose(r.height, 240, 'lanes-flow-tolerance-scale-0099 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 203.33, 'lanes-flow-tolerance-scale-0099 item[1].x');
      expectClose(r.y - container.y, 0, 'lanes-flow-tolerance-scale-0099 item[1].y');
      expectClose(r.width, 193.33, 'lanes-flow-tolerance-scale-0099 item[1].w');
      expectClose(r.height, 160, 'lanes-flow-tolerance-scale-0099 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 406.66, 'lanes-flow-tolerance-scale-0099 item[2].x');
      expectClose(r.y - container.y, 0, 'lanes-flow-tolerance-scale-0099 item[2].y');
      expectClose(r.width, 193.34, 'lanes-flow-tolerance-scale-0099 item[2].w');
      expectClose(r.height, 80, 'lanes-flow-tolerance-scale-0099 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 406.66, 'lanes-flow-tolerance-scale-0099 item[3].x');
      expectClose(r.y - container.y, 90, 'lanes-flow-tolerance-scale-0099 item[3].y');
      expectClose(r.width, 193.34, 'lanes-flow-tolerance-scale-0099 item[3].w');
      expectClose(r.height, 30, 'lanes-flow-tolerance-scale-0099 item[3].h');
    }
    {
      const r = rectOf(itemRefs[4]);
      expectClose(r.x - container.x, 406.66, 'lanes-flow-tolerance-scale-0099 item[4].x');
      expectClose(r.y - container.y, 130, 'lanes-flow-tolerance-scale-0099 item[4].y');
      expectClose(r.width, 193.34, 'lanes-flow-tolerance-scale-0099 item[4].w');
      expectClose(r.height, 30, 'lanes-flow-tolerance-scale-0099 item[4].h');
    }
  });
});

describe("lanes-flow-tolerance-em", () => {
  it("lanes-flow-tolerance-em-0100: flow-tolerance:normal resolves to 1em at font-size 8px", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid-lanes","width":600,"gridTemplateColumns":"1fr 1fr 1fr","gap":10,"flowTolerance":"normal","fontSize":8}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":60}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":40}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30}}
          />
          <View
            collapsable={false}
            ref={itemRefs[4]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30}}
          />
          <View
            collapsable={false}
            ref={itemRefs[5]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'lanes-flow-tolerance-em-0100 container.width');
    expectClose(container.height, 100, 'lanes-flow-tolerance-em-0100 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'lanes-flow-tolerance-em-0100 item[0].x');
      expectClose(r.y - container.y, 0, 'lanes-flow-tolerance-em-0100 item[0].y');
      expectClose(r.width, 193.33, 'lanes-flow-tolerance-em-0100 item[0].w');
      expectClose(r.height, 60, 'lanes-flow-tolerance-em-0100 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 203.33, 'lanes-flow-tolerance-em-0100 item[1].x');
      expectClose(r.y - container.y, 0, 'lanes-flow-tolerance-em-0100 item[1].y');
      expectClose(r.width, 193.33, 'lanes-flow-tolerance-em-0100 item[1].w');
      expectClose(r.height, 40, 'lanes-flow-tolerance-em-0100 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 406.66, 'lanes-flow-tolerance-em-0100 item[2].x');
      expectClose(r.y - container.y, 0, 'lanes-flow-tolerance-em-0100 item[2].y');
      expectClose(r.width, 193.34, 'lanes-flow-tolerance-em-0100 item[2].w');
      expectClose(r.height, 20, 'lanes-flow-tolerance-em-0100 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 406.66, 'lanes-flow-tolerance-em-0100 item[3].x');
      expectClose(r.y - container.y, 30, 'lanes-flow-tolerance-em-0100 item[3].y');
      expectClose(r.width, 193.34, 'lanes-flow-tolerance-em-0100 item[3].w');
      expectClose(r.height, 30, 'lanes-flow-tolerance-em-0100 item[3].h');
    }
    {
      const r = rectOf(itemRefs[4]);
      expectClose(r.x - container.x, 203.33, 'lanes-flow-tolerance-em-0100 item[4].x');
      expectClose(r.y - container.y, 50, 'lanes-flow-tolerance-em-0100 item[4].y');
      expectClose(r.width, 193.33, 'lanes-flow-tolerance-em-0100 item[4].w');
      expectClose(r.height, 30, 'lanes-flow-tolerance-em-0100 item[4].h');
    }
    {
      const r = rectOf(itemRefs[5]);
      expectClose(r.x - container.x, 406.66, 'lanes-flow-tolerance-em-0100 item[5].x');
      expectClose(r.y - container.y, 70, 'lanes-flow-tolerance-em-0100 item[5].y');
      expectClose(r.width, 193.34, 'lanes-flow-tolerance-em-0100 item[5].w');
      expectClose(r.height, 30, 'lanes-flow-tolerance-em-0100 item[5].h');
    }
  });
  it("lanes-flow-tolerance-em-0101: flow-tolerance:normal resolves to 1em at font-size 16px", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid-lanes","width":600,"gridTemplateColumns":"1fr 1fr 1fr","gap":10,"flowTolerance":"normal","fontSize":16}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":60}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":40}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30}}
          />
          <View
            collapsable={false}
            ref={itemRefs[4]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30}}
          />
          <View
            collapsable={false}
            ref={itemRefs[5]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'lanes-flow-tolerance-em-0101 container.width');
    expectClose(container.height, 100, 'lanes-flow-tolerance-em-0101 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'lanes-flow-tolerance-em-0101 item[0].x');
      expectClose(r.y - container.y, 0, 'lanes-flow-tolerance-em-0101 item[0].y');
      expectClose(r.width, 193.33, 'lanes-flow-tolerance-em-0101 item[0].w');
      expectClose(r.height, 60, 'lanes-flow-tolerance-em-0101 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 203.33, 'lanes-flow-tolerance-em-0101 item[1].x');
      expectClose(r.y - container.y, 0, 'lanes-flow-tolerance-em-0101 item[1].y');
      expectClose(r.width, 193.33, 'lanes-flow-tolerance-em-0101 item[1].w');
      expectClose(r.height, 40, 'lanes-flow-tolerance-em-0101 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 406.66, 'lanes-flow-tolerance-em-0101 item[2].x');
      expectClose(r.y - container.y, 0, 'lanes-flow-tolerance-em-0101 item[2].y');
      expectClose(r.width, 193.34, 'lanes-flow-tolerance-em-0101 item[2].w');
      expectClose(r.height, 20, 'lanes-flow-tolerance-em-0101 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 406.66, 'lanes-flow-tolerance-em-0101 item[3].x');
      expectClose(r.y - container.y, 30, 'lanes-flow-tolerance-em-0101 item[3].y');
      expectClose(r.width, 193.34, 'lanes-flow-tolerance-em-0101 item[3].w');
      expectClose(r.height, 30, 'lanes-flow-tolerance-em-0101 item[3].h');
    }
    {
      const r = rectOf(itemRefs[4]);
      expectClose(r.x - container.x, 203.33, 'lanes-flow-tolerance-em-0101 item[4].x');
      expectClose(r.y - container.y, 50, 'lanes-flow-tolerance-em-0101 item[4].y');
      expectClose(r.width, 193.33, 'lanes-flow-tolerance-em-0101 item[4].w');
      expectClose(r.height, 30, 'lanes-flow-tolerance-em-0101 item[4].h');
    }
    {
      const r = rectOf(itemRefs[5]);
      expectClose(r.x - container.x, 406.66, 'lanes-flow-tolerance-em-0101 item[5].x');
      expectClose(r.y - container.y, 70, 'lanes-flow-tolerance-em-0101 item[5].y');
      expectClose(r.width, 193.34, 'lanes-flow-tolerance-em-0101 item[5].w');
      expectClose(r.height, 30, 'lanes-flow-tolerance-em-0101 item[5].h');
    }
  });
  it("lanes-flow-tolerance-em-0102: flow-tolerance:normal resolves to 1em at font-size 24px", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid-lanes","width":600,"gridTemplateColumns":"1fr 1fr 1fr","gap":10,"flowTolerance":"normal","fontSize":24}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":60}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":40}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30}}
          />
          <View
            collapsable={false}
            ref={itemRefs[4]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30}}
          />
          <View
            collapsable={false}
            ref={itemRefs[5]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'lanes-flow-tolerance-em-0102 container.width');
    expectClose(container.height, 100, 'lanes-flow-tolerance-em-0102 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'lanes-flow-tolerance-em-0102 item[0].x');
      expectClose(r.y - container.y, 0, 'lanes-flow-tolerance-em-0102 item[0].y');
      expectClose(r.width, 193.33, 'lanes-flow-tolerance-em-0102 item[0].w');
      expectClose(r.height, 60, 'lanes-flow-tolerance-em-0102 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 203.33, 'lanes-flow-tolerance-em-0102 item[1].x');
      expectClose(r.y - container.y, 0, 'lanes-flow-tolerance-em-0102 item[1].y');
      expectClose(r.width, 193.33, 'lanes-flow-tolerance-em-0102 item[1].w');
      expectClose(r.height, 40, 'lanes-flow-tolerance-em-0102 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 406.66, 'lanes-flow-tolerance-em-0102 item[2].x');
      expectClose(r.y - container.y, 0, 'lanes-flow-tolerance-em-0102 item[2].y');
      expectClose(r.width, 193.34, 'lanes-flow-tolerance-em-0102 item[2].w');
      expectClose(r.height, 20, 'lanes-flow-tolerance-em-0102 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 203.33, 'lanes-flow-tolerance-em-0102 item[3].x');
      expectClose(r.y - container.y, 50, 'lanes-flow-tolerance-em-0102 item[3].y');
      expectClose(r.width, 193.33, 'lanes-flow-tolerance-em-0102 item[3].w');
      expectClose(r.height, 30, 'lanes-flow-tolerance-em-0102 item[3].h');
    }
    {
      const r = rectOf(itemRefs[4]);
      expectClose(r.x - container.x, 406.66, 'lanes-flow-tolerance-em-0102 item[4].x');
      expectClose(r.y - container.y, 30, 'lanes-flow-tolerance-em-0102 item[4].y');
      expectClose(r.width, 193.34, 'lanes-flow-tolerance-em-0102 item[4].w');
      expectClose(r.height, 30, 'lanes-flow-tolerance-em-0102 item[4].h');
    }
    {
      const r = rectOf(itemRefs[5]);
      expectClose(r.x - container.x, 0, 'lanes-flow-tolerance-em-0102 item[5].x');
      expectClose(r.y - container.y, 70, 'lanes-flow-tolerance-em-0102 item[5].y');
      expectClose(r.width, 193.33, 'lanes-flow-tolerance-em-0102 item[5].w');
      expectClose(r.height, 30, 'lanes-flow-tolerance-em-0102 item[5].h');
    }
  });
  it("lanes-flow-tolerance-em-0103: flow-tolerance:normal resolves to 1em at font-size 32px", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid-lanes","width":600,"gridTemplateColumns":"1fr 1fr 1fr","gap":10,"flowTolerance":"normal","fontSize":32}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":60}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":40}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30}}
          />
          <View
            collapsable={false}
            ref={itemRefs[4]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30}}
          />
          <View
            collapsable={false}
            ref={itemRefs[5]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'lanes-flow-tolerance-em-0103 container.width');
    expectClose(container.height, 100, 'lanes-flow-tolerance-em-0103 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'lanes-flow-tolerance-em-0103 item[0].x');
      expectClose(r.y - container.y, 0, 'lanes-flow-tolerance-em-0103 item[0].y');
      expectClose(r.width, 193.33, 'lanes-flow-tolerance-em-0103 item[0].w');
      expectClose(r.height, 60, 'lanes-flow-tolerance-em-0103 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 203.33, 'lanes-flow-tolerance-em-0103 item[1].x');
      expectClose(r.y - container.y, 0, 'lanes-flow-tolerance-em-0103 item[1].y');
      expectClose(r.width, 193.33, 'lanes-flow-tolerance-em-0103 item[1].w');
      expectClose(r.height, 40, 'lanes-flow-tolerance-em-0103 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 406.66, 'lanes-flow-tolerance-em-0103 item[2].x');
      expectClose(r.y - container.y, 0, 'lanes-flow-tolerance-em-0103 item[2].y');
      expectClose(r.width, 193.34, 'lanes-flow-tolerance-em-0103 item[2].w');
      expectClose(r.height, 20, 'lanes-flow-tolerance-em-0103 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 203.33, 'lanes-flow-tolerance-em-0103 item[3].x');
      expectClose(r.y - container.y, 50, 'lanes-flow-tolerance-em-0103 item[3].y');
      expectClose(r.width, 193.33, 'lanes-flow-tolerance-em-0103 item[3].w');
      expectClose(r.height, 30, 'lanes-flow-tolerance-em-0103 item[3].h');
    }
    {
      const r = rectOf(itemRefs[4]);
      expectClose(r.x - container.x, 406.66, 'lanes-flow-tolerance-em-0103 item[4].x');
      expectClose(r.y - container.y, 30, 'lanes-flow-tolerance-em-0103 item[4].y');
      expectClose(r.width, 193.34, 'lanes-flow-tolerance-em-0103 item[4].w');
      expectClose(r.height, 30, 'lanes-flow-tolerance-em-0103 item[4].h');
    }
    {
      const r = rectOf(itemRefs[5]);
      expectClose(r.x - container.x, 0, 'lanes-flow-tolerance-em-0103 item[5].x');
      expectClose(r.y - container.y, 70, 'lanes-flow-tolerance-em-0103 item[5].y');
      expectClose(r.width, 193.33, 'lanes-flow-tolerance-em-0103 item[5].w');
      expectClose(r.height, 30, 'lanes-flow-tolerance-em-0103 item[5].h');
    }
  });
});

describe("lanes-span", () => {
  it("lanes-span-0107: an item spanning 2 lanes", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid-lanes","width":600,"gridTemplateColumns":"1fr 1fr 1fr","gap":10,"flowTolerance":0}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":50}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":40,"gridColumnEnd":"span 2"}}
          />
          <View
            collapsable={false}
            ref={itemRefs[4]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":25}}
          />
          <View
            collapsable={false}
            ref={itemRefs[5]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":35}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'lanes-span-0107 container.width');
    expectClose(container.height, 125, 'lanes-span-0107 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'lanes-span-0107 item[0].x');
      expectClose(r.y - container.y, 0, 'lanes-span-0107 item[0].y');
      expectClose(r.width, 193.33, 'lanes-span-0107 item[0].w');
      expectClose(r.height, 50, 'lanes-span-0107 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 203.33, 'lanes-span-0107 item[1].x');
      expectClose(r.y - container.y, 0, 'lanes-span-0107 item[1].y');
      expectClose(r.width, 193.33, 'lanes-span-0107 item[1].w');
      expectClose(r.height, 30, 'lanes-span-0107 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 406.66, 'lanes-span-0107 item[2].x');
      expectClose(r.y - container.y, 0, 'lanes-span-0107 item[2].y');
      expectClose(r.width, 193.34, 'lanes-span-0107 item[2].w');
      expectClose(r.height, 20, 'lanes-span-0107 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 203.33, 'lanes-span-0107 item[3].x');
      expectClose(r.y - container.y, 40, 'lanes-span-0107 item[3].y');
      expectClose(r.width, 396.67, 'lanes-span-0107 item[3].w');
      expectClose(r.height, 40, 'lanes-span-0107 item[3].h');
    }
    {
      const r = rectOf(itemRefs[4]);
      expectClose(r.x - container.x, 0, 'lanes-span-0107 item[4].x');
      expectClose(r.y - container.y, 60, 'lanes-span-0107 item[4].y');
      expectClose(r.width, 193.33, 'lanes-span-0107 item[4].w');
      expectClose(r.height, 25, 'lanes-span-0107 item[4].h');
    }
    {
      const r = rectOf(itemRefs[5]);
      expectClose(r.x - container.x, 203.33, 'lanes-span-0107 item[5].x');
      expectClose(r.y - container.y, 90, 'lanes-span-0107 item[5].y');
      expectClose(r.width, 193.33, 'lanes-span-0107 item[5].w');
      expectClose(r.height, 35, 'lanes-span-0107 item[5].h');
    }
  });
  it("lanes-span-0108: an item spanning 3 lanes", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid-lanes","width":600,"gridTemplateColumns":"1fr 1fr 1fr","gap":10,"flowTolerance":0}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":50}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":40,"gridColumnEnd":"span 3"}}
          />
          <View
            collapsable={false}
            ref={itemRefs[4]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":25}}
          />
          <View
            collapsable={false}
            ref={itemRefs[5]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":35}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'lanes-span-0108 container.width');
    expectClose(container.height, 145, 'lanes-span-0108 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'lanes-span-0108 item[0].x');
      expectClose(r.y - container.y, 0, 'lanes-span-0108 item[0].y');
      expectClose(r.width, 193.33, 'lanes-span-0108 item[0].w');
      expectClose(r.height, 50, 'lanes-span-0108 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 203.33, 'lanes-span-0108 item[1].x');
      expectClose(r.y - container.y, 0, 'lanes-span-0108 item[1].y');
      expectClose(r.width, 193.33, 'lanes-span-0108 item[1].w');
      expectClose(r.height, 30, 'lanes-span-0108 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 406.66, 'lanes-span-0108 item[2].x');
      expectClose(r.y - container.y, 0, 'lanes-span-0108 item[2].y');
      expectClose(r.width, 193.34, 'lanes-span-0108 item[2].w');
      expectClose(r.height, 20, 'lanes-span-0108 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 0, 'lanes-span-0108 item[3].x');
      expectClose(r.y - container.y, 60, 'lanes-span-0108 item[3].y');
      expectClose(r.width, 600, 'lanes-span-0108 item[3].w');
      expectClose(r.height, 40, 'lanes-span-0108 item[3].h');
    }
    {
      const r = rectOf(itemRefs[4]);
      expectClose(r.x - container.x, 0, 'lanes-span-0108 item[4].x');
      expectClose(r.y - container.y, 110, 'lanes-span-0108 item[4].y');
      expectClose(r.width, 193.33, 'lanes-span-0108 item[4].w');
      expectClose(r.height, 25, 'lanes-span-0108 item[4].h');
    }
    {
      const r = rectOf(itemRefs[5]);
      expectClose(r.x - container.x, 203.33, 'lanes-span-0108 item[5].x');
      expectClose(r.y - container.y, 110, 'lanes-span-0108 item[5].y');
      expectClose(r.width, 193.33, 'lanes-span-0108 item[5].w');
      expectClose(r.height, 35, 'lanes-span-0108 item[5].h');
    }
  });
});

describe("lanes-span-full", () => {
  it("lanes-span-full-0109: an item spanning every lane acts as a barrier", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid-lanes","width":600,"gridTemplateColumns":"1fr 1fr 1fr","gap":10,"flowTolerance":0}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":50}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":40,"gridColumnEnd":"span 3"}}
          />
          <View
            collapsable={false}
            ref={itemRefs[4]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":25}}
          />
          <View
            collapsable={false}
            ref={itemRefs[5]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":35}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'lanes-span-full-0109 container.width');
    expectClose(container.height, 145, 'lanes-span-full-0109 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'lanes-span-full-0109 item[0].x');
      expectClose(r.y - container.y, 0, 'lanes-span-full-0109 item[0].y');
      expectClose(r.width, 193.33, 'lanes-span-full-0109 item[0].w');
      expectClose(r.height, 50, 'lanes-span-full-0109 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 203.33, 'lanes-span-full-0109 item[1].x');
      expectClose(r.y - container.y, 0, 'lanes-span-full-0109 item[1].y');
      expectClose(r.width, 193.33, 'lanes-span-full-0109 item[1].w');
      expectClose(r.height, 30, 'lanes-span-full-0109 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 406.66, 'lanes-span-full-0109 item[2].x');
      expectClose(r.y - container.y, 0, 'lanes-span-full-0109 item[2].y');
      expectClose(r.width, 193.34, 'lanes-span-full-0109 item[2].w');
      expectClose(r.height, 20, 'lanes-span-full-0109 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 0, 'lanes-span-full-0109 item[3].x');
      expectClose(r.y - container.y, 60, 'lanes-span-full-0109 item[3].y');
      expectClose(r.width, 600, 'lanes-span-full-0109 item[3].w');
      expectClose(r.height, 40, 'lanes-span-full-0109 item[3].h');
    }
    {
      const r = rectOf(itemRefs[4]);
      expectClose(r.x - container.x, 0, 'lanes-span-full-0109 item[4].x');
      expectClose(r.y - container.y, 110, 'lanes-span-full-0109 item[4].y');
      expectClose(r.width, 193.33, 'lanes-span-full-0109 item[4].w');
      expectClose(r.height, 25, 'lanes-span-full-0109 item[4].h');
    }
    {
      const r = rectOf(itemRefs[5]);
      expectClose(r.x - container.x, 203.33, 'lanes-span-full-0109 item[5].x');
      expectClose(r.y - container.y, 110, 'lanes-span-full-0109 item[5].y');
      expectClose(r.width, 193.33, 'lanes-span-full-0109 item[5].w');
      expectClose(r.height, 35, 'lanes-span-full-0109 item[5].h');
    }
  });
});

describe("lanes-explicit-placement", () => {
  it("lanes-explicit-placement-0110: third item pinned to lane 1", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid-lanes","width":600,"gridTemplateColumns":"1fr 1fr 1fr","gap":10,"flowTolerance":0}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":50}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":60,"gridColumnStart":1}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[4]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":40}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'lanes-explicit-placement-0110 container.width');
    expectClose(container.height, 120, 'lanes-explicit-placement-0110 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'lanes-explicit-placement-0110 item[0].x');
      expectClose(r.y - container.y, 0, 'lanes-explicit-placement-0110 item[0].y');
      expectClose(r.width, 193.33, 'lanes-explicit-placement-0110 item[0].w');
      expectClose(r.height, 50, 'lanes-explicit-placement-0110 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 203.33, 'lanes-explicit-placement-0110 item[1].x');
      expectClose(r.y - container.y, 0, 'lanes-explicit-placement-0110 item[1].y');
      expectClose(r.width, 193.33, 'lanes-explicit-placement-0110 item[1].w');
      expectClose(r.height, 30, 'lanes-explicit-placement-0110 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 0, 'lanes-explicit-placement-0110 item[2].x');
      expectClose(r.y - container.y, 60, 'lanes-explicit-placement-0110 item[2].y');
      expectClose(r.width, 193.33, 'lanes-explicit-placement-0110 item[2].w');
      expectClose(r.height, 60, 'lanes-explicit-placement-0110 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 406.66, 'lanes-explicit-placement-0110 item[3].x');
      expectClose(r.y - container.y, 0, 'lanes-explicit-placement-0110 item[3].y');
      expectClose(r.width, 193.34, 'lanes-explicit-placement-0110 item[3].w');
      expectClose(r.height, 20, 'lanes-explicit-placement-0110 item[3].h');
    }
    {
      const r = rectOf(itemRefs[4]);
      expectClose(r.x - container.x, 406.66, 'lanes-explicit-placement-0110 item[4].x');
      expectClose(r.y - container.y, 30, 'lanes-explicit-placement-0110 item[4].y');
      expectClose(r.width, 193.34, 'lanes-explicit-placement-0110 item[4].w');
      expectClose(r.height, 40, 'lanes-explicit-placement-0110 item[4].h');
    }
  });
  it("lanes-explicit-placement-0111: third item pinned to lane 2", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid-lanes","width":600,"gridTemplateColumns":"1fr 1fr 1fr","gap":10,"flowTolerance":0}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":50}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":60,"gridColumnStart":2}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[4]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":40}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'lanes-explicit-placement-0111 container.width');
    expectClose(container.height, 100, 'lanes-explicit-placement-0111 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'lanes-explicit-placement-0111 item[0].x');
      expectClose(r.y - container.y, 0, 'lanes-explicit-placement-0111 item[0].y');
      expectClose(r.width, 193.33, 'lanes-explicit-placement-0111 item[0].w');
      expectClose(r.height, 50, 'lanes-explicit-placement-0111 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 203.33, 'lanes-explicit-placement-0111 item[1].x');
      expectClose(r.y - container.y, 0, 'lanes-explicit-placement-0111 item[1].y');
      expectClose(r.width, 193.33, 'lanes-explicit-placement-0111 item[1].w');
      expectClose(r.height, 30, 'lanes-explicit-placement-0111 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 203.33, 'lanes-explicit-placement-0111 item[2].x');
      expectClose(r.y - container.y, 40, 'lanes-explicit-placement-0111 item[2].y');
      expectClose(r.width, 193.33, 'lanes-explicit-placement-0111 item[2].w');
      expectClose(r.height, 60, 'lanes-explicit-placement-0111 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 406.66, 'lanes-explicit-placement-0111 item[3].x');
      expectClose(r.y - container.y, 0, 'lanes-explicit-placement-0111 item[3].y');
      expectClose(r.width, 193.34, 'lanes-explicit-placement-0111 item[3].w');
      expectClose(r.height, 20, 'lanes-explicit-placement-0111 item[3].h');
    }
    {
      const r = rectOf(itemRefs[4]);
      expectClose(r.x - container.x, 406.66, 'lanes-explicit-placement-0111 item[4].x');
      expectClose(r.y - container.y, 30, 'lanes-explicit-placement-0111 item[4].y');
      expectClose(r.width, 193.34, 'lanes-explicit-placement-0111 item[4].w');
      expectClose(r.height, 40, 'lanes-explicit-placement-0111 item[4].h');
    }
  });
  it("lanes-explicit-placement-0112: third item pinned to lane 3", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid-lanes","width":600,"gridTemplateColumns":"1fr 1fr 1fr","gap":10,"flowTolerance":0}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":50}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":60,"gridColumnStart":3}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[4]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":40}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'lanes-explicit-placement-0112 container.width');
    expectClose(container.height, 100, 'lanes-explicit-placement-0112 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'lanes-explicit-placement-0112 item[0].x');
      expectClose(r.y - container.y, 0, 'lanes-explicit-placement-0112 item[0].y');
      expectClose(r.width, 193.33, 'lanes-explicit-placement-0112 item[0].w');
      expectClose(r.height, 50, 'lanes-explicit-placement-0112 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 203.33, 'lanes-explicit-placement-0112 item[1].x');
      expectClose(r.y - container.y, 0, 'lanes-explicit-placement-0112 item[1].y');
      expectClose(r.width, 193.33, 'lanes-explicit-placement-0112 item[1].w');
      expectClose(r.height, 30, 'lanes-explicit-placement-0112 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 406.66, 'lanes-explicit-placement-0112 item[2].x');
      expectClose(r.y - container.y, 0, 'lanes-explicit-placement-0112 item[2].y');
      expectClose(r.width, 193.34, 'lanes-explicit-placement-0112 item[2].w');
      expectClose(r.height, 60, 'lanes-explicit-placement-0112 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 203.33, 'lanes-explicit-placement-0112 item[3].x');
      expectClose(r.y - container.y, 40, 'lanes-explicit-placement-0112 item[3].y');
      expectClose(r.width, 193.33, 'lanes-explicit-placement-0112 item[3].w');
      expectClose(r.height, 20, 'lanes-explicit-placement-0112 item[3].h');
    }
    {
      const r = rectOf(itemRefs[4]);
      expectClose(r.x - container.x, 0, 'lanes-explicit-placement-0112 item[4].x');
      expectClose(r.y - container.y, 60, 'lanes-explicit-placement-0112 item[4].y');
      expectClose(r.width, 193.33, 'lanes-explicit-placement-0112 item[4].w');
      expectClose(r.height, 40, 'lanes-explicit-placement-0112 item[4].h');
    }
  });
});

describe("lanes-x-gap", () => {
  it("lanes-x-gap-0114: lane offsets accumulate gap 0", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid-lanes","width":600,"gridTemplateColumns":"1fr 1fr 1fr","gap":0,"flowTolerance":0}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":50}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":45}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":40}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":10}}
          />
          <View
            collapsable={false}
            ref={itemRefs[4]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":10}}
          />
          <View
            collapsable={false}
            ref={itemRefs[5]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":10}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'lanes-x-gap-0114 container.width');
    expectClose(container.height, 60, 'lanes-x-gap-0114 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'lanes-x-gap-0114 item[0].x');
      expectClose(r.y - container.y, 0, 'lanes-x-gap-0114 item[0].y');
      expectClose(r.width, 200, 'lanes-x-gap-0114 item[0].w');
      expectClose(r.height, 50, 'lanes-x-gap-0114 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 200, 'lanes-x-gap-0114 item[1].x');
      expectClose(r.y - container.y, 0, 'lanes-x-gap-0114 item[1].y');
      expectClose(r.width, 200, 'lanes-x-gap-0114 item[1].w');
      expectClose(r.height, 45, 'lanes-x-gap-0114 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 400, 'lanes-x-gap-0114 item[2].x');
      expectClose(r.y - container.y, 0, 'lanes-x-gap-0114 item[2].y');
      expectClose(r.width, 200, 'lanes-x-gap-0114 item[2].w');
      expectClose(r.height, 40, 'lanes-x-gap-0114 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 400, 'lanes-x-gap-0114 item[3].x');
      expectClose(r.y - container.y, 40, 'lanes-x-gap-0114 item[3].y');
      expectClose(r.width, 200, 'lanes-x-gap-0114 item[3].w');
      expectClose(r.height, 10, 'lanes-x-gap-0114 item[3].h');
    }
    {
      const r = rectOf(itemRefs[4]);
      expectClose(r.x - container.x, 200, 'lanes-x-gap-0114 item[4].x');
      expectClose(r.y - container.y, 45, 'lanes-x-gap-0114 item[4].y');
      expectClose(r.width, 200, 'lanes-x-gap-0114 item[4].w');
      expectClose(r.height, 10, 'lanes-x-gap-0114 item[4].h');
    }
    {
      const r = rectOf(itemRefs[5]);
      expectClose(r.x - container.x, 400, 'lanes-x-gap-0114 item[5].x');
      expectClose(r.y - container.y, 50, 'lanes-x-gap-0114 item[5].y');
      expectClose(r.width, 200, 'lanes-x-gap-0114 item[5].w');
      expectClose(r.height, 10, 'lanes-x-gap-0114 item[5].h');
    }
  });
  it("lanes-x-gap-0115: lane offsets accumulate gap 5", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid-lanes","width":600,"gridTemplateColumns":"1fr 1fr 1fr","gap":5,"flowTolerance":0}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":50}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":45}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":40}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":10}}
          />
          <View
            collapsable={false}
            ref={itemRefs[4]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":10}}
          />
          <View
            collapsable={false}
            ref={itemRefs[5]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":10}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'lanes-x-gap-0115 container.width');
    expectClose(container.height, 65, 'lanes-x-gap-0115 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'lanes-x-gap-0115 item[0].x');
      expectClose(r.y - container.y, 0, 'lanes-x-gap-0115 item[0].y');
      expectClose(r.width, 196.66, 'lanes-x-gap-0115 item[0].w');
      expectClose(r.height, 50, 'lanes-x-gap-0115 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 201.66, 'lanes-x-gap-0115 item[1].x');
      expectClose(r.y - container.y, 0, 'lanes-x-gap-0115 item[1].y');
      expectClose(r.width, 196.67, 'lanes-x-gap-0115 item[1].w');
      expectClose(r.height, 45, 'lanes-x-gap-0115 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 403.33, 'lanes-x-gap-0115 item[2].x');
      expectClose(r.y - container.y, 0, 'lanes-x-gap-0115 item[2].y');
      expectClose(r.width, 196.66, 'lanes-x-gap-0115 item[2].w');
      expectClose(r.height, 40, 'lanes-x-gap-0115 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 403.33, 'lanes-x-gap-0115 item[3].x');
      expectClose(r.y - container.y, 45, 'lanes-x-gap-0115 item[3].y');
      expectClose(r.width, 196.66, 'lanes-x-gap-0115 item[3].w');
      expectClose(r.height, 10, 'lanes-x-gap-0115 item[3].h');
    }
    {
      const r = rectOf(itemRefs[4]);
      expectClose(r.x - container.x, 201.66, 'lanes-x-gap-0115 item[4].x');
      expectClose(r.y - container.y, 50, 'lanes-x-gap-0115 item[4].y');
      expectClose(r.width, 196.67, 'lanes-x-gap-0115 item[4].w');
      expectClose(r.height, 10, 'lanes-x-gap-0115 item[4].h');
    }
    {
      const r = rectOf(itemRefs[5]);
      expectClose(r.x - container.x, 0, 'lanes-x-gap-0115 item[5].x');
      expectClose(r.y - container.y, 55, 'lanes-x-gap-0115 item[5].y');
      expectClose(r.width, 196.66, 'lanes-x-gap-0115 item[5].w');
      expectClose(r.height, 10, 'lanes-x-gap-0115 item[5].h');
    }
  });
  it("lanes-x-gap-0116: lane offsets accumulate gap 20", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid-lanes","width":600,"gridTemplateColumns":"1fr 1fr 1fr","gap":20,"flowTolerance":0}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":50}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":45}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":40}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":10}}
          />
          <View
            collapsable={false}
            ref={itemRefs[4]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":10}}
          />
          <View
            collapsable={false}
            ref={itemRefs[5]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":10}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'lanes-x-gap-0116 container.width');
    expectClose(container.height, 80, 'lanes-x-gap-0116 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'lanes-x-gap-0116 item[0].x');
      expectClose(r.y - container.y, 0, 'lanes-x-gap-0116 item[0].y');
      expectClose(r.width, 186.66, 'lanes-x-gap-0116 item[0].w');
      expectClose(r.height, 50, 'lanes-x-gap-0116 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 206.66, 'lanes-x-gap-0116 item[1].x');
      expectClose(r.y - container.y, 0, 'lanes-x-gap-0116 item[1].y');
      expectClose(r.width, 186.67, 'lanes-x-gap-0116 item[1].w');
      expectClose(r.height, 45, 'lanes-x-gap-0116 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 413.33, 'lanes-x-gap-0116 item[2].x');
      expectClose(r.y - container.y, 0, 'lanes-x-gap-0116 item[2].y');
      expectClose(r.width, 186.66, 'lanes-x-gap-0116 item[2].w');
      expectClose(r.height, 40, 'lanes-x-gap-0116 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 413.33, 'lanes-x-gap-0116 item[3].x');
      expectClose(r.y - container.y, 60, 'lanes-x-gap-0116 item[3].y');
      expectClose(r.width, 186.66, 'lanes-x-gap-0116 item[3].w');
      expectClose(r.height, 10, 'lanes-x-gap-0116 item[3].h');
    }
    {
      const r = rectOf(itemRefs[4]);
      expectClose(r.x - container.x, 206.66, 'lanes-x-gap-0116 item[4].x');
      expectClose(r.y - container.y, 65, 'lanes-x-gap-0116 item[4].y');
      expectClose(r.width, 186.67, 'lanes-x-gap-0116 item[4].w');
      expectClose(r.height, 10, 'lanes-x-gap-0116 item[4].h');
    }
    {
      const r = rectOf(itemRefs[5]);
      expectClose(r.x - container.x, 0, 'lanes-x-gap-0116 item[5].x');
      expectClose(r.y - container.y, 70, 'lanes-x-gap-0116 item[5].y');
      expectClose(r.width, 186.66, 'lanes-x-gap-0116 item[5].w');
      expectClose(r.height, 10, 'lanes-x-gap-0116 item[5].h');
    }
  });
  it("lanes-x-gap-0117: lane offsets accumulate gap 50", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid-lanes","width":600,"gridTemplateColumns":"1fr 1fr 1fr","gap":50,"flowTolerance":0}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":50}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":45}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":40}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":10}}
          />
          <View
            collapsable={false}
            ref={itemRefs[4]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":10}}
          />
          <View
            collapsable={false}
            ref={itemRefs[5]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":10}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'lanes-x-gap-0117 container.width');
    expectClose(container.height, 110, 'lanes-x-gap-0117 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'lanes-x-gap-0117 item[0].x');
      expectClose(r.y - container.y, 0, 'lanes-x-gap-0117 item[0].y');
      expectClose(r.width, 166.66, 'lanes-x-gap-0117 item[0].w');
      expectClose(r.height, 50, 'lanes-x-gap-0117 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 216.66, 'lanes-x-gap-0117 item[1].x');
      expectClose(r.y - container.y, 0, 'lanes-x-gap-0117 item[1].y');
      expectClose(r.width, 166.67, 'lanes-x-gap-0117 item[1].w');
      expectClose(r.height, 45, 'lanes-x-gap-0117 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 433.33, 'lanes-x-gap-0117 item[2].x');
      expectClose(r.y - container.y, 0, 'lanes-x-gap-0117 item[2].y');
      expectClose(r.width, 166.66, 'lanes-x-gap-0117 item[2].w');
      expectClose(r.height, 40, 'lanes-x-gap-0117 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 433.33, 'lanes-x-gap-0117 item[3].x');
      expectClose(r.y - container.y, 90, 'lanes-x-gap-0117 item[3].y');
      expectClose(r.width, 166.66, 'lanes-x-gap-0117 item[3].w');
      expectClose(r.height, 10, 'lanes-x-gap-0117 item[3].h');
    }
    {
      const r = rectOf(itemRefs[4]);
      expectClose(r.x - container.x, 216.66, 'lanes-x-gap-0117 item[4].x');
      expectClose(r.y - container.y, 95, 'lanes-x-gap-0117 item[4].y');
      expectClose(r.width, 166.67, 'lanes-x-gap-0117 item[4].w');
      expectClose(r.height, 10, 'lanes-x-gap-0117 item[4].h');
    }
    {
      const r = rectOf(itemRefs[5]);
      expectClose(r.x - container.x, 0, 'lanes-x-gap-0117 item[5].x');
      expectClose(r.y - container.y, 100, 'lanes-x-gap-0117 item[5].y');
      expectClose(r.width, 166.66, 'lanes-x-gap-0117 item[5].w');
      expectClose(r.height, 10, 'lanes-x-gap-0117 item[5].h');
    }
  });
});

describe("lanes-x-margin", () => {
  it("lanes-x-margin-0118: item margin 0 feeds the lane offset", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid-lanes","width":600,"gridTemplateColumns":"1fr 1fr 1fr","gap":10,"flowTolerance":0}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":40,"margin":0}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":50,"margin":0}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30,"margin":0}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":20,"margin":0}}
          />
          <View
            collapsable={false}
            ref={itemRefs[4]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":60,"margin":0}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'lanes-x-margin-0118 container.width');
    expectClose(container.height, 110, 'lanes-x-margin-0118 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'lanes-x-margin-0118 item[0].x');
      expectClose(r.y - container.y, 0, 'lanes-x-margin-0118 item[0].y');
      expectClose(r.width, 193.33, 'lanes-x-margin-0118 item[0].w');
      expectClose(r.height, 40, 'lanes-x-margin-0118 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 203.33, 'lanes-x-margin-0118 item[1].x');
      expectClose(r.y - container.y, 0, 'lanes-x-margin-0118 item[1].y');
      expectClose(r.width, 193.33, 'lanes-x-margin-0118 item[1].w');
      expectClose(r.height, 50, 'lanes-x-margin-0118 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 406.66, 'lanes-x-margin-0118 item[2].x');
      expectClose(r.y - container.y, 0, 'lanes-x-margin-0118 item[2].y');
      expectClose(r.width, 193.34, 'lanes-x-margin-0118 item[2].w');
      expectClose(r.height, 30, 'lanes-x-margin-0118 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 406.66, 'lanes-x-margin-0118 item[3].x');
      expectClose(r.y - container.y, 40, 'lanes-x-margin-0118 item[3].y');
      expectClose(r.width, 193.34, 'lanes-x-margin-0118 item[3].w');
      expectClose(r.height, 20, 'lanes-x-margin-0118 item[3].h');
    }
    {
      const r = rectOf(itemRefs[4]);
      expectClose(r.x - container.x, 0, 'lanes-x-margin-0118 item[4].x');
      expectClose(r.y - container.y, 50, 'lanes-x-margin-0118 item[4].y');
      expectClose(r.width, 193.33, 'lanes-x-margin-0118 item[4].w');
      expectClose(r.height, 60, 'lanes-x-margin-0118 item[4].h');
    }
  });
  it("lanes-x-margin-0119: item margin 10 feeds the lane offset", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid-lanes","width":600,"gridTemplateColumns":"1fr 1fr 1fr","gap":10,"flowTolerance":0}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":40,"margin":10}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":50,"margin":10}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30,"margin":10}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":20,"margin":10}}
          />
          <View
            collapsable={false}
            ref={itemRefs[4]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":60,"margin":10}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'lanes-x-margin-0119 container.width');
    expectClose(container.height, 150, 'lanes-x-margin-0119 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 10, 'lanes-x-margin-0119 item[0].x');
      expectClose(r.y - container.y, 10, 'lanes-x-margin-0119 item[0].y');
      expectClose(r.width, 173.33, 'lanes-x-margin-0119 item[0].w');
      expectClose(r.height, 40, 'lanes-x-margin-0119 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 213.33, 'lanes-x-margin-0119 item[1].x');
      expectClose(r.y - container.y, 10, 'lanes-x-margin-0119 item[1].y');
      expectClose(r.width, 173.33, 'lanes-x-margin-0119 item[1].w');
      expectClose(r.height, 50, 'lanes-x-margin-0119 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 416.66, 'lanes-x-margin-0119 item[2].x');
      expectClose(r.y - container.y, 10, 'lanes-x-margin-0119 item[2].y');
      expectClose(r.width, 173.34, 'lanes-x-margin-0119 item[2].w');
      expectClose(r.height, 30, 'lanes-x-margin-0119 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 416.66, 'lanes-x-margin-0119 item[3].x');
      expectClose(r.y - container.y, 70, 'lanes-x-margin-0119 item[3].y');
      expectClose(r.width, 173.34, 'lanes-x-margin-0119 item[3].w');
      expectClose(r.height, 20, 'lanes-x-margin-0119 item[3].h');
    }
    {
      const r = rectOf(itemRefs[4]);
      expectClose(r.x - container.x, 10, 'lanes-x-margin-0119 item[4].x');
      expectClose(r.y - container.y, 80, 'lanes-x-margin-0119 item[4].y');
      expectClose(r.width, 173.33, 'lanes-x-margin-0119 item[4].w');
      expectClose(r.height, 60, 'lanes-x-margin-0119 item[4].h');
    }
  });
  it("lanes-x-margin-0120: item margin -8 feeds the lane offset", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid-lanes","width":600,"gridTemplateColumns":"1fr 1fr 1fr","gap":10,"flowTolerance":0}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":40,"margin":-8}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":50,"margin":-8}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30,"margin":-8}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":20,"margin":-8}}
          />
          <View
            collapsable={false}
            ref={itemRefs[4]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":60,"margin":-8}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'lanes-x-margin-0120 container.width');
    expectClose(container.height, 78, 'lanes-x-margin-0120 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, -8, 'lanes-x-margin-0120 item[0].x');
      expectClose(r.y - container.y, -8, 'lanes-x-margin-0120 item[0].y');
      expectClose(r.width, 209.33, 'lanes-x-margin-0120 item[0].w');
      expectClose(r.height, 40, 'lanes-x-margin-0120 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 195.33, 'lanes-x-margin-0120 item[1].x');
      expectClose(r.y - container.y, -8, 'lanes-x-margin-0120 item[1].y');
      expectClose(r.width, 209.33, 'lanes-x-margin-0120 item[1].w');
      expectClose(r.height, 50, 'lanes-x-margin-0120 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 398.66, 'lanes-x-margin-0120 item[2].x');
      expectClose(r.y - container.y, -8, 'lanes-x-margin-0120 item[2].y');
      expectClose(r.width, 209.34, 'lanes-x-margin-0120 item[2].w');
      expectClose(r.height, 30, 'lanes-x-margin-0120 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 398.66, 'lanes-x-margin-0120 item[3].x');
      expectClose(r.y - container.y, 16, 'lanes-x-margin-0120 item[3].y');
      expectClose(r.width, 209.34, 'lanes-x-margin-0120 item[3].w');
      expectClose(r.height, 20, 'lanes-x-margin-0120 item[3].h');
    }
    {
      const r = rectOf(itemRefs[4]);
      expectClose(r.x - container.x, -8, 'lanes-x-margin-0120 item[4].x');
      expectClose(r.y - container.y, 26, 'lanes-x-margin-0120 item[4].y');
      expectClose(r.width, 209.33, 'lanes-x-margin-0120 item[4].w');
      expectClose(r.height, 60, 'lanes-x-margin-0120 item[4].h');
    }
  });
});

describe("lanes-intrinsic-height", () => {
  it("lanes-intrinsic-height-0121: container height is the longest lane", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid-lanes","width":600,"gridTemplateColumns":"1fr 1fr 1fr","gap":10,"flowTolerance":0}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":100}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":25}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'lanes-intrinsic-height-0121 container.width');
    expectClose(container.height, 100, 'lanes-intrinsic-height-0121 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'lanes-intrinsic-height-0121 item[0].x');
      expectClose(r.y - container.y, 0, 'lanes-intrinsic-height-0121 item[0].y');
      expectClose(r.width, 193.33, 'lanes-intrinsic-height-0121 item[0].w');
      expectClose(r.height, 100, 'lanes-intrinsic-height-0121 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 203.33, 'lanes-intrinsic-height-0121 item[1].x');
      expectClose(r.y - container.y, 0, 'lanes-intrinsic-height-0121 item[1].y');
      expectClose(r.width, 193.33, 'lanes-intrinsic-height-0121 item[1].w');
      expectClose(r.height, 30, 'lanes-intrinsic-height-0121 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 406.66, 'lanes-intrinsic-height-0121 item[2].x');
      expectClose(r.y - container.y, 0, 'lanes-intrinsic-height-0121 item[2].y');
      expectClose(r.width, 193.34, 'lanes-intrinsic-height-0121 item[2].w');
      expectClose(r.height, 20, 'lanes-intrinsic-height-0121 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 406.66, 'lanes-intrinsic-height-0121 item[3].x');
      expectClose(r.y - container.y, 30, 'lanes-intrinsic-height-0121 item[3].y');
      expectClose(r.width, 193.34, 'lanes-intrinsic-height-0121 item[3].w');
      expectClose(r.height, 25, 'lanes-intrinsic-height-0121 item[3].h');
    }
  });
});

describe("lanes-dense", () => {
  it("lanes-dense-0122: grid-auto-flow row with spanning items", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid-lanes","width":600,"gridTemplateColumns":"1fr 1fr 1fr","gap":10,"gridAutoFlow":"row","flowTolerance":0}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30,"gridColumnEnd":"span 2"}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":40}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":50,"gridColumnEnd":"span 2"}}
          />
          <View
            collapsable={false}
            ref={itemRefs[4]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":25}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'lanes-dense-0122 container.width');
    expectClose(container.height, 100, 'lanes-dense-0122 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'lanes-dense-0122 item[0].x');
      expectClose(r.y - container.y, 0, 'lanes-dense-0122 item[0].y');
      expectClose(r.width, 396.66, 'lanes-dense-0122 item[0].w');
      expectClose(r.height, 30, 'lanes-dense-0122 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 406.66, 'lanes-dense-0122 item[1].x');
      expectClose(r.y - container.y, 0, 'lanes-dense-0122 item[1].y');
      expectClose(r.width, 193.34, 'lanes-dense-0122 item[1].w');
      expectClose(r.height, 40, 'lanes-dense-0122 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 0, 'lanes-dense-0122 item[2].x');
      expectClose(r.y - container.y, 40, 'lanes-dense-0122 item[2].y');
      expectClose(r.width, 193.33, 'lanes-dense-0122 item[2].w');
      expectClose(r.height, 20, 'lanes-dense-0122 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 203.33, 'lanes-dense-0122 item[3].x');
      expectClose(r.y - container.y, 50, 'lanes-dense-0122 item[3].y');
      expectClose(r.width, 396.67, 'lanes-dense-0122 item[3].w');
      expectClose(r.height, 50, 'lanes-dense-0122 item[3].h');
    }
    {
      const r = rectOf(itemRefs[4]);
      expectClose(r.x - container.x, 0, 'lanes-dense-0122 item[4].x');
      expectClose(r.y - container.y, 70, 'lanes-dense-0122 item[4].y');
      expectClose(r.width, 193.33, 'lanes-dense-0122 item[4].w');
      expectClose(r.height, 25, 'lanes-dense-0122 item[4].h');
    }
  });
  it("lanes-dense-0123: grid-auto-flow row dense with spanning items", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid-lanes","width":600,"gridTemplateColumns":"1fr 1fr 1fr","gap":10,"gridAutoFlow":"row dense","flowTolerance":0}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30,"gridColumnEnd":"span 2"}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":40}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":50,"gridColumnEnd":"span 2"}}
          />
          <View
            collapsable={false}
            ref={itemRefs[4]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":25}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'lanes-dense-0123 container.width');
    expectClose(container.height, 100, 'lanes-dense-0123 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'lanes-dense-0123 item[0].x');
      expectClose(r.y - container.y, 0, 'lanes-dense-0123 item[0].y');
      expectClose(r.width, 396.66, 'lanes-dense-0123 item[0].w');
      expectClose(r.height, 30, 'lanes-dense-0123 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 406.66, 'lanes-dense-0123 item[1].x');
      expectClose(r.y - container.y, 0, 'lanes-dense-0123 item[1].y');
      expectClose(r.width, 193.34, 'lanes-dense-0123 item[1].w');
      expectClose(r.height, 40, 'lanes-dense-0123 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 0, 'lanes-dense-0123 item[2].x');
      expectClose(r.y - container.y, 40, 'lanes-dense-0123 item[2].y');
      expectClose(r.width, 193.33, 'lanes-dense-0123 item[2].w');
      expectClose(r.height, 20, 'lanes-dense-0123 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 203.33, 'lanes-dense-0123 item[3].x');
      expectClose(r.y - container.y, 50, 'lanes-dense-0123 item[3].y');
      expectClose(r.width, 396.67, 'lanes-dense-0123 item[3].w');
      expectClose(r.height, 50, 'lanes-dense-0123 item[3].h');
    }
    {
      const r = rectOf(itemRefs[4]);
      expectClose(r.x - container.x, 0, 'lanes-dense-0123 item[4].x');
      expectClose(r.y - container.y, 70, 'lanes-dense-0123 item[4].y');
      expectClose(r.width, 193.33, 'lanes-dense-0123 item[4].w');
      expectClose(r.height, 25, 'lanes-dense-0123 item[4].h');
    }
  });
});

describe("lanes-empty", () => {
  it("lanes-empty-0124: no items", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs: Array<{current: HostInstance | null}> = [];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid-lanes","width":600,"gridTemplateColumns":"1fr 1fr","gap":10}}>
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'lanes-empty-0124 container.width');
    expectClose(container.height, 0, 'lanes-empty-0124 container.height');
  });
});

describe("lanes-single", () => {
  it("lanes-single-0125: a single item occupies lane 1 only", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid-lanes","width":600,"gridTemplateColumns":"1fr 1fr 1fr","gap":10}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":40}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'lanes-single-0125 container.width');
    expectClose(container.height, 40, 'lanes-single-0125 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'lanes-single-0125 item[0].x');
      expectClose(r.y - container.y, 0, 'lanes-single-0125 item[0].y');
      expectClose(r.width, 193.33, 'lanes-single-0125 item[0].w');
      expectClose(r.height, 40, 'lanes-single-0125 item[0].h');
    }
  });
});

describe("lanes-underfull", () => {
  it("lanes-underfull-0126: fewer items than lanes", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid-lanes","width":600,"gridTemplateColumns":"1fr 1fr 1fr 1fr","gap":10,"flowTolerance":0}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":40}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'lanes-underfull-0126 container.width');
    expectClose(container.height, 40, 'lanes-underfull-0126 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'lanes-underfull-0126 item[0].x');
      expectClose(r.y - container.y, 0, 'lanes-underfull-0126 item[0].y');
      expectClose(r.width, 142.5, 'lanes-underfull-0126 item[0].w');
      expectClose(r.height, 40, 'lanes-underfull-0126 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 152.5, 'lanes-underfull-0126 item[1].x');
      expectClose(r.y - container.y, 0, 'lanes-underfull-0126 item[1].y');
      expectClose(r.width, 142.5, 'lanes-underfull-0126 item[1].w');
      expectClose(r.height, 30, 'lanes-underfull-0126 item[1].h');
    }
  });
});

describe("lanes-tolerance-boundary", () => {
  it("lanes-tolerance-boundary-0127: boundary sweep: tolerance 0 against a 20px lane delta", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid-lanes","width":600,"gridTemplateColumns":"1fr 1fr 1fr","gap":0,"flowTolerance":0}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":10}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":50}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":25}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'lanes-tolerance-boundary-0127 container.width');
    expectClose(container.height, 50, 'lanes-tolerance-boundary-0127 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'lanes-tolerance-boundary-0127 item[0].x');
      expectClose(r.y - container.y, 0, 'lanes-tolerance-boundary-0127 item[0].y');
      expectClose(r.width, 200, 'lanes-tolerance-boundary-0127 item[0].w');
      expectClose(r.height, 30, 'lanes-tolerance-boundary-0127 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 200, 'lanes-tolerance-boundary-0127 item[1].x');
      expectClose(r.y - container.y, 0, 'lanes-tolerance-boundary-0127 item[1].y');
      expectClose(r.width, 200, 'lanes-tolerance-boundary-0127 item[1].w');
      expectClose(r.height, 10, 'lanes-tolerance-boundary-0127 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 400, 'lanes-tolerance-boundary-0127 item[2].x');
      expectClose(r.y - container.y, 0, 'lanes-tolerance-boundary-0127 item[2].y');
      expectClose(r.width, 200, 'lanes-tolerance-boundary-0127 item[2].w');
      expectClose(r.height, 50, 'lanes-tolerance-boundary-0127 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 200, 'lanes-tolerance-boundary-0127 item[3].x');
      expectClose(r.y - container.y, 10, 'lanes-tolerance-boundary-0127 item[3].y');
      expectClose(r.width, 200, 'lanes-tolerance-boundary-0127 item[3].w');
      expectClose(r.height, 25, 'lanes-tolerance-boundary-0127 item[3].h');
    }
  });
  it("lanes-tolerance-boundary-0128: boundary sweep: tolerance 10 against a 20px lane delta", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid-lanes","width":600,"gridTemplateColumns":"1fr 1fr 1fr","gap":0,"flowTolerance":10}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":10}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":50}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":25}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'lanes-tolerance-boundary-0128 container.width');
    expectClose(container.height, 50, 'lanes-tolerance-boundary-0128 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'lanes-tolerance-boundary-0128 item[0].x');
      expectClose(r.y - container.y, 0, 'lanes-tolerance-boundary-0128 item[0].y');
      expectClose(r.width, 200, 'lanes-tolerance-boundary-0128 item[0].w');
      expectClose(r.height, 30, 'lanes-tolerance-boundary-0128 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 200, 'lanes-tolerance-boundary-0128 item[1].x');
      expectClose(r.y - container.y, 0, 'lanes-tolerance-boundary-0128 item[1].y');
      expectClose(r.width, 200, 'lanes-tolerance-boundary-0128 item[1].w');
      expectClose(r.height, 10, 'lanes-tolerance-boundary-0128 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 400, 'lanes-tolerance-boundary-0128 item[2].x');
      expectClose(r.y - container.y, 0, 'lanes-tolerance-boundary-0128 item[2].y');
      expectClose(r.width, 200, 'lanes-tolerance-boundary-0128 item[2].w');
      expectClose(r.height, 50, 'lanes-tolerance-boundary-0128 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 200, 'lanes-tolerance-boundary-0128 item[3].x');
      expectClose(r.y - container.y, 10, 'lanes-tolerance-boundary-0128 item[3].y');
      expectClose(r.width, 200, 'lanes-tolerance-boundary-0128 item[3].w');
      expectClose(r.height, 25, 'lanes-tolerance-boundary-0128 item[3].h');
    }
  });
  it("lanes-tolerance-boundary-0129: boundary sweep: tolerance 18 against a 20px lane delta", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid-lanes","width":600,"gridTemplateColumns":"1fr 1fr 1fr","gap":0,"flowTolerance":18}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":10}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":50}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":25}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'lanes-tolerance-boundary-0129 container.width');
    expectClose(container.height, 50, 'lanes-tolerance-boundary-0129 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'lanes-tolerance-boundary-0129 item[0].x');
      expectClose(r.y - container.y, 0, 'lanes-tolerance-boundary-0129 item[0].y');
      expectClose(r.width, 200, 'lanes-tolerance-boundary-0129 item[0].w');
      expectClose(r.height, 30, 'lanes-tolerance-boundary-0129 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 200, 'lanes-tolerance-boundary-0129 item[1].x');
      expectClose(r.y - container.y, 0, 'lanes-tolerance-boundary-0129 item[1].y');
      expectClose(r.width, 200, 'lanes-tolerance-boundary-0129 item[1].w');
      expectClose(r.height, 10, 'lanes-tolerance-boundary-0129 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 400, 'lanes-tolerance-boundary-0129 item[2].x');
      expectClose(r.y - container.y, 0, 'lanes-tolerance-boundary-0129 item[2].y');
      expectClose(r.width, 200, 'lanes-tolerance-boundary-0129 item[2].w');
      expectClose(r.height, 50, 'lanes-tolerance-boundary-0129 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 200, 'lanes-tolerance-boundary-0129 item[3].x');
      expectClose(r.y - container.y, 10, 'lanes-tolerance-boundary-0129 item[3].y');
      expectClose(r.width, 200, 'lanes-tolerance-boundary-0129 item[3].w');
      expectClose(r.height, 25, 'lanes-tolerance-boundary-0129 item[3].h');
    }
  });
  it("lanes-tolerance-boundary-0130: boundary sweep: tolerance 19 against a 20px lane delta", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid-lanes","width":600,"gridTemplateColumns":"1fr 1fr 1fr","gap":0,"flowTolerance":19}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":10}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":50}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":25}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'lanes-tolerance-boundary-0130 container.width');
    expectClose(container.height, 50, 'lanes-tolerance-boundary-0130 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'lanes-tolerance-boundary-0130 item[0].x');
      expectClose(r.y - container.y, 0, 'lanes-tolerance-boundary-0130 item[0].y');
      expectClose(r.width, 200, 'lanes-tolerance-boundary-0130 item[0].w');
      expectClose(r.height, 30, 'lanes-tolerance-boundary-0130 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 200, 'lanes-tolerance-boundary-0130 item[1].x');
      expectClose(r.y - container.y, 0, 'lanes-tolerance-boundary-0130 item[1].y');
      expectClose(r.width, 200, 'lanes-tolerance-boundary-0130 item[1].w');
      expectClose(r.height, 10, 'lanes-tolerance-boundary-0130 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 400, 'lanes-tolerance-boundary-0130 item[2].x');
      expectClose(r.y - container.y, 0, 'lanes-tolerance-boundary-0130 item[2].y');
      expectClose(r.width, 200, 'lanes-tolerance-boundary-0130 item[2].w');
      expectClose(r.height, 50, 'lanes-tolerance-boundary-0130 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 200, 'lanes-tolerance-boundary-0130 item[3].x');
      expectClose(r.y - container.y, 10, 'lanes-tolerance-boundary-0130 item[3].y');
      expectClose(r.width, 200, 'lanes-tolerance-boundary-0130 item[3].w');
      expectClose(r.height, 25, 'lanes-tolerance-boundary-0130 item[3].h');
    }
  });
  it("lanes-tolerance-boundary-0131: boundary sweep: tolerance 19.5 against a 20px lane delta", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid-lanes","width":600,"gridTemplateColumns":"1fr 1fr 1fr","gap":0,"flowTolerance":19.5}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":10}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":50}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":25}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'lanes-tolerance-boundary-0131 container.width');
    expectClose(container.height, 50, 'lanes-tolerance-boundary-0131 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'lanes-tolerance-boundary-0131 item[0].x');
      expectClose(r.y - container.y, 0, 'lanes-tolerance-boundary-0131 item[0].y');
      expectClose(r.width, 200, 'lanes-tolerance-boundary-0131 item[0].w');
      expectClose(r.height, 30, 'lanes-tolerance-boundary-0131 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 200, 'lanes-tolerance-boundary-0131 item[1].x');
      expectClose(r.y - container.y, 0, 'lanes-tolerance-boundary-0131 item[1].y');
      expectClose(r.width, 200, 'lanes-tolerance-boundary-0131 item[1].w');
      expectClose(r.height, 10, 'lanes-tolerance-boundary-0131 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 400, 'lanes-tolerance-boundary-0131 item[2].x');
      expectClose(r.y - container.y, 0, 'lanes-tolerance-boundary-0131 item[2].y');
      expectClose(r.width, 200, 'lanes-tolerance-boundary-0131 item[2].w');
      expectClose(r.height, 50, 'lanes-tolerance-boundary-0131 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 200, 'lanes-tolerance-boundary-0131 item[3].x');
      expectClose(r.y - container.y, 10, 'lanes-tolerance-boundary-0131 item[3].y');
      expectClose(r.width, 200, 'lanes-tolerance-boundary-0131 item[3].w');
      expectClose(r.height, 25, 'lanes-tolerance-boundary-0131 item[3].h');
    }
  });
  it("lanes-tolerance-boundary-0132: boundary sweep: tolerance 20 against a 20px lane delta", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid-lanes","width":600,"gridTemplateColumns":"1fr 1fr 1fr","gap":0,"flowTolerance":20}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":10}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":50}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":25}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'lanes-tolerance-boundary-0132 container.width');
    expectClose(container.height, 55, 'lanes-tolerance-boundary-0132 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'lanes-tolerance-boundary-0132 item[0].x');
      expectClose(r.y - container.y, 0, 'lanes-tolerance-boundary-0132 item[0].y');
      expectClose(r.width, 200, 'lanes-tolerance-boundary-0132 item[0].w');
      expectClose(r.height, 30, 'lanes-tolerance-boundary-0132 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 200, 'lanes-tolerance-boundary-0132 item[1].x');
      expectClose(r.y - container.y, 0, 'lanes-tolerance-boundary-0132 item[1].y');
      expectClose(r.width, 200, 'lanes-tolerance-boundary-0132 item[1].w');
      expectClose(r.height, 10, 'lanes-tolerance-boundary-0132 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 400, 'lanes-tolerance-boundary-0132 item[2].x');
      expectClose(r.y - container.y, 0, 'lanes-tolerance-boundary-0132 item[2].y');
      expectClose(r.width, 200, 'lanes-tolerance-boundary-0132 item[2].w');
      expectClose(r.height, 50, 'lanes-tolerance-boundary-0132 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 0, 'lanes-tolerance-boundary-0132 item[3].x');
      expectClose(r.y - container.y, 30, 'lanes-tolerance-boundary-0132 item[3].y');
      expectClose(r.width, 200, 'lanes-tolerance-boundary-0132 item[3].w');
      expectClose(r.height, 25, 'lanes-tolerance-boundary-0132 item[3].h');
    }
  });
  it("lanes-tolerance-boundary-0133: boundary sweep: tolerance 20.5 against a 20px lane delta", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid-lanes","width":600,"gridTemplateColumns":"1fr 1fr 1fr","gap":0,"flowTolerance":20.5}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":10}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":50}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":25}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'lanes-tolerance-boundary-0133 container.width');
    expectClose(container.height, 55, 'lanes-tolerance-boundary-0133 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'lanes-tolerance-boundary-0133 item[0].x');
      expectClose(r.y - container.y, 0, 'lanes-tolerance-boundary-0133 item[0].y');
      expectClose(r.width, 200, 'lanes-tolerance-boundary-0133 item[0].w');
      expectClose(r.height, 30, 'lanes-tolerance-boundary-0133 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 200, 'lanes-tolerance-boundary-0133 item[1].x');
      expectClose(r.y - container.y, 0, 'lanes-tolerance-boundary-0133 item[1].y');
      expectClose(r.width, 200, 'lanes-tolerance-boundary-0133 item[1].w');
      expectClose(r.height, 10, 'lanes-tolerance-boundary-0133 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 400, 'lanes-tolerance-boundary-0133 item[2].x');
      expectClose(r.y - container.y, 0, 'lanes-tolerance-boundary-0133 item[2].y');
      expectClose(r.width, 200, 'lanes-tolerance-boundary-0133 item[2].w');
      expectClose(r.height, 50, 'lanes-tolerance-boundary-0133 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 0, 'lanes-tolerance-boundary-0133 item[3].x');
      expectClose(r.y - container.y, 30, 'lanes-tolerance-boundary-0133 item[3].y');
      expectClose(r.width, 200, 'lanes-tolerance-boundary-0133 item[3].w');
      expectClose(r.height, 25, 'lanes-tolerance-boundary-0133 item[3].h');
    }
  });
  it("lanes-tolerance-boundary-0134: boundary sweep: tolerance 21 against a 20px lane delta", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid-lanes","width":600,"gridTemplateColumns":"1fr 1fr 1fr","gap":0,"flowTolerance":21}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":10}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":50}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":25}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'lanes-tolerance-boundary-0134 container.width');
    expectClose(container.height, 55, 'lanes-tolerance-boundary-0134 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'lanes-tolerance-boundary-0134 item[0].x');
      expectClose(r.y - container.y, 0, 'lanes-tolerance-boundary-0134 item[0].y');
      expectClose(r.width, 200, 'lanes-tolerance-boundary-0134 item[0].w');
      expectClose(r.height, 30, 'lanes-tolerance-boundary-0134 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 200, 'lanes-tolerance-boundary-0134 item[1].x');
      expectClose(r.y - container.y, 0, 'lanes-tolerance-boundary-0134 item[1].y');
      expectClose(r.width, 200, 'lanes-tolerance-boundary-0134 item[1].w');
      expectClose(r.height, 10, 'lanes-tolerance-boundary-0134 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 400, 'lanes-tolerance-boundary-0134 item[2].x');
      expectClose(r.y - container.y, 0, 'lanes-tolerance-boundary-0134 item[2].y');
      expectClose(r.width, 200, 'lanes-tolerance-boundary-0134 item[2].w');
      expectClose(r.height, 50, 'lanes-tolerance-boundary-0134 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 0, 'lanes-tolerance-boundary-0134 item[3].x');
      expectClose(r.y - container.y, 30, 'lanes-tolerance-boundary-0134 item[3].y');
      expectClose(r.width, 200, 'lanes-tolerance-boundary-0134 item[3].w');
      expectClose(r.height, 25, 'lanes-tolerance-boundary-0134 item[3].h');
    }
  });
  it("lanes-tolerance-boundary-0135: boundary sweep: tolerance 30 against a 20px lane delta", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid-lanes","width":600,"gridTemplateColumns":"1fr 1fr 1fr","gap":0,"flowTolerance":30}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":10}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":50}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":25}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'lanes-tolerance-boundary-0135 container.width');
    expectClose(container.height, 55, 'lanes-tolerance-boundary-0135 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'lanes-tolerance-boundary-0135 item[0].x');
      expectClose(r.y - container.y, 0, 'lanes-tolerance-boundary-0135 item[0].y');
      expectClose(r.width, 200, 'lanes-tolerance-boundary-0135 item[0].w');
      expectClose(r.height, 30, 'lanes-tolerance-boundary-0135 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 200, 'lanes-tolerance-boundary-0135 item[1].x');
      expectClose(r.y - container.y, 0, 'lanes-tolerance-boundary-0135 item[1].y');
      expectClose(r.width, 200, 'lanes-tolerance-boundary-0135 item[1].w');
      expectClose(r.height, 10, 'lanes-tolerance-boundary-0135 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 400, 'lanes-tolerance-boundary-0135 item[2].x');
      expectClose(r.y - container.y, 0, 'lanes-tolerance-boundary-0135 item[2].y');
      expectClose(r.width, 200, 'lanes-tolerance-boundary-0135 item[2].w');
      expectClose(r.height, 50, 'lanes-tolerance-boundary-0135 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 0, 'lanes-tolerance-boundary-0135 item[3].x');
      expectClose(r.y - container.y, 30, 'lanes-tolerance-boundary-0135 item[3].y');
      expectClose(r.width, 200, 'lanes-tolerance-boundary-0135 item[3].w');
      expectClose(r.height, 25, 'lanes-tolerance-boundary-0135 item[3].h');
    }
  });
});

describe("lanes-tolerance-boundary-45", () => {
  it("lanes-tolerance-boundary-45-0136: boundary sweep: tolerance 40 against a 45px lane delta", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid-lanes","width":600,"gridTemplateColumns":"1fr 1fr 1fr","gap":0,"flowTolerance":40}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":50}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":5}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":80}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":25}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'lanes-tolerance-boundary-45-0136 container.width');
    expectClose(container.height, 80, 'lanes-tolerance-boundary-45-0136 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'lanes-tolerance-boundary-45-0136 item[0].x');
      expectClose(r.y - container.y, 0, 'lanes-tolerance-boundary-45-0136 item[0].y');
      expectClose(r.width, 200, 'lanes-tolerance-boundary-45-0136 item[0].w');
      expectClose(r.height, 50, 'lanes-tolerance-boundary-45-0136 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 200, 'lanes-tolerance-boundary-45-0136 item[1].x');
      expectClose(r.y - container.y, 0, 'lanes-tolerance-boundary-45-0136 item[1].y');
      expectClose(r.width, 200, 'lanes-tolerance-boundary-45-0136 item[1].w');
      expectClose(r.height, 5, 'lanes-tolerance-boundary-45-0136 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 400, 'lanes-tolerance-boundary-45-0136 item[2].x');
      expectClose(r.y - container.y, 0, 'lanes-tolerance-boundary-45-0136 item[2].y');
      expectClose(r.width, 200, 'lanes-tolerance-boundary-45-0136 item[2].w');
      expectClose(r.height, 80, 'lanes-tolerance-boundary-45-0136 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 200, 'lanes-tolerance-boundary-45-0136 item[3].x');
      expectClose(r.y - container.y, 5, 'lanes-tolerance-boundary-45-0136 item[3].y');
      expectClose(r.width, 200, 'lanes-tolerance-boundary-45-0136 item[3].w');
      expectClose(r.height, 25, 'lanes-tolerance-boundary-45-0136 item[3].h');
    }
  });
  it("lanes-tolerance-boundary-45-0137: boundary sweep: tolerance 44 against a 45px lane delta", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid-lanes","width":600,"gridTemplateColumns":"1fr 1fr 1fr","gap":0,"flowTolerance":44}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":50}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":5}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":80}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":25}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'lanes-tolerance-boundary-45-0137 container.width');
    expectClose(container.height, 80, 'lanes-tolerance-boundary-45-0137 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'lanes-tolerance-boundary-45-0137 item[0].x');
      expectClose(r.y - container.y, 0, 'lanes-tolerance-boundary-45-0137 item[0].y');
      expectClose(r.width, 200, 'lanes-tolerance-boundary-45-0137 item[0].w');
      expectClose(r.height, 50, 'lanes-tolerance-boundary-45-0137 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 200, 'lanes-tolerance-boundary-45-0137 item[1].x');
      expectClose(r.y - container.y, 0, 'lanes-tolerance-boundary-45-0137 item[1].y');
      expectClose(r.width, 200, 'lanes-tolerance-boundary-45-0137 item[1].w');
      expectClose(r.height, 5, 'lanes-tolerance-boundary-45-0137 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 400, 'lanes-tolerance-boundary-45-0137 item[2].x');
      expectClose(r.y - container.y, 0, 'lanes-tolerance-boundary-45-0137 item[2].y');
      expectClose(r.width, 200, 'lanes-tolerance-boundary-45-0137 item[2].w');
      expectClose(r.height, 80, 'lanes-tolerance-boundary-45-0137 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 200, 'lanes-tolerance-boundary-45-0137 item[3].x');
      expectClose(r.y - container.y, 5, 'lanes-tolerance-boundary-45-0137 item[3].y');
      expectClose(r.width, 200, 'lanes-tolerance-boundary-45-0137 item[3].w');
      expectClose(r.height, 25, 'lanes-tolerance-boundary-45-0137 item[3].h');
    }
  });
  it("lanes-tolerance-boundary-45-0138: boundary sweep: tolerance 45 against a 45px lane delta", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid-lanes","width":600,"gridTemplateColumns":"1fr 1fr 1fr","gap":0,"flowTolerance":45}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":50}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":5}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":80}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":25}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'lanes-tolerance-boundary-45-0138 container.width');
    expectClose(container.height, 80, 'lanes-tolerance-boundary-45-0138 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'lanes-tolerance-boundary-45-0138 item[0].x');
      expectClose(r.y - container.y, 0, 'lanes-tolerance-boundary-45-0138 item[0].y');
      expectClose(r.width, 200, 'lanes-tolerance-boundary-45-0138 item[0].w');
      expectClose(r.height, 50, 'lanes-tolerance-boundary-45-0138 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 200, 'lanes-tolerance-boundary-45-0138 item[1].x');
      expectClose(r.y - container.y, 0, 'lanes-tolerance-boundary-45-0138 item[1].y');
      expectClose(r.width, 200, 'lanes-tolerance-boundary-45-0138 item[1].w');
      expectClose(r.height, 5, 'lanes-tolerance-boundary-45-0138 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 400, 'lanes-tolerance-boundary-45-0138 item[2].x');
      expectClose(r.y - container.y, 0, 'lanes-tolerance-boundary-45-0138 item[2].y');
      expectClose(r.width, 200, 'lanes-tolerance-boundary-45-0138 item[2].w');
      expectClose(r.height, 80, 'lanes-tolerance-boundary-45-0138 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 0, 'lanes-tolerance-boundary-45-0138 item[3].x');
      expectClose(r.y - container.y, 50, 'lanes-tolerance-boundary-45-0138 item[3].y');
      expectClose(r.width, 200, 'lanes-tolerance-boundary-45-0138 item[3].w');
      expectClose(r.height, 25, 'lanes-tolerance-boundary-45-0138 item[3].h');
    }
  });
  it("lanes-tolerance-boundary-45-0139: boundary sweep: tolerance 46 against a 45px lane delta", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid-lanes","width":600,"gridTemplateColumns":"1fr 1fr 1fr","gap":0,"flowTolerance":46}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":50}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":5}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":80}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":25}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'lanes-tolerance-boundary-45-0139 container.width');
    expectClose(container.height, 80, 'lanes-tolerance-boundary-45-0139 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'lanes-tolerance-boundary-45-0139 item[0].x');
      expectClose(r.y - container.y, 0, 'lanes-tolerance-boundary-45-0139 item[0].y');
      expectClose(r.width, 200, 'lanes-tolerance-boundary-45-0139 item[0].w');
      expectClose(r.height, 50, 'lanes-tolerance-boundary-45-0139 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 200, 'lanes-tolerance-boundary-45-0139 item[1].x');
      expectClose(r.y - container.y, 0, 'lanes-tolerance-boundary-45-0139 item[1].y');
      expectClose(r.width, 200, 'lanes-tolerance-boundary-45-0139 item[1].w');
      expectClose(r.height, 5, 'lanes-tolerance-boundary-45-0139 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 400, 'lanes-tolerance-boundary-45-0139 item[2].x');
      expectClose(r.y - container.y, 0, 'lanes-tolerance-boundary-45-0139 item[2].y');
      expectClose(r.width, 200, 'lanes-tolerance-boundary-45-0139 item[2].w');
      expectClose(r.height, 80, 'lanes-tolerance-boundary-45-0139 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 0, 'lanes-tolerance-boundary-45-0139 item[3].x');
      expectClose(r.y - container.y, 50, 'lanes-tolerance-boundary-45-0139 item[3].y');
      expectClose(r.width, 200, 'lanes-tolerance-boundary-45-0139 item[3].w');
      expectClose(r.height, 25, 'lanes-tolerance-boundary-45-0139 item[3].h');
    }
  });
  it("lanes-tolerance-boundary-45-0140: boundary sweep: tolerance 50 against a 45px lane delta", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid-lanes","width":600,"gridTemplateColumns":"1fr 1fr 1fr","gap":0,"flowTolerance":50}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":50}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":5}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":80}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":25}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'lanes-tolerance-boundary-45-0140 container.width');
    expectClose(container.height, 80, 'lanes-tolerance-boundary-45-0140 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'lanes-tolerance-boundary-45-0140 item[0].x');
      expectClose(r.y - container.y, 0, 'lanes-tolerance-boundary-45-0140 item[0].y');
      expectClose(r.width, 200, 'lanes-tolerance-boundary-45-0140 item[0].w');
      expectClose(r.height, 50, 'lanes-tolerance-boundary-45-0140 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 200, 'lanes-tolerance-boundary-45-0140 item[1].x');
      expectClose(r.y - container.y, 0, 'lanes-tolerance-boundary-45-0140 item[1].y');
      expectClose(r.width, 200, 'lanes-tolerance-boundary-45-0140 item[1].w');
      expectClose(r.height, 5, 'lanes-tolerance-boundary-45-0140 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 400, 'lanes-tolerance-boundary-45-0140 item[2].x');
      expectClose(r.y - container.y, 0, 'lanes-tolerance-boundary-45-0140 item[2].y');
      expectClose(r.width, 200, 'lanes-tolerance-boundary-45-0140 item[2].w');
      expectClose(r.height, 80, 'lanes-tolerance-boundary-45-0140 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 0, 'lanes-tolerance-boundary-45-0140 item[3].x');
      expectClose(r.y - container.y, 50, 'lanes-tolerance-boundary-45-0140 item[3].y');
      expectClose(r.width, 200, 'lanes-tolerance-boundary-45-0140 item[3].w');
      expectClose(r.height, 25, 'lanes-tolerance-boundary-45-0140 item[3].h');
    }
  });
});

describe("lanes-tolerance-x-gap", () => {
  it("lanes-tolerance-x-gap-0141: tolerance 19 with gap 0 in the lane offsets", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid-lanes","width":600,"gridTemplateColumns":"1fr 1fr 1fr","gap":0,"flowTolerance":19}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":10}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":50}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":25}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'lanes-tolerance-x-gap-0141 container.width');
    expectClose(container.height, 50, 'lanes-tolerance-x-gap-0141 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'lanes-tolerance-x-gap-0141 item[0].x');
      expectClose(r.y - container.y, 0, 'lanes-tolerance-x-gap-0141 item[0].y');
      expectClose(r.width, 200, 'lanes-tolerance-x-gap-0141 item[0].w');
      expectClose(r.height, 30, 'lanes-tolerance-x-gap-0141 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 200, 'lanes-tolerance-x-gap-0141 item[1].x');
      expectClose(r.y - container.y, 0, 'lanes-tolerance-x-gap-0141 item[1].y');
      expectClose(r.width, 200, 'lanes-tolerance-x-gap-0141 item[1].w');
      expectClose(r.height, 10, 'lanes-tolerance-x-gap-0141 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 400, 'lanes-tolerance-x-gap-0141 item[2].x');
      expectClose(r.y - container.y, 0, 'lanes-tolerance-x-gap-0141 item[2].y');
      expectClose(r.width, 200, 'lanes-tolerance-x-gap-0141 item[2].w');
      expectClose(r.height, 50, 'lanes-tolerance-x-gap-0141 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 200, 'lanes-tolerance-x-gap-0141 item[3].x');
      expectClose(r.y - container.y, 10, 'lanes-tolerance-x-gap-0141 item[3].y');
      expectClose(r.width, 200, 'lanes-tolerance-x-gap-0141 item[3].w');
      expectClose(r.height, 25, 'lanes-tolerance-x-gap-0141 item[3].h');
    }
  });
  it("lanes-tolerance-x-gap-0142: tolerance 20 with gap 0 in the lane offsets", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid-lanes","width":600,"gridTemplateColumns":"1fr 1fr 1fr","gap":0,"flowTolerance":20}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":10}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":50}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":25}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'lanes-tolerance-x-gap-0142 container.width');
    expectClose(container.height, 55, 'lanes-tolerance-x-gap-0142 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'lanes-tolerance-x-gap-0142 item[0].x');
      expectClose(r.y - container.y, 0, 'lanes-tolerance-x-gap-0142 item[0].y');
      expectClose(r.width, 200, 'lanes-tolerance-x-gap-0142 item[0].w');
      expectClose(r.height, 30, 'lanes-tolerance-x-gap-0142 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 200, 'lanes-tolerance-x-gap-0142 item[1].x');
      expectClose(r.y - container.y, 0, 'lanes-tolerance-x-gap-0142 item[1].y');
      expectClose(r.width, 200, 'lanes-tolerance-x-gap-0142 item[1].w');
      expectClose(r.height, 10, 'lanes-tolerance-x-gap-0142 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 400, 'lanes-tolerance-x-gap-0142 item[2].x');
      expectClose(r.y - container.y, 0, 'lanes-tolerance-x-gap-0142 item[2].y');
      expectClose(r.width, 200, 'lanes-tolerance-x-gap-0142 item[2].w');
      expectClose(r.height, 50, 'lanes-tolerance-x-gap-0142 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 0, 'lanes-tolerance-x-gap-0142 item[3].x');
      expectClose(r.y - container.y, 30, 'lanes-tolerance-x-gap-0142 item[3].y');
      expectClose(r.width, 200, 'lanes-tolerance-x-gap-0142 item[3].w');
      expectClose(r.height, 25, 'lanes-tolerance-x-gap-0142 item[3].h');
    }
  });
  it("lanes-tolerance-x-gap-0143: tolerance 21 with gap 0 in the lane offsets", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid-lanes","width":600,"gridTemplateColumns":"1fr 1fr 1fr","gap":0,"flowTolerance":21}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":10}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":50}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":25}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'lanes-tolerance-x-gap-0143 container.width');
    expectClose(container.height, 55, 'lanes-tolerance-x-gap-0143 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'lanes-tolerance-x-gap-0143 item[0].x');
      expectClose(r.y - container.y, 0, 'lanes-tolerance-x-gap-0143 item[0].y');
      expectClose(r.width, 200, 'lanes-tolerance-x-gap-0143 item[0].w');
      expectClose(r.height, 30, 'lanes-tolerance-x-gap-0143 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 200, 'lanes-tolerance-x-gap-0143 item[1].x');
      expectClose(r.y - container.y, 0, 'lanes-tolerance-x-gap-0143 item[1].y');
      expectClose(r.width, 200, 'lanes-tolerance-x-gap-0143 item[1].w');
      expectClose(r.height, 10, 'lanes-tolerance-x-gap-0143 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 400, 'lanes-tolerance-x-gap-0143 item[2].x');
      expectClose(r.y - container.y, 0, 'lanes-tolerance-x-gap-0143 item[2].y');
      expectClose(r.width, 200, 'lanes-tolerance-x-gap-0143 item[2].w');
      expectClose(r.height, 50, 'lanes-tolerance-x-gap-0143 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 0, 'lanes-tolerance-x-gap-0143 item[3].x');
      expectClose(r.y - container.y, 30, 'lanes-tolerance-x-gap-0143 item[3].y');
      expectClose(r.width, 200, 'lanes-tolerance-x-gap-0143 item[3].w');
      expectClose(r.height, 25, 'lanes-tolerance-x-gap-0143 item[3].h');
    }
  });
  it("lanes-tolerance-x-gap-0144: tolerance 19 with gap 10 in the lane offsets", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid-lanes","width":600,"gridTemplateColumns":"1fr 1fr 1fr","gap":10,"flowTolerance":19}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":10}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":50}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":25}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'lanes-tolerance-x-gap-0144 container.width');
    expectClose(container.height, 50, 'lanes-tolerance-x-gap-0144 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'lanes-tolerance-x-gap-0144 item[0].x');
      expectClose(r.y - container.y, 0, 'lanes-tolerance-x-gap-0144 item[0].y');
      expectClose(r.width, 193.33, 'lanes-tolerance-x-gap-0144 item[0].w');
      expectClose(r.height, 30, 'lanes-tolerance-x-gap-0144 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 203.33, 'lanes-tolerance-x-gap-0144 item[1].x');
      expectClose(r.y - container.y, 0, 'lanes-tolerance-x-gap-0144 item[1].y');
      expectClose(r.width, 193.33, 'lanes-tolerance-x-gap-0144 item[1].w');
      expectClose(r.height, 10, 'lanes-tolerance-x-gap-0144 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 406.66, 'lanes-tolerance-x-gap-0144 item[2].x');
      expectClose(r.y - container.y, 0, 'lanes-tolerance-x-gap-0144 item[2].y');
      expectClose(r.width, 193.34, 'lanes-tolerance-x-gap-0144 item[2].w');
      expectClose(r.height, 50, 'lanes-tolerance-x-gap-0144 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 203.33, 'lanes-tolerance-x-gap-0144 item[3].x');
      expectClose(r.y - container.y, 20, 'lanes-tolerance-x-gap-0144 item[3].y');
      expectClose(r.width, 193.33, 'lanes-tolerance-x-gap-0144 item[3].w');
      expectClose(r.height, 25, 'lanes-tolerance-x-gap-0144 item[3].h');
    }
  });
  it("lanes-tolerance-x-gap-0145: tolerance 20 with gap 10 in the lane offsets", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid-lanes","width":600,"gridTemplateColumns":"1fr 1fr 1fr","gap":10,"flowTolerance":20}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":10}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":50}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":25}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'lanes-tolerance-x-gap-0145 container.width');
    expectClose(container.height, 65, 'lanes-tolerance-x-gap-0145 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'lanes-tolerance-x-gap-0145 item[0].x');
      expectClose(r.y - container.y, 0, 'lanes-tolerance-x-gap-0145 item[0].y');
      expectClose(r.width, 193.33, 'lanes-tolerance-x-gap-0145 item[0].w');
      expectClose(r.height, 30, 'lanes-tolerance-x-gap-0145 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 203.33, 'lanes-tolerance-x-gap-0145 item[1].x');
      expectClose(r.y - container.y, 0, 'lanes-tolerance-x-gap-0145 item[1].y');
      expectClose(r.width, 193.33, 'lanes-tolerance-x-gap-0145 item[1].w');
      expectClose(r.height, 10, 'lanes-tolerance-x-gap-0145 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 406.66, 'lanes-tolerance-x-gap-0145 item[2].x');
      expectClose(r.y - container.y, 0, 'lanes-tolerance-x-gap-0145 item[2].y');
      expectClose(r.width, 193.34, 'lanes-tolerance-x-gap-0145 item[2].w');
      expectClose(r.height, 50, 'lanes-tolerance-x-gap-0145 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 0, 'lanes-tolerance-x-gap-0145 item[3].x');
      expectClose(r.y - container.y, 40, 'lanes-tolerance-x-gap-0145 item[3].y');
      expectClose(r.width, 193.33, 'lanes-tolerance-x-gap-0145 item[3].w');
      expectClose(r.height, 25, 'lanes-tolerance-x-gap-0145 item[3].h');
    }
  });
  it("lanes-tolerance-x-gap-0146: tolerance 21 with gap 10 in the lane offsets", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid-lanes","width":600,"gridTemplateColumns":"1fr 1fr 1fr","gap":10,"flowTolerance":21}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":10}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":50}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":25}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'lanes-tolerance-x-gap-0146 container.width');
    expectClose(container.height, 65, 'lanes-tolerance-x-gap-0146 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'lanes-tolerance-x-gap-0146 item[0].x');
      expectClose(r.y - container.y, 0, 'lanes-tolerance-x-gap-0146 item[0].y');
      expectClose(r.width, 193.33, 'lanes-tolerance-x-gap-0146 item[0].w');
      expectClose(r.height, 30, 'lanes-tolerance-x-gap-0146 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 203.33, 'lanes-tolerance-x-gap-0146 item[1].x');
      expectClose(r.y - container.y, 0, 'lanes-tolerance-x-gap-0146 item[1].y');
      expectClose(r.width, 193.33, 'lanes-tolerance-x-gap-0146 item[1].w');
      expectClose(r.height, 10, 'lanes-tolerance-x-gap-0146 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 406.66, 'lanes-tolerance-x-gap-0146 item[2].x');
      expectClose(r.y - container.y, 0, 'lanes-tolerance-x-gap-0146 item[2].y');
      expectClose(r.width, 193.34, 'lanes-tolerance-x-gap-0146 item[2].w');
      expectClose(r.height, 50, 'lanes-tolerance-x-gap-0146 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 0, 'lanes-tolerance-x-gap-0146 item[3].x');
      expectClose(r.y - container.y, 40, 'lanes-tolerance-x-gap-0146 item[3].y');
      expectClose(r.width, 193.33, 'lanes-tolerance-x-gap-0146 item[3].w');
      expectClose(r.height, 25, 'lanes-tolerance-x-gap-0146 item[3].h');
    }
  });
  it("lanes-tolerance-x-gap-0147: tolerance 19 with gap 20 in the lane offsets", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid-lanes","width":600,"gridTemplateColumns":"1fr 1fr 1fr","gap":20,"flowTolerance":19}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":10}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":50}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":25}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'lanes-tolerance-x-gap-0147 container.width');
    expectClose(container.height, 55, 'lanes-tolerance-x-gap-0147 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'lanes-tolerance-x-gap-0147 item[0].x');
      expectClose(r.y - container.y, 0, 'lanes-tolerance-x-gap-0147 item[0].y');
      expectClose(r.width, 186.66, 'lanes-tolerance-x-gap-0147 item[0].w');
      expectClose(r.height, 30, 'lanes-tolerance-x-gap-0147 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 206.66, 'lanes-tolerance-x-gap-0147 item[1].x');
      expectClose(r.y - container.y, 0, 'lanes-tolerance-x-gap-0147 item[1].y');
      expectClose(r.width, 186.67, 'lanes-tolerance-x-gap-0147 item[1].w');
      expectClose(r.height, 10, 'lanes-tolerance-x-gap-0147 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 413.33, 'lanes-tolerance-x-gap-0147 item[2].x');
      expectClose(r.y - container.y, 0, 'lanes-tolerance-x-gap-0147 item[2].y');
      expectClose(r.width, 186.66, 'lanes-tolerance-x-gap-0147 item[2].w');
      expectClose(r.height, 50, 'lanes-tolerance-x-gap-0147 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 206.66, 'lanes-tolerance-x-gap-0147 item[3].x');
      expectClose(r.y - container.y, 30, 'lanes-tolerance-x-gap-0147 item[3].y');
      expectClose(r.width, 186.67, 'lanes-tolerance-x-gap-0147 item[3].w');
      expectClose(r.height, 25, 'lanes-tolerance-x-gap-0147 item[3].h');
    }
  });
  it("lanes-tolerance-x-gap-0148: tolerance 20 with gap 20 in the lane offsets", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid-lanes","width":600,"gridTemplateColumns":"1fr 1fr 1fr","gap":20,"flowTolerance":20}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":10}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":50}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":25}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'lanes-tolerance-x-gap-0148 container.width');
    expectClose(container.height, 75, 'lanes-tolerance-x-gap-0148 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'lanes-tolerance-x-gap-0148 item[0].x');
      expectClose(r.y - container.y, 0, 'lanes-tolerance-x-gap-0148 item[0].y');
      expectClose(r.width, 186.66, 'lanes-tolerance-x-gap-0148 item[0].w');
      expectClose(r.height, 30, 'lanes-tolerance-x-gap-0148 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 206.66, 'lanes-tolerance-x-gap-0148 item[1].x');
      expectClose(r.y - container.y, 0, 'lanes-tolerance-x-gap-0148 item[1].y');
      expectClose(r.width, 186.67, 'lanes-tolerance-x-gap-0148 item[1].w');
      expectClose(r.height, 10, 'lanes-tolerance-x-gap-0148 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 413.33, 'lanes-tolerance-x-gap-0148 item[2].x');
      expectClose(r.y - container.y, 0, 'lanes-tolerance-x-gap-0148 item[2].y');
      expectClose(r.width, 186.66, 'lanes-tolerance-x-gap-0148 item[2].w');
      expectClose(r.height, 50, 'lanes-tolerance-x-gap-0148 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 0, 'lanes-tolerance-x-gap-0148 item[3].x');
      expectClose(r.y - container.y, 50, 'lanes-tolerance-x-gap-0148 item[3].y');
      expectClose(r.width, 186.66, 'lanes-tolerance-x-gap-0148 item[3].w');
      expectClose(r.height, 25, 'lanes-tolerance-x-gap-0148 item[3].h');
    }
  });
  it("lanes-tolerance-x-gap-0149: tolerance 21 with gap 20 in the lane offsets", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid-lanes","width":600,"gridTemplateColumns":"1fr 1fr 1fr","gap":20,"flowTolerance":21}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":10}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":50}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":25}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'lanes-tolerance-x-gap-0149 container.width');
    expectClose(container.height, 75, 'lanes-tolerance-x-gap-0149 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'lanes-tolerance-x-gap-0149 item[0].x');
      expectClose(r.y - container.y, 0, 'lanes-tolerance-x-gap-0149 item[0].y');
      expectClose(r.width, 186.66, 'lanes-tolerance-x-gap-0149 item[0].w');
      expectClose(r.height, 30, 'lanes-tolerance-x-gap-0149 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 206.66, 'lanes-tolerance-x-gap-0149 item[1].x');
      expectClose(r.y - container.y, 0, 'lanes-tolerance-x-gap-0149 item[1].y');
      expectClose(r.width, 186.67, 'lanes-tolerance-x-gap-0149 item[1].w');
      expectClose(r.height, 10, 'lanes-tolerance-x-gap-0149 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 413.33, 'lanes-tolerance-x-gap-0149 item[2].x');
      expectClose(r.y - container.y, 0, 'lanes-tolerance-x-gap-0149 item[2].y');
      expectClose(r.width, 186.66, 'lanes-tolerance-x-gap-0149 item[2].w');
      expectClose(r.height, 50, 'lanes-tolerance-x-gap-0149 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 0, 'lanes-tolerance-x-gap-0149 item[3].x');
      expectClose(r.y - container.y, 50, 'lanes-tolerance-x-gap-0149 item[3].y');
      expectClose(r.width, 186.66, 'lanes-tolerance-x-gap-0149 item[3].w');
      expectClose(r.height, 25, 'lanes-tolerance-x-gap-0149 item[3].h');
    }
  });
});

describe("lanes-tolerance-x-span", () => {
  it("lanes-tolerance-x-span-0150: spanning item under flow-tolerance 0", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid-lanes","width":600,"gridTemplateColumns":"1fr 1fr 1fr","gap":10,"flowTolerance":0}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":40}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":60}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30,"gridColumnEnd":"span 2"}}
          />
          <View
            collapsable={false}
            ref={itemRefs[4]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":20}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'lanes-tolerance-x-span-0150 container.width');
    expectClose(container.height, 90, 'lanes-tolerance-x-span-0150 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'lanes-tolerance-x-span-0150 item[0].x');
      expectClose(r.y - container.y, 0, 'lanes-tolerance-x-span-0150 item[0].y');
      expectClose(r.width, 193.33, 'lanes-tolerance-x-span-0150 item[0].w');
      expectClose(r.height, 40, 'lanes-tolerance-x-span-0150 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 203.33, 'lanes-tolerance-x-span-0150 item[1].x');
      expectClose(r.y - container.y, 0, 'lanes-tolerance-x-span-0150 item[1].y');
      expectClose(r.width, 193.33, 'lanes-tolerance-x-span-0150 item[1].w');
      expectClose(r.height, 20, 'lanes-tolerance-x-span-0150 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 406.66, 'lanes-tolerance-x-span-0150 item[2].x');
      expectClose(r.y - container.y, 0, 'lanes-tolerance-x-span-0150 item[2].y');
      expectClose(r.width, 193.34, 'lanes-tolerance-x-span-0150 item[2].w');
      expectClose(r.height, 60, 'lanes-tolerance-x-span-0150 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 0, 'lanes-tolerance-x-span-0150 item[3].x');
      expectClose(r.y - container.y, 50, 'lanes-tolerance-x-span-0150 item[3].y');
      expectClose(r.width, 396.66, 'lanes-tolerance-x-span-0150 item[3].w');
      expectClose(r.height, 30, 'lanes-tolerance-x-span-0150 item[3].h');
    }
    {
      const r = rectOf(itemRefs[4]);
      expectClose(r.x - container.x, 406.66, 'lanes-tolerance-x-span-0150 item[4].x');
      expectClose(r.y - container.y, 70, 'lanes-tolerance-x-span-0150 item[4].y');
      expectClose(r.width, 193.34, 'lanes-tolerance-x-span-0150 item[4].w');
      expectClose(r.height, 20, 'lanes-tolerance-x-span-0150 item[4].h');
    }
  });
  it("lanes-tolerance-x-span-0152: spanning item under flow-tolerance normal", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid-lanes","width":600,"gridTemplateColumns":"1fr 1fr 1fr","gap":10,"flowTolerance":"normal"}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":40}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":60}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30,"gridColumnEnd":"span 2"}}
          />
          <View
            collapsable={false}
            ref={itemRefs[4]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":20}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'lanes-tolerance-x-span-0152 container.width');
    expectClose(container.height, 90, 'lanes-tolerance-x-span-0152 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'lanes-tolerance-x-span-0152 item[0].x');
      expectClose(r.y - container.y, 0, 'lanes-tolerance-x-span-0152 item[0].y');
      expectClose(r.width, 193.33, 'lanes-tolerance-x-span-0152 item[0].w');
      expectClose(r.height, 40, 'lanes-tolerance-x-span-0152 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 203.33, 'lanes-tolerance-x-span-0152 item[1].x');
      expectClose(r.y - container.y, 0, 'lanes-tolerance-x-span-0152 item[1].y');
      expectClose(r.width, 193.33, 'lanes-tolerance-x-span-0152 item[1].w');
      expectClose(r.height, 20, 'lanes-tolerance-x-span-0152 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 406.66, 'lanes-tolerance-x-span-0152 item[2].x');
      expectClose(r.y - container.y, 0, 'lanes-tolerance-x-span-0152 item[2].y');
      expectClose(r.width, 193.34, 'lanes-tolerance-x-span-0152 item[2].w');
      expectClose(r.height, 60, 'lanes-tolerance-x-span-0152 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 0, 'lanes-tolerance-x-span-0152 item[3].x');
      expectClose(r.y - container.y, 50, 'lanes-tolerance-x-span-0152 item[3].y');
      expectClose(r.width, 396.66, 'lanes-tolerance-x-span-0152 item[3].w');
      expectClose(r.height, 30, 'lanes-tolerance-x-span-0152 item[3].h');
    }
    {
      const r = rectOf(itemRefs[4]);
      expectClose(r.x - container.x, 406.66, 'lanes-tolerance-x-span-0152 item[4].x');
      expectClose(r.y - container.y, 70, 'lanes-tolerance-x-span-0152 item[4].y');
      expectClose(r.width, 193.34, 'lanes-tolerance-x-span-0152 item[4].w');
      expectClose(r.height, 20, 'lanes-tolerance-x-span-0152 item[4].h');
    }
  });
  it("lanes-tolerance-x-span-0154: spanning item under flow-tolerance infinite", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid-lanes","width":600,"gridTemplateColumns":"1fr 1fr 1fr","gap":10,"flowTolerance":"infinite"}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":40}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":60}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30,"gridColumnEnd":"span 2"}}
          />
          <View
            collapsable={false}
            ref={itemRefs[4]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":20}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'lanes-tolerance-x-span-0154 container.width');
    expectClose(container.height, 90, 'lanes-tolerance-x-span-0154 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'lanes-tolerance-x-span-0154 item[0].x');
      expectClose(r.y - container.y, 0, 'lanes-tolerance-x-span-0154 item[0].y');
      expectClose(r.width, 193.33, 'lanes-tolerance-x-span-0154 item[0].w');
      expectClose(r.height, 40, 'lanes-tolerance-x-span-0154 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 203.33, 'lanes-tolerance-x-span-0154 item[1].x');
      expectClose(r.y - container.y, 0, 'lanes-tolerance-x-span-0154 item[1].y');
      expectClose(r.width, 193.33, 'lanes-tolerance-x-span-0154 item[1].w');
      expectClose(r.height, 20, 'lanes-tolerance-x-span-0154 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 406.66, 'lanes-tolerance-x-span-0154 item[2].x');
      expectClose(r.y - container.y, 0, 'lanes-tolerance-x-span-0154 item[2].y');
      expectClose(r.width, 193.34, 'lanes-tolerance-x-span-0154 item[2].w');
      expectClose(r.height, 60, 'lanes-tolerance-x-span-0154 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 0, 'lanes-tolerance-x-span-0154 item[3].x');
      expectClose(r.y - container.y, 50, 'lanes-tolerance-x-span-0154 item[3].y');
      expectClose(r.width, 396.66, 'lanes-tolerance-x-span-0154 item[3].w');
      expectClose(r.height, 30, 'lanes-tolerance-x-span-0154 item[3].h');
    }
    {
      const r = rectOf(itemRefs[4]);
      expectClose(r.x - container.x, 406.66, 'lanes-tolerance-x-span-0154 item[4].x');
      expectClose(r.y - container.y, 70, 'lanes-tolerance-x-span-0154 item[4].y');
      expectClose(r.width, 193.34, 'lanes-tolerance-x-span-0154 item[4].w');
      expectClose(r.height, 20, 'lanes-tolerance-x-span-0154 item[4].h');
    }
  });
});

describe("lanes-tolerance-x-dense", () => {
  it("lanes-tolerance-x-dense-0151: dense packing under flow-tolerance 0", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid-lanes","width":600,"gridTemplateColumns":"1fr 1fr 1fr","gap":10,"gridAutoFlow":"row dense","flowTolerance":0}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30,"gridColumnEnd":"span 2"}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":40}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":50}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'lanes-tolerance-x-dense-0151 container.width');
    expectClose(container.height, 90, 'lanes-tolerance-x-dense-0151 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'lanes-tolerance-x-dense-0151 item[0].x');
      expectClose(r.y - container.y, 0, 'lanes-tolerance-x-dense-0151 item[0].y');
      expectClose(r.width, 396.66, 'lanes-tolerance-x-dense-0151 item[0].w');
      expectClose(r.height, 30, 'lanes-tolerance-x-dense-0151 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 406.66, 'lanes-tolerance-x-dense-0151 item[1].x');
      expectClose(r.y - container.y, 0, 'lanes-tolerance-x-dense-0151 item[1].y');
      expectClose(r.width, 193.34, 'lanes-tolerance-x-dense-0151 item[1].w');
      expectClose(r.height, 40, 'lanes-tolerance-x-dense-0151 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 0, 'lanes-tolerance-x-dense-0151 item[2].x');
      expectClose(r.y - container.y, 40, 'lanes-tolerance-x-dense-0151 item[2].y');
      expectClose(r.width, 193.33, 'lanes-tolerance-x-dense-0151 item[2].w');
      expectClose(r.height, 20, 'lanes-tolerance-x-dense-0151 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 203.33, 'lanes-tolerance-x-dense-0151 item[3].x');
      expectClose(r.y - container.y, 40, 'lanes-tolerance-x-dense-0151 item[3].y');
      expectClose(r.width, 193.33, 'lanes-tolerance-x-dense-0151 item[3].w');
      expectClose(r.height, 50, 'lanes-tolerance-x-dense-0151 item[3].h');
    }
  });
  it("lanes-tolerance-x-dense-0153: dense packing under flow-tolerance normal", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid-lanes","width":600,"gridTemplateColumns":"1fr 1fr 1fr","gap":10,"gridAutoFlow":"row dense","flowTolerance":"normal"}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30,"gridColumnEnd":"span 2"}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":40}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":50}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'lanes-tolerance-x-dense-0153 container.width');
    expectClose(container.height, 90, 'lanes-tolerance-x-dense-0153 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'lanes-tolerance-x-dense-0153 item[0].x');
      expectClose(r.y - container.y, 0, 'lanes-tolerance-x-dense-0153 item[0].y');
      expectClose(r.width, 396.66, 'lanes-tolerance-x-dense-0153 item[0].w');
      expectClose(r.height, 30, 'lanes-tolerance-x-dense-0153 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 406.66, 'lanes-tolerance-x-dense-0153 item[1].x');
      expectClose(r.y - container.y, 0, 'lanes-tolerance-x-dense-0153 item[1].y');
      expectClose(r.width, 193.34, 'lanes-tolerance-x-dense-0153 item[1].w');
      expectClose(r.height, 40, 'lanes-tolerance-x-dense-0153 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 0, 'lanes-tolerance-x-dense-0153 item[2].x');
      expectClose(r.y - container.y, 40, 'lanes-tolerance-x-dense-0153 item[2].y');
      expectClose(r.width, 193.33, 'lanes-tolerance-x-dense-0153 item[2].w');
      expectClose(r.height, 20, 'lanes-tolerance-x-dense-0153 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 203.33, 'lanes-tolerance-x-dense-0153 item[3].x');
      expectClose(r.y - container.y, 40, 'lanes-tolerance-x-dense-0153 item[3].y');
      expectClose(r.width, 193.33, 'lanes-tolerance-x-dense-0153 item[3].w');
      expectClose(r.height, 50, 'lanes-tolerance-x-dense-0153 item[3].h');
    }
  });
  it("lanes-tolerance-x-dense-0155: dense packing under flow-tolerance infinite", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid-lanes","width":600,"gridTemplateColumns":"1fr 1fr 1fr","gap":10,"gridAutoFlow":"row dense","flowTolerance":"infinite"}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30,"gridColumnEnd":"span 2"}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":40}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":50}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'lanes-tolerance-x-dense-0155 container.width');
    expectClose(container.height, 90, 'lanes-tolerance-x-dense-0155 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'lanes-tolerance-x-dense-0155 item[0].x');
      expectClose(r.y - container.y, 0, 'lanes-tolerance-x-dense-0155 item[0].y');
      expectClose(r.width, 396.66, 'lanes-tolerance-x-dense-0155 item[0].w');
      expectClose(r.height, 30, 'lanes-tolerance-x-dense-0155 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 406.66, 'lanes-tolerance-x-dense-0155 item[1].x');
      expectClose(r.y - container.y, 0, 'lanes-tolerance-x-dense-0155 item[1].y');
      expectClose(r.width, 193.34, 'lanes-tolerance-x-dense-0155 item[1].w');
      expectClose(r.height, 40, 'lanes-tolerance-x-dense-0155 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 0, 'lanes-tolerance-x-dense-0155 item[2].x');
      expectClose(r.y - container.y, 40, 'lanes-tolerance-x-dense-0155 item[2].y');
      expectClose(r.width, 193.33, 'lanes-tolerance-x-dense-0155 item[2].w');
      expectClose(r.height, 20, 'lanes-tolerance-x-dense-0155 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 203.33, 'lanes-tolerance-x-dense-0155 item[3].x');
      expectClose(r.y - container.y, 40, 'lanes-tolerance-x-dense-0155 item[3].y');
      expectClose(r.width, 193.33, 'lanes-tolerance-x-dense-0155 item[3].w');
      expectClose(r.height, 50, 'lanes-tolerance-x-dense-0155 item[3].h');
    }
  });
});

describe("lanes-x-container-box", () => {
  it("lanes-x-container-box-0156: lanes inside padding 0 border 0", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid-lanes","width":600,"gridTemplateColumns":"1fr 1fr 1fr","gap":10,"flowTolerance":0}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":40}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":60}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":50}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'lanes-x-container-box-0156 container.width');
    expectClose(container.height, 90, 'lanes-x-container-box-0156 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'lanes-x-container-box-0156 item[0].x');
      expectClose(r.y - container.y, 0, 'lanes-x-container-box-0156 item[0].y');
      expectClose(r.width, 193.33, 'lanes-x-container-box-0156 item[0].w');
      expectClose(r.height, 40, 'lanes-x-container-box-0156 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 203.33, 'lanes-x-container-box-0156 item[1].x');
      expectClose(r.y - container.y, 0, 'lanes-x-container-box-0156 item[1].y');
      expectClose(r.width, 193.33, 'lanes-x-container-box-0156 item[1].w');
      expectClose(r.height, 60, 'lanes-x-container-box-0156 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 406.66, 'lanes-x-container-box-0156 item[2].x');
      expectClose(r.y - container.y, 0, 'lanes-x-container-box-0156 item[2].y');
      expectClose(r.width, 193.34, 'lanes-x-container-box-0156 item[2].w');
      expectClose(r.height, 30, 'lanes-x-container-box-0156 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 406.66, 'lanes-x-container-box-0156 item[3].x');
      expectClose(r.y - container.y, 40, 'lanes-x-container-box-0156 item[3].y');
      expectClose(r.width, 193.34, 'lanes-x-container-box-0156 item[3].w');
      expectClose(r.height, 50, 'lanes-x-container-box-0156 item[3].h');
    }
  });
  it("lanes-x-container-box-0157: lanes inside padding 16 border 0", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid-lanes","width":600,"gridTemplateColumns":"1fr 1fr 1fr","gap":10,"padding":16,"flowTolerance":0}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":40}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":60}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":50}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'lanes-x-container-box-0157 container.width');
    expectClose(container.height, 122, 'lanes-x-container-box-0157 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 16, 'lanes-x-container-box-0157 item[0].x');
      expectClose(r.y - container.y, 16, 'lanes-x-container-box-0157 item[0].y');
      expectClose(r.width, 182.66, 'lanes-x-container-box-0157 item[0].w');
      expectClose(r.height, 40, 'lanes-x-container-box-0157 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 208.66, 'lanes-x-container-box-0157 item[1].x');
      expectClose(r.y - container.y, 16, 'lanes-x-container-box-0157 item[1].y');
      expectClose(r.width, 182.67, 'lanes-x-container-box-0157 item[1].w');
      expectClose(r.height, 60, 'lanes-x-container-box-0157 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 401.33, 'lanes-x-container-box-0157 item[2].x');
      expectClose(r.y - container.y, 16, 'lanes-x-container-box-0157 item[2].y');
      expectClose(r.width, 182.66, 'lanes-x-container-box-0157 item[2].w');
      expectClose(r.height, 30, 'lanes-x-container-box-0157 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 401.33, 'lanes-x-container-box-0157 item[3].x');
      expectClose(r.y - container.y, 56, 'lanes-x-container-box-0157 item[3].y');
      expectClose(r.width, 182.66, 'lanes-x-container-box-0157 item[3].w');
      expectClose(r.height, 50, 'lanes-x-container-box-0157 item[3].h');
    }
  });
  it("lanes-x-container-box-0158: lanes inside padding 0 border 4", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid-lanes","width":600,"gridTemplateColumns":"1fr 1fr 1fr","gap":10,"borderWidth":4,"borderColor":"transparent","flowTolerance":0}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":40}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":60}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":50}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'lanes-x-container-box-0158 container.width');
    expectClose(container.height, 98, 'lanes-x-container-box-0158 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 4, 'lanes-x-container-box-0158 item[0].x');
      expectClose(r.y - container.y, 4, 'lanes-x-container-box-0158 item[0].y');
      expectClose(r.width, 190.66, 'lanes-x-container-box-0158 item[0].w');
      expectClose(r.height, 40, 'lanes-x-container-box-0158 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 204.66, 'lanes-x-container-box-0158 item[1].x');
      expectClose(r.y - container.y, 4, 'lanes-x-container-box-0158 item[1].y');
      expectClose(r.width, 190.67, 'lanes-x-container-box-0158 item[1].w');
      expectClose(r.height, 60, 'lanes-x-container-box-0158 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 405.33, 'lanes-x-container-box-0158 item[2].x');
      expectClose(r.y - container.y, 4, 'lanes-x-container-box-0158 item[2].y');
      expectClose(r.width, 190.66, 'lanes-x-container-box-0158 item[2].w');
      expectClose(r.height, 30, 'lanes-x-container-box-0158 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 405.33, 'lanes-x-container-box-0158 item[3].x');
      expectClose(r.y - container.y, 44, 'lanes-x-container-box-0158 item[3].y');
      expectClose(r.width, 190.66, 'lanes-x-container-box-0158 item[3].w');
      expectClose(r.height, 50, 'lanes-x-container-box-0158 item[3].h');
    }
  });
  it("lanes-x-container-box-0159: lanes inside padding 10 border 2", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid-lanes","width":600,"gridTemplateColumns":"1fr 1fr 1fr","gap":10,"padding":10,"borderWidth":2,"borderColor":"transparent","flowTolerance":0}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":40}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":60}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":50}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'lanes-x-container-box-0159 container.width');
    expectClose(container.height, 114, 'lanes-x-container-box-0159 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 12, 'lanes-x-container-box-0159 item[0].x');
      expectClose(r.y - container.y, 12, 'lanes-x-container-box-0159 item[0].y');
      expectClose(r.width, 185.33, 'lanes-x-container-box-0159 item[0].w');
      expectClose(r.height, 40, 'lanes-x-container-box-0159 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 207.33, 'lanes-x-container-box-0159 item[1].x');
      expectClose(r.y - container.y, 12, 'lanes-x-container-box-0159 item[1].y');
      expectClose(r.width, 185.33, 'lanes-x-container-box-0159 item[1].w');
      expectClose(r.height, 60, 'lanes-x-container-box-0159 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 402.66, 'lanes-x-container-box-0159 item[2].x');
      expectClose(r.y - container.y, 12, 'lanes-x-container-box-0159 item[2].y');
      expectClose(r.width, 185.34, 'lanes-x-container-box-0159 item[2].w');
      expectClose(r.height, 30, 'lanes-x-container-box-0159 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 402.66, 'lanes-x-container-box-0159 item[3].x');
      expectClose(r.y - container.y, 52, 'lanes-x-container-box-0159 item[3].y');
      expectClose(r.width, 185.34, 'lanes-x-container-box-0159 item[3].w');
      expectClose(r.height, 50, 'lanes-x-container-box-0159 item[3].h');
    }
  });
});

describe("lanes-x-item-box", () => {
  it("lanes-x-item-box-0160: items with padding 0 border 0", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid-lanes","width":600,"gridTemplateColumns":"1fr 1fr 1fr","gap":10,"flowTolerance":0}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":40,"padding":0,"borderWidth":0,"borderColor":"transparent"}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":60,"padding":0,"borderWidth":0,"borderColor":"transparent"}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30,"padding":0,"borderWidth":0,"borderColor":"transparent"}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":50,"padding":0,"borderWidth":0,"borderColor":"transparent"}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'lanes-x-item-box-0160 container.width');
    expectClose(container.height, 90, 'lanes-x-item-box-0160 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'lanes-x-item-box-0160 item[0].x');
      expectClose(r.y - container.y, 0, 'lanes-x-item-box-0160 item[0].y');
      expectClose(r.width, 193.33, 'lanes-x-item-box-0160 item[0].w');
      expectClose(r.height, 40, 'lanes-x-item-box-0160 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 203.33, 'lanes-x-item-box-0160 item[1].x');
      expectClose(r.y - container.y, 0, 'lanes-x-item-box-0160 item[1].y');
      expectClose(r.width, 193.33, 'lanes-x-item-box-0160 item[1].w');
      expectClose(r.height, 60, 'lanes-x-item-box-0160 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 406.66, 'lanes-x-item-box-0160 item[2].x');
      expectClose(r.y - container.y, 0, 'lanes-x-item-box-0160 item[2].y');
      expectClose(r.width, 193.34, 'lanes-x-item-box-0160 item[2].w');
      expectClose(r.height, 30, 'lanes-x-item-box-0160 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 406.66, 'lanes-x-item-box-0160 item[3].x');
      expectClose(r.y - container.y, 40, 'lanes-x-item-box-0160 item[3].y');
      expectClose(r.width, 193.34, 'lanes-x-item-box-0160 item[3].w');
      expectClose(r.height, 50, 'lanes-x-item-box-0160 item[3].h');
    }
  });
  it("lanes-x-item-box-0161: items with padding 8 border 0", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid-lanes","width":600,"gridTemplateColumns":"1fr 1fr 1fr","gap":10,"flowTolerance":0}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":40,"padding":8,"borderWidth":0,"borderColor":"transparent"}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":60,"padding":8,"borderWidth":0,"borderColor":"transparent"}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30,"padding":8,"borderWidth":0,"borderColor":"transparent"}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":50,"padding":8,"borderWidth":0,"borderColor":"transparent"}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'lanes-x-item-box-0161 container.width');
    expectClose(container.height, 90, 'lanes-x-item-box-0161 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'lanes-x-item-box-0161 item[0].x');
      expectClose(r.y - container.y, 0, 'lanes-x-item-box-0161 item[0].y');
      expectClose(r.width, 193.33, 'lanes-x-item-box-0161 item[0].w');
      expectClose(r.height, 40, 'lanes-x-item-box-0161 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 203.33, 'lanes-x-item-box-0161 item[1].x');
      expectClose(r.y - container.y, 0, 'lanes-x-item-box-0161 item[1].y');
      expectClose(r.width, 193.33, 'lanes-x-item-box-0161 item[1].w');
      expectClose(r.height, 60, 'lanes-x-item-box-0161 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 406.66, 'lanes-x-item-box-0161 item[2].x');
      expectClose(r.y - container.y, 0, 'lanes-x-item-box-0161 item[2].y');
      expectClose(r.width, 193.34, 'lanes-x-item-box-0161 item[2].w');
      expectClose(r.height, 30, 'lanes-x-item-box-0161 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 406.66, 'lanes-x-item-box-0161 item[3].x');
      expectClose(r.y - container.y, 40, 'lanes-x-item-box-0161 item[3].y');
      expectClose(r.width, 193.34, 'lanes-x-item-box-0161 item[3].w');
      expectClose(r.height, 50, 'lanes-x-item-box-0161 item[3].h');
    }
  });
  it("lanes-x-item-box-0162: items with padding 0 border 3", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid-lanes","width":600,"gridTemplateColumns":"1fr 1fr 1fr","gap":10,"flowTolerance":0}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":40,"padding":0,"borderWidth":3,"borderColor":"transparent"}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":60,"padding":0,"borderWidth":3,"borderColor":"transparent"}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30,"padding":0,"borderWidth":3,"borderColor":"transparent"}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":50,"padding":0,"borderWidth":3,"borderColor":"transparent"}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'lanes-x-item-box-0162 container.width');
    expectClose(container.height, 90, 'lanes-x-item-box-0162 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'lanes-x-item-box-0162 item[0].x');
      expectClose(r.y - container.y, 0, 'lanes-x-item-box-0162 item[0].y');
      expectClose(r.width, 193.33, 'lanes-x-item-box-0162 item[0].w');
      expectClose(r.height, 40, 'lanes-x-item-box-0162 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 203.33, 'lanes-x-item-box-0162 item[1].x');
      expectClose(r.y - container.y, 0, 'lanes-x-item-box-0162 item[1].y');
      expectClose(r.width, 193.33, 'lanes-x-item-box-0162 item[1].w');
      expectClose(r.height, 60, 'lanes-x-item-box-0162 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 406.66, 'lanes-x-item-box-0162 item[2].x');
      expectClose(r.y - container.y, 0, 'lanes-x-item-box-0162 item[2].y');
      expectClose(r.width, 193.34, 'lanes-x-item-box-0162 item[2].w');
      expectClose(r.height, 30, 'lanes-x-item-box-0162 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 406.66, 'lanes-x-item-box-0162 item[3].x');
      expectClose(r.y - container.y, 40, 'lanes-x-item-box-0162 item[3].y');
      expectClose(r.width, 193.34, 'lanes-x-item-box-0162 item[3].w');
      expectClose(r.height, 50, 'lanes-x-item-box-0162 item[3].h');
    }
  });
  it("lanes-x-item-box-0163: items with padding 6 border 2", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid-lanes","width":600,"gridTemplateColumns":"1fr 1fr 1fr","gap":10,"flowTolerance":0}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":40,"padding":6,"borderWidth":2,"borderColor":"transparent"}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":60,"padding":6,"borderWidth":2,"borderColor":"transparent"}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30,"padding":6,"borderWidth":2,"borderColor":"transparent"}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":50,"padding":6,"borderWidth":2,"borderColor":"transparent"}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'lanes-x-item-box-0163 container.width');
    expectClose(container.height, 90, 'lanes-x-item-box-0163 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'lanes-x-item-box-0163 item[0].x');
      expectClose(r.y - container.y, 0, 'lanes-x-item-box-0163 item[0].y');
      expectClose(r.width, 193.33, 'lanes-x-item-box-0163 item[0].w');
      expectClose(r.height, 40, 'lanes-x-item-box-0163 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 203.33, 'lanes-x-item-box-0163 item[1].x');
      expectClose(r.y - container.y, 0, 'lanes-x-item-box-0163 item[1].y');
      expectClose(r.width, 193.33, 'lanes-x-item-box-0163 item[1].w');
      expectClose(r.height, 60, 'lanes-x-item-box-0163 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 406.66, 'lanes-x-item-box-0163 item[2].x');
      expectClose(r.y - container.y, 0, 'lanes-x-item-box-0163 item[2].y');
      expectClose(r.width, 193.34, 'lanes-x-item-box-0163 item[2].w');
      expectClose(r.height, 30, 'lanes-x-item-box-0163 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 406.66, 'lanes-x-item-box-0163 item[3].x');
      expectClose(r.y - container.y, 40, 'lanes-x-item-box-0163 item[3].y');
      expectClose(r.width, 193.34, 'lanes-x-item-box-0163 item[3].w');
      expectClose(r.height, 50, 'lanes-x-item-box-0163 item[3].h');
    }
  });
});

describe("lanes-x-justify-content", () => {
  it("lanes-x-justify-content-0164: justify-content:start with slack in a lanes container", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid-lanes","width":600,"gridTemplateColumns":"120px 120px 120px","gap":10,"flowTolerance":0,"justifyContent":"start"}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":40}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":60}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":50}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'lanes-x-justify-content-0164 container.width');
    expectClose(container.height, 90, 'lanes-x-justify-content-0164 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'lanes-x-justify-content-0164 item[0].x');
      expectClose(r.y - container.y, 0, 'lanes-x-justify-content-0164 item[0].y');
      expectClose(r.width, 120, 'lanes-x-justify-content-0164 item[0].w');
      expectClose(r.height, 40, 'lanes-x-justify-content-0164 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 130, 'lanes-x-justify-content-0164 item[1].x');
      expectClose(r.y - container.y, 0, 'lanes-x-justify-content-0164 item[1].y');
      expectClose(r.width, 120, 'lanes-x-justify-content-0164 item[1].w');
      expectClose(r.height, 60, 'lanes-x-justify-content-0164 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 260, 'lanes-x-justify-content-0164 item[2].x');
      expectClose(r.y - container.y, 0, 'lanes-x-justify-content-0164 item[2].y');
      expectClose(r.width, 120, 'lanes-x-justify-content-0164 item[2].w');
      expectClose(r.height, 30, 'lanes-x-justify-content-0164 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 260, 'lanes-x-justify-content-0164 item[3].x');
      expectClose(r.y - container.y, 40, 'lanes-x-justify-content-0164 item[3].y');
      expectClose(r.width, 120, 'lanes-x-justify-content-0164 item[3].w');
      expectClose(r.height, 50, 'lanes-x-justify-content-0164 item[3].h');
    }
  });
  it("lanes-x-justify-content-0165: justify-content:center with slack in a lanes container", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid-lanes","width":600,"gridTemplateColumns":"120px 120px 120px","gap":10,"flowTolerance":0,"justifyContent":"center"}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":40}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":60}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":50}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'lanes-x-justify-content-0165 container.width');
    expectClose(container.height, 90, 'lanes-x-justify-content-0165 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 110, 'lanes-x-justify-content-0165 item[0].x');
      expectClose(r.y - container.y, 0, 'lanes-x-justify-content-0165 item[0].y');
      expectClose(r.width, 120, 'lanes-x-justify-content-0165 item[0].w');
      expectClose(r.height, 40, 'lanes-x-justify-content-0165 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 240, 'lanes-x-justify-content-0165 item[1].x');
      expectClose(r.y - container.y, 0, 'lanes-x-justify-content-0165 item[1].y');
      expectClose(r.width, 120, 'lanes-x-justify-content-0165 item[1].w');
      expectClose(r.height, 60, 'lanes-x-justify-content-0165 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 370, 'lanes-x-justify-content-0165 item[2].x');
      expectClose(r.y - container.y, 0, 'lanes-x-justify-content-0165 item[2].y');
      expectClose(r.width, 120, 'lanes-x-justify-content-0165 item[2].w');
      expectClose(r.height, 30, 'lanes-x-justify-content-0165 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 370, 'lanes-x-justify-content-0165 item[3].x');
      expectClose(r.y - container.y, 40, 'lanes-x-justify-content-0165 item[3].y');
      expectClose(r.width, 120, 'lanes-x-justify-content-0165 item[3].w');
      expectClose(r.height, 50, 'lanes-x-justify-content-0165 item[3].h');
    }
  });
  it("lanes-x-justify-content-0166: justify-content:end with slack in a lanes container", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid-lanes","width":600,"gridTemplateColumns":"120px 120px 120px","gap":10,"flowTolerance":0,"justifyContent":"end"}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":40}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":60}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":50}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'lanes-x-justify-content-0166 container.width');
    expectClose(container.height, 90, 'lanes-x-justify-content-0166 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 220, 'lanes-x-justify-content-0166 item[0].x');
      expectClose(r.y - container.y, 0, 'lanes-x-justify-content-0166 item[0].y');
      expectClose(r.width, 120, 'lanes-x-justify-content-0166 item[0].w');
      expectClose(r.height, 40, 'lanes-x-justify-content-0166 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 350, 'lanes-x-justify-content-0166 item[1].x');
      expectClose(r.y - container.y, 0, 'lanes-x-justify-content-0166 item[1].y');
      expectClose(r.width, 120, 'lanes-x-justify-content-0166 item[1].w');
      expectClose(r.height, 60, 'lanes-x-justify-content-0166 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 480, 'lanes-x-justify-content-0166 item[2].x');
      expectClose(r.y - container.y, 0, 'lanes-x-justify-content-0166 item[2].y');
      expectClose(r.width, 120, 'lanes-x-justify-content-0166 item[2].w');
      expectClose(r.height, 30, 'lanes-x-justify-content-0166 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 480, 'lanes-x-justify-content-0166 item[3].x');
      expectClose(r.y - container.y, 40, 'lanes-x-justify-content-0166 item[3].y');
      expectClose(r.width, 120, 'lanes-x-justify-content-0166 item[3].w');
      expectClose(r.height, 50, 'lanes-x-justify-content-0166 item[3].h');
    }
  });
  it("lanes-x-justify-content-0167: justify-content:space-between with slack in a lanes container", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid-lanes","width":600,"gridTemplateColumns":"120px 120px 120px","gap":10,"flowTolerance":0,"justifyContent":"space-between"}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":40}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":60}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":50}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'lanes-x-justify-content-0167 container.width');
    expectClose(container.height, 90, 'lanes-x-justify-content-0167 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'lanes-x-justify-content-0167 item[0].x');
      expectClose(r.y - container.y, 0, 'lanes-x-justify-content-0167 item[0].y');
      expectClose(r.width, 120, 'lanes-x-justify-content-0167 item[0].w');
      expectClose(r.height, 40, 'lanes-x-justify-content-0167 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 240, 'lanes-x-justify-content-0167 item[1].x');
      expectClose(r.y - container.y, 0, 'lanes-x-justify-content-0167 item[1].y');
      expectClose(r.width, 120, 'lanes-x-justify-content-0167 item[1].w');
      expectClose(r.height, 60, 'lanes-x-justify-content-0167 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 480, 'lanes-x-justify-content-0167 item[2].x');
      expectClose(r.y - container.y, 0, 'lanes-x-justify-content-0167 item[2].y');
      expectClose(r.width, 120, 'lanes-x-justify-content-0167 item[2].w');
      expectClose(r.height, 30, 'lanes-x-justify-content-0167 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 480, 'lanes-x-justify-content-0167 item[3].x');
      expectClose(r.y - container.y, 40, 'lanes-x-justify-content-0167 item[3].y');
      expectClose(r.width, 120, 'lanes-x-justify-content-0167 item[3].w');
      expectClose(r.height, 50, 'lanes-x-justify-content-0167 item[3].h');
    }
  });
});

describe("lanes-x-justify-items", () => {
  it("lanes-x-justify-items-0168: justify-items:start on narrow items in lanes", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid-lanes","width":600,"gridTemplateColumns":"1fr 1fr 1fr","gap":10,"flowTolerance":0,"justifyItems":"start"}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":80,"height":40}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":80,"height":60}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":80,"height":30}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":80,"height":50}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'lanes-x-justify-items-0168 container.width');
    expectClose(container.height, 90, 'lanes-x-justify-items-0168 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'lanes-x-justify-items-0168 item[0].x');
      expectClose(r.y - container.y, 0, 'lanes-x-justify-items-0168 item[0].y');
      expectClose(r.width, 80, 'lanes-x-justify-items-0168 item[0].w');
      expectClose(r.height, 40, 'lanes-x-justify-items-0168 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 203.33, 'lanes-x-justify-items-0168 item[1].x');
      expectClose(r.y - container.y, 0, 'lanes-x-justify-items-0168 item[1].y');
      expectClose(r.width, 80, 'lanes-x-justify-items-0168 item[1].w');
      expectClose(r.height, 60, 'lanes-x-justify-items-0168 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 406.66, 'lanes-x-justify-items-0168 item[2].x');
      expectClose(r.y - container.y, 0, 'lanes-x-justify-items-0168 item[2].y');
      expectClose(r.width, 80, 'lanes-x-justify-items-0168 item[2].w');
      expectClose(r.height, 30, 'lanes-x-justify-items-0168 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 406.66, 'lanes-x-justify-items-0168 item[3].x');
      expectClose(r.y - container.y, 40, 'lanes-x-justify-items-0168 item[3].y');
      expectClose(r.width, 80, 'lanes-x-justify-items-0168 item[3].w');
      expectClose(r.height, 50, 'lanes-x-justify-items-0168 item[3].h');
    }
  });
  it("lanes-x-justify-items-0169: justify-items:center on narrow items in lanes", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid-lanes","width":600,"gridTemplateColumns":"1fr 1fr 1fr","gap":10,"flowTolerance":0,"justifyItems":"center"}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":80,"height":40}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":80,"height":60}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":80,"height":30}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":80,"height":50}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'lanes-x-justify-items-0169 container.width');
    expectClose(container.height, 90, 'lanes-x-justify-items-0169 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 56.66, 'lanes-x-justify-items-0169 item[0].x');
      expectClose(r.y - container.y, 0, 'lanes-x-justify-items-0169 item[0].y');
      expectClose(r.width, 80, 'lanes-x-justify-items-0169 item[0].w');
      expectClose(r.height, 40, 'lanes-x-justify-items-0169 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 259.98, 'lanes-x-justify-items-0169 item[1].x');
      expectClose(r.y - container.y, 0, 'lanes-x-justify-items-0169 item[1].y');
      expectClose(r.width, 80, 'lanes-x-justify-items-0169 item[1].w');
      expectClose(r.height, 60, 'lanes-x-justify-items-0169 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 463.33, 'lanes-x-justify-items-0169 item[2].x');
      expectClose(r.y - container.y, 0, 'lanes-x-justify-items-0169 item[2].y');
      expectClose(r.width, 80, 'lanes-x-justify-items-0169 item[2].w');
      expectClose(r.height, 30, 'lanes-x-justify-items-0169 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 463.33, 'lanes-x-justify-items-0169 item[3].x');
      expectClose(r.y - container.y, 40, 'lanes-x-justify-items-0169 item[3].y');
      expectClose(r.width, 80, 'lanes-x-justify-items-0169 item[3].w');
      expectClose(r.height, 50, 'lanes-x-justify-items-0169 item[3].h');
    }
  });
  it("lanes-x-justify-items-0170: justify-items:end on narrow items in lanes", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid-lanes","width":600,"gridTemplateColumns":"1fr 1fr 1fr","gap":10,"flowTolerance":0,"justifyItems":"end"}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":80,"height":40}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":80,"height":60}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":80,"height":30}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":80,"height":50}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'lanes-x-justify-items-0170 container.width');
    expectClose(container.height, 90, 'lanes-x-justify-items-0170 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 113.33, 'lanes-x-justify-items-0170 item[0].x');
      expectClose(r.y - container.y, 0, 'lanes-x-justify-items-0170 item[0].y');
      expectClose(r.width, 80, 'lanes-x-justify-items-0170 item[0].w');
      expectClose(r.height, 40, 'lanes-x-justify-items-0170 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 316.66, 'lanes-x-justify-items-0170 item[1].x');
      expectClose(r.y - container.y, 0, 'lanes-x-justify-items-0170 item[1].y');
      expectClose(r.width, 80, 'lanes-x-justify-items-0170 item[1].w');
      expectClose(r.height, 60, 'lanes-x-justify-items-0170 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 520, 'lanes-x-justify-items-0170 item[2].x');
      expectClose(r.y - container.y, 0, 'lanes-x-justify-items-0170 item[2].y');
      expectClose(r.width, 80, 'lanes-x-justify-items-0170 item[2].w');
      expectClose(r.height, 30, 'lanes-x-justify-items-0170 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 520, 'lanes-x-justify-items-0170 item[3].x');
      expectClose(r.y - container.y, 40, 'lanes-x-justify-items-0170 item[3].y');
      expectClose(r.width, 80, 'lanes-x-justify-items-0170 item[3].w');
      expectClose(r.height, 50, 'lanes-x-justify-items-0170 item[3].h');
    }
  });
  it("lanes-x-justify-items-0171: justify-items:stretch on narrow items in lanes", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid-lanes","width":600,"gridTemplateColumns":"1fr 1fr 1fr","gap":10,"flowTolerance":0,"justifyItems":"stretch"}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":80,"height":40}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":80,"height":60}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":80,"height":30}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":80,"height":50}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'lanes-x-justify-items-0171 container.width');
    expectClose(container.height, 90, 'lanes-x-justify-items-0171 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'lanes-x-justify-items-0171 item[0].x');
      expectClose(r.y - container.y, 0, 'lanes-x-justify-items-0171 item[0].y');
      expectClose(r.width, 80, 'lanes-x-justify-items-0171 item[0].w');
      expectClose(r.height, 40, 'lanes-x-justify-items-0171 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 203.33, 'lanes-x-justify-items-0171 item[1].x');
      expectClose(r.y - container.y, 0, 'lanes-x-justify-items-0171 item[1].y');
      expectClose(r.width, 80, 'lanes-x-justify-items-0171 item[1].w');
      expectClose(r.height, 60, 'lanes-x-justify-items-0171 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 406.66, 'lanes-x-justify-items-0171 item[2].x');
      expectClose(r.y - container.y, 0, 'lanes-x-justify-items-0171 item[2].y');
      expectClose(r.width, 80, 'lanes-x-justify-items-0171 item[2].w');
      expectClose(r.height, 30, 'lanes-x-justify-items-0171 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 406.66, 'lanes-x-justify-items-0171 item[3].x');
      expectClose(r.y - container.y, 40, 'lanes-x-justify-items-0171 item[3].y');
      expectClose(r.width, 80, 'lanes-x-justify-items-0171 item[3].w');
      expectClose(r.height, 50, 'lanes-x-justify-items-0171 item[3].h');
    }
  });
});

describe("lanes-subpixel", () => {
  it("lanes-subpixel-0172: fractional item heights accumulate down a lane", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid-lanes","width":600,"gridTemplateColumns":"1fr 1fr","gap":0.5,"flowTolerance":0}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":10.3}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":10.3}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":10.3}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":10.3}}
          />
          <View
            collapsable={false}
            ref={itemRefs[4]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":10.3}}
          />
          <View
            collapsable={false}
            ref={itemRefs[5]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":10.3}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'lanes-subpixel-0172 container.width');
    expectClose(container.height, 31.89, 'lanes-subpixel-0172 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'lanes-subpixel-0172 item[0].x');
      expectClose(r.y - container.y, 0, 'lanes-subpixel-0172 item[0].y');
      expectClose(r.width, 299.75, 'lanes-subpixel-0172 item[0].w');
      expectClose(r.height, 10.3, 'lanes-subpixel-0172 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 300.25, 'lanes-subpixel-0172 item[1].x');
      expectClose(r.y - container.y, 0, 'lanes-subpixel-0172 item[1].y');
      expectClose(r.width, 299.75, 'lanes-subpixel-0172 item[1].w');
      expectClose(r.height, 10.3, 'lanes-subpixel-0172 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 0, 'lanes-subpixel-0172 item[2].x');
      expectClose(r.y - container.y, 10.8, 'lanes-subpixel-0172 item[2].y');
      expectClose(r.width, 299.75, 'lanes-subpixel-0172 item[2].w');
      expectClose(r.height, 10.3, 'lanes-subpixel-0172 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 300.25, 'lanes-subpixel-0172 item[3].x');
      expectClose(r.y - container.y, 10.8, 'lanes-subpixel-0172 item[3].y');
      expectClose(r.width, 299.75, 'lanes-subpixel-0172 item[3].w');
      expectClose(r.height, 10.3, 'lanes-subpixel-0172 item[3].h');
    }
    {
      const r = rectOf(itemRefs[4]);
      expectClose(r.x - container.x, 0, 'lanes-subpixel-0172 item[4].x');
      expectClose(r.y - container.y, 21.59, 'lanes-subpixel-0172 item[4].y');
      expectClose(r.width, 299.75, 'lanes-subpixel-0172 item[4].w');
      expectClose(r.height, 10.3, 'lanes-subpixel-0172 item[4].h');
    }
    {
      const r = rectOf(itemRefs[5]);
      expectClose(r.x - container.x, 300.25, 'lanes-subpixel-0172 item[5].x');
      expectClose(r.y - container.y, 21.59, 'lanes-subpixel-0172 item[5].y');
      expectClose(r.width, 299.75, 'lanes-subpixel-0172 item[5].w');
      expectClose(r.height, 10.3, 'lanes-subpixel-0172 item[5].h');
    }
  });
});

describe("lanes-fixed-height", () => {
  it("lanes-fixed-height-0173: lanes in a container with a fixed height of 80", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid-lanes","width":600,"height":80,"gridTemplateColumns":"1fr 1fr 1fr","gap":10,"flowTolerance":0}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":40}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":60}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":50}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'lanes-fixed-height-0173 container.width');
    expectClose(container.height, 80, 'lanes-fixed-height-0173 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'lanes-fixed-height-0173 item[0].x');
      expectClose(r.y - container.y, 0, 'lanes-fixed-height-0173 item[0].y');
      expectClose(r.width, 193.33, 'lanes-fixed-height-0173 item[0].w');
      expectClose(r.height, 40, 'lanes-fixed-height-0173 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 203.33, 'lanes-fixed-height-0173 item[1].x');
      expectClose(r.y - container.y, 0, 'lanes-fixed-height-0173 item[1].y');
      expectClose(r.width, 193.33, 'lanes-fixed-height-0173 item[1].w');
      expectClose(r.height, 60, 'lanes-fixed-height-0173 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 406.66, 'lanes-fixed-height-0173 item[2].x');
      expectClose(r.y - container.y, 0, 'lanes-fixed-height-0173 item[2].y');
      expectClose(r.width, 193.34, 'lanes-fixed-height-0173 item[2].w');
      expectClose(r.height, 30, 'lanes-fixed-height-0173 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 406.66, 'lanes-fixed-height-0173 item[3].x');
      expectClose(r.y - container.y, 40, 'lanes-fixed-height-0173 item[3].y');
      expectClose(r.width, 193.34, 'lanes-fixed-height-0173 item[3].w');
      expectClose(r.height, 50, 'lanes-fixed-height-0173 item[3].h');
    }
  });
  it("lanes-fixed-height-0174: lanes in a container with a fixed height of 200", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid-lanes","width":600,"height":200,"gridTemplateColumns":"1fr 1fr 1fr","gap":10,"flowTolerance":0}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":40}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":60}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":50}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'lanes-fixed-height-0174 container.width');
    expectClose(container.height, 200, 'lanes-fixed-height-0174 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'lanes-fixed-height-0174 item[0].x');
      expectClose(r.y - container.y, 0, 'lanes-fixed-height-0174 item[0].y');
      expectClose(r.width, 193.33, 'lanes-fixed-height-0174 item[0].w');
      expectClose(r.height, 40, 'lanes-fixed-height-0174 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 203.33, 'lanes-fixed-height-0174 item[1].x');
      expectClose(r.y - container.y, 0, 'lanes-fixed-height-0174 item[1].y');
      expectClose(r.width, 193.33, 'lanes-fixed-height-0174 item[1].w');
      expectClose(r.height, 60, 'lanes-fixed-height-0174 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 406.66, 'lanes-fixed-height-0174 item[2].x');
      expectClose(r.y - container.y, 0, 'lanes-fixed-height-0174 item[2].y');
      expectClose(r.width, 193.34, 'lanes-fixed-height-0174 item[2].w');
      expectClose(r.height, 30, 'lanes-fixed-height-0174 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 406.66, 'lanes-fixed-height-0174 item[3].x');
      expectClose(r.y - container.y, 40, 'lanes-fixed-height-0174 item[3].y');
      expectClose(r.width, 193.34, 'lanes-fixed-height-0174 item[3].w');
      expectClose(r.height, 50, 'lanes-fixed-height-0174 item[3].h');
    }
  });
});

describe("lanes-invalid-span-zero", () => {
  it("lanes-invalid-span-zero-0175: span 1 is the identity case for span clamping", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid-lanes","width":600,"gridTemplateColumns":"1fr 1fr 1fr","gap":10,"flowTolerance":0}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":40}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30,"gridColumnEnd":"span 1"}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":50}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'lanes-invalid-span-zero-0175 container.width');
    expectClose(container.height, 50, 'lanes-invalid-span-zero-0175 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'lanes-invalid-span-zero-0175 item[0].x');
      expectClose(r.y - container.y, 0, 'lanes-invalid-span-zero-0175 item[0].y');
      expectClose(r.width, 193.33, 'lanes-invalid-span-zero-0175 item[0].w');
      expectClose(r.height, 40, 'lanes-invalid-span-zero-0175 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 203.33, 'lanes-invalid-span-zero-0175 item[1].x');
      expectClose(r.y - container.y, 0, 'lanes-invalid-span-zero-0175 item[1].y');
      expectClose(r.width, 193.33, 'lanes-invalid-span-zero-0175 item[1].w');
      expectClose(r.height, 30, 'lanes-invalid-span-zero-0175 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 406.66, 'lanes-invalid-span-zero-0175 item[2].x');
      expectClose(r.y - container.y, 0, 'lanes-invalid-span-zero-0175 item[2].y');
      expectClose(r.width, 193.34, 'lanes-invalid-span-zero-0175 item[2].w');
      expectClose(r.height, 50, 'lanes-invalid-span-zero-0175 item[2].h');
    }
  });
});

describe("lanes-span-exceeds-lanes", () => {
  it("lanes-span-exceeds-lanes-0176: an item spanning more lanes than exist", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid-lanes","width":600,"gridTemplateColumns":"1fr 1fr","gap":10,"flowTolerance":0}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":40}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30,"gridColumnEnd":"span 5"}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":50}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'lanes-span-exceeds-lanes-0176 container.width');
    expectClose(container.height, 140, 'lanes-span-exceeds-lanes-0176 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'lanes-span-exceeds-lanes-0176 item[0].x');
      expectClose(r.y - container.y, 0, 'lanes-span-exceeds-lanes-0176 item[0].y');
      expectClose(r.width, 280, 'lanes-span-exceeds-lanes-0176 item[0].w');
      expectClose(r.height, 40, 'lanes-span-exceeds-lanes-0176 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 0, 'lanes-span-exceeds-lanes-0176 item[1].x');
      expectClose(r.y - container.y, 50, 'lanes-span-exceeds-lanes-0176 item[1].y');
      expectClose(r.width, 600, 'lanes-span-exceeds-lanes-0176 item[1].w');
      expectClose(r.height, 30, 'lanes-span-exceeds-lanes-0176 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 0, 'lanes-span-exceeds-lanes-0176 item[2].x');
      expectClose(r.y - container.y, 90, 'lanes-span-exceeds-lanes-0176 item[2].y');
      expectClose(r.width, 280, 'lanes-span-exceeds-lanes-0176 item[2].w');
      expectClose(r.height, 50, 'lanes-span-exceeds-lanes-0176 item[2].h');
    }
  });
});

describe("lanes-zero-height-items", () => {
  it("lanes-zero-height-items-0177: zero-height items still consume a lane slot", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid-lanes","width":600,"gridTemplateColumns":"1fr 1fr 1fr","gap":10,"flowTolerance":0}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":0}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":0}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":40}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":0}}
          />
          <View
            collapsable={false}
            ref={itemRefs[4]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":20}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'lanes-zero-height-items-0177 container.width');
    expectClose(container.height, 40, 'lanes-zero-height-items-0177 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'lanes-zero-height-items-0177 item[0].x');
      expectClose(r.y - container.y, 0, 'lanes-zero-height-items-0177 item[0].y');
      expectClose(r.width, 193.33, 'lanes-zero-height-items-0177 item[0].w');
      expectClose(r.height, 0, 'lanes-zero-height-items-0177 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 203.33, 'lanes-zero-height-items-0177 item[1].x');
      expectClose(r.y - container.y, 0, 'lanes-zero-height-items-0177 item[1].y');
      expectClose(r.width, 193.33, 'lanes-zero-height-items-0177 item[1].w');
      expectClose(r.height, 0, 'lanes-zero-height-items-0177 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 406.66, 'lanes-zero-height-items-0177 item[2].x');
      expectClose(r.y - container.y, 0, 'lanes-zero-height-items-0177 item[2].y');
      expectClose(r.width, 193.34, 'lanes-zero-height-items-0177 item[2].w');
      expectClose(r.height, 40, 'lanes-zero-height-items-0177 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 0, 'lanes-zero-height-items-0177 item[3].x');
      expectClose(r.y - container.y, 10, 'lanes-zero-height-items-0177 item[3].y');
      expectClose(r.width, 193.33, 'lanes-zero-height-items-0177 item[3].w');
      expectClose(r.height, 0, 'lanes-zero-height-items-0177 item[3].h');
    }
    {
      const r = rectOf(itemRefs[4]);
      expectClose(r.x - container.x, 203.33, 'lanes-zero-height-items-0177 item[4].x');
      expectClose(r.y - container.y, 10, 'lanes-zero-height-items-0177 item[4].y');
      expectClose(r.width, 193.33, 'lanes-zero-height-items-0177 item[4].w');
      expectClose(r.height, 20, 'lanes-zero-height-items-0177 item[4].h');
    }
  });
});

describe("lanes-many-items", () => {
  it("lanes-many-items-0178: 24 items over 4 lanes", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid-lanes","width":600,"gridTemplateColumns":"1fr 1fr 1fr 1fr","gap":8,"flowTolerance":0}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":33}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":46}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":59}}
          />
          <View
            collapsable={false}
            ref={itemRefs[4]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":25}}
          />
          <View
            collapsable={false}
            ref={itemRefs[5]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":38}}
          />
          <View
            collapsable={false}
            ref={itemRefs[6]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":51}}
          />
          <View
            collapsable={false}
            ref={itemRefs[7]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":64}}
          />
          <View
            collapsable={false}
            ref={itemRefs[8]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30}}
          />
          <View
            collapsable={false}
            ref={itemRefs[9]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":43}}
          />
          <View
            collapsable={false}
            ref={itemRefs[10]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":56}}
          />
          <View
            collapsable={false}
            ref={itemRefs[11]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":22}}
          />
          <View
            collapsable={false}
            ref={itemRefs[12]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":35}}
          />
          <View
            collapsable={false}
            ref={itemRefs[13]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":48}}
          />
          <View
            collapsable={false}
            ref={itemRefs[14]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":61}}
          />
          <View
            collapsable={false}
            ref={itemRefs[15]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":27}}
          />
          <View
            collapsable={false}
            ref={itemRefs[16]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":40}}
          />
          <View
            collapsable={false}
            ref={itemRefs[17]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":53}}
          />
          <View
            collapsable={false}
            ref={itemRefs[18]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":66}}
          />
          <View
            collapsable={false}
            ref={itemRefs[19]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":32}}
          />
          <View
            collapsable={false}
            ref={itemRefs[20]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":45}}
          />
          <View
            collapsable={false}
            ref={itemRefs[21]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":58}}
          />
          <View
            collapsable={false}
            ref={itemRefs[22]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":24}}
          />
          <View
            collapsable={false}
            ref={itemRefs[23]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":37}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'lanes-many-items-0178 container.width');
    expectClose(container.height, 314, 'lanes-many-items-0178 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'lanes-many-items-0178 item[0].x');
      expectClose(r.y - container.y, 0, 'lanes-many-items-0178 item[0].y');
      expectClose(r.width, 144, 'lanes-many-items-0178 item[0].w');
      expectClose(r.height, 20, 'lanes-many-items-0178 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 152, 'lanes-many-items-0178 item[1].x');
      expectClose(r.y - container.y, 0, 'lanes-many-items-0178 item[1].y');
      expectClose(r.width, 144, 'lanes-many-items-0178 item[1].w');
      expectClose(r.height, 33, 'lanes-many-items-0178 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 304, 'lanes-many-items-0178 item[2].x');
      expectClose(r.y - container.y, 0, 'lanes-many-items-0178 item[2].y');
      expectClose(r.width, 144, 'lanes-many-items-0178 item[2].w');
      expectClose(r.height, 46, 'lanes-many-items-0178 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 456, 'lanes-many-items-0178 item[3].x');
      expectClose(r.y - container.y, 0, 'lanes-many-items-0178 item[3].y');
      expectClose(r.width, 144, 'lanes-many-items-0178 item[3].w');
      expectClose(r.height, 59, 'lanes-many-items-0178 item[3].h');
    }
    {
      const r = rectOf(itemRefs[4]);
      expectClose(r.x - container.x, 0, 'lanes-many-items-0178 item[4].x');
      expectClose(r.y - container.y, 28, 'lanes-many-items-0178 item[4].y');
      expectClose(r.width, 144, 'lanes-many-items-0178 item[4].w');
      expectClose(r.height, 25, 'lanes-many-items-0178 item[4].h');
    }
    {
      const r = rectOf(itemRefs[5]);
      expectClose(r.x - container.x, 152, 'lanes-many-items-0178 item[5].x');
      expectClose(r.y - container.y, 41, 'lanes-many-items-0178 item[5].y');
      expectClose(r.width, 144, 'lanes-many-items-0178 item[5].w');
      expectClose(r.height, 38, 'lanes-many-items-0178 item[5].h');
    }
    {
      const r = rectOf(itemRefs[6]);
      expectClose(r.x - container.x, 304, 'lanes-many-items-0178 item[6].x');
      expectClose(r.y - container.y, 54, 'lanes-many-items-0178 item[6].y');
      expectClose(r.width, 144, 'lanes-many-items-0178 item[6].w');
      expectClose(r.height, 51, 'lanes-many-items-0178 item[6].h');
    }
    {
      const r = rectOf(itemRefs[7]);
      expectClose(r.x - container.x, 0, 'lanes-many-items-0178 item[7].x');
      expectClose(r.y - container.y, 61, 'lanes-many-items-0178 item[7].y');
      expectClose(r.width, 144, 'lanes-many-items-0178 item[7].w');
      expectClose(r.height, 64, 'lanes-many-items-0178 item[7].h');
    }
    {
      const r = rectOf(itemRefs[8]);
      expectClose(r.x - container.x, 456, 'lanes-many-items-0178 item[8].x');
      expectClose(r.y - container.y, 67, 'lanes-many-items-0178 item[8].y');
      expectClose(r.width, 144, 'lanes-many-items-0178 item[8].w');
      expectClose(r.height, 30, 'lanes-many-items-0178 item[8].h');
    }
    {
      const r = rectOf(itemRefs[9]);
      expectClose(r.x - container.x, 152, 'lanes-many-items-0178 item[9].x');
      expectClose(r.y - container.y, 87, 'lanes-many-items-0178 item[9].y');
      expectClose(r.width, 144, 'lanes-many-items-0178 item[9].w');
      expectClose(r.height, 43, 'lanes-many-items-0178 item[9].h');
    }
    {
      const r = rectOf(itemRefs[10]);
      expectClose(r.x - container.x, 456, 'lanes-many-items-0178 item[10].x');
      expectClose(r.y - container.y, 105, 'lanes-many-items-0178 item[10].y');
      expectClose(r.width, 144, 'lanes-many-items-0178 item[10].w');
      expectClose(r.height, 56, 'lanes-many-items-0178 item[10].h');
    }
    {
      const r = rectOf(itemRefs[11]);
      expectClose(r.x - container.x, 304, 'lanes-many-items-0178 item[11].x');
      expectClose(r.y - container.y, 113, 'lanes-many-items-0178 item[11].y');
      expectClose(r.width, 144, 'lanes-many-items-0178 item[11].w');
      expectClose(r.height, 22, 'lanes-many-items-0178 item[11].h');
    }
    {
      const r = rectOf(itemRefs[12]);
      expectClose(r.x - container.x, 0, 'lanes-many-items-0178 item[12].x');
      expectClose(r.y - container.y, 133, 'lanes-many-items-0178 item[12].y');
      expectClose(r.width, 144, 'lanes-many-items-0178 item[12].w');
      expectClose(r.height, 35, 'lanes-many-items-0178 item[12].h');
    }
    {
      const r = rectOf(itemRefs[13]);
      expectClose(r.x - container.x, 152, 'lanes-many-items-0178 item[13].x');
      expectClose(r.y - container.y, 138, 'lanes-many-items-0178 item[13].y');
      expectClose(r.width, 144, 'lanes-many-items-0178 item[13].w');
      expectClose(r.height, 48, 'lanes-many-items-0178 item[13].h');
    }
    {
      const r = rectOf(itemRefs[14]);
      expectClose(r.x - container.x, 304, 'lanes-many-items-0178 item[14].x');
      expectClose(r.y - container.y, 143, 'lanes-many-items-0178 item[14].y');
      expectClose(r.width, 144, 'lanes-many-items-0178 item[14].w');
      expectClose(r.height, 61, 'lanes-many-items-0178 item[14].h');
    }
    {
      const r = rectOf(itemRefs[15]);
      expectClose(r.x - container.x, 456, 'lanes-many-items-0178 item[15].x');
      expectClose(r.y - container.y, 169, 'lanes-many-items-0178 item[15].y');
      expectClose(r.width, 144, 'lanes-many-items-0178 item[15].w');
      expectClose(r.height, 27, 'lanes-many-items-0178 item[15].h');
    }
    {
      const r = rectOf(itemRefs[16]);
      expectClose(r.x - container.x, 0, 'lanes-many-items-0178 item[16].x');
      expectClose(r.y - container.y, 176, 'lanes-many-items-0178 item[16].y');
      expectClose(r.width, 144, 'lanes-many-items-0178 item[16].w');
      expectClose(r.height, 40, 'lanes-many-items-0178 item[16].h');
    }
    {
      const r = rectOf(itemRefs[17]);
      expectClose(r.x - container.x, 152, 'lanes-many-items-0178 item[17].x');
      expectClose(r.y - container.y, 194, 'lanes-many-items-0178 item[17].y');
      expectClose(r.width, 144, 'lanes-many-items-0178 item[17].w');
      expectClose(r.height, 53, 'lanes-many-items-0178 item[17].h');
    }
    {
      const r = rectOf(itemRefs[18]);
      expectClose(r.x - container.x, 456, 'lanes-many-items-0178 item[18].x');
      expectClose(r.y - container.y, 204, 'lanes-many-items-0178 item[18].y');
      expectClose(r.width, 144, 'lanes-many-items-0178 item[18].w');
      expectClose(r.height, 66, 'lanes-many-items-0178 item[18].h');
    }
    {
      const r = rectOf(itemRefs[19]);
      expectClose(r.x - container.x, 304, 'lanes-many-items-0178 item[19].x');
      expectClose(r.y - container.y, 212, 'lanes-many-items-0178 item[19].y');
      expectClose(r.width, 144, 'lanes-many-items-0178 item[19].w');
      expectClose(r.height, 32, 'lanes-many-items-0178 item[19].h');
    }
    {
      const r = rectOf(itemRefs[20]);
      expectClose(r.x - container.x, 0, 'lanes-many-items-0178 item[20].x');
      expectClose(r.y - container.y, 224, 'lanes-many-items-0178 item[20].y');
      expectClose(r.width, 144, 'lanes-many-items-0178 item[20].w');
      expectClose(r.height, 45, 'lanes-many-items-0178 item[20].h');
    }
    {
      const r = rectOf(itemRefs[21]);
      expectClose(r.x - container.x, 304, 'lanes-many-items-0178 item[21].x');
      expectClose(r.y - container.y, 252, 'lanes-many-items-0178 item[21].y');
      expectClose(r.width, 144, 'lanes-many-items-0178 item[21].w');
      expectClose(r.height, 58, 'lanes-many-items-0178 item[21].h');
    }
    {
      const r = rectOf(itemRefs[22]);
      expectClose(r.x - container.x, 152, 'lanes-many-items-0178 item[22].x');
      expectClose(r.y - container.y, 255, 'lanes-many-items-0178 item[22].y');
      expectClose(r.width, 144, 'lanes-many-items-0178 item[22].w');
      expectClose(r.height, 24, 'lanes-many-items-0178 item[22].h');
    }
    {
      const r = rectOf(itemRefs[23]);
      expectClose(r.x - container.x, 0, 'lanes-many-items-0178 item[23].x');
      expectClose(r.y - container.y, 277, 'lanes-many-items-0178 item[23].y');
      expectClose(r.width, 144, 'lanes-many-items-0178 item[23].w');
      expectClose(r.height, 37, 'lanes-many-items-0178 item[23].h');
    }
  });
});

describe("lanes-nested", () => {
  it("lanes-nested-0179: baseline for the nested comparison", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid-lanes","width":600,"gridTemplateColumns":"1fr 1fr","gap":10,"flowTolerance":0}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":40}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":70}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":50}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'lanes-nested-0179 container.width');
    expectClose(container.height, 130, 'lanes-nested-0179 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'lanes-nested-0179 item[0].x');
      expectClose(r.y - container.y, 0, 'lanes-nested-0179 item[0].y');
      expectClose(r.width, 295, 'lanes-nested-0179 item[0].w');
      expectClose(r.height, 40, 'lanes-nested-0179 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 305, 'lanes-nested-0179 item[1].x');
      expectClose(r.y - container.y, 0, 'lanes-nested-0179 item[1].y');
      expectClose(r.width, 295, 'lanes-nested-0179 item[1].w');
      expectClose(r.height, 70, 'lanes-nested-0179 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 0, 'lanes-nested-0179 item[2].x');
      expectClose(r.y - container.y, 50, 'lanes-nested-0179 item[2].y');
      expectClose(r.width, 295, 'lanes-nested-0179 item[2].w');
      expectClose(r.height, 30, 'lanes-nested-0179 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 305, 'lanes-nested-0179 item[3].x');
      expectClose(r.y - container.y, 80, 'lanes-nested-0179 item[3].y');
      expectClose(r.width, 295, 'lanes-nested-0179 item[3].w');
      expectClose(r.height, 50, 'lanes-nested-0179 item[3].h');
    }
  });
});

describe("lanes-direction", () => {
  it("lanes-direction-0182: lanes under direction:ltr", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid-lanes","direction":"ltr","width":600,"gridTemplateColumns":"1fr 1fr 1fr","gap":10,"flowTolerance":0}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":40}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":60}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":50}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'lanes-direction-0182 container.width');
    expectClose(container.height, 90, 'lanes-direction-0182 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'lanes-direction-0182 item[0].x');
      expectClose(r.y - container.y, 0, 'lanes-direction-0182 item[0].y');
      expectClose(r.width, 193.33, 'lanes-direction-0182 item[0].w');
      expectClose(r.height, 40, 'lanes-direction-0182 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 203.33, 'lanes-direction-0182 item[1].x');
      expectClose(r.y - container.y, 0, 'lanes-direction-0182 item[1].y');
      expectClose(r.width, 193.33, 'lanes-direction-0182 item[1].w');
      expectClose(r.height, 60, 'lanes-direction-0182 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 406.66, 'lanes-direction-0182 item[2].x');
      expectClose(r.y - container.y, 0, 'lanes-direction-0182 item[2].y');
      expectClose(r.width, 193.34, 'lanes-direction-0182 item[2].w');
      expectClose(r.height, 30, 'lanes-direction-0182 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 406.66, 'lanes-direction-0182 item[3].x');
      expectClose(r.y - container.y, 40, 'lanes-direction-0182 item[3].y');
      expectClose(r.width, 193.34, 'lanes-direction-0182 item[3].w');
      expectClose(r.height, 50, 'lanes-direction-0182 item[3].h');
    }
  });
  it("lanes-direction-0183: lanes under direction:rtl", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid-lanes","direction":"rtl","width":600,"gridTemplateColumns":"1fr 1fr 1fr","gap":10,"flowTolerance":0}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":40}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":60}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":50}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'lanes-direction-0183 container.width');
    expectClose(container.height, 90, 'lanes-direction-0183 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 406.67, 'lanes-direction-0183 item[0].x');
      expectClose(r.y - container.y, 0, 'lanes-direction-0183 item[0].y');
      expectClose(r.width, 193.33, 'lanes-direction-0183 item[0].w');
      expectClose(r.height, 40, 'lanes-direction-0183 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 203.34, 'lanes-direction-0183 item[1].x');
      expectClose(r.y - container.y, 0, 'lanes-direction-0183 item[1].y');
      expectClose(r.width, 193.33, 'lanes-direction-0183 item[1].w');
      expectClose(r.height, 60, 'lanes-direction-0183 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 0, 'lanes-direction-0183 item[2].x');
      expectClose(r.y - container.y, 0, 'lanes-direction-0183 item[2].y');
      expectClose(r.width, 193.34, 'lanes-direction-0183 item[2].w');
      expectClose(r.height, 30, 'lanes-direction-0183 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 0, 'lanes-direction-0183 item[3].x');
      expectClose(r.y - container.y, 40, 'lanes-direction-0183 item[3].y');
      expectClose(r.width, 193.34, 'lanes-direction-0183 item[3].w');
      expectClose(r.height, 50, 'lanes-direction-0183 item[3].h');
    }
  });
});

describe("repeat-forms", () => {
  it("repeat-forms-0184: repeat() expansion", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid","width":600,"gridTemplateColumns":"repeat(3, 1fr)","gap":10}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":40,"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":40,"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":40,"height":20}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'repeat-forms-0184 container.width');
    expectClose(container.height, 20, 'repeat-forms-0184 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'repeat-forms-0184 item[0].x');
      expectClose(r.y - container.y, 0, 'repeat-forms-0184 item[0].y');
      expectClose(r.width, 40, 'repeat-forms-0184 item[0].w');
      expectClose(r.height, 20, 'repeat-forms-0184 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 203.33, 'repeat-forms-0184 item[1].x');
      expectClose(r.y - container.y, 0, 'repeat-forms-0184 item[1].y');
      expectClose(r.width, 40, 'repeat-forms-0184 item[1].w');
      expectClose(r.height, 20, 'repeat-forms-0184 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 406.66, 'repeat-forms-0184 item[2].x');
      expectClose(r.y - container.y, 0, 'repeat-forms-0184 item[2].y');
      expectClose(r.width, 40, 'repeat-forms-0184 item[2].w');
      expectClose(r.height, 20, 'repeat-forms-0184 item[2].h');
    }
  });
  it("repeat-forms-0186: repeat() expansion", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid","width":600,"gridTemplateColumns":"repeat(2, 100px 1fr)","gap":10}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":40,"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":40,"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":40,"height":20}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'repeat-forms-0186 container.width');
    expectClose(container.height, 20, 'repeat-forms-0186 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'repeat-forms-0186 item[0].x');
      expectClose(r.y - container.y, 0, 'repeat-forms-0186 item[0].y');
      expectClose(r.width, 40, 'repeat-forms-0186 item[0].w');
      expectClose(r.height, 20, 'repeat-forms-0186 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 110, 'repeat-forms-0186 item[1].x');
      expectClose(r.y - container.y, 0, 'repeat-forms-0186 item[1].y');
      expectClose(r.width, 40, 'repeat-forms-0186 item[1].w');
      expectClose(r.height, 20, 'repeat-forms-0186 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 305, 'repeat-forms-0186 item[2].x');
      expectClose(r.y - container.y, 0, 'repeat-forms-0186 item[2].y');
      expectClose(r.width, 40, 'repeat-forms-0186 item[2].w');
      expectClose(r.height, 20, 'repeat-forms-0186 item[2].h');
    }
  });
  it("repeat-forms-0188: repeat() expansion", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid","width":600,"gridTemplateColumns":"repeat(auto-fill, minmax(120px, 1fr))","gap":10}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":40,"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":40,"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":40,"height":20}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'repeat-forms-0188 container.width');
    expectClose(container.height, 20, 'repeat-forms-0188 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'repeat-forms-0188 item[0].x');
      expectClose(r.y - container.y, 0, 'repeat-forms-0188 item[0].y');
      expectClose(r.width, 40, 'repeat-forms-0188 item[0].w');
      expectClose(r.height, 20, 'repeat-forms-0188 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 152.5, 'repeat-forms-0188 item[1].x');
      expectClose(r.y - container.y, 0, 'repeat-forms-0188 item[1].y');
      expectClose(r.width, 40, 'repeat-forms-0188 item[1].w');
      expectClose(r.height, 20, 'repeat-forms-0188 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 305, 'repeat-forms-0188 item[2].x');
      expectClose(r.y - container.y, 0, 'repeat-forms-0188 item[2].y');
      expectClose(r.width, 40, 'repeat-forms-0188 item[2].w');
      expectClose(r.height, 20, 'repeat-forms-0188 item[2].h');
    }
  });
  it("repeat-forms-0190: repeat() expansion", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid","width":600,"gridTemplateColumns":"repeat(auto-fit, minmax(120px, 1fr))","gap":10}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":40,"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":40,"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":40,"height":20}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'repeat-forms-0190 container.width');
    expectClose(container.height, 20, 'repeat-forms-0190 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'repeat-forms-0190 item[0].x');
      expectClose(r.y - container.y, 0, 'repeat-forms-0190 item[0].y');
      expectClose(r.width, 40, 'repeat-forms-0190 item[0].w');
      expectClose(r.height, 20, 'repeat-forms-0190 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 203.33, 'repeat-forms-0190 item[1].x');
      expectClose(r.y - container.y, 0, 'repeat-forms-0190 item[1].y');
      expectClose(r.width, 40, 'repeat-forms-0190 item[1].w');
      expectClose(r.height, 20, 'repeat-forms-0190 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 406.66, 'repeat-forms-0190 item[2].x');
      expectClose(r.y - container.y, 0, 'repeat-forms-0190 item[2].y');
      expectClose(r.width, 40, 'repeat-forms-0190 item[2].w');
      expectClose(r.height, 20, 'repeat-forms-0190 item[2].h');
    }
  });
});

describe("lanes-repeat-forms", () => {
  it("lanes-repeat-forms-0185: repeat() under grid-lanes", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid-lanes","width":600,"gridTemplateColumns":"repeat(3, 1fr)","gap":10,"flowTolerance":0}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":40}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":60}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":50}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'lanes-repeat-forms-0185 container.width');
    expectClose(container.height, 90, 'lanes-repeat-forms-0185 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'lanes-repeat-forms-0185 item[0].x');
      expectClose(r.y - container.y, 0, 'lanes-repeat-forms-0185 item[0].y');
      expectClose(r.width, 193.33, 'lanes-repeat-forms-0185 item[0].w');
      expectClose(r.height, 40, 'lanes-repeat-forms-0185 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 203.33, 'lanes-repeat-forms-0185 item[1].x');
      expectClose(r.y - container.y, 0, 'lanes-repeat-forms-0185 item[1].y');
      expectClose(r.width, 193.33, 'lanes-repeat-forms-0185 item[1].w');
      expectClose(r.height, 60, 'lanes-repeat-forms-0185 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 406.66, 'lanes-repeat-forms-0185 item[2].x');
      expectClose(r.y - container.y, 0, 'lanes-repeat-forms-0185 item[2].y');
      expectClose(r.width, 193.34, 'lanes-repeat-forms-0185 item[2].w');
      expectClose(r.height, 30, 'lanes-repeat-forms-0185 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 406.66, 'lanes-repeat-forms-0185 item[3].x');
      expectClose(r.y - container.y, 40, 'lanes-repeat-forms-0185 item[3].y');
      expectClose(r.width, 193.34, 'lanes-repeat-forms-0185 item[3].w');
      expectClose(r.height, 50, 'lanes-repeat-forms-0185 item[3].h');
    }
  });
  it("lanes-repeat-forms-0187: repeat() under grid-lanes", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid-lanes","width":600,"gridTemplateColumns":"repeat(2, 100px 1fr)","gap":10,"flowTolerance":0}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":40}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":60}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":50}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'lanes-repeat-forms-0187 container.width');
    expectClose(container.height, 60, 'lanes-repeat-forms-0187 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'lanes-repeat-forms-0187 item[0].x');
      expectClose(r.y - container.y, 0, 'lanes-repeat-forms-0187 item[0].y');
      expectClose(r.width, 100, 'lanes-repeat-forms-0187 item[0].w');
      expectClose(r.height, 40, 'lanes-repeat-forms-0187 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 110, 'lanes-repeat-forms-0187 item[1].x');
      expectClose(r.y - container.y, 0, 'lanes-repeat-forms-0187 item[1].y');
      expectClose(r.width, 185, 'lanes-repeat-forms-0187 item[1].w');
      expectClose(r.height, 60, 'lanes-repeat-forms-0187 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 305, 'lanes-repeat-forms-0187 item[2].x');
      expectClose(r.y - container.y, 0, 'lanes-repeat-forms-0187 item[2].y');
      expectClose(r.width, 100, 'lanes-repeat-forms-0187 item[2].w');
      expectClose(r.height, 30, 'lanes-repeat-forms-0187 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 415, 'lanes-repeat-forms-0187 item[3].x');
      expectClose(r.y - container.y, 0, 'lanes-repeat-forms-0187 item[3].y');
      expectClose(r.width, 185, 'lanes-repeat-forms-0187 item[3].w');
      expectClose(r.height, 50, 'lanes-repeat-forms-0187 item[3].h');
    }
  });
  it("lanes-repeat-forms-0189: repeat() under grid-lanes", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid-lanes","width":600,"gridTemplateColumns":"repeat(auto-fill, minmax(120px, 1fr))","gap":10,"flowTolerance":0}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":40}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":60}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":50}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'lanes-repeat-forms-0189 container.width');
    expectClose(container.height, 60, 'lanes-repeat-forms-0189 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'lanes-repeat-forms-0189 item[0].x');
      expectClose(r.y - container.y, 0, 'lanes-repeat-forms-0189 item[0].y');
      expectClose(r.width, 142.5, 'lanes-repeat-forms-0189 item[0].w');
      expectClose(r.height, 40, 'lanes-repeat-forms-0189 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 152.5, 'lanes-repeat-forms-0189 item[1].x');
      expectClose(r.y - container.y, 0, 'lanes-repeat-forms-0189 item[1].y');
      expectClose(r.width, 142.5, 'lanes-repeat-forms-0189 item[1].w');
      expectClose(r.height, 60, 'lanes-repeat-forms-0189 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 305, 'lanes-repeat-forms-0189 item[2].x');
      expectClose(r.y - container.y, 0, 'lanes-repeat-forms-0189 item[2].y');
      expectClose(r.width, 142.5, 'lanes-repeat-forms-0189 item[2].w');
      expectClose(r.height, 30, 'lanes-repeat-forms-0189 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 457.5, 'lanes-repeat-forms-0189 item[3].x');
      expectClose(r.y - container.y, 0, 'lanes-repeat-forms-0189 item[3].y');
      expectClose(r.width, 142.5, 'lanes-repeat-forms-0189 item[3].w');
      expectClose(r.height, 50, 'lanes-repeat-forms-0189 item[3].h');
    }
  });
  it("lanes-repeat-forms-0191: repeat() under grid-lanes", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid-lanes","width":600,"gridTemplateColumns":"repeat(auto-fit, minmax(120px, 1fr))","gap":10,"flowTolerance":0}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":40}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":60}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":50}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'lanes-repeat-forms-0191 container.width');
    expectClose(container.height, 60, 'lanes-repeat-forms-0191 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'lanes-repeat-forms-0191 item[0].x');
      expectClose(r.y - container.y, 0, 'lanes-repeat-forms-0191 item[0].y');
      expectClose(r.width, 142.5, 'lanes-repeat-forms-0191 item[0].w');
      expectClose(r.height, 40, 'lanes-repeat-forms-0191 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 152.5, 'lanes-repeat-forms-0191 item[1].x');
      expectClose(r.y - container.y, 0, 'lanes-repeat-forms-0191 item[1].y');
      expectClose(r.width, 142.5, 'lanes-repeat-forms-0191 item[1].w');
      expectClose(r.height, 60, 'lanes-repeat-forms-0191 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 305, 'lanes-repeat-forms-0191 item[2].x');
      expectClose(r.y - container.y, 0, 'lanes-repeat-forms-0191 item[2].y');
      expectClose(r.width, 142.5, 'lanes-repeat-forms-0191 item[2].w');
      expectClose(r.height, 30, 'lanes-repeat-forms-0191 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 457.5, 'lanes-repeat-forms-0191 item[3].x');
      expectClose(r.y - container.y, 0, 'lanes-repeat-forms-0191 item[3].y');
      expectClose(r.width, 142.5, 'lanes-repeat-forms-0191 item[3].w');
      expectClose(r.height, 50, 'lanes-repeat-forms-0191 item[3].h');
    }
  });
});

describe("auto-fill-minmax", () => {
  it("auto-fill-minmax-0192: repeat(auto-fill, minmax(120px, 1fr)) at width 200, gap 0", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid","width":200,"gridTemplateColumns":"repeat(auto-fill, minmax(120px, 1fr))","gap":0}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":40}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":50}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 200, 'auto-fill-minmax-0192 container.width');
    expectClose(container.height, 140, 'auto-fill-minmax-0192 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'auto-fill-minmax-0192 item[0].x');
      expectClose(r.y - container.y, 0, 'auto-fill-minmax-0192 item[0].y');
      expectClose(r.width, 200, 'auto-fill-minmax-0192 item[0].w');
      expectClose(r.height, 30, 'auto-fill-minmax-0192 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 0, 'auto-fill-minmax-0192 item[1].x');
      expectClose(r.y - container.y, 30, 'auto-fill-minmax-0192 item[1].y');
      expectClose(r.width, 200, 'auto-fill-minmax-0192 item[1].w');
      expectClose(r.height, 40, 'auto-fill-minmax-0192 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 0, 'auto-fill-minmax-0192 item[2].x');
      expectClose(r.y - container.y, 70, 'auto-fill-minmax-0192 item[2].y');
      expectClose(r.width, 200, 'auto-fill-minmax-0192 item[2].w');
      expectClose(r.height, 20, 'auto-fill-minmax-0192 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 0, 'auto-fill-minmax-0192 item[3].x');
      expectClose(r.y - container.y, 90, 'auto-fill-minmax-0192 item[3].y');
      expectClose(r.width, 200, 'auto-fill-minmax-0192 item[3].w');
      expectClose(r.height, 50, 'auto-fill-minmax-0192 item[3].h');
    }
  });
  it("auto-fill-minmax-0193: repeat(auto-fill, minmax(120px, 1fr)) at width 200, gap 16", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid","width":200,"gridTemplateColumns":"repeat(auto-fill, minmax(120px, 1fr))","gap":16}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":40}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":50}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 200, 'auto-fill-minmax-0193 container.width');
    expectClose(container.height, 188, 'auto-fill-minmax-0193 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'auto-fill-minmax-0193 item[0].x');
      expectClose(r.y - container.y, 0, 'auto-fill-minmax-0193 item[0].y');
      expectClose(r.width, 200, 'auto-fill-minmax-0193 item[0].w');
      expectClose(r.height, 30, 'auto-fill-minmax-0193 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 0, 'auto-fill-minmax-0193 item[1].x');
      expectClose(r.y - container.y, 46, 'auto-fill-minmax-0193 item[1].y');
      expectClose(r.width, 200, 'auto-fill-minmax-0193 item[1].w');
      expectClose(r.height, 40, 'auto-fill-minmax-0193 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 0, 'auto-fill-minmax-0193 item[2].x');
      expectClose(r.y - container.y, 102, 'auto-fill-minmax-0193 item[2].y');
      expectClose(r.width, 200, 'auto-fill-minmax-0193 item[2].w');
      expectClose(r.height, 20, 'auto-fill-minmax-0193 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 0, 'auto-fill-minmax-0193 item[3].x');
      expectClose(r.y - container.y, 138, 'auto-fill-minmax-0193 item[3].y');
      expectClose(r.width, 200, 'auto-fill-minmax-0193 item[3].w');
      expectClose(r.height, 50, 'auto-fill-minmax-0193 item[3].h');
    }
  });
  it("auto-fill-minmax-0194: repeat(auto-fill, minmax(120px, 1fr)) at width 320, gap 0", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid","width":320,"gridTemplateColumns":"repeat(auto-fill, minmax(120px, 1fr))","gap":0}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":40}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":50}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 320, 'auto-fill-minmax-0194 container.width');
    expectClose(container.height, 90, 'auto-fill-minmax-0194 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'auto-fill-minmax-0194 item[0].x');
      expectClose(r.y - container.y, 0, 'auto-fill-minmax-0194 item[0].y');
      expectClose(r.width, 160, 'auto-fill-minmax-0194 item[0].w');
      expectClose(r.height, 30, 'auto-fill-minmax-0194 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 160, 'auto-fill-minmax-0194 item[1].x');
      expectClose(r.y - container.y, 0, 'auto-fill-minmax-0194 item[1].y');
      expectClose(r.width, 160, 'auto-fill-minmax-0194 item[1].w');
      expectClose(r.height, 40, 'auto-fill-minmax-0194 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 0, 'auto-fill-minmax-0194 item[2].x');
      expectClose(r.y - container.y, 40, 'auto-fill-minmax-0194 item[2].y');
      expectClose(r.width, 160, 'auto-fill-minmax-0194 item[2].w');
      expectClose(r.height, 20, 'auto-fill-minmax-0194 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 160, 'auto-fill-minmax-0194 item[3].x');
      expectClose(r.y - container.y, 40, 'auto-fill-minmax-0194 item[3].y');
      expectClose(r.width, 160, 'auto-fill-minmax-0194 item[3].w');
      expectClose(r.height, 50, 'auto-fill-minmax-0194 item[3].h');
    }
  });
  it("auto-fill-minmax-0195: repeat(auto-fill, minmax(120px, 1fr)) at width 320, gap 16", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid","width":320,"gridTemplateColumns":"repeat(auto-fill, minmax(120px, 1fr))","gap":16}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":40}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":50}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 320, 'auto-fill-minmax-0195 container.width');
    expectClose(container.height, 106, 'auto-fill-minmax-0195 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'auto-fill-minmax-0195 item[0].x');
      expectClose(r.y - container.y, 0, 'auto-fill-minmax-0195 item[0].y');
      expectClose(r.width, 152, 'auto-fill-minmax-0195 item[0].w');
      expectClose(r.height, 30, 'auto-fill-minmax-0195 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 168, 'auto-fill-minmax-0195 item[1].x');
      expectClose(r.y - container.y, 0, 'auto-fill-minmax-0195 item[1].y');
      expectClose(r.width, 152, 'auto-fill-minmax-0195 item[1].w');
      expectClose(r.height, 40, 'auto-fill-minmax-0195 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 0, 'auto-fill-minmax-0195 item[2].x');
      expectClose(r.y - container.y, 56, 'auto-fill-minmax-0195 item[2].y');
      expectClose(r.width, 152, 'auto-fill-minmax-0195 item[2].w');
      expectClose(r.height, 20, 'auto-fill-minmax-0195 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 168, 'auto-fill-minmax-0195 item[3].x');
      expectClose(r.y - container.y, 56, 'auto-fill-minmax-0195 item[3].y');
      expectClose(r.width, 152, 'auto-fill-minmax-0195 item[3].w');
      expectClose(r.height, 50, 'auto-fill-minmax-0195 item[3].h');
    }
  });
  it("auto-fill-minmax-0196: repeat(auto-fill, minmax(120px, 1fr)) at width 480, gap 0", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid","width":480,"gridTemplateColumns":"repeat(auto-fill, minmax(120px, 1fr))","gap":0}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":40}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":50}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 480, 'auto-fill-minmax-0196 container.width');
    expectClose(container.height, 50, 'auto-fill-minmax-0196 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'auto-fill-minmax-0196 item[0].x');
      expectClose(r.y - container.y, 0, 'auto-fill-minmax-0196 item[0].y');
      expectClose(r.width, 120, 'auto-fill-minmax-0196 item[0].w');
      expectClose(r.height, 30, 'auto-fill-minmax-0196 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 120, 'auto-fill-minmax-0196 item[1].x');
      expectClose(r.y - container.y, 0, 'auto-fill-minmax-0196 item[1].y');
      expectClose(r.width, 120, 'auto-fill-minmax-0196 item[1].w');
      expectClose(r.height, 40, 'auto-fill-minmax-0196 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 240, 'auto-fill-minmax-0196 item[2].x');
      expectClose(r.y - container.y, 0, 'auto-fill-minmax-0196 item[2].y');
      expectClose(r.width, 120, 'auto-fill-minmax-0196 item[2].w');
      expectClose(r.height, 20, 'auto-fill-minmax-0196 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 360, 'auto-fill-minmax-0196 item[3].x');
      expectClose(r.y - container.y, 0, 'auto-fill-minmax-0196 item[3].y');
      expectClose(r.width, 120, 'auto-fill-minmax-0196 item[3].w');
      expectClose(r.height, 50, 'auto-fill-minmax-0196 item[3].h');
    }
  });
  it("auto-fill-minmax-0197: repeat(auto-fill, minmax(120px, 1fr)) at width 480, gap 16", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid","width":480,"gridTemplateColumns":"repeat(auto-fill, minmax(120px, 1fr))","gap":16}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":40}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":50}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 480, 'auto-fill-minmax-0197 container.width');
    expectClose(container.height, 106, 'auto-fill-minmax-0197 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'auto-fill-minmax-0197 item[0].x');
      expectClose(r.y - container.y, 0, 'auto-fill-minmax-0197 item[0].y');
      expectClose(r.width, 149.33, 'auto-fill-minmax-0197 item[0].w');
      expectClose(r.height, 30, 'auto-fill-minmax-0197 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 165.33, 'auto-fill-minmax-0197 item[1].x');
      expectClose(r.y - container.y, 0, 'auto-fill-minmax-0197 item[1].y');
      expectClose(r.width, 149.33, 'auto-fill-minmax-0197 item[1].w');
      expectClose(r.height, 40, 'auto-fill-minmax-0197 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 330.66, 'auto-fill-minmax-0197 item[2].x');
      expectClose(r.y - container.y, 0, 'auto-fill-minmax-0197 item[2].y');
      expectClose(r.width, 149.34, 'auto-fill-minmax-0197 item[2].w');
      expectClose(r.height, 20, 'auto-fill-minmax-0197 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 0, 'auto-fill-minmax-0197 item[3].x');
      expectClose(r.y - container.y, 56, 'auto-fill-minmax-0197 item[3].y');
      expectClose(r.width, 149.33, 'auto-fill-minmax-0197 item[3].w');
      expectClose(r.height, 50, 'auto-fill-minmax-0197 item[3].h');
    }
  });
  it("auto-fill-minmax-0198: repeat(auto-fill, minmax(120px, 1fr)) at width 600, gap 0", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid","width":600,"gridTemplateColumns":"repeat(auto-fill, minmax(120px, 1fr))","gap":0}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":40}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":50}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'auto-fill-minmax-0198 container.width');
    expectClose(container.height, 50, 'auto-fill-minmax-0198 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'auto-fill-minmax-0198 item[0].x');
      expectClose(r.y - container.y, 0, 'auto-fill-minmax-0198 item[0].y');
      expectClose(r.width, 120, 'auto-fill-minmax-0198 item[0].w');
      expectClose(r.height, 30, 'auto-fill-minmax-0198 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 120, 'auto-fill-minmax-0198 item[1].x');
      expectClose(r.y - container.y, 0, 'auto-fill-minmax-0198 item[1].y');
      expectClose(r.width, 120, 'auto-fill-minmax-0198 item[1].w');
      expectClose(r.height, 40, 'auto-fill-minmax-0198 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 240, 'auto-fill-minmax-0198 item[2].x');
      expectClose(r.y - container.y, 0, 'auto-fill-minmax-0198 item[2].y');
      expectClose(r.width, 120, 'auto-fill-minmax-0198 item[2].w');
      expectClose(r.height, 20, 'auto-fill-minmax-0198 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 360, 'auto-fill-minmax-0198 item[3].x');
      expectClose(r.y - container.y, 0, 'auto-fill-minmax-0198 item[3].y');
      expectClose(r.width, 120, 'auto-fill-minmax-0198 item[3].w');
      expectClose(r.height, 50, 'auto-fill-minmax-0198 item[3].h');
    }
  });
  it("auto-fill-minmax-0199: repeat(auto-fill, minmax(120px, 1fr)) at width 600, gap 16", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid","width":600,"gridTemplateColumns":"repeat(auto-fill, minmax(120px, 1fr))","gap":16}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":40}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":50}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'auto-fill-minmax-0199 container.width');
    expectClose(container.height, 50, 'auto-fill-minmax-0199 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'auto-fill-minmax-0199 item[0].x');
      expectClose(r.y - container.y, 0, 'auto-fill-minmax-0199 item[0].y');
      expectClose(r.width, 138, 'auto-fill-minmax-0199 item[0].w');
      expectClose(r.height, 30, 'auto-fill-minmax-0199 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 154, 'auto-fill-minmax-0199 item[1].x');
      expectClose(r.y - container.y, 0, 'auto-fill-minmax-0199 item[1].y');
      expectClose(r.width, 138, 'auto-fill-minmax-0199 item[1].w');
      expectClose(r.height, 40, 'auto-fill-minmax-0199 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 308, 'auto-fill-minmax-0199 item[2].x');
      expectClose(r.y - container.y, 0, 'auto-fill-minmax-0199 item[2].y');
      expectClose(r.width, 138, 'auto-fill-minmax-0199 item[2].w');
      expectClose(r.height, 20, 'auto-fill-minmax-0199 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 462, 'auto-fill-minmax-0199 item[3].x');
      expectClose(r.y - container.y, 0, 'auto-fill-minmax-0199 item[3].y');
      expectClose(r.width, 138, 'auto-fill-minmax-0199 item[3].w');
      expectClose(r.height, 50, 'auto-fill-minmax-0199 item[3].h');
    }
  });
  it("auto-fill-minmax-0200: repeat(auto-fill, minmax(120px, 1fr)) at width 720, gap 0", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid","width":720,"gridTemplateColumns":"repeat(auto-fill, minmax(120px, 1fr))","gap":0}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":40}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":50}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 720, 'auto-fill-minmax-0200 container.width');
    expectClose(container.height, 50, 'auto-fill-minmax-0200 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'auto-fill-minmax-0200 item[0].x');
      expectClose(r.y - container.y, 0, 'auto-fill-minmax-0200 item[0].y');
      expectClose(r.width, 120, 'auto-fill-minmax-0200 item[0].w');
      expectClose(r.height, 30, 'auto-fill-minmax-0200 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 120, 'auto-fill-minmax-0200 item[1].x');
      expectClose(r.y - container.y, 0, 'auto-fill-minmax-0200 item[1].y');
      expectClose(r.width, 120, 'auto-fill-minmax-0200 item[1].w');
      expectClose(r.height, 40, 'auto-fill-minmax-0200 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 240, 'auto-fill-minmax-0200 item[2].x');
      expectClose(r.y - container.y, 0, 'auto-fill-minmax-0200 item[2].y');
      expectClose(r.width, 120, 'auto-fill-minmax-0200 item[2].w');
      expectClose(r.height, 20, 'auto-fill-minmax-0200 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 360, 'auto-fill-minmax-0200 item[3].x');
      expectClose(r.y - container.y, 0, 'auto-fill-minmax-0200 item[3].y');
      expectClose(r.width, 120, 'auto-fill-minmax-0200 item[3].w');
      expectClose(r.height, 50, 'auto-fill-minmax-0200 item[3].h');
    }
  });
  it("auto-fill-minmax-0201: repeat(auto-fill, minmax(120px, 1fr)) at width 720, gap 16", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid","width":720,"gridTemplateColumns":"repeat(auto-fill, minmax(120px, 1fr))","gap":16}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":40}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":50}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 720, 'auto-fill-minmax-0201 container.width');
    expectClose(container.height, 50, 'auto-fill-minmax-0201 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'auto-fill-minmax-0201 item[0].x');
      expectClose(r.y - container.y, 0, 'auto-fill-minmax-0201 item[0].y');
      expectClose(r.width, 131.19, 'auto-fill-minmax-0201 item[0].w');
      expectClose(r.height, 30, 'auto-fill-minmax-0201 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 147.19, 'auto-fill-minmax-0201 item[1].x');
      expectClose(r.y - container.y, 0, 'auto-fill-minmax-0201 item[1].y');
      expectClose(r.width, 131.2, 'auto-fill-minmax-0201 item[1].w');
      expectClose(r.height, 40, 'auto-fill-minmax-0201 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 294.39, 'auto-fill-minmax-0201 item[2].x');
      expectClose(r.y - container.y, 0, 'auto-fill-minmax-0201 item[2].y');
      expectClose(r.width, 131.2, 'auto-fill-minmax-0201 item[2].w');
      expectClose(r.height, 20, 'auto-fill-minmax-0201 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 441.59, 'auto-fill-minmax-0201 item[3].x');
      expectClose(r.y - container.y, 0, 'auto-fill-minmax-0201 item[3].y');
      expectClose(r.width, 131.2, 'auto-fill-minmax-0201 item[3].w');
      expectClose(r.height, 50, 'auto-fill-minmax-0201 item[3].h');
    }
  });
  it("auto-fill-minmax-0202: repeat(auto-fill, minmax(120px, 1fr)) at width 900, gap 0", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid","width":900,"gridTemplateColumns":"repeat(auto-fill, minmax(120px, 1fr))","gap":0}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":40}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":50}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 900, 'auto-fill-minmax-0202 container.width');
    expectClose(container.height, 50, 'auto-fill-minmax-0202 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'auto-fill-minmax-0202 item[0].x');
      expectClose(r.y - container.y, 0, 'auto-fill-minmax-0202 item[0].y');
      expectClose(r.width, 128.56, 'auto-fill-minmax-0202 item[0].w');
      expectClose(r.height, 30, 'auto-fill-minmax-0202 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 128.56, 'auto-fill-minmax-0202 item[1].x');
      expectClose(r.y - container.y, 0, 'auto-fill-minmax-0202 item[1].y');
      expectClose(r.width, 128.58, 'auto-fill-minmax-0202 item[1].w');
      expectClose(r.height, 40, 'auto-fill-minmax-0202 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 257.14, 'auto-fill-minmax-0202 item[2].x');
      expectClose(r.y - container.y, 0, 'auto-fill-minmax-0202 item[2].y');
      expectClose(r.width, 128.56, 'auto-fill-minmax-0202 item[2].w');
      expectClose(r.height, 20, 'auto-fill-minmax-0202 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 385.7, 'auto-fill-minmax-0202 item[3].x');
      expectClose(r.y - container.y, 0, 'auto-fill-minmax-0202 item[3].y');
      expectClose(r.width, 128.58, 'auto-fill-minmax-0202 item[3].w');
      expectClose(r.height, 50, 'auto-fill-minmax-0202 item[3].h');
    }
  });
  it("auto-fill-minmax-0203: repeat(auto-fill, minmax(120px, 1fr)) at width 900, gap 16", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid","width":900,"gridTemplateColumns":"repeat(auto-fill, minmax(120px, 1fr))","gap":16}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":40}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":50}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 900, 'auto-fill-minmax-0203 container.width');
    expectClose(container.height, 50, 'auto-fill-minmax-0203 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'auto-fill-minmax-0203 item[0].x');
      expectClose(r.y - container.y, 0, 'auto-fill-minmax-0203 item[0].y');
      expectClose(r.width, 136.66, 'auto-fill-minmax-0203 item[0].w');
      expectClose(r.height, 30, 'auto-fill-minmax-0203 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 152.66, 'auto-fill-minmax-0203 item[1].x');
      expectClose(r.y - container.y, 0, 'auto-fill-minmax-0203 item[1].y');
      expectClose(r.width, 136.67, 'auto-fill-minmax-0203 item[1].w');
      expectClose(r.height, 40, 'auto-fill-minmax-0203 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 305.33, 'auto-fill-minmax-0203 item[2].x');
      expectClose(r.y - container.y, 0, 'auto-fill-minmax-0203 item[2].y');
      expectClose(r.width, 136.66, 'auto-fill-minmax-0203 item[2].w');
      expectClose(r.height, 20, 'auto-fill-minmax-0203 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 457.98, 'auto-fill-minmax-0203 item[3].x');
      expectClose(r.y - container.y, 0, 'auto-fill-minmax-0203 item[3].y');
      expectClose(r.width, 136.67, 'auto-fill-minmax-0203 item[3].w');
      expectClose(r.height, 50, 'auto-fill-minmax-0203 item[3].h');
    }
  });
  it("auto-fill-minmax-0204: repeat(auto-fill, minmax(120px, 1fr)) at width 1000, gap 0", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid","width":1000,"gridTemplateColumns":"repeat(auto-fill, minmax(120px, 1fr))","gap":0}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":40}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":50}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 1000, 'auto-fill-minmax-0204 container.width');
    expectClose(container.height, 50, 'auto-fill-minmax-0204 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'auto-fill-minmax-0204 item[0].x');
      expectClose(r.y - container.y, 0, 'auto-fill-minmax-0204 item[0].y');
      expectClose(r.width, 125, 'auto-fill-minmax-0204 item[0].w');
      expectClose(r.height, 30, 'auto-fill-minmax-0204 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 125, 'auto-fill-minmax-0204 item[1].x');
      expectClose(r.y - container.y, 0, 'auto-fill-minmax-0204 item[1].y');
      expectClose(r.width, 125, 'auto-fill-minmax-0204 item[1].w');
      expectClose(r.height, 40, 'auto-fill-minmax-0204 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 250, 'auto-fill-minmax-0204 item[2].x');
      expectClose(r.y - container.y, 0, 'auto-fill-minmax-0204 item[2].y');
      expectClose(r.width, 125, 'auto-fill-minmax-0204 item[2].w');
      expectClose(r.height, 20, 'auto-fill-minmax-0204 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 375, 'auto-fill-minmax-0204 item[3].x');
      expectClose(r.y - container.y, 0, 'auto-fill-minmax-0204 item[3].y');
      expectClose(r.width, 125, 'auto-fill-minmax-0204 item[3].w');
      expectClose(r.height, 50, 'auto-fill-minmax-0204 item[3].h');
    }
  });
  it("auto-fill-minmax-0205: repeat(auto-fill, minmax(120px, 1fr)) at width 1000, gap 16", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid","width":1000,"gridTemplateColumns":"repeat(auto-fill, minmax(120px, 1fr))","gap":16}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":40}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":50}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 1000, 'auto-fill-minmax-0205 container.width');
    expectClose(container.height, 50, 'auto-fill-minmax-0205 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'auto-fill-minmax-0205 item[0].x');
      expectClose(r.y - container.y, 0, 'auto-fill-minmax-0205 item[0].y');
      expectClose(r.width, 129.14, 'auto-fill-minmax-0205 item[0].w');
      expectClose(r.height, 30, 'auto-fill-minmax-0205 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 145.14, 'auto-fill-minmax-0205 item[1].x');
      expectClose(r.y - container.y, 0, 'auto-fill-minmax-0205 item[1].y');
      expectClose(r.width, 129.14, 'auto-fill-minmax-0205 item[1].w');
      expectClose(r.height, 40, 'auto-fill-minmax-0205 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 290.28, 'auto-fill-minmax-0205 item[2].x');
      expectClose(r.y - container.y, 0, 'auto-fill-minmax-0205 item[2].y');
      expectClose(r.width, 129.14, 'auto-fill-minmax-0205 item[2].w');
      expectClose(r.height, 20, 'auto-fill-minmax-0205 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 435.42, 'auto-fill-minmax-0205 item[3].x');
      expectClose(r.y - container.y, 0, 'auto-fill-minmax-0205 item[3].y');
      expectClose(r.width, 129.14, 'auto-fill-minmax-0205 item[3].w');
      expectClose(r.height, 50, 'auto-fill-minmax-0205 item[3].h');
    }
  });
});

describe("auto-fit-collapse", () => {
  it("auto-fit-collapse-0206: repeat(auto-fit, minmax(120px, 1fr)) at width 480 with 1 items", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid","width":480,"gridTemplateColumns":"repeat(auto-fit, minmax(120px, 1fr))","gap":16}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 480, 'auto-fit-collapse-0206 container.width');
    expectClose(container.height, 30, 'auto-fit-collapse-0206 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'auto-fit-collapse-0206 item[0].x');
      expectClose(r.y - container.y, 0, 'auto-fit-collapse-0206 item[0].y');
      expectClose(r.width, 480, 'auto-fit-collapse-0206 item[0].w');
      expectClose(r.height, 30, 'auto-fit-collapse-0206 item[0].h');
    }
  });
  it("auto-fit-collapse-0208: repeat(auto-fit, minmax(120px, 1fr)) at width 480 with 2 items", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid","width":480,"gridTemplateColumns":"repeat(auto-fit, minmax(120px, 1fr))","gap":16}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":35}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 480, 'auto-fit-collapse-0208 container.width');
    expectClose(container.height, 35, 'auto-fit-collapse-0208 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'auto-fit-collapse-0208 item[0].x');
      expectClose(r.y - container.y, 0, 'auto-fit-collapse-0208 item[0].y');
      expectClose(r.width, 232, 'auto-fit-collapse-0208 item[0].w');
      expectClose(r.height, 30, 'auto-fit-collapse-0208 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 248, 'auto-fit-collapse-0208 item[1].x');
      expectClose(r.y - container.y, 0, 'auto-fit-collapse-0208 item[1].y');
      expectClose(r.width, 232, 'auto-fit-collapse-0208 item[1].w');
      expectClose(r.height, 35, 'auto-fit-collapse-0208 item[1].h');
    }
  });
  it("auto-fit-collapse-0210: repeat(auto-fit, minmax(120px, 1fr)) at width 480 with 6 items", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid","width":480,"gridTemplateColumns":"repeat(auto-fit, minmax(120px, 1fr))","gap":16}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":35}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":40}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":45}}
          />
          <View
            collapsable={false}
            ref={itemRefs[4]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":50}}
          />
          <View
            collapsable={false}
            ref={itemRefs[5]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":55}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 480, 'auto-fit-collapse-0210 container.width');
    expectClose(container.height, 111, 'auto-fit-collapse-0210 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'auto-fit-collapse-0210 item[0].x');
      expectClose(r.y - container.y, 0, 'auto-fit-collapse-0210 item[0].y');
      expectClose(r.width, 149.33, 'auto-fit-collapse-0210 item[0].w');
      expectClose(r.height, 30, 'auto-fit-collapse-0210 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 165.33, 'auto-fit-collapse-0210 item[1].x');
      expectClose(r.y - container.y, 0, 'auto-fit-collapse-0210 item[1].y');
      expectClose(r.width, 149.33, 'auto-fit-collapse-0210 item[1].w');
      expectClose(r.height, 35, 'auto-fit-collapse-0210 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 330.66, 'auto-fit-collapse-0210 item[2].x');
      expectClose(r.y - container.y, 0, 'auto-fit-collapse-0210 item[2].y');
      expectClose(r.width, 149.34, 'auto-fit-collapse-0210 item[2].w');
      expectClose(r.height, 40, 'auto-fit-collapse-0210 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 0, 'auto-fit-collapse-0210 item[3].x');
      expectClose(r.y - container.y, 56, 'auto-fit-collapse-0210 item[3].y');
      expectClose(r.width, 149.33, 'auto-fit-collapse-0210 item[3].w');
      expectClose(r.height, 45, 'auto-fit-collapse-0210 item[3].h');
    }
    {
      const r = rectOf(itemRefs[4]);
      expectClose(r.x - container.x, 165.33, 'auto-fit-collapse-0210 item[4].x');
      expectClose(r.y - container.y, 56, 'auto-fit-collapse-0210 item[4].y');
      expectClose(r.width, 149.33, 'auto-fit-collapse-0210 item[4].w');
      expectClose(r.height, 50, 'auto-fit-collapse-0210 item[4].h');
    }
    {
      const r = rectOf(itemRefs[5]);
      expectClose(r.x - container.x, 330.66, 'auto-fit-collapse-0210 item[5].x');
      expectClose(r.y - container.y, 56, 'auto-fit-collapse-0210 item[5].y');
      expectClose(r.width, 149.34, 'auto-fit-collapse-0210 item[5].w');
      expectClose(r.height, 55, 'auto-fit-collapse-0210 item[5].h');
    }
  });
  it("auto-fit-collapse-0212: repeat(auto-fit, minmax(120px, 1fr)) at width 720 with 1 items", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid","width":720,"gridTemplateColumns":"repeat(auto-fit, minmax(120px, 1fr))","gap":16}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 720, 'auto-fit-collapse-0212 container.width');
    expectClose(container.height, 30, 'auto-fit-collapse-0212 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'auto-fit-collapse-0212 item[0].x');
      expectClose(r.y - container.y, 0, 'auto-fit-collapse-0212 item[0].y');
      expectClose(r.width, 720, 'auto-fit-collapse-0212 item[0].w');
      expectClose(r.height, 30, 'auto-fit-collapse-0212 item[0].h');
    }
  });
  it("auto-fit-collapse-0214: repeat(auto-fit, minmax(120px, 1fr)) at width 720 with 2 items", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid","width":720,"gridTemplateColumns":"repeat(auto-fit, minmax(120px, 1fr))","gap":16}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":35}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 720, 'auto-fit-collapse-0214 container.width');
    expectClose(container.height, 35, 'auto-fit-collapse-0214 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'auto-fit-collapse-0214 item[0].x');
      expectClose(r.y - container.y, 0, 'auto-fit-collapse-0214 item[0].y');
      expectClose(r.width, 352, 'auto-fit-collapse-0214 item[0].w');
      expectClose(r.height, 30, 'auto-fit-collapse-0214 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 368, 'auto-fit-collapse-0214 item[1].x');
      expectClose(r.y - container.y, 0, 'auto-fit-collapse-0214 item[1].y');
      expectClose(r.width, 352, 'auto-fit-collapse-0214 item[1].w');
      expectClose(r.height, 35, 'auto-fit-collapse-0214 item[1].h');
    }
  });
  it("auto-fit-collapse-0216: repeat(auto-fit, minmax(120px, 1fr)) at width 720 with 6 items", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid","width":720,"gridTemplateColumns":"repeat(auto-fit, minmax(120px, 1fr))","gap":16}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":35}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":40}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":45}}
          />
          <View
            collapsable={false}
            ref={itemRefs[4]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":50}}
          />
          <View
            collapsable={false}
            ref={itemRefs[5]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":55}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 720, 'auto-fit-collapse-0216 container.width');
    expectClose(container.height, 121, 'auto-fit-collapse-0216 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'auto-fit-collapse-0216 item[0].x');
      expectClose(r.y - container.y, 0, 'auto-fit-collapse-0216 item[0].y');
      expectClose(r.width, 131.19, 'auto-fit-collapse-0216 item[0].w');
      expectClose(r.height, 30, 'auto-fit-collapse-0216 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 147.19, 'auto-fit-collapse-0216 item[1].x');
      expectClose(r.y - container.y, 0, 'auto-fit-collapse-0216 item[1].y');
      expectClose(r.width, 131.2, 'auto-fit-collapse-0216 item[1].w');
      expectClose(r.height, 35, 'auto-fit-collapse-0216 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 294.39, 'auto-fit-collapse-0216 item[2].x');
      expectClose(r.y - container.y, 0, 'auto-fit-collapse-0216 item[2].y');
      expectClose(r.width, 131.2, 'auto-fit-collapse-0216 item[2].w');
      expectClose(r.height, 40, 'auto-fit-collapse-0216 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 441.59, 'auto-fit-collapse-0216 item[3].x');
      expectClose(r.y - container.y, 0, 'auto-fit-collapse-0216 item[3].y');
      expectClose(r.width, 131.2, 'auto-fit-collapse-0216 item[3].w');
      expectClose(r.height, 45, 'auto-fit-collapse-0216 item[3].h');
    }
    {
      const r = rectOf(itemRefs[4]);
      expectClose(r.x - container.x, 588.8, 'auto-fit-collapse-0216 item[4].x');
      expectClose(r.y - container.y, 0, 'auto-fit-collapse-0216 item[4].y');
      expectClose(r.width, 131.19, 'auto-fit-collapse-0216 item[4].w');
      expectClose(r.height, 50, 'auto-fit-collapse-0216 item[4].h');
    }
    {
      const r = rectOf(itemRefs[5]);
      expectClose(r.x - container.x, 0, 'auto-fit-collapse-0216 item[5].x');
      expectClose(r.y - container.y, 66, 'auto-fit-collapse-0216 item[5].y');
      expectClose(r.width, 131.19, 'auto-fit-collapse-0216 item[5].w');
      expectClose(r.height, 55, 'auto-fit-collapse-0216 item[5].h');
    }
  });
  it("auto-fit-collapse-0218: repeat(auto-fit, minmax(120px, 1fr)) at width 1000 with 1 items", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid","width":1000,"gridTemplateColumns":"repeat(auto-fit, minmax(120px, 1fr))","gap":16}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 1000, 'auto-fit-collapse-0218 container.width');
    expectClose(container.height, 30, 'auto-fit-collapse-0218 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'auto-fit-collapse-0218 item[0].x');
      expectClose(r.y - container.y, 0, 'auto-fit-collapse-0218 item[0].y');
      expectClose(r.width, 1000, 'auto-fit-collapse-0218 item[0].w');
      expectClose(r.height, 30, 'auto-fit-collapse-0218 item[0].h');
    }
  });
  it("auto-fit-collapse-0220: repeat(auto-fit, minmax(120px, 1fr)) at width 1000 with 2 items", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid","width":1000,"gridTemplateColumns":"repeat(auto-fit, minmax(120px, 1fr))","gap":16}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":35}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 1000, 'auto-fit-collapse-0220 container.width');
    expectClose(container.height, 35, 'auto-fit-collapse-0220 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'auto-fit-collapse-0220 item[0].x');
      expectClose(r.y - container.y, 0, 'auto-fit-collapse-0220 item[0].y');
      expectClose(r.width, 492, 'auto-fit-collapse-0220 item[0].w');
      expectClose(r.height, 30, 'auto-fit-collapse-0220 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 508, 'auto-fit-collapse-0220 item[1].x');
      expectClose(r.y - container.y, 0, 'auto-fit-collapse-0220 item[1].y');
      expectClose(r.width, 492, 'auto-fit-collapse-0220 item[1].w');
      expectClose(r.height, 35, 'auto-fit-collapse-0220 item[1].h');
    }
  });
  it("auto-fit-collapse-0222: repeat(auto-fit, minmax(120px, 1fr)) at width 1000 with 6 items", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid","width":1000,"gridTemplateColumns":"repeat(auto-fit, minmax(120px, 1fr))","gap":16}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":35}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":40}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":45}}
          />
          <View
            collapsable={false}
            ref={itemRefs[4]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":50}}
          />
          <View
            collapsable={false}
            ref={itemRefs[5]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":55}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 1000, 'auto-fit-collapse-0222 container.width');
    expectClose(container.height, 55, 'auto-fit-collapse-0222 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'auto-fit-collapse-0222 item[0].x');
      expectClose(r.y - container.y, 0, 'auto-fit-collapse-0222 item[0].y');
      expectClose(r.width, 153.33, 'auto-fit-collapse-0222 item[0].w');
      expectClose(r.height, 30, 'auto-fit-collapse-0222 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 169.33, 'auto-fit-collapse-0222 item[1].x');
      expectClose(r.y - container.y, 0, 'auto-fit-collapse-0222 item[1].y');
      expectClose(r.width, 153.33, 'auto-fit-collapse-0222 item[1].w');
      expectClose(r.height, 35, 'auto-fit-collapse-0222 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 338.66, 'auto-fit-collapse-0222 item[2].x');
      expectClose(r.y - container.y, 0, 'auto-fit-collapse-0222 item[2].y');
      expectClose(r.width, 153.34, 'auto-fit-collapse-0222 item[2].w');
      expectClose(r.height, 40, 'auto-fit-collapse-0222 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 508, 'auto-fit-collapse-0222 item[3].x');
      expectClose(r.y - container.y, 0, 'auto-fit-collapse-0222 item[3].y');
      expectClose(r.width, 153.33, 'auto-fit-collapse-0222 item[3].w');
      expectClose(r.height, 45, 'auto-fit-collapse-0222 item[3].h');
    }
    {
      const r = rectOf(itemRefs[4]);
      expectClose(r.x - container.x, 677.33, 'auto-fit-collapse-0222 item[4].x');
      expectClose(r.y - container.y, 0, 'auto-fit-collapse-0222 item[4].y');
      expectClose(r.width, 153.33, 'auto-fit-collapse-0222 item[4].w');
      expectClose(r.height, 50, 'auto-fit-collapse-0222 item[4].h');
    }
    {
      const r = rectOf(itemRefs[5]);
      expectClose(r.x - container.x, 846.66, 'auto-fit-collapse-0222 item[5].x');
      expectClose(r.y - container.y, 0, 'auto-fit-collapse-0222 item[5].y');
      expectClose(r.width, 153.34, 'auto-fit-collapse-0222 item[5].w');
      expectClose(r.height, 55, 'auto-fit-collapse-0222 item[5].h');
    }
  });
});

describe("auto-fill-vs-fit", () => {
  it("auto-fill-vs-fit-0207: auto-fill counterpart at width 480 with 1 items", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid","width":480,"gridTemplateColumns":"repeat(auto-fill, minmax(120px, 1fr))","gap":16}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 480, 'auto-fill-vs-fit-0207 container.width');
    expectClose(container.height, 30, 'auto-fill-vs-fit-0207 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'auto-fill-vs-fit-0207 item[0].x');
      expectClose(r.y - container.y, 0, 'auto-fill-vs-fit-0207 item[0].y');
      expectClose(r.width, 149.33, 'auto-fill-vs-fit-0207 item[0].w');
      expectClose(r.height, 30, 'auto-fill-vs-fit-0207 item[0].h');
    }
  });
  it("auto-fill-vs-fit-0209: auto-fill counterpart at width 480 with 2 items", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid","width":480,"gridTemplateColumns":"repeat(auto-fill, minmax(120px, 1fr))","gap":16}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":35}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 480, 'auto-fill-vs-fit-0209 container.width');
    expectClose(container.height, 35, 'auto-fill-vs-fit-0209 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'auto-fill-vs-fit-0209 item[0].x');
      expectClose(r.y - container.y, 0, 'auto-fill-vs-fit-0209 item[0].y');
      expectClose(r.width, 149.33, 'auto-fill-vs-fit-0209 item[0].w');
      expectClose(r.height, 30, 'auto-fill-vs-fit-0209 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 165.33, 'auto-fill-vs-fit-0209 item[1].x');
      expectClose(r.y - container.y, 0, 'auto-fill-vs-fit-0209 item[1].y');
      expectClose(r.width, 149.33, 'auto-fill-vs-fit-0209 item[1].w');
      expectClose(r.height, 35, 'auto-fill-vs-fit-0209 item[1].h');
    }
  });
  it("auto-fill-vs-fit-0211: auto-fill counterpart at width 480 with 6 items", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid","width":480,"gridTemplateColumns":"repeat(auto-fill, minmax(120px, 1fr))","gap":16}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":35}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":40}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":45}}
          />
          <View
            collapsable={false}
            ref={itemRefs[4]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":50}}
          />
          <View
            collapsable={false}
            ref={itemRefs[5]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":55}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 480, 'auto-fill-vs-fit-0211 container.width');
    expectClose(container.height, 111, 'auto-fill-vs-fit-0211 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'auto-fill-vs-fit-0211 item[0].x');
      expectClose(r.y - container.y, 0, 'auto-fill-vs-fit-0211 item[0].y');
      expectClose(r.width, 149.33, 'auto-fill-vs-fit-0211 item[0].w');
      expectClose(r.height, 30, 'auto-fill-vs-fit-0211 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 165.33, 'auto-fill-vs-fit-0211 item[1].x');
      expectClose(r.y - container.y, 0, 'auto-fill-vs-fit-0211 item[1].y');
      expectClose(r.width, 149.33, 'auto-fill-vs-fit-0211 item[1].w');
      expectClose(r.height, 35, 'auto-fill-vs-fit-0211 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 330.66, 'auto-fill-vs-fit-0211 item[2].x');
      expectClose(r.y - container.y, 0, 'auto-fill-vs-fit-0211 item[2].y');
      expectClose(r.width, 149.34, 'auto-fill-vs-fit-0211 item[2].w');
      expectClose(r.height, 40, 'auto-fill-vs-fit-0211 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 0, 'auto-fill-vs-fit-0211 item[3].x');
      expectClose(r.y - container.y, 56, 'auto-fill-vs-fit-0211 item[3].y');
      expectClose(r.width, 149.33, 'auto-fill-vs-fit-0211 item[3].w');
      expectClose(r.height, 45, 'auto-fill-vs-fit-0211 item[3].h');
    }
    {
      const r = rectOf(itemRefs[4]);
      expectClose(r.x - container.x, 165.33, 'auto-fill-vs-fit-0211 item[4].x');
      expectClose(r.y - container.y, 56, 'auto-fill-vs-fit-0211 item[4].y');
      expectClose(r.width, 149.33, 'auto-fill-vs-fit-0211 item[4].w');
      expectClose(r.height, 50, 'auto-fill-vs-fit-0211 item[4].h');
    }
    {
      const r = rectOf(itemRefs[5]);
      expectClose(r.x - container.x, 330.66, 'auto-fill-vs-fit-0211 item[5].x');
      expectClose(r.y - container.y, 56, 'auto-fill-vs-fit-0211 item[5].y');
      expectClose(r.width, 149.34, 'auto-fill-vs-fit-0211 item[5].w');
      expectClose(r.height, 55, 'auto-fill-vs-fit-0211 item[5].h');
    }
  });
  it("auto-fill-vs-fit-0213: auto-fill counterpart at width 720 with 1 items", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid","width":720,"gridTemplateColumns":"repeat(auto-fill, minmax(120px, 1fr))","gap":16}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 720, 'auto-fill-vs-fit-0213 container.width');
    expectClose(container.height, 30, 'auto-fill-vs-fit-0213 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'auto-fill-vs-fit-0213 item[0].x');
      expectClose(r.y - container.y, 0, 'auto-fill-vs-fit-0213 item[0].y');
      expectClose(r.width, 131.19, 'auto-fill-vs-fit-0213 item[0].w');
      expectClose(r.height, 30, 'auto-fill-vs-fit-0213 item[0].h');
    }
  });
  it("auto-fill-vs-fit-0215: auto-fill counterpart at width 720 with 2 items", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid","width":720,"gridTemplateColumns":"repeat(auto-fill, minmax(120px, 1fr))","gap":16}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":35}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 720, 'auto-fill-vs-fit-0215 container.width');
    expectClose(container.height, 35, 'auto-fill-vs-fit-0215 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'auto-fill-vs-fit-0215 item[0].x');
      expectClose(r.y - container.y, 0, 'auto-fill-vs-fit-0215 item[0].y');
      expectClose(r.width, 131.19, 'auto-fill-vs-fit-0215 item[0].w');
      expectClose(r.height, 30, 'auto-fill-vs-fit-0215 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 147.19, 'auto-fill-vs-fit-0215 item[1].x');
      expectClose(r.y - container.y, 0, 'auto-fill-vs-fit-0215 item[1].y');
      expectClose(r.width, 131.2, 'auto-fill-vs-fit-0215 item[1].w');
      expectClose(r.height, 35, 'auto-fill-vs-fit-0215 item[1].h');
    }
  });
  it("auto-fill-vs-fit-0217: auto-fill counterpart at width 720 with 6 items", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid","width":720,"gridTemplateColumns":"repeat(auto-fill, minmax(120px, 1fr))","gap":16}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":35}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":40}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":45}}
          />
          <View
            collapsable={false}
            ref={itemRefs[4]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":50}}
          />
          <View
            collapsable={false}
            ref={itemRefs[5]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":55}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 720, 'auto-fill-vs-fit-0217 container.width');
    expectClose(container.height, 121, 'auto-fill-vs-fit-0217 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'auto-fill-vs-fit-0217 item[0].x');
      expectClose(r.y - container.y, 0, 'auto-fill-vs-fit-0217 item[0].y');
      expectClose(r.width, 131.19, 'auto-fill-vs-fit-0217 item[0].w');
      expectClose(r.height, 30, 'auto-fill-vs-fit-0217 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 147.19, 'auto-fill-vs-fit-0217 item[1].x');
      expectClose(r.y - container.y, 0, 'auto-fill-vs-fit-0217 item[1].y');
      expectClose(r.width, 131.2, 'auto-fill-vs-fit-0217 item[1].w');
      expectClose(r.height, 35, 'auto-fill-vs-fit-0217 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 294.39, 'auto-fill-vs-fit-0217 item[2].x');
      expectClose(r.y - container.y, 0, 'auto-fill-vs-fit-0217 item[2].y');
      expectClose(r.width, 131.2, 'auto-fill-vs-fit-0217 item[2].w');
      expectClose(r.height, 40, 'auto-fill-vs-fit-0217 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 441.59, 'auto-fill-vs-fit-0217 item[3].x');
      expectClose(r.y - container.y, 0, 'auto-fill-vs-fit-0217 item[3].y');
      expectClose(r.width, 131.2, 'auto-fill-vs-fit-0217 item[3].w');
      expectClose(r.height, 45, 'auto-fill-vs-fit-0217 item[3].h');
    }
    {
      const r = rectOf(itemRefs[4]);
      expectClose(r.x - container.x, 588.8, 'auto-fill-vs-fit-0217 item[4].x');
      expectClose(r.y - container.y, 0, 'auto-fill-vs-fit-0217 item[4].y');
      expectClose(r.width, 131.19, 'auto-fill-vs-fit-0217 item[4].w');
      expectClose(r.height, 50, 'auto-fill-vs-fit-0217 item[4].h');
    }
    {
      const r = rectOf(itemRefs[5]);
      expectClose(r.x - container.x, 0, 'auto-fill-vs-fit-0217 item[5].x');
      expectClose(r.y - container.y, 66, 'auto-fill-vs-fit-0217 item[5].y');
      expectClose(r.width, 131.19, 'auto-fill-vs-fit-0217 item[5].w');
      expectClose(r.height, 55, 'auto-fill-vs-fit-0217 item[5].h');
    }
  });
  it("auto-fill-vs-fit-0219: auto-fill counterpart at width 1000 with 1 items", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid","width":1000,"gridTemplateColumns":"repeat(auto-fill, minmax(120px, 1fr))","gap":16}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 1000, 'auto-fill-vs-fit-0219 container.width');
    expectClose(container.height, 30, 'auto-fill-vs-fit-0219 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'auto-fill-vs-fit-0219 item[0].x');
      expectClose(r.y - container.y, 0, 'auto-fill-vs-fit-0219 item[0].y');
      expectClose(r.width, 129.14, 'auto-fill-vs-fit-0219 item[0].w');
      expectClose(r.height, 30, 'auto-fill-vs-fit-0219 item[0].h');
    }
  });
  it("auto-fill-vs-fit-0221: auto-fill counterpart at width 1000 with 2 items", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid","width":1000,"gridTemplateColumns":"repeat(auto-fill, minmax(120px, 1fr))","gap":16}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":35}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 1000, 'auto-fill-vs-fit-0221 container.width');
    expectClose(container.height, 35, 'auto-fill-vs-fit-0221 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'auto-fill-vs-fit-0221 item[0].x');
      expectClose(r.y - container.y, 0, 'auto-fill-vs-fit-0221 item[0].y');
      expectClose(r.width, 129.14, 'auto-fill-vs-fit-0221 item[0].w');
      expectClose(r.height, 30, 'auto-fill-vs-fit-0221 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 145.14, 'auto-fill-vs-fit-0221 item[1].x');
      expectClose(r.y - container.y, 0, 'auto-fill-vs-fit-0221 item[1].y');
      expectClose(r.width, 129.14, 'auto-fill-vs-fit-0221 item[1].w');
      expectClose(r.height, 35, 'auto-fill-vs-fit-0221 item[1].h');
    }
  });
  it("auto-fill-vs-fit-0223: auto-fill counterpart at width 1000 with 6 items", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid","width":1000,"gridTemplateColumns":"repeat(auto-fill, minmax(120px, 1fr))","gap":16}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":35}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":40}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":45}}
          />
          <View
            collapsable={false}
            ref={itemRefs[4]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":50}}
          />
          <View
            collapsable={false}
            ref={itemRefs[5]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":55}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 1000, 'auto-fill-vs-fit-0223 container.width');
    expectClose(container.height, 55, 'auto-fill-vs-fit-0223 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'auto-fill-vs-fit-0223 item[0].x');
      expectClose(r.y - container.y, 0, 'auto-fill-vs-fit-0223 item[0].y');
      expectClose(r.width, 129.14, 'auto-fill-vs-fit-0223 item[0].w');
      expectClose(r.height, 30, 'auto-fill-vs-fit-0223 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 145.14, 'auto-fill-vs-fit-0223 item[1].x');
      expectClose(r.y - container.y, 0, 'auto-fill-vs-fit-0223 item[1].y');
      expectClose(r.width, 129.14, 'auto-fill-vs-fit-0223 item[1].w');
      expectClose(r.height, 35, 'auto-fill-vs-fit-0223 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 290.28, 'auto-fill-vs-fit-0223 item[2].x');
      expectClose(r.y - container.y, 0, 'auto-fill-vs-fit-0223 item[2].y');
      expectClose(r.width, 129.14, 'auto-fill-vs-fit-0223 item[2].w');
      expectClose(r.height, 40, 'auto-fill-vs-fit-0223 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 435.42, 'auto-fill-vs-fit-0223 item[3].x');
      expectClose(r.y - container.y, 0, 'auto-fill-vs-fit-0223 item[3].y');
      expectClose(r.width, 129.14, 'auto-fill-vs-fit-0223 item[3].w');
      expectClose(r.height, 45, 'auto-fill-vs-fit-0223 item[3].h');
    }
    {
      const r = rectOf(itemRefs[4]);
      expectClose(r.x - container.x, 580.56, 'auto-fill-vs-fit-0223 item[4].x');
      expectClose(r.y - container.y, 0, 'auto-fill-vs-fit-0223 item[4].y');
      expectClose(r.width, 129.14, 'auto-fill-vs-fit-0223 item[4].w');
      expectClose(r.height, 50, 'auto-fill-vs-fit-0223 item[4].h');
    }
    {
      const r = rectOf(itemRefs[5]);
      expectClose(r.x - container.x, 725.7, 'auto-fill-vs-fit-0223 item[5].x');
      expectClose(r.y - container.y, 0, 'auto-fill-vs-fit-0223 item[5].y');
      expectClose(r.width, 129.14, 'auto-fill-vs-fit-0223 item[5].w');
      expectClose(r.height, 55, 'auto-fill-vs-fit-0223 item[5].h');
    }
  });
});

describe("repeat-integer", () => {
  it("repeat-integer-0224: repeat(3, ...) expands statically", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid","width":600,"gridTemplateColumns":"repeat(3, 1fr)","gap":10}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":40,"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":40,"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":40,"height":20}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'repeat-integer-0224 container.width');
    expectClose(container.height, 20, 'repeat-integer-0224 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'repeat-integer-0224 item[0].x');
      expectClose(r.y - container.y, 0, 'repeat-integer-0224 item[0].y');
      expectClose(r.width, 40, 'repeat-integer-0224 item[0].w');
      expectClose(r.height, 20, 'repeat-integer-0224 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 203.33, 'repeat-integer-0224 item[1].x');
      expectClose(r.y - container.y, 0, 'repeat-integer-0224 item[1].y');
      expectClose(r.width, 40, 'repeat-integer-0224 item[1].w');
      expectClose(r.height, 20, 'repeat-integer-0224 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 406.66, 'repeat-integer-0224 item[2].x');
      expectClose(r.y - container.y, 0, 'repeat-integer-0224 item[2].y');
      expectClose(r.width, 40, 'repeat-integer-0224 item[2].w');
      expectClose(r.height, 20, 'repeat-integer-0224 item[2].h');
    }
  });
  it("repeat-integer-0225: repeat(2, ...) expands statically", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid","width":600,"gridTemplateColumns":"repeat(2, 100px 1fr)","gap":10}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":40,"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":40,"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":40,"height":20}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'repeat-integer-0225 container.width');
    expectClose(container.height, 20, 'repeat-integer-0225 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'repeat-integer-0225 item[0].x');
      expectClose(r.y - container.y, 0, 'repeat-integer-0225 item[0].y');
      expectClose(r.width, 40, 'repeat-integer-0225 item[0].w');
      expectClose(r.height, 20, 'repeat-integer-0225 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 110, 'repeat-integer-0225 item[1].x');
      expectClose(r.y - container.y, 0, 'repeat-integer-0225 item[1].y');
      expectClose(r.width, 40, 'repeat-integer-0225 item[1].w');
      expectClose(r.height, 20, 'repeat-integer-0225 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 305, 'repeat-integer-0225 item[2].x');
      expectClose(r.y - container.y, 0, 'repeat-integer-0225 item[2].y');
      expectClose(r.width, 40, 'repeat-integer-0225 item[2].w');
      expectClose(r.height, 20, 'repeat-integer-0225 item[2].h');
    }
  });
  it("repeat-integer-0226: repeat(4, ...) expands statically", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid","width":600,"gridTemplateColumns":"repeat(4, 60px)","gap":10}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":40,"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":40,"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":40,"height":20}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'repeat-integer-0226 container.width');
    expectClose(container.height, 20, 'repeat-integer-0226 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'repeat-integer-0226 item[0].x');
      expectClose(r.y - container.y, 0, 'repeat-integer-0226 item[0].y');
      expectClose(r.width, 40, 'repeat-integer-0226 item[0].w');
      expectClose(r.height, 20, 'repeat-integer-0226 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 70, 'repeat-integer-0226 item[1].x');
      expectClose(r.y - container.y, 0, 'repeat-integer-0226 item[1].y');
      expectClose(r.width, 40, 'repeat-integer-0226 item[1].w');
      expectClose(r.height, 20, 'repeat-integer-0226 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 140, 'repeat-integer-0226 item[2].x');
      expectClose(r.y - container.y, 0, 'repeat-integer-0226 item[2].y');
      expectClose(r.width, 40, 'repeat-integer-0226 item[2].w');
      expectClose(r.height, 20, 'repeat-integer-0226 item[2].h');
    }
  });
});

describe("auto-fill-with-fixed-sides", () => {
  it("auto-fill-with-fixed-sides-0227: fixed 80px sides around an auto-fill run at width 500", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid","width":500,"gridTemplateColumns":"80px repeat(auto-fill, minmax(160px, 1fr)) 80px","gap":16}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":40}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":50}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 500, 'auto-fill-with-fixed-sides-0227 container.width');
    expectClose(container.height, 106, 'auto-fill-with-fixed-sides-0227 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'auto-fill-with-fixed-sides-0227 item[0].x');
      expectClose(r.y - container.y, 0, 'auto-fill-with-fixed-sides-0227 item[0].y');
      expectClose(r.width, 80, 'auto-fill-with-fixed-sides-0227 item[0].w');
      expectClose(r.height, 30, 'auto-fill-with-fixed-sides-0227 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 96, 'auto-fill-with-fixed-sides-0227 item[1].x');
      expectClose(r.y - container.y, 0, 'auto-fill-with-fixed-sides-0227 item[1].y');
      expectClose(r.width, 308, 'auto-fill-with-fixed-sides-0227 item[1].w');
      expectClose(r.height, 40, 'auto-fill-with-fixed-sides-0227 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 420, 'auto-fill-with-fixed-sides-0227 item[2].x');
      expectClose(r.y - container.y, 0, 'auto-fill-with-fixed-sides-0227 item[2].y');
      expectClose(r.width, 80, 'auto-fill-with-fixed-sides-0227 item[2].w');
      expectClose(r.height, 20, 'auto-fill-with-fixed-sides-0227 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 0, 'auto-fill-with-fixed-sides-0227 item[3].x');
      expectClose(r.y - container.y, 56, 'auto-fill-with-fixed-sides-0227 item[3].y');
      expectClose(r.width, 80, 'auto-fill-with-fixed-sides-0227 item[3].w');
      expectClose(r.height, 50, 'auto-fill-with-fixed-sides-0227 item[3].h');
    }
  });
  it("auto-fill-with-fixed-sides-0228: fixed 80px sides around an auto-fill run at width 800", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid","width":800,"gridTemplateColumns":"80px repeat(auto-fill, minmax(160px, 1fr)) 80px","gap":16}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":40}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":50}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 800, 'auto-fill-with-fixed-sides-0228 container.width');
    expectClose(container.height, 50, 'auto-fill-with-fixed-sides-0228 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'auto-fill-with-fixed-sides-0228 item[0].x');
      expectClose(r.y - container.y, 0, 'auto-fill-with-fixed-sides-0228 item[0].y');
      expectClose(r.width, 80, 'auto-fill-with-fixed-sides-0228 item[0].w');
      expectClose(r.height, 30, 'auto-fill-with-fixed-sides-0228 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 96, 'auto-fill-with-fixed-sides-0228 item[1].x');
      expectClose(r.y - container.y, 0, 'auto-fill-with-fixed-sides-0228 item[1].y');
      expectClose(r.width, 192, 'auto-fill-with-fixed-sides-0228 item[1].w');
      expectClose(r.height, 40, 'auto-fill-with-fixed-sides-0228 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 304, 'auto-fill-with-fixed-sides-0228 item[2].x');
      expectClose(r.y - container.y, 0, 'auto-fill-with-fixed-sides-0228 item[2].y');
      expectClose(r.width, 192, 'auto-fill-with-fixed-sides-0228 item[2].w');
      expectClose(r.height, 20, 'auto-fill-with-fixed-sides-0228 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 512, 'auto-fill-with-fixed-sides-0228 item[3].x');
      expectClose(r.y - container.y, 0, 'auto-fill-with-fixed-sides-0228 item[3].y');
      expectClose(r.width, 192, 'auto-fill-with-fixed-sides-0228 item[3].w');
      expectClose(r.height, 50, 'auto-fill-with-fixed-sides-0228 item[3].h');
    }
  });
  it("auto-fill-with-fixed-sides-0229: fixed 80px sides around an auto-fill run at width 1100", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid","width":1100,"gridTemplateColumns":"80px repeat(auto-fill, minmax(160px, 1fr)) 80px","gap":16}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":40}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":50}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 1100, 'auto-fill-with-fixed-sides-0229 container.width');
    expectClose(container.height, 50, 'auto-fill-with-fixed-sides-0229 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'auto-fill-with-fixed-sides-0229 item[0].x');
      expectClose(r.y - container.y, 0, 'auto-fill-with-fixed-sides-0229 item[0].y');
      expectClose(r.width, 80, 'auto-fill-with-fixed-sides-0229 item[0].w');
      expectClose(r.height, 30, 'auto-fill-with-fixed-sides-0229 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 96, 'auto-fill-with-fixed-sides-0229 item[1].x');
      expectClose(r.y - container.y, 0, 'auto-fill-with-fixed-sides-0229 item[1].y');
      expectClose(r.width, 168.8, 'auto-fill-with-fixed-sides-0229 item[1].w');
      expectClose(r.height, 40, 'auto-fill-with-fixed-sides-0229 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 280.8, 'auto-fill-with-fixed-sides-0229 item[2].x');
      expectClose(r.y - container.y, 0, 'auto-fill-with-fixed-sides-0229 item[2].y');
      expectClose(r.width, 168.8, 'auto-fill-with-fixed-sides-0229 item[2].w');
      expectClose(r.height, 20, 'auto-fill-with-fixed-sides-0229 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 465.59, 'auto-fill-with-fixed-sides-0229 item[3].x');
      expectClose(r.y - container.y, 0, 'auto-fill-with-fixed-sides-0229 item[3].y');
      expectClose(r.width, 168.8, 'auto-fill-with-fixed-sides-0229 item[3].w');
      expectClose(r.height, 50, 'auto-fill-with-fixed-sides-0229 item[3].h');
    }
  });
});

describe("auto-fill-multi-track", () => {
  it("auto-fill-multi-track-0230: two-track auto-fill pattern at width 600", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid","width":600,"gridTemplateColumns":"repeat(auto-fill, minmax(90px, 1fr) minmax(150px, 2fr))","gap":16}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":40}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":50}}
          />
          <View
            collapsable={false}
            ref={itemRefs[4]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":25}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'auto-fill-multi-track-0230 container.width');
    expectClose(container.height, 91, 'auto-fill-multi-track-0230 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'auto-fill-multi-track-0230 item[0].x');
      expectClose(r.y - container.y, 0, 'auto-fill-multi-track-0230 item[0].y');
      expectClose(r.width, 92, 'auto-fill-multi-track-0230 item[0].w');
      expectClose(r.height, 30, 'auto-fill-multi-track-0230 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 108, 'auto-fill-multi-track-0230 item[1].x');
      expectClose(r.y - container.y, 0, 'auto-fill-multi-track-0230 item[1].y');
      expectClose(r.width, 184, 'auto-fill-multi-track-0230 item[1].w');
      expectClose(r.height, 40, 'auto-fill-multi-track-0230 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 308, 'auto-fill-multi-track-0230 item[2].x');
      expectClose(r.y - container.y, 0, 'auto-fill-multi-track-0230 item[2].y');
      expectClose(r.width, 92, 'auto-fill-multi-track-0230 item[2].w');
      expectClose(r.height, 20, 'auto-fill-multi-track-0230 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 416, 'auto-fill-multi-track-0230 item[3].x');
      expectClose(r.y - container.y, 0, 'auto-fill-multi-track-0230 item[3].y');
      expectClose(r.width, 184, 'auto-fill-multi-track-0230 item[3].w');
      expectClose(r.height, 50, 'auto-fill-multi-track-0230 item[3].h');
    }
    {
      const r = rectOf(itemRefs[4]);
      expectClose(r.x - container.x, 0, 'auto-fill-multi-track-0230 item[4].x');
      expectClose(r.y - container.y, 66, 'auto-fill-multi-track-0230 item[4].y');
      expectClose(r.width, 92, 'auto-fill-multi-track-0230 item[4].w');
      expectClose(r.height, 25, 'auto-fill-multi-track-0230 item[4].h');
    }
  });
  it("auto-fill-multi-track-0231: two-track auto-fill pattern at width 900", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid","width":900,"gridTemplateColumns":"repeat(auto-fill, minmax(90px, 1fr) minmax(150px, 2fr))","gap":16}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":40}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":50}}
          />
          <View
            collapsable={false}
            ref={itemRefs[4]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":25}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 900, 'auto-fill-multi-track-0231 container.width');
    expectClose(container.height, 50, 'auto-fill-multi-track-0231 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'auto-fill-multi-track-0231 item[0].x');
      expectClose(r.y - container.y, 0, 'auto-fill-multi-track-0231 item[0].y');
      expectClose(r.width, 91.11, 'auto-fill-multi-track-0231 item[0].w');
      expectClose(r.height, 30, 'auto-fill-multi-track-0231 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 107.11, 'auto-fill-multi-track-0231 item[1].x');
      expectClose(r.y - container.y, 0, 'auto-fill-multi-track-0231 item[1].y');
      expectClose(r.width, 182.22, 'auto-fill-multi-track-0231 item[1].w');
      expectClose(r.height, 40, 'auto-fill-multi-track-0231 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 305.33, 'auto-fill-multi-track-0231 item[2].x');
      expectClose(r.y - container.y, 0, 'auto-fill-multi-track-0231 item[2].y');
      expectClose(r.width, 91.11, 'auto-fill-multi-track-0231 item[2].w');
      expectClose(r.height, 20, 'auto-fill-multi-track-0231 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 412.44, 'auto-fill-multi-track-0231 item[3].x');
      expectClose(r.y - container.y, 0, 'auto-fill-multi-track-0231 item[3].y');
      expectClose(r.width, 182.22, 'auto-fill-multi-track-0231 item[3].w');
      expectClose(r.height, 50, 'auto-fill-multi-track-0231 item[3].h');
    }
    {
      const r = rectOf(itemRefs[4]);
      expectClose(r.x - container.x, 610.66, 'auto-fill-multi-track-0231 item[4].x');
      expectClose(r.y - container.y, 0, 'auto-fill-multi-track-0231 item[4].y');
      expectClose(r.width, 91.11, 'auto-fill-multi-track-0231 item[4].w');
      expectClose(r.height, 25, 'auto-fill-multi-track-0231 item[4].h');
    }
  });
});

describe("auto-fill-overflow", () => {
  it("auto-fill-overflow-0232: a pattern wider than the container still repeats once", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid","width":300,"gridTemplateColumns":"repeat(auto-fill, minmax(400px, 1fr))","gap":10}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":40}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 300, 'auto-fill-overflow-0232 container.width');
    expectClose(container.height, 80, 'auto-fill-overflow-0232 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'auto-fill-overflow-0232 item[0].x');
      expectClose(r.y - container.y, 0, 'auto-fill-overflow-0232 item[0].y');
      expectClose(r.width, 400, 'auto-fill-overflow-0232 item[0].w');
      expectClose(r.height, 30, 'auto-fill-overflow-0232 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 0, 'auto-fill-overflow-0232 item[1].x');
      expectClose(r.y - container.y, 40, 'auto-fill-overflow-0232 item[1].y');
      expectClose(r.width, 400, 'auto-fill-overflow-0232 item[1].w');
      expectClose(r.height, 40, 'auto-fill-overflow-0232 item[1].h');
    }
  });
});

describe("auto-fill-fixed", () => {
  it("auto-fill-fixed-0233: repeat(auto-fill, 100px) at width 300 with gap 10", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid","width":300,"gridTemplateColumns":"repeat(auto-fill, 100px)","gap":10}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":25}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 300, 'auto-fill-fixed-0233 container.width');
    expectClose(container.height, 65, 'auto-fill-fixed-0233 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'auto-fill-fixed-0233 item[0].x');
      expectClose(r.y - container.y, 0, 'auto-fill-fixed-0233 item[0].y');
      expectClose(r.width, 100, 'auto-fill-fixed-0233 item[0].w');
      expectClose(r.height, 20, 'auto-fill-fixed-0233 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 110, 'auto-fill-fixed-0233 item[1].x');
      expectClose(r.y - container.y, 0, 'auto-fill-fixed-0233 item[1].y');
      expectClose(r.width, 100, 'auto-fill-fixed-0233 item[1].w');
      expectClose(r.height, 30, 'auto-fill-fixed-0233 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 0, 'auto-fill-fixed-0233 item[2].x');
      expectClose(r.y - container.y, 40, 'auto-fill-fixed-0233 item[2].y');
      expectClose(r.width, 100, 'auto-fill-fixed-0233 item[2].w');
      expectClose(r.height, 25, 'auto-fill-fixed-0233 item[2].h');
    }
  });
  it("auto-fill-fixed-0234: repeat(auto-fill, 100px) at width 600 with gap 10", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid","width":600,"gridTemplateColumns":"repeat(auto-fill, 100px)","gap":10}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":25}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'auto-fill-fixed-0234 container.width');
    expectClose(container.height, 30, 'auto-fill-fixed-0234 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'auto-fill-fixed-0234 item[0].x');
      expectClose(r.y - container.y, 0, 'auto-fill-fixed-0234 item[0].y');
      expectClose(r.width, 100, 'auto-fill-fixed-0234 item[0].w');
      expectClose(r.height, 20, 'auto-fill-fixed-0234 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 110, 'auto-fill-fixed-0234 item[1].x');
      expectClose(r.y - container.y, 0, 'auto-fill-fixed-0234 item[1].y');
      expectClose(r.width, 100, 'auto-fill-fixed-0234 item[1].w');
      expectClose(r.height, 30, 'auto-fill-fixed-0234 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 220, 'auto-fill-fixed-0234 item[2].x');
      expectClose(r.y - container.y, 0, 'auto-fill-fixed-0234 item[2].y');
      expectClose(r.width, 100, 'auto-fill-fixed-0234 item[2].w');
      expectClose(r.height, 25, 'auto-fill-fixed-0234 item[2].h');
    }
  });
  it("auto-fill-fixed-0235: repeat(auto-fill, 100px) at width 630 with gap 10", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid","width":630,"gridTemplateColumns":"repeat(auto-fill, 100px)","gap":10}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":25}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 630, 'auto-fill-fixed-0235 container.width');
    expectClose(container.height, 30, 'auto-fill-fixed-0235 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'auto-fill-fixed-0235 item[0].x');
      expectClose(r.y - container.y, 0, 'auto-fill-fixed-0235 item[0].y');
      expectClose(r.width, 100, 'auto-fill-fixed-0235 item[0].w');
      expectClose(r.height, 20, 'auto-fill-fixed-0235 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 110, 'auto-fill-fixed-0235 item[1].x');
      expectClose(r.y - container.y, 0, 'auto-fill-fixed-0235 item[1].y');
      expectClose(r.width, 100, 'auto-fill-fixed-0235 item[1].w');
      expectClose(r.height, 30, 'auto-fill-fixed-0235 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 220, 'auto-fill-fixed-0235 item[2].x');
      expectClose(r.y - container.y, 0, 'auto-fill-fixed-0235 item[2].y');
      expectClose(r.width, 100, 'auto-fill-fixed-0235 item[2].w');
      expectClose(r.height, 25, 'auto-fill-fixed-0235 item[2].h');
    }
  });
});

describe("auto-fit-x-explicit-line", () => {
  it("auto-fit-x-explicit-line-0236: auto-fit with an item pinned to line 3 at width 500", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid","width":500,"gridTemplateColumns":"repeat(auto-fit, minmax(120px, 1fr))","gap":16}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30,"gridColumnStart":3}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":40}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 500, 'auto-fit-x-explicit-line-0236 container.width');
    expectClose(container.height, 86, 'auto-fit-x-explicit-line-0236 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 258, 'auto-fit-x-explicit-line-0236 item[0].x');
      expectClose(r.y - container.y, 0, 'auto-fit-x-explicit-line-0236 item[0].y');
      expectClose(r.width, 242, 'auto-fit-x-explicit-line-0236 item[0].w');
      expectClose(r.height, 30, 'auto-fit-x-explicit-line-0236 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 0, 'auto-fit-x-explicit-line-0236 item[1].x');
      expectClose(r.y - container.y, 46, 'auto-fit-x-explicit-line-0236 item[1].y');
      expectClose(r.width, 242, 'auto-fit-x-explicit-line-0236 item[1].w');
      expectClose(r.height, 40, 'auto-fit-x-explicit-line-0236 item[1].h');
    }
  });
  it("auto-fit-x-explicit-line-0239: auto-fit with an item pinned to line 3 at width 800", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid","width":800,"gridTemplateColumns":"repeat(auto-fit, minmax(120px, 1fr))","gap":16}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30,"gridColumnStart":3}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":40}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 800, 'auto-fit-x-explicit-line-0239 container.width');
    expectClose(container.height, 40, 'auto-fit-x-explicit-line-0239 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'auto-fit-x-explicit-line-0239 item[0].x');
      expectClose(r.y - container.y, 0, 'auto-fit-x-explicit-line-0239 item[0].y');
      expectClose(r.width, 392, 'auto-fit-x-explicit-line-0239 item[0].w');
      expectClose(r.height, 30, 'auto-fit-x-explicit-line-0239 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 408, 'auto-fit-x-explicit-line-0239 item[1].x');
      expectClose(r.y - container.y, 0, 'auto-fit-x-explicit-line-0239 item[1].y');
      expectClose(r.width, 392, 'auto-fit-x-explicit-line-0239 item[1].w');
      expectClose(r.height, 40, 'auto-fit-x-explicit-line-0239 item[1].h');
    }
  });
});

describe("auto-fit-x-full-bleed", () => {
  it("auto-fit-x-full-bleed-0237: auto-fit with a spanning item at width 500", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid","width":500,"gridTemplateColumns":"repeat(auto-fit, minmax(120px, 1fr))","gap":16}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30,"gridColumnEnd":"span 2"}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":40}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 500, 'auto-fit-x-full-bleed-0237 container.width');
    expectClose(container.height, 40, 'auto-fit-x-full-bleed-0237 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'auto-fit-x-full-bleed-0237 item[0].x');
      expectClose(r.y - container.y, 0, 'auto-fit-x-full-bleed-0237 item[0].y');
      expectClose(r.width, 328, 'auto-fit-x-full-bleed-0237 item[0].w');
      expectClose(r.height, 30, 'auto-fit-x-full-bleed-0237 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 344, 'auto-fit-x-full-bleed-0237 item[1].x');
      expectClose(r.y - container.y, 0, 'auto-fit-x-full-bleed-0237 item[1].y');
      expectClose(r.width, 156, 'auto-fit-x-full-bleed-0237 item[1].w');
      expectClose(r.height, 40, 'auto-fit-x-full-bleed-0237 item[1].h');
    }
  });
  it("auto-fit-x-full-bleed-0240: auto-fit with a spanning item at width 800", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid","width":800,"gridTemplateColumns":"repeat(auto-fit, minmax(120px, 1fr))","gap":16}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30,"gridColumnEnd":"span 2"}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":40}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 800, 'auto-fit-x-full-bleed-0240 container.width');
    expectClose(container.height, 40, 'auto-fit-x-full-bleed-0240 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'auto-fit-x-full-bleed-0240 item[0].x');
      expectClose(r.y - container.y, 0, 'auto-fit-x-full-bleed-0240 item[0].y');
      expectClose(r.width, 528, 'auto-fit-x-full-bleed-0240 item[0].w');
      expectClose(r.height, 30, 'auto-fit-x-full-bleed-0240 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 544, 'auto-fit-x-full-bleed-0240 item[1].x');
      expectClose(r.y - container.y, 0, 'auto-fit-x-full-bleed-0240 item[1].y');
      expectClose(r.width, 256, 'auto-fit-x-full-bleed-0240 item[1].w');
      expectClose(r.height, 40, 'auto-fit-x-full-bleed-0240 item[1].h');
    }
  });
});

describe("auto-fit-x-trailing-hole", () => {
  it("auto-fit-x-trailing-hole-0238: auto-fit with an interior hole at width 500", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid","width":500,"gridTemplateColumns":"repeat(auto-fit, minmax(120px, 1fr))","gap":16}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":40,"gridColumnStart":2}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 500, 'auto-fit-x-trailing-hole-0238 container.width');
    expectClose(container.height, 40, 'auto-fit-x-trailing-hole-0238 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'auto-fit-x-trailing-hole-0238 item[0].x');
      expectClose(r.y - container.y, 0, 'auto-fit-x-trailing-hole-0238 item[0].y');
      expectClose(r.width, 242, 'auto-fit-x-trailing-hole-0238 item[0].w');
      expectClose(r.height, 30, 'auto-fit-x-trailing-hole-0238 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 258, 'auto-fit-x-trailing-hole-0238 item[1].x');
      expectClose(r.y - container.y, 0, 'auto-fit-x-trailing-hole-0238 item[1].y');
      expectClose(r.width, 242, 'auto-fit-x-trailing-hole-0238 item[1].w');
      expectClose(r.height, 40, 'auto-fit-x-trailing-hole-0238 item[1].h');
    }
  });
  it("auto-fit-x-trailing-hole-0241: auto-fit with an interior hole at width 800", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid","width":800,"gridTemplateColumns":"repeat(auto-fit, minmax(120px, 1fr))","gap":16}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":40,"gridColumnStart":2}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 800, 'auto-fit-x-trailing-hole-0241 container.width');
    expectClose(container.height, 40, 'auto-fit-x-trailing-hole-0241 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'auto-fit-x-trailing-hole-0241 item[0].x');
      expectClose(r.y - container.y, 0, 'auto-fit-x-trailing-hole-0241 item[0].y');
      expectClose(r.width, 392, 'auto-fit-x-trailing-hole-0241 item[0].w');
      expectClose(r.height, 30, 'auto-fit-x-trailing-hole-0241 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 408, 'auto-fit-x-trailing-hole-0241 item[1].x');
      expectClose(r.y - container.y, 0, 'auto-fit-x-trailing-hole-0241 item[1].y');
      expectClose(r.width, 392, 'auto-fit-x-trailing-hole-0241 item[1].w');
      expectClose(r.height, 40, 'auto-fit-x-trailing-hole-0241 item[1].h');
    }
  });
});

describe("intrinsic-max-content", () => {
  it("intrinsic-max-content-0243: max-content track sizing", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid","width":600,"gridTemplateColumns":"max-content 1fr","gap":10}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":90,"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":40,"height":20}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'intrinsic-max-content-0243 container.width');
    expectClose(container.height, 20, 'intrinsic-max-content-0243 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'intrinsic-max-content-0243 item[0].x');
      expectClose(r.y - container.y, 0, 'intrinsic-max-content-0243 item[0].y');
      expectClose(r.width, 90, 'intrinsic-max-content-0243 item[0].w');
      expectClose(r.height, 20, 'intrinsic-max-content-0243 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 100, 'intrinsic-max-content-0243 item[1].x');
      expectClose(r.y - container.y, 0, 'intrinsic-max-content-0243 item[1].y');
      expectClose(r.width, 40, 'intrinsic-max-content-0243 item[1].w');
      expectClose(r.height, 20, 'intrinsic-max-content-0243 item[1].h');
    }
  });
});

describe("intrinsic-fit-content", () => {
  it("intrinsic-fit-content-0244: fit-content track sizing", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid","width":600,"gridTemplateColumns":"fit-content(140px) 1fr","gap":10}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":90,"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":40,"height":20}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'intrinsic-fit-content-0244 container.width');
    expectClose(container.height, 20, 'intrinsic-fit-content-0244 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'intrinsic-fit-content-0244 item[0].x');
      expectClose(r.y - container.y, 0, 'intrinsic-fit-content-0244 item[0].y');
      expectClose(r.width, 90, 'intrinsic-fit-content-0244 item[0].w');
      expectClose(r.height, 20, 'intrinsic-fit-content-0244 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 100, 'intrinsic-fit-content-0244 item[1].x');
      expectClose(r.y - container.y, 0, 'intrinsic-fit-content-0244 item[1].y');
      expectClose(r.width, 40, 'intrinsic-fit-content-0244 item[1].w');
      expectClose(r.height, 20, 'intrinsic-fit-content-0244 item[1].h');
    }
  });
});

describe("implicit-rows", () => {
  it("implicit-rows-0245: implicit rows sized by content", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid","width":600,"gridTemplateColumns":"100px 100px","gridTemplateRows":"40px","gap":10}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":30,"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":30,"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":30,"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":30,"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[4]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":30,"height":20}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'implicit-rows-0245 container.width');
    expectClose(container.height, 100, 'implicit-rows-0245 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'implicit-rows-0245 item[0].x');
      expectClose(r.y - container.y, 0, 'implicit-rows-0245 item[0].y');
      expectClose(r.width, 30, 'implicit-rows-0245 item[0].w');
      expectClose(r.height, 20, 'implicit-rows-0245 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 110, 'implicit-rows-0245 item[1].x');
      expectClose(r.y - container.y, 0, 'implicit-rows-0245 item[1].y');
      expectClose(r.width, 30, 'implicit-rows-0245 item[1].w');
      expectClose(r.height, 20, 'implicit-rows-0245 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 0, 'implicit-rows-0245 item[2].x');
      expectClose(r.y - container.y, 50, 'implicit-rows-0245 item[2].y');
      expectClose(r.width, 30, 'implicit-rows-0245 item[2].w');
      expectClose(r.height, 20, 'implicit-rows-0245 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 110, 'implicit-rows-0245 item[3].x');
      expectClose(r.y - container.y, 50, 'implicit-rows-0245 item[3].y');
      expectClose(r.width, 30, 'implicit-rows-0245 item[3].w');
      expectClose(r.height, 20, 'implicit-rows-0245 item[3].h');
    }
    {
      const r = rectOf(itemRefs[4]);
      expectClose(r.x - container.x, 0, 'implicit-rows-0245 item[4].x');
      expectClose(r.y - container.y, 80, 'implicit-rows-0245 item[4].y');
      expectClose(r.width, 30, 'implicit-rows-0245 item[4].w');
      expectClose(r.height, 20, 'implicit-rows-0245 item[4].h');
    }
  });
  it("implicit-rows-0246: implicit rows sized by grid-auto-rows", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid","width":600,"gridTemplateColumns":"100px 100px","gridTemplateRows":"40px","gap":10,"gridAutoRows":"50px"}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":30,"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":30,"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":30,"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":30,"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[4]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":30,"height":20}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'implicit-rows-0246 container.width');
    expectClose(container.height, 160, 'implicit-rows-0246 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'implicit-rows-0246 item[0].x');
      expectClose(r.y - container.y, 0, 'implicit-rows-0246 item[0].y');
      expectClose(r.width, 30, 'implicit-rows-0246 item[0].w');
      expectClose(r.height, 20, 'implicit-rows-0246 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 110, 'implicit-rows-0246 item[1].x');
      expectClose(r.y - container.y, 0, 'implicit-rows-0246 item[1].y');
      expectClose(r.width, 30, 'implicit-rows-0246 item[1].w');
      expectClose(r.height, 20, 'implicit-rows-0246 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 0, 'implicit-rows-0246 item[2].x');
      expectClose(r.y - container.y, 50, 'implicit-rows-0246 item[2].y');
      expectClose(r.width, 30, 'implicit-rows-0246 item[2].w');
      expectClose(r.height, 20, 'implicit-rows-0246 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 110, 'implicit-rows-0246 item[3].x');
      expectClose(r.y - container.y, 50, 'implicit-rows-0246 item[3].y');
      expectClose(r.width, 30, 'implicit-rows-0246 item[3].w');
      expectClose(r.height, 20, 'implicit-rows-0246 item[3].h');
    }
    {
      const r = rectOf(itemRefs[4]);
      expectClose(r.x - container.x, 0, 'implicit-rows-0246 item[4].x');
      expectClose(r.y - container.y, 110, 'implicit-rows-0246 item[4].y');
      expectClose(r.width, 30, 'implicit-rows-0246 item[4].w');
      expectClose(r.height, 20, 'implicit-rows-0246 item[4].h');
    }
  });
  it("implicit-rows-0247: implicit rows sized by grid-auto-rows", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid","width":600,"gridTemplateColumns":"100px 100px","gridTemplateRows":"40px","gap":10,"gridAutoRows":"30px 60px"}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":30,"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":30,"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":30,"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":30,"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[4]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":30,"height":20}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'implicit-rows-0247 container.width');
    expectClose(container.height, 150, 'implicit-rows-0247 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'implicit-rows-0247 item[0].x');
      expectClose(r.y - container.y, 0, 'implicit-rows-0247 item[0].y');
      expectClose(r.width, 30, 'implicit-rows-0247 item[0].w');
      expectClose(r.height, 20, 'implicit-rows-0247 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 110, 'implicit-rows-0247 item[1].x');
      expectClose(r.y - container.y, 0, 'implicit-rows-0247 item[1].y');
      expectClose(r.width, 30, 'implicit-rows-0247 item[1].w');
      expectClose(r.height, 20, 'implicit-rows-0247 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 0, 'implicit-rows-0247 item[2].x');
      expectClose(r.y - container.y, 50, 'implicit-rows-0247 item[2].y');
      expectClose(r.width, 30, 'implicit-rows-0247 item[2].w');
      expectClose(r.height, 20, 'implicit-rows-0247 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 110, 'implicit-rows-0247 item[3].x');
      expectClose(r.y - container.y, 50, 'implicit-rows-0247 item[3].y');
      expectClose(r.width, 30, 'implicit-rows-0247 item[3].w');
      expectClose(r.height, 20, 'implicit-rows-0247 item[3].h');
    }
    {
      const r = rectOf(itemRefs[4]);
      expectClose(r.x - container.x, 0, 'implicit-rows-0247 item[4].x');
      expectClose(r.y - container.y, 90, 'implicit-rows-0247 item[4].y');
      expectClose(r.width, 30, 'implicit-rows-0247 item[4].w');
      expectClose(r.height, 20, 'implicit-rows-0247 item[4].h');
    }
  });
});

describe("percentage-gap", () => {
  it("percentage-gap-0248: percentage gap 2%", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid","width":600,"gridTemplateColumns":"1fr 1fr 1fr","gap":"2%"}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":25}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'percentage-gap-0248 container.width');
    expectClose(container.height, 30, 'percentage-gap-0248 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'percentage-gap-0248 item[0].x');
      expectClose(r.y - container.y, 0, 'percentage-gap-0248 item[0].y');
      expectClose(r.width, 192, 'percentage-gap-0248 item[0].w');
      expectClose(r.height, 20, 'percentage-gap-0248 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 204, 'percentage-gap-0248 item[1].x');
      expectClose(r.y - container.y, 0, 'percentage-gap-0248 item[1].y');
      expectClose(r.width, 192, 'percentage-gap-0248 item[1].w');
      expectClose(r.height, 30, 'percentage-gap-0248 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 408, 'percentage-gap-0248 item[2].x');
      expectClose(r.y - container.y, 0, 'percentage-gap-0248 item[2].y');
      expectClose(r.width, 192, 'percentage-gap-0248 item[2].w');
      expectClose(r.height, 25, 'percentage-gap-0248 item[2].h');
    }
  });
  it("percentage-gap-0249: percentage gap 5%", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid","width":600,"gridTemplateColumns":"1fr 1fr 1fr","gap":"5%"}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":25}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'percentage-gap-0249 container.width');
    expectClose(container.height, 30, 'percentage-gap-0249 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'percentage-gap-0249 item[0].x');
      expectClose(r.y - container.y, 0, 'percentage-gap-0249 item[0].y');
      expectClose(r.width, 180, 'percentage-gap-0249 item[0].w');
      expectClose(r.height, 20, 'percentage-gap-0249 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 210, 'percentage-gap-0249 item[1].x');
      expectClose(r.y - container.y, 0, 'percentage-gap-0249 item[1].y');
      expectClose(r.width, 180, 'percentage-gap-0249 item[1].w');
      expectClose(r.height, 30, 'percentage-gap-0249 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 420, 'percentage-gap-0249 item[2].x');
      expectClose(r.y - container.y, 0, 'percentage-gap-0249 item[2].y');
      expectClose(r.width, 180, 'percentage-gap-0249 item[2].w');
      expectClose(r.height, 25, 'percentage-gap-0249 item[2].h');
    }
  });
  it("percentage-gap-0250: percentage gap 10%", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid","width":600,"gridTemplateColumns":"1fr 1fr 1fr","gap":"10%"}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":25}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'percentage-gap-0250 container.width');
    expectClose(container.height, 30, 'percentage-gap-0250 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'percentage-gap-0250 item[0].x');
      expectClose(r.y - container.y, 0, 'percentage-gap-0250 item[0].y');
      expectClose(r.width, 160, 'percentage-gap-0250 item[0].w');
      expectClose(r.height, 20, 'percentage-gap-0250 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 220, 'percentage-gap-0250 item[1].x');
      expectClose(r.y - container.y, 0, 'percentage-gap-0250 item[1].y');
      expectClose(r.width, 160, 'percentage-gap-0250 item[1].w');
      expectClose(r.height, 30, 'percentage-gap-0250 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 440, 'percentage-gap-0250 item[2].x');
      expectClose(r.y - container.y, 0, 'percentage-gap-0250 item[2].y');
      expectClose(r.width, 160, 'percentage-gap-0250 item[2].w');
      expectClose(r.height, 25, 'percentage-gap-0250 item[2].h');
    }
  });
});

describe("container-min-max", () => {
  it("container-min-max-0251: container min-width - max-width 300", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid","gridTemplateColumns":"auto auto","gap":10,"maxWidth":300}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":150,"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":150,"height":30}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 300, 'container-min-max-0251 container.width');
    expectClose(container.height, 30, 'container-min-max-0251 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'container-min-max-0251 item[0].x');
      expectClose(r.y - container.y, 0, 'container-min-max-0251 item[0].y');
      expectClose(r.width, 150, 'container-min-max-0251 item[0].w');
      expectClose(r.height, 20, 'container-min-max-0251 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 160, 'container-min-max-0251 item[1].x');
      expectClose(r.y - container.y, 0, 'container-min-max-0251 item[1].y');
      expectClose(r.width, 150, 'container-min-max-0251 item[1].w');
      expectClose(r.height, 30, 'container-min-max-0251 item[1].h');
    }
  });
  it("container-min-max-0252: container min-width 800 max-width -", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid","gridTemplateColumns":"auto auto","gap":10,"minWidth":800}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":150,"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":150,"height":30}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 900, 'container-min-max-0252 container.width');
    expectClose(container.height, 30, 'container-min-max-0252 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'container-min-max-0252 item[0].x');
      expectClose(r.y - container.y, 0, 'container-min-max-0252 item[0].y');
      expectClose(r.width, 150, 'container-min-max-0252 item[0].w');
      expectClose(r.height, 20, 'container-min-max-0252 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 455, 'container-min-max-0252 item[1].x');
      expectClose(r.y - container.y, 0, 'container-min-max-0252 item[1].y');
      expectClose(r.width, 150, 'container-min-max-0252 item[1].w');
      expectClose(r.height, 30, 'container-min-max-0252 item[1].h');
    }
  });
  it("container-min-max-0253: container min-width 200 max-width 400", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid","gridTemplateColumns":"auto auto","gap":10,"minWidth":200,"maxWidth":400}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":150,"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":150,"height":30}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 400, 'container-min-max-0253 container.width');
    expectClose(container.height, 30, 'container-min-max-0253 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'container-min-max-0253 item[0].x');
      expectClose(r.y - container.y, 0, 'container-min-max-0253 item[0].y');
      expectClose(r.width, 150, 'container-min-max-0253 item[0].w');
      expectClose(r.height, 20, 'container-min-max-0253 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 205, 'container-min-max-0253 item[1].x');
      expectClose(r.y - container.y, 0, 'container-min-max-0253 item[1].y');
      expectClose(r.width, 150, 'container-min-max-0253 item[1].w');
      expectClose(r.height, 30, 'container-min-max-0253 item[1].h');
    }
  });
});

describe("container-min-max-block", () => {
  it("container-min-max-block-0254: container min-height - max-height 40", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid","width":600,"gridTemplateColumns":"100px 100px","gap":10,"maxHeight":40}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":30,"height":60}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":30,"height":80}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'container-min-max-block-0254 container.width');
    expectClose(container.height, 40, 'container-min-max-block-0254 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'container-min-max-block-0254 item[0].x');
      expectClose(r.y - container.y, 0, 'container-min-max-block-0254 item[0].y');
      expectClose(r.width, 30, 'container-min-max-block-0254 item[0].w');
      expectClose(r.height, 60, 'container-min-max-block-0254 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 110, 'container-min-max-block-0254 item[1].x');
      expectClose(r.y - container.y, 0, 'container-min-max-block-0254 item[1].y');
      expectClose(r.width, 30, 'container-min-max-block-0254 item[1].w');
      expectClose(r.height, 80, 'container-min-max-block-0254 item[1].h');
    }
  });
  it("container-min-max-block-0255: container min-height 200 max-height -", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid","width":600,"gridTemplateColumns":"100px 100px","gap":10,"minHeight":200}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":30,"height":60}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":30,"height":80}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'container-min-max-block-0255 container.width');
    expectClose(container.height, 200, 'container-min-max-block-0255 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'container-min-max-block-0255 item[0].x');
      expectClose(r.y - container.y, 0, 'container-min-max-block-0255 item[0].y');
      expectClose(r.width, 30, 'container-min-max-block-0255 item[0].w');
      expectClose(r.height, 60, 'container-min-max-block-0255 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 110, 'container-min-max-block-0255 item[1].x');
      expectClose(r.y - container.y, 0, 'container-min-max-block-0255 item[1].y');
      expectClose(r.width, 30, 'container-min-max-block-0255 item[1].w');
      expectClose(r.height, 80, 'container-min-max-block-0255 item[1].h');
    }
  });
});

describe("span-across-intrinsic", () => {
  it("span-across-intrinsic-0256: a wide item spanning 2 auto tracks distributes its size", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid","gridTemplateColumns":"auto auto auto","gap":10}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":40,"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":40,"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":40,"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":300,"height":20,"gridColumnEnd":"span 2"}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 900, 'span-across-intrinsic-0256 container.width');
    expectClose(container.height, 50, 'span-across-intrinsic-0256 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'span-across-intrinsic-0256 item[0].x');
      expectClose(r.y - container.y, 0, 'span-across-intrinsic-0256 item[0].y');
      expectClose(r.width, 40, 'span-across-intrinsic-0256 item[0].w');
      expectClose(r.height, 20, 'span-across-intrinsic-0256 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 338.33, 'span-across-intrinsic-0256 item[1].x');
      expectClose(r.y - container.y, 0, 'span-across-intrinsic-0256 item[1].y');
      expectClose(r.width, 40, 'span-across-intrinsic-0256 item[1].w');
      expectClose(r.height, 20, 'span-across-intrinsic-0256 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 676.66, 'span-across-intrinsic-0256 item[2].x');
      expectClose(r.y - container.y, 0, 'span-across-intrinsic-0256 item[2].y');
      expectClose(r.width, 40, 'span-across-intrinsic-0256 item[2].w');
      expectClose(r.height, 20, 'span-across-intrinsic-0256 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 0, 'span-across-intrinsic-0256 item[3].x');
      expectClose(r.y - container.y, 30, 'span-across-intrinsic-0256 item[3].y');
      expectClose(r.width, 300, 'span-across-intrinsic-0256 item[3].w');
      expectClose(r.height, 20, 'span-across-intrinsic-0256 item[3].h');
    }
  });
  it("span-across-intrinsic-0258: a wide item spanning 3 auto tracks distributes its size", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid","gridTemplateColumns":"auto auto auto","gap":10}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":40,"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":40,"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":40,"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":300,"height":20,"gridColumnEnd":"span 3"}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 900, 'span-across-intrinsic-0258 container.width');
    expectClose(container.height, 50, 'span-across-intrinsic-0258 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'span-across-intrinsic-0258 item[0].x');
      expectClose(r.y - container.y, 0, 'span-across-intrinsic-0258 item[0].y');
      expectClose(r.width, 40, 'span-across-intrinsic-0258 item[0].w');
      expectClose(r.height, 20, 'span-across-intrinsic-0258 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 303.33, 'span-across-intrinsic-0258 item[1].x');
      expectClose(r.y - container.y, 0, 'span-across-intrinsic-0258 item[1].y');
      expectClose(r.width, 40, 'span-across-intrinsic-0258 item[1].w');
      expectClose(r.height, 20, 'span-across-intrinsic-0258 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 606.66, 'span-across-intrinsic-0258 item[2].x');
      expectClose(r.y - container.y, 0, 'span-across-intrinsic-0258 item[2].y');
      expectClose(r.width, 40, 'span-across-intrinsic-0258 item[2].w');
      expectClose(r.height, 20, 'span-across-intrinsic-0258 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 0, 'span-across-intrinsic-0258 item[3].x');
      expectClose(r.y - container.y, 30, 'span-across-intrinsic-0258 item[3].y');
      expectClose(r.width, 300, 'span-across-intrinsic-0258 item[3].w');
      expectClose(r.height, 20, 'span-across-intrinsic-0258 item[3].h');
    }
  });
});

describe("span-across-minmax", () => {
  it("span-across-minmax-0257: a wide item spanning 2 minmax(50px, auto) tracks", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid","gridTemplateColumns":"minmax(50px, auto) minmax(50px, auto) minmax(50px, auto)","gap":10}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":40,"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":300,"height":20,"gridColumnEnd":"span 2"}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 900, 'span-across-minmax-0257 container.width');
    expectClose(container.height, 20, 'span-across-minmax-0257 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'span-across-minmax-0257 item[0].x');
      expectClose(r.y - container.y, 0, 'span-across-minmax-0257 item[0].y');
      expectClose(r.width, 40, 'span-across-minmax-0257 item[0].w');
      expectClose(r.height, 20, 'span-across-minmax-0257 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 240, 'span-across-minmax-0257 item[1].x');
      expectClose(r.y - container.y, 0, 'span-across-minmax-0257 item[1].y');
      expectClose(r.width, 300, 'span-across-minmax-0257 item[1].w');
      expectClose(r.height, 20, 'span-across-minmax-0257 item[1].h');
    }
  });
  it("span-across-minmax-0259: a wide item spanning 3 minmax(50px, auto) tracks", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid","gridTemplateColumns":"minmax(50px, auto) minmax(50px, auto) minmax(50px, auto)","gap":10}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":40,"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":300,"height":20,"gridColumnEnd":"span 3"}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 900, 'span-across-minmax-0259 container.width');
    expectClose(container.height, 50, 'span-across-minmax-0259 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'span-across-minmax-0259 item[0].x');
      expectClose(r.y - container.y, 0, 'span-across-minmax-0259 item[0].y');
      expectClose(r.width, 40, 'span-across-minmax-0259 item[0].w');
      expectClose(r.height, 20, 'span-across-minmax-0259 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 0, 'span-across-minmax-0259 item[1].x');
      expectClose(r.y - container.y, 30, 'span-across-minmax-0259 item[1].y');
      expectClose(r.width, 300, 'span-across-minmax-0259 item[1].w');
      expectClose(r.height, 20, 'span-across-minmax-0259 item[1].h');
    }
  });
});

describe("item-overflows-track", () => {
  it("item-overflows-track-0260: an item wider than its fixed track overflows it", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid","width":600,"gridTemplateColumns":"80px 80px","gap":10}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":200,"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":30,"height":20}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'item-overflows-track-0260 container.width');
    expectClose(container.height, 20, 'item-overflows-track-0260 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'item-overflows-track-0260 item[0].x');
      expectClose(r.y - container.y, 0, 'item-overflows-track-0260 item[0].y');
      expectClose(r.width, 200, 'item-overflows-track-0260 item[0].w');
      expectClose(r.height, 20, 'item-overflows-track-0260 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 90, 'item-overflows-track-0260 item[1].x');
      expectClose(r.y - container.y, 0, 'item-overflows-track-0260 item[1].y');
      expectClose(r.width, 30, 'item-overflows-track-0260 item[1].w');
      expectClose(r.height, 20, 'item-overflows-track-0260 item[1].h');
    }
  });
});

describe("item-percentage-size", () => {
  it("item-percentage-size-0261: item width 50% of its track", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid","width":600,"gridTemplateColumns":"200px 200px","gap":10}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":20,"width":"50%"}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":30,"height":20}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'item-percentage-size-0261 container.width');
    expectClose(container.height, 20, 'item-percentage-size-0261 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'item-percentage-size-0261 item[0].x');
      expectClose(r.y - container.y, 0, 'item-percentage-size-0261 item[0].y');
      expectClose(r.width, 100, 'item-percentage-size-0261 item[0].w');
      expectClose(r.height, 20, 'item-percentage-size-0261 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 210, 'item-percentage-size-0261 item[1].x');
      expectClose(r.y - container.y, 0, 'item-percentage-size-0261 item[1].y');
      expectClose(r.width, 30, 'item-percentage-size-0261 item[1].w');
      expectClose(r.height, 20, 'item-percentage-size-0261 item[1].h');
    }
  });
  it("item-percentage-size-0262: item width 100% of its track", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid","width":600,"gridTemplateColumns":"200px 200px","gap":10}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":20,"width":"100%"}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":30,"height":20}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'item-percentage-size-0262 container.width');
    expectClose(container.height, 20, 'item-percentage-size-0262 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'item-percentage-size-0262 item[0].x');
      expectClose(r.y - container.y, 0, 'item-percentage-size-0262 item[0].y');
      expectClose(r.width, 200, 'item-percentage-size-0262 item[0].w');
      expectClose(r.height, 20, 'item-percentage-size-0262 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 210, 'item-percentage-size-0262 item[1].x');
      expectClose(r.y - container.y, 0, 'item-percentage-size-0262 item[1].y');
      expectClose(r.width, 30, 'item-percentage-size-0262 item[1].w');
      expectClose(r.height, 20, 'item-percentage-size-0262 item[1].h');
    }
  });
});

describe("nested-grid", () => {
  it("nested-grid-0263: baseline for the nested case", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid","width":600,"gridTemplateColumns":"1fr 2fr","gap":10}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":40}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":40}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'nested-grid-0263 container.width');
    expectClose(container.height, 40, 'nested-grid-0263 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'nested-grid-0263 item[0].x');
      expectClose(r.y - container.y, 0, 'nested-grid-0263 item[0].y');
      expectClose(r.width, 196.66, 'nested-grid-0263 item[0].w');
      expectClose(r.height, 40, 'nested-grid-0263 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 206.66, 'nested-grid-0263 item[1].x');
      expectClose(r.y - container.y, 0, 'nested-grid-0263 item[1].y');
      expectClose(r.width, 393.34, 'nested-grid-0263 item[1].w');
      expectClose(r.height, 40, 'nested-grid-0263 item[1].h');
    }
  });
});

describe("fr-rows-definite-height", () => {
  it("fr-rows-definite-height-0264: fr rows in a 200px-tall container", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid","width":600,"height":200,"gridTemplateColumns":"100px","gridTemplateRows":"1fr 2fr","gap":10}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":30}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":30}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'fr-rows-definite-height-0264 container.width');
    expectClose(container.height, 200, 'fr-rows-definite-height-0264 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'fr-rows-definite-height-0264 item[0].x');
      expectClose(r.y - container.y, 0, 'fr-rows-definite-height-0264 item[0].y');
      expectClose(r.width, 30, 'fr-rows-definite-height-0264 item[0].w');
      expectClose(r.height, 63.33, 'fr-rows-definite-height-0264 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 0, 'fr-rows-definite-height-0264 item[1].x');
      expectClose(r.y - container.y, 73.33, 'fr-rows-definite-height-0264 item[1].y');
      expectClose(r.width, 30, 'fr-rows-definite-height-0264 item[1].w');
      expectClose(r.height, 126.67, 'fr-rows-definite-height-0264 item[1].h');
    }
  });
  it("fr-rows-definite-height-0265: fr rows in a 400px-tall container", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid","width":600,"height":400,"gridTemplateColumns":"100px","gridTemplateRows":"1fr 2fr","gap":10}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":30}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":30}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'fr-rows-definite-height-0265 container.width');
    expectClose(container.height, 400, 'fr-rows-definite-height-0265 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'fr-rows-definite-height-0265 item[0].x');
      expectClose(r.y - container.y, 0, 'fr-rows-definite-height-0265 item[0].y');
      expectClose(r.width, 30, 'fr-rows-definite-height-0265 item[0].w');
      expectClose(r.height, 130, 'fr-rows-definite-height-0265 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 0, 'fr-rows-definite-height-0265 item[1].x');
      expectClose(r.y - container.y, 140, 'fr-rows-definite-height-0265 item[1].y');
      expectClose(r.width, 30, 'fr-rows-definite-height-0265 item[1].w');
      expectClose(r.height, 260, 'fr-rows-definite-height-0265 item[1].h');
    }
  });
});

describe("fr-rows-indefinite-height", () => {
  it("fr-rows-indefinite-height-0266: fr rows with an indefinite container height", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid","width":600,"gridTemplateColumns":"100px","gridTemplateRows":"1fr 2fr","gap":10}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":30,"height":40}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":30,"height":60}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'fr-rows-indefinite-height-0266 container.width');
    expectClose(container.height, 130, 'fr-rows-indefinite-height-0266 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'fr-rows-indefinite-height-0266 item[0].x');
      expectClose(r.y - container.y, 0, 'fr-rows-indefinite-height-0266 item[0].y');
      expectClose(r.width, 30, 'fr-rows-indefinite-height-0266 item[0].w');
      expectClose(r.height, 40, 'fr-rows-indefinite-height-0266 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 0, 'fr-rows-indefinite-height-0266 item[1].x');
      expectClose(r.y - container.y, 50, 'fr-rows-indefinite-height-0266 item[1].y');
      expectClose(r.width, 30, 'fr-rows-indefinite-height-0266 item[1].w');
      expectClose(r.height, 60, 'fr-rows-indefinite-height-0266 item[1].h');
    }
  });
});

describe("zero-tracks", () => {
  it("zero-tracks-0267: zero-width tracks still take part in gap accounting", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid","width":600,"gridTemplateColumns":"0px 0px 100px","gap":10}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":20}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'zero-tracks-0267 container.width');
    expectClose(container.height, 20, 'zero-tracks-0267 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'zero-tracks-0267 item[0].x');
      expectClose(r.y - container.y, 0, 'zero-tracks-0267 item[0].y');
      expectClose(r.width, 0, 'zero-tracks-0267 item[0].w');
      expectClose(r.height, 20, 'zero-tracks-0267 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 10, 'zero-tracks-0267 item[1].x');
      expectClose(r.y - container.y, 0, 'zero-tracks-0267 item[1].y');
      expectClose(r.width, 0, 'zero-tracks-0267 item[1].w');
      expectClose(r.height, 20, 'zero-tracks-0267 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 20, 'zero-tracks-0267 item[2].x');
      expectClose(r.y - container.y, 0, 'zero-tracks-0267 item[2].y');
      expectClose(r.width, 100, 'zero-tracks-0267 item[2].w');
      expectClose(r.height, 20, 'zero-tracks-0267 item[2].h');
    }
  });
});

describe("zero-gap-zero-tracks", () => {
  it("zero-gap-zero-tracks-0268: a zero track next to an fr with no gap", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid","width":600,"gridTemplateColumns":"0px 1fr","gap":0}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":20}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'zero-gap-zero-tracks-0268 container.width');
    expectClose(container.height, 20, 'zero-gap-zero-tracks-0268 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'zero-gap-zero-tracks-0268 item[0].x');
      expectClose(r.y - container.y, 0, 'zero-gap-zero-tracks-0268 item[0].y');
      expectClose(r.width, 0, 'zero-gap-zero-tracks-0268 item[0].w');
      expectClose(r.height, 20, 'zero-gap-zero-tracks-0268 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 0, 'zero-gap-zero-tracks-0268 item[1].x');
      expectClose(r.y - container.y, 0, 'zero-gap-zero-tracks-0268 item[1].y');
      expectClose(r.width, 600, 'zero-gap-zero-tracks-0268 item[1].w');
      expectClose(r.height, 20, 'zero-gap-zero-tracks-0268 item[1].h');
    }
  });
});

describe("span-across-flexible", () => {
  it("span-across-flexible-0269: a 100px item spanning 2 flexible tracks", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid","gridTemplateColumns":"1fr 1fr 2fr","gap":10}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":40,"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":100,"height":20,"gridColumnEnd":"span 2"}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 900, 'span-across-flexible-0269 container.width');
    expectClose(container.height, 20, 'span-across-flexible-0269 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'span-across-flexible-0269 item[0].x');
      expectClose(r.y - container.y, 0, 'span-across-flexible-0269 item[0].y');
      expectClose(r.width, 40, 'span-across-flexible-0269 item[0].w');
      expectClose(r.height, 20, 'span-across-flexible-0269 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 230, 'span-across-flexible-0269 item[1].x');
      expectClose(r.y - container.y, 0, 'span-across-flexible-0269 item[1].y');
      expectClose(r.width, 100, 'span-across-flexible-0269 item[1].w');
      expectClose(r.height, 20, 'span-across-flexible-0269 item[1].h');
    }
  });
  it("span-across-flexible-0270: a 400px item spanning 2 flexible tracks", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid","gridTemplateColumns":"1fr 1fr 2fr","gap":10}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":40,"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":400,"height":20,"gridColumnEnd":"span 2"}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 900, 'span-across-flexible-0270 container.width');
    expectClose(container.height, 20, 'span-across-flexible-0270 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'span-across-flexible-0270 item[0].x');
      expectClose(r.y - container.y, 0, 'span-across-flexible-0270 item[0].y');
      expectClose(r.width, 40, 'span-across-flexible-0270 item[0].w');
      expectClose(r.height, 20, 'span-across-flexible-0270 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 230, 'span-across-flexible-0270 item[1].x');
      expectClose(r.y - container.y, 0, 'span-across-flexible-0270 item[1].y');
      expectClose(r.width, 400, 'span-across-flexible-0270 item[1].w');
      expectClose(r.height, 20, 'span-across-flexible-0270 item[1].h');
    }
  });
  it("span-across-flexible-0271: a 700px item spanning 2 flexible tracks", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid","gridTemplateColumns":"1fr 1fr 2fr","gap":10}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":40,"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":700,"height":20,"gridColumnEnd":"span 2"}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 900, 'span-across-flexible-0271 container.width');
    expectClose(container.height, 20, 'span-across-flexible-0271 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'span-across-flexible-0271 item[0].x');
      expectClose(r.y - container.y, 0, 'span-across-flexible-0271 item[0].y');
      expectClose(r.width, 40, 'span-across-flexible-0271 item[0].w');
      expectClose(r.height, 20, 'span-across-flexible-0271 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 200, 'span-across-flexible-0271 item[1].x');
      expectClose(r.y - container.y, 0, 'span-across-flexible-0271 item[1].y');
      expectClose(r.width, 700, 'span-across-flexible-0271 item[1].w');
      expectClose(r.height, 20, 'span-across-flexible-0271 item[1].h');
    }
  });
  it("span-across-flexible-0272: a 100px item spanning 3 flexible tracks", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid","gridTemplateColumns":"1fr 1fr 2fr","gap":10}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":40,"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":100,"height":20,"gridColumnEnd":"span 3"}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 900, 'span-across-flexible-0272 container.width');
    expectClose(container.height, 50, 'span-across-flexible-0272 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'span-across-flexible-0272 item[0].x');
      expectClose(r.y - container.y, 0, 'span-across-flexible-0272 item[0].y');
      expectClose(r.width, 40, 'span-across-flexible-0272 item[0].w');
      expectClose(r.height, 20, 'span-across-flexible-0272 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 0, 'span-across-flexible-0272 item[1].x');
      expectClose(r.y - container.y, 30, 'span-across-flexible-0272 item[1].y');
      expectClose(r.width, 100, 'span-across-flexible-0272 item[1].w');
      expectClose(r.height, 20, 'span-across-flexible-0272 item[1].h');
    }
  });
  it("span-across-flexible-0273: a 400px item spanning 3 flexible tracks", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid","gridTemplateColumns":"1fr 1fr 2fr","gap":10}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":40,"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":400,"height":20,"gridColumnEnd":"span 3"}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 900, 'span-across-flexible-0273 container.width');
    expectClose(container.height, 50, 'span-across-flexible-0273 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'span-across-flexible-0273 item[0].x');
      expectClose(r.y - container.y, 0, 'span-across-flexible-0273 item[0].y');
      expectClose(r.width, 40, 'span-across-flexible-0273 item[0].w');
      expectClose(r.height, 20, 'span-across-flexible-0273 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 0, 'span-across-flexible-0273 item[1].x');
      expectClose(r.y - container.y, 30, 'span-across-flexible-0273 item[1].y');
      expectClose(r.width, 400, 'span-across-flexible-0273 item[1].w');
      expectClose(r.height, 20, 'span-across-flexible-0273 item[1].h');
    }
  });
  it("span-across-flexible-0274: a 700px item spanning 3 flexible tracks", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid","gridTemplateColumns":"1fr 1fr 2fr","gap":10}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":40,"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":700,"height":20,"gridColumnEnd":"span 3"}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 900, 'span-across-flexible-0274 container.width');
    expectClose(container.height, 50, 'span-across-flexible-0274 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'span-across-flexible-0274 item[0].x');
      expectClose(r.y - container.y, 0, 'span-across-flexible-0274 item[0].y');
      expectClose(r.width, 40, 'span-across-flexible-0274 item[0].w');
      expectClose(r.height, 20, 'span-across-flexible-0274 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 0, 'span-across-flexible-0274 item[1].x');
      expectClose(r.y - container.y, 30, 'span-across-flexible-0274 item[1].y');
      expectClose(r.width, 700, 'span-across-flexible-0274 item[1].w');
      expectClose(r.height, 20, 'span-across-flexible-0274 item[1].h');
    }
  });
});

describe("span-mixed-tracks", () => {
  it("span-mixed-tracks-0275: a 200px item spanning fixed and flexible tracks", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid","gridTemplateColumns":"80px 1fr 60px 2fr","gap":10}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":200,"height":20,"gridColumnEnd":"span 3"}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":40,"height":20}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 900, 'span-mixed-tracks-0275 container.width');
    expectClose(container.height, 20, 'span-mixed-tracks-0275 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'span-mixed-tracks-0275 item[0].x');
      expectClose(r.y - container.y, 0, 'span-mixed-tracks-0275 item[0].y');
      expectClose(r.width, 200, 'span-mixed-tracks-0275 item[0].w');
      expectClose(r.height, 20, 'span-mixed-tracks-0275 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 413.33, 'span-mixed-tracks-0275 item[1].x');
      expectClose(r.y - container.y, 0, 'span-mixed-tracks-0275 item[1].y');
      expectClose(r.width, 40, 'span-mixed-tracks-0275 item[1].w');
      expectClose(r.height, 20, 'span-mixed-tracks-0275 item[1].h');
    }
  });
  it("span-mixed-tracks-0276: a 500px item spanning fixed and flexible tracks", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid","gridTemplateColumns":"80px 1fr 60px 2fr","gap":10}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":500,"height":20,"gridColumnEnd":"span 3"}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":40,"height":20}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 900, 'span-mixed-tracks-0276 container.width');
    expectClose(container.height, 20, 'span-mixed-tracks-0276 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'span-mixed-tracks-0276 item[0].x');
      expectClose(r.y - container.y, 0, 'span-mixed-tracks-0276 item[0].y');
      expectClose(r.width, 500, 'span-mixed-tracks-0276 item[0].w');
      expectClose(r.height, 20, 'span-mixed-tracks-0276 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 510, 'span-mixed-tracks-0276 item[1].x');
      expectClose(r.y - container.y, 0, 'span-mixed-tracks-0276 item[1].y');
      expectClose(r.width, 40, 'span-mixed-tracks-0276 item[1].w');
      expectClose(r.height, 20, 'span-mixed-tracks-0276 item[1].h');
    }
  });
});

describe("fr-floor-overflow", () => {
  it("fr-floor-overflow-0277: minmax(100px, 1fr) floor against a 600px container", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid","width":600,"gridTemplateColumns":"minmax(100px, 1fr) 1fr 1fr","gap":10}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":20}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'fr-floor-overflow-0277 container.width');
    expectClose(container.height, 20, 'fr-floor-overflow-0277 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'fr-floor-overflow-0277 item[0].x');
      expectClose(r.y - container.y, 0, 'fr-floor-overflow-0277 item[0].y');
      expectClose(r.width, 193.33, 'fr-floor-overflow-0277 item[0].w');
      expectClose(r.height, 20, 'fr-floor-overflow-0277 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 203.33, 'fr-floor-overflow-0277 item[1].x');
      expectClose(r.y - container.y, 0, 'fr-floor-overflow-0277 item[1].y');
      expectClose(r.width, 193.33, 'fr-floor-overflow-0277 item[1].w');
      expectClose(r.height, 20, 'fr-floor-overflow-0277 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 406.66, 'fr-floor-overflow-0277 item[2].x');
      expectClose(r.y - container.y, 0, 'fr-floor-overflow-0277 item[2].y');
      expectClose(r.width, 193.34, 'fr-floor-overflow-0277 item[2].w');
      expectClose(r.height, 20, 'fr-floor-overflow-0277 item[2].h');
    }
  });
  it("fr-floor-overflow-0278: minmax(300px, 1fr) floor against a 600px container", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid","width":600,"gridTemplateColumns":"minmax(300px, 1fr) 1fr 1fr","gap":10}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":20}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'fr-floor-overflow-0278 container.width');
    expectClose(container.height, 20, 'fr-floor-overflow-0278 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'fr-floor-overflow-0278 item[0].x');
      expectClose(r.y - container.y, 0, 'fr-floor-overflow-0278 item[0].y');
      expectClose(r.width, 300, 'fr-floor-overflow-0278 item[0].w');
      expectClose(r.height, 20, 'fr-floor-overflow-0278 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 310, 'fr-floor-overflow-0278 item[1].x');
      expectClose(r.y - container.y, 0, 'fr-floor-overflow-0278 item[1].y');
      expectClose(r.width, 140, 'fr-floor-overflow-0278 item[1].w');
      expectClose(r.height, 20, 'fr-floor-overflow-0278 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 460, 'fr-floor-overflow-0278 item[2].x');
      expectClose(r.y - container.y, 0, 'fr-floor-overflow-0278 item[2].y');
      expectClose(r.width, 140, 'fr-floor-overflow-0278 item[2].w');
      expectClose(r.height, 20, 'fr-floor-overflow-0278 item[2].h');
    }
  });
  it("fr-floor-overflow-0279: minmax(500px, 1fr) floor against a 600px container", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid","width":600,"gridTemplateColumns":"minmax(500px, 1fr) 1fr 1fr","gap":10}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":20}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'fr-floor-overflow-0279 container.width');
    expectClose(container.height, 20, 'fr-floor-overflow-0279 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'fr-floor-overflow-0279 item[0].x');
      expectClose(r.y - container.y, 0, 'fr-floor-overflow-0279 item[0].y');
      expectClose(r.width, 500, 'fr-floor-overflow-0279 item[0].w');
      expectClose(r.height, 20, 'fr-floor-overflow-0279 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 510, 'fr-floor-overflow-0279 item[1].x');
      expectClose(r.y - container.y, 0, 'fr-floor-overflow-0279 item[1].y');
      expectClose(r.width, 40, 'fr-floor-overflow-0279 item[1].w');
      expectClose(r.height, 20, 'fr-floor-overflow-0279 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 560, 'fr-floor-overflow-0279 item[2].x');
      expectClose(r.y - container.y, 0, 'fr-floor-overflow-0279 item[2].y');
      expectClose(r.width, 40, 'fr-floor-overflow-0279 item[2].w');
      expectClose(r.height, 20, 'fr-floor-overflow-0279 item[2].h');
    }
  });
});

describe("baseline-alignment", () => {
  it("baseline-alignment-0280: align-items:baseline with items of differing height and padding", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid","width":600,"gridTemplateColumns":"150px 150px 150px","gridTemplateRows":"auto","gap":10,"alignItems":"baseline"}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":40,"height":30,"padding":10}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":40,"height":60,"padding":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":40,"height":20,"padding":5}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'baseline-alignment-0280 container.width');
    expectClose(container.height, 60, 'baseline-alignment-0280 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'baseline-alignment-0280 item[0].x');
      expectClose(r.y - container.y, 30, 'baseline-alignment-0280 item[0].y');
      expectClose(r.width, 40, 'baseline-alignment-0280 item[0].w');
      expectClose(r.height, 30, 'baseline-alignment-0280 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 160, 'baseline-alignment-0280 item[1].x');
      expectClose(r.y - container.y, 0, 'baseline-alignment-0280 item[1].y');
      expectClose(r.width, 40, 'baseline-alignment-0280 item[1].w');
      expectClose(r.height, 60, 'baseline-alignment-0280 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 320, 'baseline-alignment-0280 item[2].x');
      expectClose(r.y - container.y, 40, 'baseline-alignment-0280 item[2].y');
      expectClose(r.width, 40, 'baseline-alignment-0280 item[2].w');
      expectClose(r.height, 20, 'baseline-alignment-0280 item[2].h');
    }
  });
  it("baseline-alignment-0281: align-items:start with items of differing height and padding", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid","width":600,"gridTemplateColumns":"150px 150px 150px","gridTemplateRows":"auto","gap":10,"alignItems":"start"}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":40,"height":30,"padding":10}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":40,"height":60,"padding":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":40,"height":20,"padding":5}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'baseline-alignment-0281 container.width');
    expectClose(container.height, 60, 'baseline-alignment-0281 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'baseline-alignment-0281 item[0].x');
      expectClose(r.y - container.y, 0, 'baseline-alignment-0281 item[0].y');
      expectClose(r.width, 40, 'baseline-alignment-0281 item[0].w');
      expectClose(r.height, 30, 'baseline-alignment-0281 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 160, 'baseline-alignment-0281 item[1].x');
      expectClose(r.y - container.y, 0, 'baseline-alignment-0281 item[1].y');
      expectClose(r.width, 40, 'baseline-alignment-0281 item[1].w');
      expectClose(r.height, 60, 'baseline-alignment-0281 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 320, 'baseline-alignment-0281 item[2].x');
      expectClose(r.y - container.y, 0, 'baseline-alignment-0281 item[2].y');
      expectClose(r.width, 40, 'baseline-alignment-0281 item[2].w');
      expectClose(r.height, 20, 'baseline-alignment-0281 item[2].h');
    }
  });
});

describe("item-aspect-ratio", () => {
  it("item-aspect-ratio-0282: items with aspect-ratio 1 in fr tracks", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid","width":600,"gridTemplateColumns":"1fr 1fr","gap":10}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"aspectRatio":1}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"aspectRatio":1}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'item-aspect-ratio-0282 container.width');
    expectClose(container.height, 295, 'item-aspect-ratio-0282 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'item-aspect-ratio-0282 item[0].x');
      expectClose(r.y - container.y, 0, 'item-aspect-ratio-0282 item[0].y');
      expectClose(r.width, 295, 'item-aspect-ratio-0282 item[0].w');
      expectClose(r.height, 295, 'item-aspect-ratio-0282 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 305, 'item-aspect-ratio-0282 item[1].x');
      expectClose(r.y - container.y, 0, 'item-aspect-ratio-0282 item[1].y');
      expectClose(r.width, 295, 'item-aspect-ratio-0282 item[1].w');
      expectClose(r.height, 295, 'item-aspect-ratio-0282 item[1].h');
    }
  });
  it("item-aspect-ratio-0283: items with aspect-ratio 2 in fr tracks", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid","width":600,"gridTemplateColumns":"1fr 1fr","gap":10}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"aspectRatio":2}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"aspectRatio":2}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'item-aspect-ratio-0283 container.width');
    expectClose(container.height, 147.5, 'item-aspect-ratio-0283 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'item-aspect-ratio-0283 item[0].x');
      expectClose(r.y - container.y, 0, 'item-aspect-ratio-0283 item[0].y');
      expectClose(r.width, 295, 'item-aspect-ratio-0283 item[0].w');
      expectClose(r.height, 147.5, 'item-aspect-ratio-0283 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 305, 'item-aspect-ratio-0283 item[1].x');
      expectClose(r.y - container.y, 0, 'item-aspect-ratio-0283 item[1].y');
      expectClose(r.width, 295, 'item-aspect-ratio-0283 item[1].w');
      expectClose(r.height, 147.5, 'item-aspect-ratio-0283 item[1].h');
    }
  });
});

describe("pct-track-indefinite", () => {
  it("pct-track-indefinite-0284: percentage tracks with an indefinite container width", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid","gridTemplateColumns":"50% 50%","gap":10}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":120,"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":80,"height":20}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 900, 'pct-track-indefinite-0284 container.width');
    expectClose(container.height, 20, 'pct-track-indefinite-0284 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'pct-track-indefinite-0284 item[0].x');
      expectClose(r.y - container.y, 0, 'pct-track-indefinite-0284 item[0].y');
      expectClose(r.width, 120, 'pct-track-indefinite-0284 item[0].w');
      expectClose(r.height, 20, 'pct-track-indefinite-0284 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 460, 'pct-track-indefinite-0284 item[1].x');
      expectClose(r.y - container.y, 0, 'pct-track-indefinite-0284 item[1].y');
      expectClose(r.width, 80, 'pct-track-indefinite-0284 item[1].w');
      expectClose(r.height, 20, 'pct-track-indefinite-0284 item[1].h');
    }
  });
});

describe("align-content-block", () => {
  it("align-content-block-0285: align-content:start with block-axis slack", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid","width":600,"height":300,"gridTemplateColumns":"100px","gridTemplateRows":"40px 40px","gap":10,"alignContent":"start"}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":30}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":30}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'align-content-block-0285 container.width');
    expectClose(container.height, 300, 'align-content-block-0285 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'align-content-block-0285 item[0].x');
      expectClose(r.y - container.y, 0, 'align-content-block-0285 item[0].y');
      expectClose(r.width, 30, 'align-content-block-0285 item[0].w');
      expectClose(r.height, 40, 'align-content-block-0285 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 0, 'align-content-block-0285 item[1].x');
      expectClose(r.y - container.y, 50, 'align-content-block-0285 item[1].y');
      expectClose(r.width, 30, 'align-content-block-0285 item[1].w');
      expectClose(r.height, 40, 'align-content-block-0285 item[1].h');
    }
  });
  it("align-content-block-0286: align-content:center with block-axis slack", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid","width":600,"height":300,"gridTemplateColumns":"100px","gridTemplateRows":"40px 40px","gap":10,"alignContent":"center"}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":30}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":30}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'align-content-block-0286 container.width');
    expectClose(container.height, 300, 'align-content-block-0286 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'align-content-block-0286 item[0].x');
      expectClose(r.y - container.y, 105, 'align-content-block-0286 item[0].y');
      expectClose(r.width, 30, 'align-content-block-0286 item[0].w');
      expectClose(r.height, 40, 'align-content-block-0286 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 0, 'align-content-block-0286 item[1].x');
      expectClose(r.y - container.y, 155, 'align-content-block-0286 item[1].y');
      expectClose(r.width, 30, 'align-content-block-0286 item[1].w');
      expectClose(r.height, 40, 'align-content-block-0286 item[1].h');
    }
  });
  it("align-content-block-0287: align-content:end with block-axis slack", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid","width":600,"height":300,"gridTemplateColumns":"100px","gridTemplateRows":"40px 40px","gap":10,"alignContent":"end"}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":30}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":30}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'align-content-block-0287 container.width');
    expectClose(container.height, 300, 'align-content-block-0287 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'align-content-block-0287 item[0].x');
      expectClose(r.y - container.y, 210, 'align-content-block-0287 item[0].y');
      expectClose(r.width, 30, 'align-content-block-0287 item[0].w');
      expectClose(r.height, 40, 'align-content-block-0287 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 0, 'align-content-block-0287 item[1].x');
      expectClose(r.y - container.y, 260, 'align-content-block-0287 item[1].y');
      expectClose(r.width, 30, 'align-content-block-0287 item[1].w');
      expectClose(r.height, 40, 'align-content-block-0287 item[1].h');
    }
  });
  it("align-content-block-0288: align-content:space-between with block-axis slack", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid","width":600,"height":300,"gridTemplateColumns":"100px","gridTemplateRows":"40px 40px","gap":10,"alignContent":"space-between"}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":30}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":30}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'align-content-block-0288 container.width');
    expectClose(container.height, 300, 'align-content-block-0288 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'align-content-block-0288 item[0].x');
      expectClose(r.y - container.y, 0, 'align-content-block-0288 item[0].y');
      expectClose(r.width, 30, 'align-content-block-0288 item[0].w');
      expectClose(r.height, 40, 'align-content-block-0288 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 0, 'align-content-block-0288 item[1].x');
      expectClose(r.y - container.y, 260, 'align-content-block-0288 item[1].y');
      expectClose(r.width, 30, 'align-content-block-0288 item[1].w');
      expectClose(r.height, 40, 'align-content-block-0288 item[1].h');
    }
  });
});

describe("fit-content-limit", () => {
  it("fit-content-limit-0289: fit-content(140px) around a 90px item", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid","width":600,"gridTemplateColumns":"fit-content(140px) 1fr","gap":10}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":90,"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":40,"height":20}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'fit-content-limit-0289 container.width');
    expectClose(container.height, 20, 'fit-content-limit-0289 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'fit-content-limit-0289 item[0].x');
      expectClose(r.y - container.y, 0, 'fit-content-limit-0289 item[0].y');
      expectClose(r.width, 90, 'fit-content-limit-0289 item[0].w');
      expectClose(r.height, 20, 'fit-content-limit-0289 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 100, 'fit-content-limit-0289 item[1].x');
      expectClose(r.y - container.y, 0, 'fit-content-limit-0289 item[1].y');
      expectClose(r.width, 40, 'fit-content-limit-0289 item[1].w');
      expectClose(r.height, 20, 'fit-content-limit-0289 item[1].h');
    }
  });
  it("fit-content-limit-0290: fit-content(50px) around a 90px item", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid","width":600,"gridTemplateColumns":"fit-content(50px) 1fr","gap":10}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":90,"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":40,"height":20}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'fit-content-limit-0290 container.width');
    expectClose(container.height, 20, 'fit-content-limit-0290 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'fit-content-limit-0290 item[0].x');
      expectClose(r.y - container.y, 0, 'fit-content-limit-0290 item[0].y');
      expectClose(r.width, 90, 'fit-content-limit-0290 item[0].w');
      expectClose(r.height, 20, 'fit-content-limit-0290 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 100, 'fit-content-limit-0290 item[1].x');
      expectClose(r.y - container.y, 0, 'fit-content-limit-0290 item[1].y');
      expectClose(r.width, 40, 'fit-content-limit-0290 item[1].w');
      expectClose(r.height, 20, 'fit-content-limit-0290 item[1].h');
    }
  });
  it("fit-content-limit-0291: fit-content(200px) around a 240px item", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid","width":600,"gridTemplateColumns":"fit-content(200px) 1fr","gap":10}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":240,"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":40,"height":20}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'fit-content-limit-0291 container.width');
    expectClose(container.height, 20, 'fit-content-limit-0291 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'fit-content-limit-0291 item[0].x');
      expectClose(r.y - container.y, 0, 'fit-content-limit-0291 item[0].y');
      expectClose(r.width, 240, 'fit-content-limit-0291 item[0].w');
      expectClose(r.height, 20, 'fit-content-limit-0291 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 250, 'fit-content-limit-0291 item[1].x');
      expectClose(r.y - container.y, 0, 'fit-content-limit-0291 item[1].y');
      expectClose(r.width, 40, 'fit-content-limit-0291 item[1].w');
      expectClose(r.height, 20, 'fit-content-limit-0291 item[1].h');
    }
  });
});

describe("auto-flow-dense-basic", () => {
  it("auto-flow-dense-basic-0292: grid-auto-flow: row with two spanning items", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid","width":600,"gridTemplateColumns":"1fr 1fr 1fr","gap":10,"gridAutoFlow":"row"}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30,"gridColumnEnd":"span 2"}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30,"gridColumnEnd":"span 2"}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'auto-flow-dense-basic-0292 container.width');
    expectClose(container.height, 110, 'auto-flow-dense-basic-0292 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'auto-flow-dense-basic-0292 item[0].x');
      expectClose(r.y - container.y, 0, 'auto-flow-dense-basic-0292 item[0].y');
      expectClose(r.width, 396.66, 'auto-flow-dense-basic-0292 item[0].w');
      expectClose(r.height, 30, 'auto-flow-dense-basic-0292 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 0, 'auto-flow-dense-basic-0292 item[1].x');
      expectClose(r.y - container.y, 40, 'auto-flow-dense-basic-0292 item[1].y');
      expectClose(r.width, 396.66, 'auto-flow-dense-basic-0292 item[1].w');
      expectClose(r.height, 30, 'auto-flow-dense-basic-0292 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 406.66, 'auto-flow-dense-basic-0292 item[2].x');
      expectClose(r.y - container.y, 40, 'auto-flow-dense-basic-0292 item[2].y');
      expectClose(r.width, 193.34, 'auto-flow-dense-basic-0292 item[2].w');
      expectClose(r.height, 30, 'auto-flow-dense-basic-0292 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 0, 'auto-flow-dense-basic-0292 item[3].x');
      expectClose(r.y - container.y, 80, 'auto-flow-dense-basic-0292 item[3].y');
      expectClose(r.width, 193.33, 'auto-flow-dense-basic-0292 item[3].w');
      expectClose(r.height, 30, 'auto-flow-dense-basic-0292 item[3].h');
    }
  });
  it("auto-flow-dense-basic-0296: grid-auto-flow: row dense with two spanning items", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid","width":600,"gridTemplateColumns":"1fr 1fr 1fr","gap":10,"gridAutoFlow":"row dense"}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30,"gridColumnEnd":"span 2"}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30,"gridColumnEnd":"span 2"}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'auto-flow-dense-basic-0296 container.width');
    expectClose(container.height, 70, 'auto-flow-dense-basic-0296 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'auto-flow-dense-basic-0296 item[0].x');
      expectClose(r.y - container.y, 0, 'auto-flow-dense-basic-0296 item[0].y');
      expectClose(r.width, 396.66, 'auto-flow-dense-basic-0296 item[0].w');
      expectClose(r.height, 30, 'auto-flow-dense-basic-0296 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 0, 'auto-flow-dense-basic-0296 item[1].x');
      expectClose(r.y - container.y, 40, 'auto-flow-dense-basic-0296 item[1].y');
      expectClose(r.width, 396.66, 'auto-flow-dense-basic-0296 item[1].w');
      expectClose(r.height, 30, 'auto-flow-dense-basic-0296 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 406.66, 'auto-flow-dense-basic-0296 item[2].x');
      expectClose(r.y - container.y, 0, 'auto-flow-dense-basic-0296 item[2].y');
      expectClose(r.width, 193.34, 'auto-flow-dense-basic-0296 item[2].w');
      expectClose(r.height, 30, 'auto-flow-dense-basic-0296 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 406.66, 'auto-flow-dense-basic-0296 item[3].x');
      expectClose(r.y - container.y, 40, 'auto-flow-dense-basic-0296 item[3].y');
      expectClose(r.width, 193.34, 'auto-flow-dense-basic-0296 item[3].w');
      expectClose(r.height, 30, 'auto-flow-dense-basic-0296 item[3].h');
    }
  });
});

describe("auto-flow-dense-mixed", () => {
  it("auto-flow-dense-mixed-0293: grid-auto-flow: row over four columns with mixed spans", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid","width":600,"gridTemplateColumns":"1fr 1fr 1fr 1fr","gap":8,"gridAutoFlow":"row"}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":24,"gridColumnEnd":"span 3"}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":24,"gridColumnEnd":"span 2"}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":24}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":24,"gridColumnEnd":"span 2"}}
          />
          <View
            collapsable={false}
            ref={itemRefs[4]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":24}}
          />
          <View
            collapsable={false}
            ref={itemRefs[5]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":24}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'auto-flow-dense-mixed-0293 container.width');
    expectClose(container.height, 88, 'auto-flow-dense-mixed-0293 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'auto-flow-dense-mixed-0293 item[0].x');
      expectClose(r.y - container.y, 0, 'auto-flow-dense-mixed-0293 item[0].y');
      expectClose(r.width, 448, 'auto-flow-dense-mixed-0293 item[0].w');
      expectClose(r.height, 24, 'auto-flow-dense-mixed-0293 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 0, 'auto-flow-dense-mixed-0293 item[1].x');
      expectClose(r.y - container.y, 32, 'auto-flow-dense-mixed-0293 item[1].y');
      expectClose(r.width, 296, 'auto-flow-dense-mixed-0293 item[1].w');
      expectClose(r.height, 24, 'auto-flow-dense-mixed-0293 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 304, 'auto-flow-dense-mixed-0293 item[2].x');
      expectClose(r.y - container.y, 32, 'auto-flow-dense-mixed-0293 item[2].y');
      expectClose(r.width, 144, 'auto-flow-dense-mixed-0293 item[2].w');
      expectClose(r.height, 24, 'auto-flow-dense-mixed-0293 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 0, 'auto-flow-dense-mixed-0293 item[3].x');
      expectClose(r.y - container.y, 64, 'auto-flow-dense-mixed-0293 item[3].y');
      expectClose(r.width, 296, 'auto-flow-dense-mixed-0293 item[3].w');
      expectClose(r.height, 24, 'auto-flow-dense-mixed-0293 item[3].h');
    }
    {
      const r = rectOf(itemRefs[4]);
      expectClose(r.x - container.x, 304, 'auto-flow-dense-mixed-0293 item[4].x');
      expectClose(r.y - container.y, 64, 'auto-flow-dense-mixed-0293 item[4].y');
      expectClose(r.width, 144, 'auto-flow-dense-mixed-0293 item[4].w');
      expectClose(r.height, 24, 'auto-flow-dense-mixed-0293 item[4].h');
    }
    {
      const r = rectOf(itemRefs[5]);
      expectClose(r.x - container.x, 456, 'auto-flow-dense-mixed-0293 item[5].x');
      expectClose(r.y - container.y, 64, 'auto-flow-dense-mixed-0293 item[5].y');
      expectClose(r.width, 144, 'auto-flow-dense-mixed-0293 item[5].w');
      expectClose(r.height, 24, 'auto-flow-dense-mixed-0293 item[5].h');
    }
  });
  it("auto-flow-dense-mixed-0297: grid-auto-flow: row dense over four columns with mixed spans", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid","width":600,"gridTemplateColumns":"1fr 1fr 1fr 1fr","gap":8,"gridAutoFlow":"row dense"}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":24,"gridColumnEnd":"span 3"}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":24,"gridColumnEnd":"span 2"}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":24}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":24,"gridColumnEnd":"span 2"}}
          />
          <View
            collapsable={false}
            ref={itemRefs[4]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":24}}
          />
          <View
            collapsable={false}
            ref={itemRefs[5]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":24}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'auto-flow-dense-mixed-0297 container.width');
    expectClose(container.height, 88, 'auto-flow-dense-mixed-0297 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'auto-flow-dense-mixed-0297 item[0].x');
      expectClose(r.y - container.y, 0, 'auto-flow-dense-mixed-0297 item[0].y');
      expectClose(r.width, 448, 'auto-flow-dense-mixed-0297 item[0].w');
      expectClose(r.height, 24, 'auto-flow-dense-mixed-0297 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 0, 'auto-flow-dense-mixed-0297 item[1].x');
      expectClose(r.y - container.y, 32, 'auto-flow-dense-mixed-0297 item[1].y');
      expectClose(r.width, 296, 'auto-flow-dense-mixed-0297 item[1].w');
      expectClose(r.height, 24, 'auto-flow-dense-mixed-0297 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 456, 'auto-flow-dense-mixed-0297 item[2].x');
      expectClose(r.y - container.y, 0, 'auto-flow-dense-mixed-0297 item[2].y');
      expectClose(r.width, 144, 'auto-flow-dense-mixed-0297 item[2].w');
      expectClose(r.height, 24, 'auto-flow-dense-mixed-0297 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 304, 'auto-flow-dense-mixed-0297 item[3].x');
      expectClose(r.y - container.y, 32, 'auto-flow-dense-mixed-0297 item[3].y');
      expectClose(r.width, 296, 'auto-flow-dense-mixed-0297 item[3].w');
      expectClose(r.height, 24, 'auto-flow-dense-mixed-0297 item[3].h');
    }
    {
      const r = rectOf(itemRefs[4]);
      expectClose(r.x - container.x, 0, 'auto-flow-dense-mixed-0297 item[4].x');
      expectClose(r.y - container.y, 64, 'auto-flow-dense-mixed-0297 item[4].y');
      expectClose(r.width, 144, 'auto-flow-dense-mixed-0297 item[4].w');
      expectClose(r.height, 24, 'auto-flow-dense-mixed-0297 item[4].h');
    }
    {
      const r = rectOf(itemRefs[5]);
      expectClose(r.x - container.x, 152, 'auto-flow-dense-mixed-0297 item[5].x');
      expectClose(r.y - container.y, 64, 'auto-flow-dense-mixed-0297 item[5].y');
      expectClose(r.width, 144, 'auto-flow-dense-mixed-0297 item[5].w');
      expectClose(r.height, 24, 'auto-flow-dense-mixed-0297 item[5].h');
    }
  });
});

describe("auto-flow-dense-explicit", () => {
  it("auto-flow-dense-explicit-0294: grid-auto-flow: row around an explicitly placed item", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid","width":600,"gridTemplateColumns":"1fr 1fr 1fr","gap":10,"gridAutoFlow":"row"}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":24,"gridColumnStart":3}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":24,"gridColumnEnd":"span 2"}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":24}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":24}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'auto-flow-dense-explicit-0294 container.width');
    expectClose(container.height, 92, 'auto-flow-dense-explicit-0294 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 406.66, 'auto-flow-dense-explicit-0294 item[0].x');
      expectClose(r.y - container.y, 0, 'auto-flow-dense-explicit-0294 item[0].y');
      expectClose(r.width, 193.34, 'auto-flow-dense-explicit-0294 item[0].w');
      expectClose(r.height, 24, 'auto-flow-dense-explicit-0294 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 0, 'auto-flow-dense-explicit-0294 item[1].x');
      expectClose(r.y - container.y, 34, 'auto-flow-dense-explicit-0294 item[1].y');
      expectClose(r.width, 396.66, 'auto-flow-dense-explicit-0294 item[1].w');
      expectClose(r.height, 24, 'auto-flow-dense-explicit-0294 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 406.66, 'auto-flow-dense-explicit-0294 item[2].x');
      expectClose(r.y - container.y, 34, 'auto-flow-dense-explicit-0294 item[2].y');
      expectClose(r.width, 193.34, 'auto-flow-dense-explicit-0294 item[2].w');
      expectClose(r.height, 24, 'auto-flow-dense-explicit-0294 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 0, 'auto-flow-dense-explicit-0294 item[3].x');
      expectClose(r.y - container.y, 68, 'auto-flow-dense-explicit-0294 item[3].y');
      expectClose(r.width, 193.33, 'auto-flow-dense-explicit-0294 item[3].w');
      expectClose(r.height, 24, 'auto-flow-dense-explicit-0294 item[3].h');
    }
  });
  it("auto-flow-dense-explicit-0298: grid-auto-flow: row dense around an explicitly placed item", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid","width":600,"gridTemplateColumns":"1fr 1fr 1fr","gap":10,"gridAutoFlow":"row dense"}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":24,"gridColumnStart":3}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":24,"gridColumnEnd":"span 2"}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":24}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":24}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'auto-flow-dense-explicit-0298 container.width');
    expectClose(container.height, 58, 'auto-flow-dense-explicit-0298 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 406.66, 'auto-flow-dense-explicit-0298 item[0].x');
      expectClose(r.y - container.y, 0, 'auto-flow-dense-explicit-0298 item[0].y');
      expectClose(r.width, 193.34, 'auto-flow-dense-explicit-0298 item[0].w');
      expectClose(r.height, 24, 'auto-flow-dense-explicit-0298 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 0, 'auto-flow-dense-explicit-0298 item[1].x');
      expectClose(r.y - container.y, 0, 'auto-flow-dense-explicit-0298 item[1].y');
      expectClose(r.width, 396.66, 'auto-flow-dense-explicit-0298 item[1].w');
      expectClose(r.height, 24, 'auto-flow-dense-explicit-0298 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 0, 'auto-flow-dense-explicit-0298 item[2].x');
      expectClose(r.y - container.y, 34, 'auto-flow-dense-explicit-0298 item[2].y');
      expectClose(r.width, 193.33, 'auto-flow-dense-explicit-0298 item[2].w');
      expectClose(r.height, 24, 'auto-flow-dense-explicit-0298 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 203.33, 'auto-flow-dense-explicit-0298 item[3].x');
      expectClose(r.y - container.y, 34, 'auto-flow-dense-explicit-0298 item[3].y');
      expectClose(r.width, 193.33, 'auto-flow-dense-explicit-0298 item[3].w');
      expectClose(r.height, 24, 'auto-flow-dense-explicit-0298 item[3].h');
    }
  });
});

describe("auto-flow-dense-noop", () => {
  it("auto-flow-dense-noop-0295: grid-auto-flow: row with no holes to fill", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid","width":600,"gridTemplateColumns":"1fr 1fr 1fr","gap":10,"gridAutoFlow":"row"}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":24}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":24}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":24}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":24}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'auto-flow-dense-noop-0295 container.width');
    expectClose(container.height, 58, 'auto-flow-dense-noop-0295 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'auto-flow-dense-noop-0295 item[0].x');
      expectClose(r.y - container.y, 0, 'auto-flow-dense-noop-0295 item[0].y');
      expectClose(r.width, 193.33, 'auto-flow-dense-noop-0295 item[0].w');
      expectClose(r.height, 24, 'auto-flow-dense-noop-0295 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 203.33, 'auto-flow-dense-noop-0295 item[1].x');
      expectClose(r.y - container.y, 0, 'auto-flow-dense-noop-0295 item[1].y');
      expectClose(r.width, 193.33, 'auto-flow-dense-noop-0295 item[1].w');
      expectClose(r.height, 24, 'auto-flow-dense-noop-0295 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 406.66, 'auto-flow-dense-noop-0295 item[2].x');
      expectClose(r.y - container.y, 0, 'auto-flow-dense-noop-0295 item[2].y');
      expectClose(r.width, 193.34, 'auto-flow-dense-noop-0295 item[2].w');
      expectClose(r.height, 24, 'auto-flow-dense-noop-0295 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 0, 'auto-flow-dense-noop-0295 item[3].x');
      expectClose(r.y - container.y, 34, 'auto-flow-dense-noop-0295 item[3].y');
      expectClose(r.width, 193.33, 'auto-flow-dense-noop-0295 item[3].w');
      expectClose(r.height, 24, 'auto-flow-dense-noop-0295 item[3].h');
    }
  });
  it("auto-flow-dense-noop-0299: grid-auto-flow: row dense with no holes to fill", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid","width":600,"gridTemplateColumns":"1fr 1fr 1fr","gap":10,"gridAutoFlow":"row dense"}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":24}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":24}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":24}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":24}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'auto-flow-dense-noop-0299 container.width');
    expectClose(container.height, 58, 'auto-flow-dense-noop-0299 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'auto-flow-dense-noop-0299 item[0].x');
      expectClose(r.y - container.y, 0, 'auto-flow-dense-noop-0299 item[0].y');
      expectClose(r.width, 193.33, 'auto-flow-dense-noop-0299 item[0].w');
      expectClose(r.height, 24, 'auto-flow-dense-noop-0299 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 203.33, 'auto-flow-dense-noop-0299 item[1].x');
      expectClose(r.y - container.y, 0, 'auto-flow-dense-noop-0299 item[1].y');
      expectClose(r.width, 193.33, 'auto-flow-dense-noop-0299 item[1].w');
      expectClose(r.height, 24, 'auto-flow-dense-noop-0299 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 406.66, 'auto-flow-dense-noop-0299 item[2].x');
      expectClose(r.y - container.y, 0, 'auto-flow-dense-noop-0299 item[2].y');
      expectClose(r.width, 193.34, 'auto-flow-dense-noop-0299 item[2].w');
      expectClose(r.height, 24, 'auto-flow-dense-noop-0299 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 0, 'auto-flow-dense-noop-0299 item[3].x');
      expectClose(r.y - container.y, 34, 'auto-flow-dense-noop-0299 item[3].y');
      expectClose(r.width, 193.33, 'auto-flow-dense-noop-0299 item[3].w');
      expectClose(r.height, 24, 'auto-flow-dense-noop-0299 item[3].h');
    }
  });
});

describe("auto-flow-column-basic", () => {
  it("auto-flow-column-basic-0300: grid-auto-flow: row filling a 3x2 grid", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid","width":600,"gridTemplateColumns":"90px 90px 90px","gridTemplateRows":"40px 40px","gap":10,"gridAutoFlow":"row"}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{}}
          />
          <View
            collapsable={false}
            ref={itemRefs[4]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{}}
          />
          <View
            collapsable={false}
            ref={itemRefs[5]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'auto-flow-column-basic-0300 container.width');
    expectClose(container.height, 90, 'auto-flow-column-basic-0300 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'auto-flow-column-basic-0300 item[0].x');
      expectClose(r.y - container.y, 0, 'auto-flow-column-basic-0300 item[0].y');
      expectClose(r.width, 90, 'auto-flow-column-basic-0300 item[0].w');
      expectClose(r.height, 40, 'auto-flow-column-basic-0300 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 100, 'auto-flow-column-basic-0300 item[1].x');
      expectClose(r.y - container.y, 0, 'auto-flow-column-basic-0300 item[1].y');
      expectClose(r.width, 90, 'auto-flow-column-basic-0300 item[1].w');
      expectClose(r.height, 40, 'auto-flow-column-basic-0300 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 200, 'auto-flow-column-basic-0300 item[2].x');
      expectClose(r.y - container.y, 0, 'auto-flow-column-basic-0300 item[2].y');
      expectClose(r.width, 90, 'auto-flow-column-basic-0300 item[2].w');
      expectClose(r.height, 40, 'auto-flow-column-basic-0300 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 0, 'auto-flow-column-basic-0300 item[3].x');
      expectClose(r.y - container.y, 50, 'auto-flow-column-basic-0300 item[3].y');
      expectClose(r.width, 90, 'auto-flow-column-basic-0300 item[3].w');
      expectClose(r.height, 40, 'auto-flow-column-basic-0300 item[3].h');
    }
    {
      const r = rectOf(itemRefs[4]);
      expectClose(r.x - container.x, 100, 'auto-flow-column-basic-0300 item[4].x');
      expectClose(r.y - container.y, 50, 'auto-flow-column-basic-0300 item[4].y');
      expectClose(r.width, 90, 'auto-flow-column-basic-0300 item[4].w');
      expectClose(r.height, 40, 'auto-flow-column-basic-0300 item[4].h');
    }
    {
      const r = rectOf(itemRefs[5]);
      expectClose(r.x - container.x, 200, 'auto-flow-column-basic-0300 item[5].x');
      expectClose(r.y - container.y, 50, 'auto-flow-column-basic-0300 item[5].y');
      expectClose(r.width, 90, 'auto-flow-column-basic-0300 item[5].w');
      expectClose(r.height, 40, 'auto-flow-column-basic-0300 item[5].h');
    }
  });
  it("auto-flow-column-basic-0303: grid-auto-flow: column filling a 3x2 grid", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid","width":600,"gridTemplateColumns":"90px 90px 90px","gridTemplateRows":"40px 40px","gap":10,"gridAutoFlow":"column"}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{}}
          />
          <View
            collapsable={false}
            ref={itemRefs[4]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{}}
          />
          <View
            collapsable={false}
            ref={itemRefs[5]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'auto-flow-column-basic-0303 container.width');
    expectClose(container.height, 90, 'auto-flow-column-basic-0303 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'auto-flow-column-basic-0303 item[0].x');
      expectClose(r.y - container.y, 0, 'auto-flow-column-basic-0303 item[0].y');
      expectClose(r.width, 90, 'auto-flow-column-basic-0303 item[0].w');
      expectClose(r.height, 40, 'auto-flow-column-basic-0303 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 0, 'auto-flow-column-basic-0303 item[1].x');
      expectClose(r.y - container.y, 50, 'auto-flow-column-basic-0303 item[1].y');
      expectClose(r.width, 90, 'auto-flow-column-basic-0303 item[1].w');
      expectClose(r.height, 40, 'auto-flow-column-basic-0303 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 100, 'auto-flow-column-basic-0303 item[2].x');
      expectClose(r.y - container.y, 0, 'auto-flow-column-basic-0303 item[2].y');
      expectClose(r.width, 90, 'auto-flow-column-basic-0303 item[2].w');
      expectClose(r.height, 40, 'auto-flow-column-basic-0303 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 100, 'auto-flow-column-basic-0303 item[3].x');
      expectClose(r.y - container.y, 50, 'auto-flow-column-basic-0303 item[3].y');
      expectClose(r.width, 90, 'auto-flow-column-basic-0303 item[3].w');
      expectClose(r.height, 40, 'auto-flow-column-basic-0303 item[3].h');
    }
    {
      const r = rectOf(itemRefs[4]);
      expectClose(r.x - container.x, 200, 'auto-flow-column-basic-0303 item[4].x');
      expectClose(r.y - container.y, 0, 'auto-flow-column-basic-0303 item[4].y');
      expectClose(r.width, 90, 'auto-flow-column-basic-0303 item[4].w');
      expectClose(r.height, 40, 'auto-flow-column-basic-0303 item[4].h');
    }
    {
      const r = rectOf(itemRefs[5]);
      expectClose(r.x - container.x, 200, 'auto-flow-column-basic-0303 item[5].x');
      expectClose(r.y - container.y, 50, 'auto-flow-column-basic-0303 item[5].y');
      expectClose(r.width, 90, 'auto-flow-column-basic-0303 item[5].w');
      expectClose(r.height, 40, 'auto-flow-column-basic-0303 item[5].h');
    }
  });
  it("auto-flow-column-basic-0306: grid-auto-flow: row dense filling a 3x2 grid", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid","width":600,"gridTemplateColumns":"90px 90px 90px","gridTemplateRows":"40px 40px","gap":10,"gridAutoFlow":"row dense"}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{}}
          />
          <View
            collapsable={false}
            ref={itemRefs[4]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{}}
          />
          <View
            collapsable={false}
            ref={itemRefs[5]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'auto-flow-column-basic-0306 container.width');
    expectClose(container.height, 90, 'auto-flow-column-basic-0306 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'auto-flow-column-basic-0306 item[0].x');
      expectClose(r.y - container.y, 0, 'auto-flow-column-basic-0306 item[0].y');
      expectClose(r.width, 90, 'auto-flow-column-basic-0306 item[0].w');
      expectClose(r.height, 40, 'auto-flow-column-basic-0306 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 100, 'auto-flow-column-basic-0306 item[1].x');
      expectClose(r.y - container.y, 0, 'auto-flow-column-basic-0306 item[1].y');
      expectClose(r.width, 90, 'auto-flow-column-basic-0306 item[1].w');
      expectClose(r.height, 40, 'auto-flow-column-basic-0306 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 200, 'auto-flow-column-basic-0306 item[2].x');
      expectClose(r.y - container.y, 0, 'auto-flow-column-basic-0306 item[2].y');
      expectClose(r.width, 90, 'auto-flow-column-basic-0306 item[2].w');
      expectClose(r.height, 40, 'auto-flow-column-basic-0306 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 0, 'auto-flow-column-basic-0306 item[3].x');
      expectClose(r.y - container.y, 50, 'auto-flow-column-basic-0306 item[3].y');
      expectClose(r.width, 90, 'auto-flow-column-basic-0306 item[3].w');
      expectClose(r.height, 40, 'auto-flow-column-basic-0306 item[3].h');
    }
    {
      const r = rectOf(itemRefs[4]);
      expectClose(r.x - container.x, 100, 'auto-flow-column-basic-0306 item[4].x');
      expectClose(r.y - container.y, 50, 'auto-flow-column-basic-0306 item[4].y');
      expectClose(r.width, 90, 'auto-flow-column-basic-0306 item[4].w');
      expectClose(r.height, 40, 'auto-flow-column-basic-0306 item[4].h');
    }
    {
      const r = rectOf(itemRefs[5]);
      expectClose(r.x - container.x, 200, 'auto-flow-column-basic-0306 item[5].x');
      expectClose(r.y - container.y, 50, 'auto-flow-column-basic-0306 item[5].y');
      expectClose(r.width, 90, 'auto-flow-column-basic-0306 item[5].w');
      expectClose(r.height, 40, 'auto-flow-column-basic-0306 item[5].h');
    }
  });
  it("auto-flow-column-basic-0309: grid-auto-flow: column dense filling a 3x2 grid", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid","width":600,"gridTemplateColumns":"90px 90px 90px","gridTemplateRows":"40px 40px","gap":10,"gridAutoFlow":"column dense"}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{}}
          />
          <View
            collapsable={false}
            ref={itemRefs[4]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{}}
          />
          <View
            collapsable={false}
            ref={itemRefs[5]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'auto-flow-column-basic-0309 container.width');
    expectClose(container.height, 90, 'auto-flow-column-basic-0309 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'auto-flow-column-basic-0309 item[0].x');
      expectClose(r.y - container.y, 0, 'auto-flow-column-basic-0309 item[0].y');
      expectClose(r.width, 90, 'auto-flow-column-basic-0309 item[0].w');
      expectClose(r.height, 40, 'auto-flow-column-basic-0309 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 0, 'auto-flow-column-basic-0309 item[1].x');
      expectClose(r.y - container.y, 50, 'auto-flow-column-basic-0309 item[1].y');
      expectClose(r.width, 90, 'auto-flow-column-basic-0309 item[1].w');
      expectClose(r.height, 40, 'auto-flow-column-basic-0309 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 100, 'auto-flow-column-basic-0309 item[2].x');
      expectClose(r.y - container.y, 0, 'auto-flow-column-basic-0309 item[2].y');
      expectClose(r.width, 90, 'auto-flow-column-basic-0309 item[2].w');
      expectClose(r.height, 40, 'auto-flow-column-basic-0309 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 100, 'auto-flow-column-basic-0309 item[3].x');
      expectClose(r.y - container.y, 50, 'auto-flow-column-basic-0309 item[3].y');
      expectClose(r.width, 90, 'auto-flow-column-basic-0309 item[3].w');
      expectClose(r.height, 40, 'auto-flow-column-basic-0309 item[3].h');
    }
    {
      const r = rectOf(itemRefs[4]);
      expectClose(r.x - container.x, 200, 'auto-flow-column-basic-0309 item[4].x');
      expectClose(r.y - container.y, 0, 'auto-flow-column-basic-0309 item[4].y');
      expectClose(r.width, 90, 'auto-flow-column-basic-0309 item[4].w');
      expectClose(r.height, 40, 'auto-flow-column-basic-0309 item[4].h');
    }
    {
      const r = rectOf(itemRefs[5]);
      expectClose(r.x - container.x, 200, 'auto-flow-column-basic-0309 item[5].x');
      expectClose(r.y - container.y, 50, 'auto-flow-column-basic-0309 item[5].y');
      expectClose(r.width, 90, 'auto-flow-column-basic-0309 item[5].w');
      expectClose(r.height, 40, 'auto-flow-column-basic-0309 item[5].h');
    }
  });
});

describe("auto-flow-column-spans", () => {
  it("auto-flow-column-spans-0301: grid-auto-flow: row with row-spanning items", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid","width":600,"gridTemplateColumns":"80px 80px 80px","gridTemplateRows":"30px 30px 30px","gap":8,"gridAutoFlow":"row"}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"gridRowEnd":"span 2"}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"gridRowEnd":"span 2"}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{}}
          />
          <View
            collapsable={false}
            ref={itemRefs[4]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'auto-flow-column-spans-0301 container.width');
    expectClose(container.height, 106, 'auto-flow-column-spans-0301 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'auto-flow-column-spans-0301 item[0].x');
      expectClose(r.y - container.y, 0, 'auto-flow-column-spans-0301 item[0].y');
      expectClose(r.width, 80, 'auto-flow-column-spans-0301 item[0].w');
      expectClose(r.height, 68, 'auto-flow-column-spans-0301 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 88, 'auto-flow-column-spans-0301 item[1].x');
      expectClose(r.y - container.y, 0, 'auto-flow-column-spans-0301 item[1].y');
      expectClose(r.width, 80, 'auto-flow-column-spans-0301 item[1].w');
      expectClose(r.height, 30, 'auto-flow-column-spans-0301 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 176, 'auto-flow-column-spans-0301 item[2].x');
      expectClose(r.y - container.y, 0, 'auto-flow-column-spans-0301 item[2].y');
      expectClose(r.width, 80, 'auto-flow-column-spans-0301 item[2].w');
      expectClose(r.height, 68, 'auto-flow-column-spans-0301 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 88, 'auto-flow-column-spans-0301 item[3].x');
      expectClose(r.y - container.y, 38, 'auto-flow-column-spans-0301 item[3].y');
      expectClose(r.width, 80, 'auto-flow-column-spans-0301 item[3].w');
      expectClose(r.height, 30, 'auto-flow-column-spans-0301 item[3].h');
    }
    {
      const r = rectOf(itemRefs[4]);
      expectClose(r.x - container.x, 0, 'auto-flow-column-spans-0301 item[4].x');
      expectClose(r.y - container.y, 76, 'auto-flow-column-spans-0301 item[4].y');
      expectClose(r.width, 80, 'auto-flow-column-spans-0301 item[4].w');
      expectClose(r.height, 30, 'auto-flow-column-spans-0301 item[4].h');
    }
  });
  it("auto-flow-column-spans-0304: grid-auto-flow: column with row-spanning items", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid","width":600,"gridTemplateColumns":"80px 80px 80px","gridTemplateRows":"30px 30px 30px","gap":8,"gridAutoFlow":"column"}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"gridRowEnd":"span 2"}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"gridRowEnd":"span 2"}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{}}
          />
          <View
            collapsable={false}
            ref={itemRefs[4]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'auto-flow-column-spans-0304 container.width');
    expectClose(container.height, 106, 'auto-flow-column-spans-0304 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'auto-flow-column-spans-0304 item[0].x');
      expectClose(r.y - container.y, 0, 'auto-flow-column-spans-0304 item[0].y');
      expectClose(r.width, 80, 'auto-flow-column-spans-0304 item[0].w');
      expectClose(r.height, 68, 'auto-flow-column-spans-0304 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 0, 'auto-flow-column-spans-0304 item[1].x');
      expectClose(r.y - container.y, 76, 'auto-flow-column-spans-0304 item[1].y');
      expectClose(r.width, 80, 'auto-flow-column-spans-0304 item[1].w');
      expectClose(r.height, 30, 'auto-flow-column-spans-0304 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 88, 'auto-flow-column-spans-0304 item[2].x');
      expectClose(r.y - container.y, 0, 'auto-flow-column-spans-0304 item[2].y');
      expectClose(r.width, 80, 'auto-flow-column-spans-0304 item[2].w');
      expectClose(r.height, 68, 'auto-flow-column-spans-0304 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 88, 'auto-flow-column-spans-0304 item[3].x');
      expectClose(r.y - container.y, 76, 'auto-flow-column-spans-0304 item[3].y');
      expectClose(r.width, 80, 'auto-flow-column-spans-0304 item[3].w');
      expectClose(r.height, 30, 'auto-flow-column-spans-0304 item[3].h');
    }
    {
      const r = rectOf(itemRefs[4]);
      expectClose(r.x - container.x, 176, 'auto-flow-column-spans-0304 item[4].x');
      expectClose(r.y - container.y, 0, 'auto-flow-column-spans-0304 item[4].y');
      expectClose(r.width, 80, 'auto-flow-column-spans-0304 item[4].w');
      expectClose(r.height, 30, 'auto-flow-column-spans-0304 item[4].h');
    }
  });
  it("auto-flow-column-spans-0307: grid-auto-flow: row dense with row-spanning items", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid","width":600,"gridTemplateColumns":"80px 80px 80px","gridTemplateRows":"30px 30px 30px","gap":8,"gridAutoFlow":"row dense"}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"gridRowEnd":"span 2"}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"gridRowEnd":"span 2"}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{}}
          />
          <View
            collapsable={false}
            ref={itemRefs[4]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'auto-flow-column-spans-0307 container.width');
    expectClose(container.height, 106, 'auto-flow-column-spans-0307 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'auto-flow-column-spans-0307 item[0].x');
      expectClose(r.y - container.y, 0, 'auto-flow-column-spans-0307 item[0].y');
      expectClose(r.width, 80, 'auto-flow-column-spans-0307 item[0].w');
      expectClose(r.height, 68, 'auto-flow-column-spans-0307 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 88, 'auto-flow-column-spans-0307 item[1].x');
      expectClose(r.y - container.y, 0, 'auto-flow-column-spans-0307 item[1].y');
      expectClose(r.width, 80, 'auto-flow-column-spans-0307 item[1].w');
      expectClose(r.height, 30, 'auto-flow-column-spans-0307 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 176, 'auto-flow-column-spans-0307 item[2].x');
      expectClose(r.y - container.y, 0, 'auto-flow-column-spans-0307 item[2].y');
      expectClose(r.width, 80, 'auto-flow-column-spans-0307 item[2].w');
      expectClose(r.height, 68, 'auto-flow-column-spans-0307 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 88, 'auto-flow-column-spans-0307 item[3].x');
      expectClose(r.y - container.y, 38, 'auto-flow-column-spans-0307 item[3].y');
      expectClose(r.width, 80, 'auto-flow-column-spans-0307 item[3].w');
      expectClose(r.height, 30, 'auto-flow-column-spans-0307 item[3].h');
    }
    {
      const r = rectOf(itemRefs[4]);
      expectClose(r.x - container.x, 0, 'auto-flow-column-spans-0307 item[4].x');
      expectClose(r.y - container.y, 76, 'auto-flow-column-spans-0307 item[4].y');
      expectClose(r.width, 80, 'auto-flow-column-spans-0307 item[4].w');
      expectClose(r.height, 30, 'auto-flow-column-spans-0307 item[4].h');
    }
  });
  it("auto-flow-column-spans-0310: grid-auto-flow: column dense with row-spanning items", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid","width":600,"gridTemplateColumns":"80px 80px 80px","gridTemplateRows":"30px 30px 30px","gap":8,"gridAutoFlow":"column dense"}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"gridRowEnd":"span 2"}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"gridRowEnd":"span 2"}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{}}
          />
          <View
            collapsable={false}
            ref={itemRefs[4]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'auto-flow-column-spans-0310 container.width');
    expectClose(container.height, 106, 'auto-flow-column-spans-0310 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'auto-flow-column-spans-0310 item[0].x');
      expectClose(r.y - container.y, 0, 'auto-flow-column-spans-0310 item[0].y');
      expectClose(r.width, 80, 'auto-flow-column-spans-0310 item[0].w');
      expectClose(r.height, 68, 'auto-flow-column-spans-0310 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 0, 'auto-flow-column-spans-0310 item[1].x');
      expectClose(r.y - container.y, 76, 'auto-flow-column-spans-0310 item[1].y');
      expectClose(r.width, 80, 'auto-flow-column-spans-0310 item[1].w');
      expectClose(r.height, 30, 'auto-flow-column-spans-0310 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 88, 'auto-flow-column-spans-0310 item[2].x');
      expectClose(r.y - container.y, 0, 'auto-flow-column-spans-0310 item[2].y');
      expectClose(r.width, 80, 'auto-flow-column-spans-0310 item[2].w');
      expectClose(r.height, 68, 'auto-flow-column-spans-0310 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 88, 'auto-flow-column-spans-0310 item[3].x');
      expectClose(r.y - container.y, 76, 'auto-flow-column-spans-0310 item[3].y');
      expectClose(r.width, 80, 'auto-flow-column-spans-0310 item[3].w');
      expectClose(r.height, 30, 'auto-flow-column-spans-0310 item[3].h');
    }
    {
      const r = rectOf(itemRefs[4]);
      expectClose(r.x - container.x, 176, 'auto-flow-column-spans-0310 item[4].x');
      expectClose(r.y - container.y, 0, 'auto-flow-column-spans-0310 item[4].y');
      expectClose(r.width, 80, 'auto-flow-column-spans-0310 item[4].w');
      expectClose(r.height, 30, 'auto-flow-column-spans-0310 item[4].h');
    }
  });
});

describe("auto-flow-column-implicit", () => {
  it("auto-flow-column-implicit-0302: grid-auto-flow: row creating implicit tracks", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid","width":600,"gridTemplateColumns":"70px","gridTemplateRows":"40px 40px","gap":10,"gridAutoFlow":"row"}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{}}
          />
          <View
            collapsable={false}
            ref={itemRefs[4]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'auto-flow-column-implicit-0302 container.width');
    expectClose(container.height, 120, 'auto-flow-column-implicit-0302 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'auto-flow-column-implicit-0302 item[0].x');
      expectClose(r.y - container.y, 0, 'auto-flow-column-implicit-0302 item[0].y');
      expectClose(r.width, 70, 'auto-flow-column-implicit-0302 item[0].w');
      expectClose(r.height, 40, 'auto-flow-column-implicit-0302 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 0, 'auto-flow-column-implicit-0302 item[1].x');
      expectClose(r.y - container.y, 50, 'auto-flow-column-implicit-0302 item[1].y');
      expectClose(r.width, 70, 'auto-flow-column-implicit-0302 item[1].w');
      expectClose(r.height, 40, 'auto-flow-column-implicit-0302 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 0, 'auto-flow-column-implicit-0302 item[2].x');
      expectClose(r.y - container.y, 100, 'auto-flow-column-implicit-0302 item[2].y');
      expectClose(r.width, 70, 'auto-flow-column-implicit-0302 item[2].w');
      expectClose(r.height, 0, 'auto-flow-column-implicit-0302 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 0, 'auto-flow-column-implicit-0302 item[3].x');
      expectClose(r.y - container.y, 110, 'auto-flow-column-implicit-0302 item[3].y');
      expectClose(r.width, 70, 'auto-flow-column-implicit-0302 item[3].w');
      expectClose(r.height, 0, 'auto-flow-column-implicit-0302 item[3].h');
    }
    {
      const r = rectOf(itemRefs[4]);
      expectClose(r.x - container.x, 0, 'auto-flow-column-implicit-0302 item[4].x');
      expectClose(r.y - container.y, 120, 'auto-flow-column-implicit-0302 item[4].y');
      expectClose(r.width, 70, 'auto-flow-column-implicit-0302 item[4].w');
      expectClose(r.height, 0, 'auto-flow-column-implicit-0302 item[4].h');
    }
  });
  it("auto-flow-column-implicit-0305: grid-auto-flow: column creating implicit tracks", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid","width":600,"gridTemplateColumns":"70px","gridTemplateRows":"40px 40px","gap":10,"gridAutoFlow":"column"}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{}}
          />
          <View
            collapsable={false}
            ref={itemRefs[4]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'auto-flow-column-implicit-0305 container.width');
    expectClose(container.height, 90, 'auto-flow-column-implicit-0305 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'auto-flow-column-implicit-0305 item[0].x');
      expectClose(r.y - container.y, 0, 'auto-flow-column-implicit-0305 item[0].y');
      expectClose(r.width, 70, 'auto-flow-column-implicit-0305 item[0].w');
      expectClose(r.height, 40, 'auto-flow-column-implicit-0305 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 0, 'auto-flow-column-implicit-0305 item[1].x');
      expectClose(r.y - container.y, 50, 'auto-flow-column-implicit-0305 item[1].y');
      expectClose(r.width, 70, 'auto-flow-column-implicit-0305 item[1].w');
      expectClose(r.height, 40, 'auto-flow-column-implicit-0305 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 80, 'auto-flow-column-implicit-0305 item[2].x');
      expectClose(r.y - container.y, 0, 'auto-flow-column-implicit-0305 item[2].y');
      expectClose(r.width, 255, 'auto-flow-column-implicit-0305 item[2].w');
      expectClose(r.height, 40, 'auto-flow-column-implicit-0305 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 80, 'auto-flow-column-implicit-0305 item[3].x');
      expectClose(r.y - container.y, 50, 'auto-flow-column-implicit-0305 item[3].y');
      expectClose(r.width, 255, 'auto-flow-column-implicit-0305 item[3].w');
      expectClose(r.height, 40, 'auto-flow-column-implicit-0305 item[3].h');
    }
    {
      const r = rectOf(itemRefs[4]);
      expectClose(r.x - container.x, 345, 'auto-flow-column-implicit-0305 item[4].x');
      expectClose(r.y - container.y, 0, 'auto-flow-column-implicit-0305 item[4].y');
      expectClose(r.width, 255, 'auto-flow-column-implicit-0305 item[4].w');
      expectClose(r.height, 40, 'auto-flow-column-implicit-0305 item[4].h');
    }
  });
  it("auto-flow-column-implicit-0308: grid-auto-flow: row dense creating implicit tracks", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid","width":600,"gridTemplateColumns":"70px","gridTemplateRows":"40px 40px","gap":10,"gridAutoFlow":"row dense"}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{}}
          />
          <View
            collapsable={false}
            ref={itemRefs[4]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'auto-flow-column-implicit-0308 container.width');
    expectClose(container.height, 120, 'auto-flow-column-implicit-0308 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'auto-flow-column-implicit-0308 item[0].x');
      expectClose(r.y - container.y, 0, 'auto-flow-column-implicit-0308 item[0].y');
      expectClose(r.width, 70, 'auto-flow-column-implicit-0308 item[0].w');
      expectClose(r.height, 40, 'auto-flow-column-implicit-0308 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 0, 'auto-flow-column-implicit-0308 item[1].x');
      expectClose(r.y - container.y, 50, 'auto-flow-column-implicit-0308 item[1].y');
      expectClose(r.width, 70, 'auto-flow-column-implicit-0308 item[1].w');
      expectClose(r.height, 40, 'auto-flow-column-implicit-0308 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 0, 'auto-flow-column-implicit-0308 item[2].x');
      expectClose(r.y - container.y, 100, 'auto-flow-column-implicit-0308 item[2].y');
      expectClose(r.width, 70, 'auto-flow-column-implicit-0308 item[2].w');
      expectClose(r.height, 0, 'auto-flow-column-implicit-0308 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 0, 'auto-flow-column-implicit-0308 item[3].x');
      expectClose(r.y - container.y, 110, 'auto-flow-column-implicit-0308 item[3].y');
      expectClose(r.width, 70, 'auto-flow-column-implicit-0308 item[3].w');
      expectClose(r.height, 0, 'auto-flow-column-implicit-0308 item[3].h');
    }
    {
      const r = rectOf(itemRefs[4]);
      expectClose(r.x - container.x, 0, 'auto-flow-column-implicit-0308 item[4].x');
      expectClose(r.y - container.y, 120, 'auto-flow-column-implicit-0308 item[4].y');
      expectClose(r.width, 70, 'auto-flow-column-implicit-0308 item[4].w');
      expectClose(r.height, 0, 'auto-flow-column-implicit-0308 item[4].h');
    }
  });
  it("auto-flow-column-implicit-0311: grid-auto-flow: column dense creating implicit tracks", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid","width":600,"gridTemplateColumns":"70px","gridTemplateRows":"40px 40px","gap":10,"gridAutoFlow":"column dense"}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{}}
          />
          <View
            collapsable={false}
            ref={itemRefs[4]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'auto-flow-column-implicit-0311 container.width');
    expectClose(container.height, 90, 'auto-flow-column-implicit-0311 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'auto-flow-column-implicit-0311 item[0].x');
      expectClose(r.y - container.y, 0, 'auto-flow-column-implicit-0311 item[0].y');
      expectClose(r.width, 70, 'auto-flow-column-implicit-0311 item[0].w');
      expectClose(r.height, 40, 'auto-flow-column-implicit-0311 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 0, 'auto-flow-column-implicit-0311 item[1].x');
      expectClose(r.y - container.y, 50, 'auto-flow-column-implicit-0311 item[1].y');
      expectClose(r.width, 70, 'auto-flow-column-implicit-0311 item[1].w');
      expectClose(r.height, 40, 'auto-flow-column-implicit-0311 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 80, 'auto-flow-column-implicit-0311 item[2].x');
      expectClose(r.y - container.y, 0, 'auto-flow-column-implicit-0311 item[2].y');
      expectClose(r.width, 255, 'auto-flow-column-implicit-0311 item[2].w');
      expectClose(r.height, 40, 'auto-flow-column-implicit-0311 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 80, 'auto-flow-column-implicit-0311 item[3].x');
      expectClose(r.y - container.y, 50, 'auto-flow-column-implicit-0311 item[3].y');
      expectClose(r.width, 255, 'auto-flow-column-implicit-0311 item[3].w');
      expectClose(r.height, 40, 'auto-flow-column-implicit-0311 item[3].h');
    }
    {
      const r = rectOf(itemRefs[4]);
      expectClose(r.x - container.x, 345, 'auto-flow-column-implicit-0311 item[4].x');
      expectClose(r.y - container.y, 0, 'auto-flow-column-implicit-0311 item[4].y');
      expectClose(r.width, 255, 'auto-flow-column-implicit-0311 item[4].w');
      expectClose(r.height, 40, 'auto-flow-column-implicit-0311 item[4].h');
    }
  });
});

describe("template-areas-basic", () => {
  it("template-areas-basic-0312: a classic header / sidebar / main template", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid","width":600,"gridTemplateColumns":"120px 120px","gridTemplateRows":"40px 60px","gap":10,"gridTemplateAreas":"\"header header\" \"sidebar main\""}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"gridArea":"header"}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"gridArea":"sidebar"}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"gridArea":"main"}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'template-areas-basic-0312 container.width');
    expectClose(container.height, 110, 'template-areas-basic-0312 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'template-areas-basic-0312 item[0].x');
      expectClose(r.y - container.y, 0, 'template-areas-basic-0312 item[0].y');
      expectClose(r.width, 250, 'template-areas-basic-0312 item[0].w');
      expectClose(r.height, 40, 'template-areas-basic-0312 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 0, 'template-areas-basic-0312 item[1].x');
      expectClose(r.y - container.y, 50, 'template-areas-basic-0312 item[1].y');
      expectClose(r.width, 120, 'template-areas-basic-0312 item[1].w');
      expectClose(r.height, 60, 'template-areas-basic-0312 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 130, 'template-areas-basic-0312 item[2].x');
      expectClose(r.y - container.y, 50, 'template-areas-basic-0312 item[2].y');
      expectClose(r.width, 120, 'template-areas-basic-0312 item[2].w');
      expectClose(r.height, 60, 'template-areas-basic-0312 item[2].h');
    }
  });
});

describe("template-areas-equivalent-lines", () => {
  it("template-areas-equivalent-lines-0313: the same placement by line number", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid","width":600,"gridTemplateColumns":"120px 120px","gridTemplateRows":"40px 60px","gap":10}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"gridColumnEnd":3,"gridColumnStart":1,"gridRowStart":1}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"gridColumnStart":1,"gridRowStart":2}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"gridColumnStart":2,"gridRowStart":2}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'template-areas-equivalent-lines-0313 container.width');
    expectClose(container.height, 110, 'template-areas-equivalent-lines-0313 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'template-areas-equivalent-lines-0313 item[0].x');
      expectClose(r.y - container.y, 0, 'template-areas-equivalent-lines-0313 item[0].y');
      expectClose(r.width, 250, 'template-areas-equivalent-lines-0313 item[0].w');
      expectClose(r.height, 40, 'template-areas-equivalent-lines-0313 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 0, 'template-areas-equivalent-lines-0313 item[1].x');
      expectClose(r.y - container.y, 50, 'template-areas-equivalent-lines-0313 item[1].y');
      expectClose(r.width, 120, 'template-areas-equivalent-lines-0313 item[1].w');
      expectClose(r.height, 60, 'template-areas-equivalent-lines-0313 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 130, 'template-areas-equivalent-lines-0313 item[2].x');
      expectClose(r.y - container.y, 50, 'template-areas-equivalent-lines-0313 item[2].y');
      expectClose(r.width, 120, 'template-areas-equivalent-lines-0313 item[2].w');
      expectClose(r.height, 60, 'template-areas-equivalent-lines-0313 item[2].h');
    }
  });
});

describe("template-areas-null-cells", () => {
  it("template-areas-null-cells-0314: null cells leave a hole in the template", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid","width":600,"gridTemplateColumns":"80px 80px 80px","gridTemplateRows":"40px 40px","gap":8,"gridTemplateAreas":"\"nav . aside\" \"nav main aside\""}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"gridArea":"nav"}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"gridArea":"main"}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"gridArea":"aside"}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'template-areas-null-cells-0314 container.width');
    expectClose(container.height, 88, 'template-areas-null-cells-0314 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'template-areas-null-cells-0314 item[0].x');
      expectClose(r.y - container.y, 0, 'template-areas-null-cells-0314 item[0].y');
      expectClose(r.width, 80, 'template-areas-null-cells-0314 item[0].w');
      expectClose(r.height, 88, 'template-areas-null-cells-0314 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 88, 'template-areas-null-cells-0314 item[1].x');
      expectClose(r.y - container.y, 48, 'template-areas-null-cells-0314 item[1].y');
      expectClose(r.width, 80, 'template-areas-null-cells-0314 item[1].w');
      expectClose(r.height, 40, 'template-areas-null-cells-0314 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 176, 'template-areas-null-cells-0314 item[2].x');
      expectClose(r.y - container.y, 0, 'template-areas-null-cells-0314 item[2].y');
      expectClose(r.width, 80, 'template-areas-null-cells-0314 item[2].w');
      expectClose(r.height, 88, 'template-areas-null-cells-0314 item[2].h');
    }
  });
});

describe("template-areas-sizes-grid", () => {
  it("template-areas-sizes-grid-0315: the template alone defines a 2x2 explicit grid", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid","width":400,"gap":10,"gridTemplateAreas":"\"a b\" \"c d\""}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30,"gridArea":"a"}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30,"gridArea":"b"}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30,"gridArea":"c"}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30,"gridArea":"d"}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 400, 'template-areas-sizes-grid-0315 container.width');
    expectClose(container.height, 70, 'template-areas-sizes-grid-0315 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'template-areas-sizes-grid-0315 item[0].x');
      expectClose(r.y - container.y, 0, 'template-areas-sizes-grid-0315 item[0].y');
      expectClose(r.width, 195, 'template-areas-sizes-grid-0315 item[0].w');
      expectClose(r.height, 30, 'template-areas-sizes-grid-0315 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 205, 'template-areas-sizes-grid-0315 item[1].x');
      expectClose(r.y - container.y, 0, 'template-areas-sizes-grid-0315 item[1].y');
      expectClose(r.width, 195, 'template-areas-sizes-grid-0315 item[1].w');
      expectClose(r.height, 30, 'template-areas-sizes-grid-0315 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 0, 'template-areas-sizes-grid-0315 item[2].x');
      expectClose(r.y - container.y, 40, 'template-areas-sizes-grid-0315 item[2].y');
      expectClose(r.width, 195, 'template-areas-sizes-grid-0315 item[2].w');
      expectClose(r.height, 30, 'template-areas-sizes-grid-0315 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 205, 'template-areas-sizes-grid-0315 item[3].x');
      expectClose(r.y - container.y, 40, 'template-areas-sizes-grid-0315 item[3].y');
      expectClose(r.width, 195, 'template-areas-sizes-grid-0315 item[3].w');
      expectClose(r.height, 30, 'template-areas-sizes-grid-0315 item[3].h');
    }
  });
});

describe("lanes-content-sized", () => {
  it("lanes-content-sized-0316: content-sized items at flow-tolerance 0", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid-lanes","width":600,"gridTemplateColumns":"1fr 1fr 1fr","gap":10,"flowTolerance":0}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{}}>
            <View style={{height: 50}} />
          </View>
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{}}>
            <View style={{height: 30}} />
          </View>
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{}}>
            <View style={{height: 70}} />
          </View>
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{}}>
            <View style={{height: 40}} />
          </View>
          <View
            collapsable={false}
            ref={itemRefs[4]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{}}>
            <View style={{height: 60}} />
          </View>
          <View
            collapsable={false}
            ref={itemRefs[5]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{}}>
            <View style={{height: 20}} />
          </View>
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'lanes-content-sized-0316 container.width');
    expectClose(container.height, 120, 'lanes-content-sized-0316 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'lanes-content-sized-0316 item[0].x');
      expectClose(r.y - container.y, 0, 'lanes-content-sized-0316 item[0].y');
      expectClose(r.width, 193.33, 'lanes-content-sized-0316 item[0].w');
      expectClose(r.height, 50, 'lanes-content-sized-0316 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 203.33, 'lanes-content-sized-0316 item[1].x');
      expectClose(r.y - container.y, 0, 'lanes-content-sized-0316 item[1].y');
      expectClose(r.width, 193.33, 'lanes-content-sized-0316 item[1].w');
      expectClose(r.height, 30, 'lanes-content-sized-0316 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 406.66, 'lanes-content-sized-0316 item[2].x');
      expectClose(r.y - container.y, 0, 'lanes-content-sized-0316 item[2].y');
      expectClose(r.width, 193.34, 'lanes-content-sized-0316 item[2].w');
      expectClose(r.height, 70, 'lanes-content-sized-0316 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 203.33, 'lanes-content-sized-0316 item[3].x');
      expectClose(r.y - container.y, 40, 'lanes-content-sized-0316 item[3].y');
      expectClose(r.width, 193.33, 'lanes-content-sized-0316 item[3].w');
      expectClose(r.height, 40, 'lanes-content-sized-0316 item[3].h');
    }
    {
      const r = rectOf(itemRefs[4]);
      expectClose(r.x - container.x, 0, 'lanes-content-sized-0316 item[4].x');
      expectClose(r.y - container.y, 60, 'lanes-content-sized-0316 item[4].y');
      expectClose(r.width, 193.33, 'lanes-content-sized-0316 item[4].w');
      expectClose(r.height, 60, 'lanes-content-sized-0316 item[4].h');
    }
    {
      const r = rectOf(itemRefs[5]);
      expectClose(r.x - container.x, 406.66, 'lanes-content-sized-0316 item[5].x');
      expectClose(r.y - container.y, 80, 'lanes-content-sized-0316 item[5].y');
      expectClose(r.width, 193.34, 'lanes-content-sized-0316 item[5].w');
      expectClose(r.height, 20, 'lanes-content-sized-0316 item[5].h');
    }
  });
  it("lanes-content-sized-0317: content-sized items at flow-tolerance normal", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid-lanes","width":600,"gridTemplateColumns":"1fr 1fr 1fr","gap":10,"flowTolerance":"normal"}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{}}>
            <View style={{height: 50}} />
          </View>
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{}}>
            <View style={{height: 30}} />
          </View>
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{}}>
            <View style={{height: 70}} />
          </View>
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{}}>
            <View style={{height: 40}} />
          </View>
          <View
            collapsable={false}
            ref={itemRefs[4]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{}}>
            <View style={{height: 60}} />
          </View>
          <View
            collapsable={false}
            ref={itemRefs[5]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{}}>
            <View style={{height: 20}} />
          </View>
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'lanes-content-sized-0317 container.width');
    expectClose(container.height, 120, 'lanes-content-sized-0317 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'lanes-content-sized-0317 item[0].x');
      expectClose(r.y - container.y, 0, 'lanes-content-sized-0317 item[0].y');
      expectClose(r.width, 193.33, 'lanes-content-sized-0317 item[0].w');
      expectClose(r.height, 50, 'lanes-content-sized-0317 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 203.33, 'lanes-content-sized-0317 item[1].x');
      expectClose(r.y - container.y, 0, 'lanes-content-sized-0317 item[1].y');
      expectClose(r.width, 193.33, 'lanes-content-sized-0317 item[1].w');
      expectClose(r.height, 30, 'lanes-content-sized-0317 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 406.66, 'lanes-content-sized-0317 item[2].x');
      expectClose(r.y - container.y, 0, 'lanes-content-sized-0317 item[2].y');
      expectClose(r.width, 193.34, 'lanes-content-sized-0317 item[2].w');
      expectClose(r.height, 70, 'lanes-content-sized-0317 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 203.33, 'lanes-content-sized-0317 item[3].x');
      expectClose(r.y - container.y, 40, 'lanes-content-sized-0317 item[3].y');
      expectClose(r.width, 193.33, 'lanes-content-sized-0317 item[3].w');
      expectClose(r.height, 40, 'lanes-content-sized-0317 item[3].h');
    }
    {
      const r = rectOf(itemRefs[4]);
      expectClose(r.x - container.x, 0, 'lanes-content-sized-0317 item[4].x');
      expectClose(r.y - container.y, 60, 'lanes-content-sized-0317 item[4].y');
      expectClose(r.width, 193.33, 'lanes-content-sized-0317 item[4].w');
      expectClose(r.height, 60, 'lanes-content-sized-0317 item[4].h');
    }
    {
      const r = rectOf(itemRefs[5]);
      expectClose(r.x - container.x, 203.33, 'lanes-content-sized-0317 item[5].x');
      expectClose(r.y - container.y, 90, 'lanes-content-sized-0317 item[5].y');
      expectClose(r.width, 193.33, 'lanes-content-sized-0317 item[5].w');
      expectClose(r.height, 20, 'lanes-content-sized-0317 item[5].h');
    }
  });
});

describe("lanes-content-sized-padded", () => {
  it("lanes-content-sized-padded-0318: content-sized items with padding", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid-lanes","width":600,"gridTemplateColumns":"1fr 1fr","gap":10,"flowTolerance":0}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"padding":8}}>
            <View style={{height: 40}} />
          </View>
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"padding":8}}>
            <View style={{height: 20}} />
          </View>
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"padding":8}}>
            <View style={{height: 60}} />
          </View>
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"padding":8}}>
            <View style={{height: 30}} />
          </View>
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'lanes-content-sized-padded-0318 container.width');
    expectClose(container.height, 122, 'lanes-content-sized-padded-0318 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'lanes-content-sized-padded-0318 item[0].x');
      expectClose(r.y - container.y, 0, 'lanes-content-sized-padded-0318 item[0].y');
      expectClose(r.width, 295, 'lanes-content-sized-padded-0318 item[0].w');
      expectClose(r.height, 56, 'lanes-content-sized-padded-0318 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 305, 'lanes-content-sized-padded-0318 item[1].x');
      expectClose(r.y - container.y, 0, 'lanes-content-sized-padded-0318 item[1].y');
      expectClose(r.width, 295, 'lanes-content-sized-padded-0318 item[1].w');
      expectClose(r.height, 36, 'lanes-content-sized-padded-0318 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 305, 'lanes-content-sized-padded-0318 item[2].x');
      expectClose(r.y - container.y, 46, 'lanes-content-sized-padded-0318 item[2].y');
      expectClose(r.width, 295, 'lanes-content-sized-padded-0318 item[2].w');
      expectClose(r.height, 76, 'lanes-content-sized-padded-0318 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 0, 'lanes-content-sized-padded-0318 item[3].x');
      expectClose(r.y - container.y, 66, 'lanes-content-sized-padded-0318 item[3].y');
      expectClose(r.width, 295, 'lanes-content-sized-padded-0318 item[3].w');
      expectClose(r.height, 46, 'lanes-content-sized-padded-0318 item[3].h');
    }
  });
});

describe("lanes-aspect-ratio", () => {
  it("lanes-aspect-ratio-0319: aspect-ratio items around 1 in lanes", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid-lanes","width":620,"gridTemplateColumns":"1fr 1fr 1fr","gap":10,"flowTolerance":0}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"aspectRatio":1}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"aspectRatio":2}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"aspectRatio":1}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"aspectRatio":0.5}}
          />
          <View
            collapsable={false}
            ref={itemRefs[4]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"aspectRatio":1}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 620, 'lanes-aspect-ratio-0319 container.width');
    expectClose(container.height, 510, 'lanes-aspect-ratio-0319 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'lanes-aspect-ratio-0319 item[0].x');
      expectClose(r.y - container.y, 0, 'lanes-aspect-ratio-0319 item[0].y');
      expectClose(r.width, 200, 'lanes-aspect-ratio-0319 item[0].w');
      expectClose(r.height, 200, 'lanes-aspect-ratio-0319 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 210, 'lanes-aspect-ratio-0319 item[1].x');
      expectClose(r.y - container.y, 0, 'lanes-aspect-ratio-0319 item[1].y');
      expectClose(r.width, 200, 'lanes-aspect-ratio-0319 item[1].w');
      expectClose(r.height, 100, 'lanes-aspect-ratio-0319 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 420, 'lanes-aspect-ratio-0319 item[2].x');
      expectClose(r.y - container.y, 0, 'lanes-aspect-ratio-0319 item[2].y');
      expectClose(r.width, 200, 'lanes-aspect-ratio-0319 item[2].w');
      expectClose(r.height, 200, 'lanes-aspect-ratio-0319 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 210, 'lanes-aspect-ratio-0319 item[3].x');
      expectClose(r.y - container.y, 110, 'lanes-aspect-ratio-0319 item[3].y');
      expectClose(r.width, 200, 'lanes-aspect-ratio-0319 item[3].w');
      expectClose(r.height, 400, 'lanes-aspect-ratio-0319 item[3].h');
    }
    {
      const r = rectOf(itemRefs[4]);
      expectClose(r.x - container.x, 420, 'lanes-aspect-ratio-0319 item[4].x');
      expectClose(r.y - container.y, 210, 'lanes-aspect-ratio-0319 item[4].y');
      expectClose(r.width, 200, 'lanes-aspect-ratio-0319 item[4].w');
      expectClose(r.height, 200, 'lanes-aspect-ratio-0319 item[4].h');
    }
  });
  it("lanes-aspect-ratio-0320: aspect-ratio items around 1.5 in lanes", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid-lanes","width":620,"gridTemplateColumns":"1fr 1fr 1fr","gap":10,"flowTolerance":0}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"aspectRatio":1.5}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"aspectRatio":3}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"aspectRatio":1.5}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"aspectRatio":0.75}}
          />
          <View
            collapsable={false}
            ref={itemRefs[4]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"aspectRatio":1.5}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 620, 'lanes-aspect-ratio-0320 container.width');
    expectClose(container.height, 343.31, 'lanes-aspect-ratio-0320 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'lanes-aspect-ratio-0320 item[0].x');
      expectClose(r.y - container.y, 0, 'lanes-aspect-ratio-0320 item[0].y');
      expectClose(r.width, 200, 'lanes-aspect-ratio-0320 item[0].w');
      expectClose(r.height, 133.33, 'lanes-aspect-ratio-0320 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 210, 'lanes-aspect-ratio-0320 item[1].x');
      expectClose(r.y - container.y, 0, 'lanes-aspect-ratio-0320 item[1].y');
      expectClose(r.width, 200, 'lanes-aspect-ratio-0320 item[1].w');
      expectClose(r.height, 66.66, 'lanes-aspect-ratio-0320 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 420, 'lanes-aspect-ratio-0320 item[2].x');
      expectClose(r.y - container.y, 0, 'lanes-aspect-ratio-0320 item[2].y');
      expectClose(r.width, 200, 'lanes-aspect-ratio-0320 item[2].w');
      expectClose(r.height, 133.33, 'lanes-aspect-ratio-0320 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 210, 'lanes-aspect-ratio-0320 item[3].x');
      expectClose(r.y - container.y, 76.66, 'lanes-aspect-ratio-0320 item[3].y');
      expectClose(r.width, 200, 'lanes-aspect-ratio-0320 item[3].w');
      expectClose(r.height, 266.66, 'lanes-aspect-ratio-0320 item[3].h');
    }
    {
      const r = rectOf(itemRefs[4]);
      expectClose(r.x - container.x, 420, 'lanes-aspect-ratio-0320 item[4].x');
      expectClose(r.y - container.y, 143.33, 'lanes-aspect-ratio-0320 item[4].y');
      expectClose(r.width, 200, 'lanes-aspect-ratio-0320 item[4].w');
      expectClose(r.height, 133.33, 'lanes-aspect-ratio-0320 item[4].h');
    }
  });
});

describe("lanes-mixed-sizing", () => {
  it("lanes-mixed-sizing-0321: explicit and content-sized items in one container", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid-lanes","width":600,"gridTemplateColumns":"1fr 1fr 1fr","gap":10,"flowTolerance":0}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":50}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{}}>
            <View style={{height: 30}} />
          </View>
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":70}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{}}>
            <View style={{height: 40}} />
          </View>
          <View
            collapsable={false}
            ref={itemRefs[4]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":20}}
          />
          <View
            collapsable={false}
            ref={itemRefs[5]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{}}>
            <View style={{height: 60}} />
          </View>
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'lanes-mixed-sizing-0321 container.width');
    expectClose(container.height, 140, 'lanes-mixed-sizing-0321 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'lanes-mixed-sizing-0321 item[0].x');
      expectClose(r.y - container.y, 0, 'lanes-mixed-sizing-0321 item[0].y');
      expectClose(r.width, 193.33, 'lanes-mixed-sizing-0321 item[0].w');
      expectClose(r.height, 50, 'lanes-mixed-sizing-0321 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 203.33, 'lanes-mixed-sizing-0321 item[1].x');
      expectClose(r.y - container.y, 0, 'lanes-mixed-sizing-0321 item[1].y');
      expectClose(r.width, 193.33, 'lanes-mixed-sizing-0321 item[1].w');
      expectClose(r.height, 30, 'lanes-mixed-sizing-0321 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 406.66, 'lanes-mixed-sizing-0321 item[2].x');
      expectClose(r.y - container.y, 0, 'lanes-mixed-sizing-0321 item[2].y');
      expectClose(r.width, 193.34, 'lanes-mixed-sizing-0321 item[2].w');
      expectClose(r.height, 70, 'lanes-mixed-sizing-0321 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 203.33, 'lanes-mixed-sizing-0321 item[3].x');
      expectClose(r.y - container.y, 40, 'lanes-mixed-sizing-0321 item[3].y');
      expectClose(r.width, 193.33, 'lanes-mixed-sizing-0321 item[3].w');
      expectClose(r.height, 40, 'lanes-mixed-sizing-0321 item[3].h');
    }
    {
      const r = rectOf(itemRefs[4]);
      expectClose(r.x - container.x, 0, 'lanes-mixed-sizing-0321 item[4].x');
      expectClose(r.y - container.y, 60, 'lanes-mixed-sizing-0321 item[4].y');
      expectClose(r.width, 193.33, 'lanes-mixed-sizing-0321 item[4].w');
      expectClose(r.height, 20, 'lanes-mixed-sizing-0321 item[4].h');
    }
    {
      const r = rectOf(itemRefs[5]);
      expectClose(r.x - container.x, 406.66, 'lanes-mixed-sizing-0321 item[5].x');
      expectClose(r.y - container.y, 80, 'lanes-mixed-sizing-0321 item[5].y');
      expectClose(r.width, 193.34, 'lanes-mixed-sizing-0321 item[5].w');
      expectClose(r.height, 60, 'lanes-mixed-sizing-0321 item[5].h');
    }
  });
});

describe("lanes-aspect-ratio-with-content", () => {
  it("lanes-aspect-ratio-with-content-0322: aspect-ratio 1.5 tiles with padding and content", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid-lanes","width":620,"gridTemplateColumns":"1fr 1fr 1fr","gap":10,"flowTolerance":0}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"padding":6,"aspectRatio":1.5}}>
            <View style={{height: 12}} />
          </View>
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"padding":6,"aspectRatio":3}}>
            <View style={{height: 12}} />
          </View>
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"padding":6,"aspectRatio":1.5}}>
            <View style={{height: 12}} />
          </View>
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"padding":6,"aspectRatio":0.75}}>
            <View style={{height: 12}} />
          </View>
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 620, 'lanes-aspect-ratio-with-content-0322 container.width');
    expectClose(container.height, 343.31, 'lanes-aspect-ratio-with-content-0322 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'lanes-aspect-ratio-with-content-0322 item[0].x');
      expectClose(r.y - container.y, 0, 'lanes-aspect-ratio-with-content-0322 item[0].y');
      expectClose(r.width, 200, 'lanes-aspect-ratio-with-content-0322 item[0].w');
      expectClose(r.height, 133.33, 'lanes-aspect-ratio-with-content-0322 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 210, 'lanes-aspect-ratio-with-content-0322 item[1].x');
      expectClose(r.y - container.y, 0, 'lanes-aspect-ratio-with-content-0322 item[1].y');
      expectClose(r.width, 200, 'lanes-aspect-ratio-with-content-0322 item[1].w');
      expectClose(r.height, 66.66, 'lanes-aspect-ratio-with-content-0322 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 420, 'lanes-aspect-ratio-with-content-0322 item[2].x');
      expectClose(r.y - container.y, 0, 'lanes-aspect-ratio-with-content-0322 item[2].y');
      expectClose(r.width, 200, 'lanes-aspect-ratio-with-content-0322 item[2].w');
      expectClose(r.height, 133.33, 'lanes-aspect-ratio-with-content-0322 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 210, 'lanes-aspect-ratio-with-content-0322 item[3].x');
      expectClose(r.y - container.y, 76.66, 'lanes-aspect-ratio-with-content-0322 item[3].y');
      expectClose(r.width, 200, 'lanes-aspect-ratio-with-content-0322 item[3].w');
      expectClose(r.height, 266.66, 'lanes-aspect-ratio-with-content-0322 item[3].h');
    }
  });
  it("lanes-aspect-ratio-with-content-0323: aspect-ratio 0.75 tiles with padding and content", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid-lanes","width":620,"gridTemplateColumns":"1fr 1fr 1fr","gap":10,"flowTolerance":0}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"padding":6,"aspectRatio":0.75}}>
            <View style={{height: 12}} />
          </View>
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"padding":6,"aspectRatio":1.5}}>
            <View style={{height: 12}} />
          </View>
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"padding":6,"aspectRatio":0.75}}>
            <View style={{height: 12}} />
          </View>
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"padding":6,"aspectRatio":0.375}}>
            <View style={{height: 12}} />
          </View>
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 620, 'lanes-aspect-ratio-with-content-0323 container.width');
    expectClose(container.height, 676.66, 'lanes-aspect-ratio-with-content-0323 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'lanes-aspect-ratio-with-content-0323 item[0].x');
      expectClose(r.y - container.y, 0, 'lanes-aspect-ratio-with-content-0323 item[0].y');
      expectClose(r.width, 200, 'lanes-aspect-ratio-with-content-0323 item[0].w');
      expectClose(r.height, 266.66, 'lanes-aspect-ratio-with-content-0323 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 210, 'lanes-aspect-ratio-with-content-0323 item[1].x');
      expectClose(r.y - container.y, 0, 'lanes-aspect-ratio-with-content-0323 item[1].y');
      expectClose(r.width, 200, 'lanes-aspect-ratio-with-content-0323 item[1].w');
      expectClose(r.height, 133.33, 'lanes-aspect-ratio-with-content-0323 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 420, 'lanes-aspect-ratio-with-content-0323 item[2].x');
      expectClose(r.y - container.y, 0, 'lanes-aspect-ratio-with-content-0323 item[2].y');
      expectClose(r.width, 200, 'lanes-aspect-ratio-with-content-0323 item[2].w');
      expectClose(r.height, 266.66, 'lanes-aspect-ratio-with-content-0323 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 210, 'lanes-aspect-ratio-with-content-0323 item[3].x');
      expectClose(r.y - container.y, 143.33, 'lanes-aspect-ratio-with-content-0323 item[3].y');
      expectClose(r.width, 200, 'lanes-aspect-ratio-with-content-0323 item[3].w');
      expectClose(r.height, 533.33, 'lanes-aspect-ratio-with-content-0323 item[3].h');
    }
  });
});

describe("lanes-stacking-content", () => {
  it("lanes-stacking-content-0324: align-content:start in the stacking axis", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid-lanes","width":600,"height":300,"gridTemplateColumns":"1fr 1fr 1fr","gap":10,"flowTolerance":0,"alignContent":"start"}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":40}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":60}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":50}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'lanes-stacking-content-0324 container.width');
    expectClose(container.height, 300, 'lanes-stacking-content-0324 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'lanes-stacking-content-0324 item[0].x');
      expectClose(r.y - container.y, 0, 'lanes-stacking-content-0324 item[0].y');
      expectClose(r.width, 193.33, 'lanes-stacking-content-0324 item[0].w');
      expectClose(r.height, 40, 'lanes-stacking-content-0324 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 203.33, 'lanes-stacking-content-0324 item[1].x');
      expectClose(r.y - container.y, 0, 'lanes-stacking-content-0324 item[1].y');
      expectClose(r.width, 193.33, 'lanes-stacking-content-0324 item[1].w');
      expectClose(r.height, 60, 'lanes-stacking-content-0324 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 406.66, 'lanes-stacking-content-0324 item[2].x');
      expectClose(r.y - container.y, 0, 'lanes-stacking-content-0324 item[2].y');
      expectClose(r.width, 193.34, 'lanes-stacking-content-0324 item[2].w');
      expectClose(r.height, 30, 'lanes-stacking-content-0324 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 406.66, 'lanes-stacking-content-0324 item[3].x');
      expectClose(r.y - container.y, 40, 'lanes-stacking-content-0324 item[3].y');
      expectClose(r.width, 193.34, 'lanes-stacking-content-0324 item[3].w');
      expectClose(r.height, 50, 'lanes-stacking-content-0324 item[3].h');
    }
  });
  it("lanes-stacking-content-0325: align-content:center in the stacking axis", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid-lanes","width":600,"height":300,"gridTemplateColumns":"1fr 1fr 1fr","gap":10,"flowTolerance":0,"alignContent":"center"}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":40}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":60}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":50}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'lanes-stacking-content-0325 container.width');
    expectClose(container.height, 300, 'lanes-stacking-content-0325 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'lanes-stacking-content-0325 item[0].x');
      expectClose(r.y - container.y, 105, 'lanes-stacking-content-0325 item[0].y');
      expectClose(r.width, 193.33, 'lanes-stacking-content-0325 item[0].w');
      expectClose(r.height, 40, 'lanes-stacking-content-0325 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 203.33, 'lanes-stacking-content-0325 item[1].x');
      expectClose(r.y - container.y, 105, 'lanes-stacking-content-0325 item[1].y');
      expectClose(r.width, 193.33, 'lanes-stacking-content-0325 item[1].w');
      expectClose(r.height, 60, 'lanes-stacking-content-0325 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 406.66, 'lanes-stacking-content-0325 item[2].x');
      expectClose(r.y - container.y, 105, 'lanes-stacking-content-0325 item[2].y');
      expectClose(r.width, 193.34, 'lanes-stacking-content-0325 item[2].w');
      expectClose(r.height, 30, 'lanes-stacking-content-0325 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 406.66, 'lanes-stacking-content-0325 item[3].x');
      expectClose(r.y - container.y, 145, 'lanes-stacking-content-0325 item[3].y');
      expectClose(r.width, 193.34, 'lanes-stacking-content-0325 item[3].w');
      expectClose(r.height, 50, 'lanes-stacking-content-0325 item[3].h');
    }
  });
  it("lanes-stacking-content-0326: align-content:end in the stacking axis", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid-lanes","width":600,"height":300,"gridTemplateColumns":"1fr 1fr 1fr","gap":10,"flowTolerance":0,"alignContent":"end"}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":40}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":60}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":50}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'lanes-stacking-content-0326 container.width');
    expectClose(container.height, 300, 'lanes-stacking-content-0326 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'lanes-stacking-content-0326 item[0].x');
      expectClose(r.y - container.y, 210, 'lanes-stacking-content-0326 item[0].y');
      expectClose(r.width, 193.33, 'lanes-stacking-content-0326 item[0].w');
      expectClose(r.height, 40, 'lanes-stacking-content-0326 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 203.33, 'lanes-stacking-content-0326 item[1].x');
      expectClose(r.y - container.y, 210, 'lanes-stacking-content-0326 item[1].y');
      expectClose(r.width, 193.33, 'lanes-stacking-content-0326 item[1].w');
      expectClose(r.height, 60, 'lanes-stacking-content-0326 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 406.66, 'lanes-stacking-content-0326 item[2].x');
      expectClose(r.y - container.y, 210, 'lanes-stacking-content-0326 item[2].y');
      expectClose(r.width, 193.34, 'lanes-stacking-content-0326 item[2].w');
      expectClose(r.height, 30, 'lanes-stacking-content-0326 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 406.66, 'lanes-stacking-content-0326 item[3].x');
      expectClose(r.y - container.y, 250, 'lanes-stacking-content-0326 item[3].y');
      expectClose(r.width, 193.34, 'lanes-stacking-content-0326 item[3].w');
      expectClose(r.height, 50, 'lanes-stacking-content-0326 item[3].h');
    }
  });
  it("lanes-stacking-content-0327: align-content:stretch in the stacking axis", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid-lanes","width":600,"height":300,"gridTemplateColumns":"1fr 1fr 1fr","gap":10,"flowTolerance":0,"alignContent":"stretch"}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":40}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":60}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":50}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'lanes-stacking-content-0327 container.width');
    expectClose(container.height, 300, 'lanes-stacking-content-0327 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'lanes-stacking-content-0327 item[0].x');
      expectClose(r.y - container.y, 0, 'lanes-stacking-content-0327 item[0].y');
      expectClose(r.width, 193.33, 'lanes-stacking-content-0327 item[0].w');
      expectClose(r.height, 40, 'lanes-stacking-content-0327 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 203.33, 'lanes-stacking-content-0327 item[1].x');
      expectClose(r.y - container.y, 0, 'lanes-stacking-content-0327 item[1].y');
      expectClose(r.width, 193.33, 'lanes-stacking-content-0327 item[1].w');
      expectClose(r.height, 60, 'lanes-stacking-content-0327 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 406.66, 'lanes-stacking-content-0327 item[2].x');
      expectClose(r.y - container.y, 0, 'lanes-stacking-content-0327 item[2].y');
      expectClose(r.width, 193.34, 'lanes-stacking-content-0327 item[2].w');
      expectClose(r.height, 30, 'lanes-stacking-content-0327 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 406.66, 'lanes-stacking-content-0327 item[3].x');
      expectClose(r.y - container.y, 40, 'lanes-stacking-content-0327 item[3].y');
      expectClose(r.width, 193.34, 'lanes-stacking-content-0327 item[3].w');
      expectClose(r.height, 50, 'lanes-stacking-content-0327 item[3].h');
    }
  });
  it("lanes-stacking-content-0328: align-content:space-between in the stacking axis", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid-lanes","width":600,"height":300,"gridTemplateColumns":"1fr 1fr 1fr","gap":10,"flowTolerance":0,"alignContent":"space-between"}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":40}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":60}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":50}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'lanes-stacking-content-0328 container.width');
    expectClose(container.height, 300, 'lanes-stacking-content-0328 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'lanes-stacking-content-0328 item[0].x');
      expectClose(r.y - container.y, 0, 'lanes-stacking-content-0328 item[0].y');
      expectClose(r.width, 193.33, 'lanes-stacking-content-0328 item[0].w');
      expectClose(r.height, 40, 'lanes-stacking-content-0328 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 203.33, 'lanes-stacking-content-0328 item[1].x');
      expectClose(r.y - container.y, 0, 'lanes-stacking-content-0328 item[1].y');
      expectClose(r.width, 193.33, 'lanes-stacking-content-0328 item[1].w');
      expectClose(r.height, 60, 'lanes-stacking-content-0328 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 406.66, 'lanes-stacking-content-0328 item[2].x');
      expectClose(r.y - container.y, 0, 'lanes-stacking-content-0328 item[2].y');
      expectClose(r.width, 193.34, 'lanes-stacking-content-0328 item[2].w');
      expectClose(r.height, 30, 'lanes-stacking-content-0328 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 406.66, 'lanes-stacking-content-0328 item[3].x');
      expectClose(r.y - container.y, 40, 'lanes-stacking-content-0328 item[3].y');
      expectClose(r.width, 193.34, 'lanes-stacking-content-0328 item[3].w');
      expectClose(r.height, 50, 'lanes-stacking-content-0328 item[3].h');
    }
  });
});

describe("lanes-stacking-content-distributed", () => {
  it("lanes-stacking-content-distributed-0329: align-content:space-around falls back to center", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid-lanes","width":600,"height":300,"gridTemplateColumns":"1fr 1fr 1fr","gap":10,"flowTolerance":0,"alignContent":"space-around"}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":40}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":60}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":50}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'lanes-stacking-content-distributed-0329 container.width');
    expectClose(container.height, 300, 'lanes-stacking-content-distributed-0329 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'lanes-stacking-content-distributed-0329 item[0].x');
      expectClose(r.y - container.y, 105, 'lanes-stacking-content-distributed-0329 item[0].y');
      expectClose(r.width, 193.33, 'lanes-stacking-content-distributed-0329 item[0].w');
      expectClose(r.height, 40, 'lanes-stacking-content-distributed-0329 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 203.33, 'lanes-stacking-content-distributed-0329 item[1].x');
      expectClose(r.y - container.y, 105, 'lanes-stacking-content-distributed-0329 item[1].y');
      expectClose(r.width, 193.33, 'lanes-stacking-content-distributed-0329 item[1].w');
      expectClose(r.height, 60, 'lanes-stacking-content-distributed-0329 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 406.66, 'lanes-stacking-content-distributed-0329 item[2].x');
      expectClose(r.y - container.y, 105, 'lanes-stacking-content-distributed-0329 item[2].y');
      expectClose(r.width, 193.34, 'lanes-stacking-content-distributed-0329 item[2].w');
      expectClose(r.height, 30, 'lanes-stacking-content-distributed-0329 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 406.66, 'lanes-stacking-content-distributed-0329 item[3].x');
      expectClose(r.y - container.y, 145, 'lanes-stacking-content-distributed-0329 item[3].y');
      expectClose(r.width, 193.34, 'lanes-stacking-content-distributed-0329 item[3].w');
      expectClose(r.height, 50, 'lanes-stacking-content-distributed-0329 item[3].h');
    }
  });
  it("lanes-stacking-content-distributed-0330: align-content:space-evenly falls back to center", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid-lanes","width":600,"height":300,"gridTemplateColumns":"1fr 1fr 1fr","gap":10,"flowTolerance":0,"alignContent":"space-evenly"}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":40}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":60}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":50}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'lanes-stacking-content-distributed-0330 container.width');
    expectClose(container.height, 300, 'lanes-stacking-content-distributed-0330 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'lanes-stacking-content-distributed-0330 item[0].x');
      expectClose(r.y - container.y, 105, 'lanes-stacking-content-distributed-0330 item[0].y');
      expectClose(r.width, 193.33, 'lanes-stacking-content-distributed-0330 item[0].w');
      expectClose(r.height, 40, 'lanes-stacking-content-distributed-0330 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 203.33, 'lanes-stacking-content-distributed-0330 item[1].x');
      expectClose(r.y - container.y, 105, 'lanes-stacking-content-distributed-0330 item[1].y');
      expectClose(r.width, 193.33, 'lanes-stacking-content-distributed-0330 item[1].w');
      expectClose(r.height, 60, 'lanes-stacking-content-distributed-0330 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 406.66, 'lanes-stacking-content-distributed-0330 item[2].x');
      expectClose(r.y - container.y, 105, 'lanes-stacking-content-distributed-0330 item[2].y');
      expectClose(r.width, 193.34, 'lanes-stacking-content-distributed-0330 item[2].w');
      expectClose(r.height, 30, 'lanes-stacking-content-distributed-0330 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 406.66, 'lanes-stacking-content-distributed-0330 item[3].x');
      expectClose(r.y - container.y, 145, 'lanes-stacking-content-distributed-0330 item[3].y');
      expectClose(r.width, 193.34, 'lanes-stacking-content-distributed-0330 item[3].w');
      expectClose(r.height, 50, 'lanes-stacking-content-distributed-0330 item[3].h');
    }
  });
});

describe("lanes-stacking-align", () => {
  it("lanes-stacking-align-0331: align-items:start on lanes ending at different depths", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid-lanes","width":600,"gridTemplateColumns":"1fr 1fr 1fr","gap":10,"flowTolerance":0,"alignItems":"start"}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":40}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":60}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":50}}
          />
          <View
            collapsable={false}
            ref={itemRefs[4]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":20}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'lanes-stacking-align-0331 container.width');
    expectClose(container.height, 90, 'lanes-stacking-align-0331 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'lanes-stacking-align-0331 item[0].x');
      expectClose(r.y - container.y, 0, 'lanes-stacking-align-0331 item[0].y');
      expectClose(r.width, 193.33, 'lanes-stacking-align-0331 item[0].w');
      expectClose(r.height, 40, 'lanes-stacking-align-0331 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 203.33, 'lanes-stacking-align-0331 item[1].x');
      expectClose(r.y - container.y, 0, 'lanes-stacking-align-0331 item[1].y');
      expectClose(r.width, 193.33, 'lanes-stacking-align-0331 item[1].w');
      expectClose(r.height, 60, 'lanes-stacking-align-0331 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 406.66, 'lanes-stacking-align-0331 item[2].x');
      expectClose(r.y - container.y, 0, 'lanes-stacking-align-0331 item[2].y');
      expectClose(r.width, 193.34, 'lanes-stacking-align-0331 item[2].w');
      expectClose(r.height, 30, 'lanes-stacking-align-0331 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 406.66, 'lanes-stacking-align-0331 item[3].x');
      expectClose(r.y - container.y, 40, 'lanes-stacking-align-0331 item[3].y');
      expectClose(r.width, 193.34, 'lanes-stacking-align-0331 item[3].w');
      expectClose(r.height, 50, 'lanes-stacking-align-0331 item[3].h');
    }
    {
      const r = rectOf(itemRefs[4]);
      expectClose(r.x - container.x, 0, 'lanes-stacking-align-0331 item[4].x');
      expectClose(r.y - container.y, 50, 'lanes-stacking-align-0331 item[4].y');
      expectClose(r.width, 193.33, 'lanes-stacking-align-0331 item[4].w');
      expectClose(r.height, 20, 'lanes-stacking-align-0331 item[4].h');
    }
  });
  it("lanes-stacking-align-0332: align-items:stretch on lanes ending at different depths", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid-lanes","width":600,"gridTemplateColumns":"1fr 1fr 1fr","gap":10,"flowTolerance":0,"alignItems":"stretch"}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":40}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":60}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":50}}
          />
          <View
            collapsable={false}
            ref={itemRefs[4]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":20}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'lanes-stacking-align-0332 container.width');
    expectClose(container.height, 90, 'lanes-stacking-align-0332 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'lanes-stacking-align-0332 item[0].x');
      expectClose(r.y - container.y, 0, 'lanes-stacking-align-0332 item[0].y');
      expectClose(r.width, 193.33, 'lanes-stacking-align-0332 item[0].w');
      expectClose(r.height, 40, 'lanes-stacking-align-0332 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 203.33, 'lanes-stacking-align-0332 item[1].x');
      expectClose(r.y - container.y, 0, 'lanes-stacking-align-0332 item[1].y');
      expectClose(r.width, 193.33, 'lanes-stacking-align-0332 item[1].w');
      expectClose(r.height, 60, 'lanes-stacking-align-0332 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 406.66, 'lanes-stacking-align-0332 item[2].x');
      expectClose(r.y - container.y, 0, 'lanes-stacking-align-0332 item[2].y');
      expectClose(r.width, 193.34, 'lanes-stacking-align-0332 item[2].w');
      expectClose(r.height, 30, 'lanes-stacking-align-0332 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 406.66, 'lanes-stacking-align-0332 item[3].x');
      expectClose(r.y - container.y, 40, 'lanes-stacking-align-0332 item[3].y');
      expectClose(r.width, 193.34, 'lanes-stacking-align-0332 item[3].w');
      expectClose(r.height, 50, 'lanes-stacking-align-0332 item[3].h');
    }
    {
      const r = rectOf(itemRefs[4]);
      expectClose(r.x - container.x, 0, 'lanes-stacking-align-0332 item[4].x');
      expectClose(r.y - container.y, 50, 'lanes-stacking-align-0332 item[4].y');
      expectClose(r.width, 193.33, 'lanes-stacking-align-0332 item[4].w');
      expectClose(r.height, 20, 'lanes-stacking-align-0332 item[4].h');
    }
  });
  it("lanes-stacking-align-0333: align-items:center on lanes ending at different depths", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid-lanes","width":600,"gridTemplateColumns":"1fr 1fr 1fr","gap":10,"flowTolerance":0,"alignItems":"center"}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":40}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":60}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":50}}
          />
          <View
            collapsable={false}
            ref={itemRefs[4]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":20}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'lanes-stacking-align-0333 container.width');
    expectClose(container.height, 90, 'lanes-stacking-align-0333 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'lanes-stacking-align-0333 item[0].x');
      expectClose(r.y - container.y, 0, 'lanes-stacking-align-0333 item[0].y');
      expectClose(r.width, 193.33, 'lanes-stacking-align-0333 item[0].w');
      expectClose(r.height, 40, 'lanes-stacking-align-0333 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 203.33, 'lanes-stacking-align-0333 item[1].x');
      expectClose(r.y - container.y, 15, 'lanes-stacking-align-0333 item[1].y');
      expectClose(r.width, 193.33, 'lanes-stacking-align-0333 item[1].w');
      expectClose(r.height, 60, 'lanes-stacking-align-0333 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 406.66, 'lanes-stacking-align-0333 item[2].x');
      expectClose(r.y - container.y, 0, 'lanes-stacking-align-0333 item[2].y');
      expectClose(r.width, 193.34, 'lanes-stacking-align-0333 item[2].w');
      expectClose(r.height, 30, 'lanes-stacking-align-0333 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 406.66, 'lanes-stacking-align-0333 item[3].x');
      expectClose(r.y - container.y, 40, 'lanes-stacking-align-0333 item[3].y');
      expectClose(r.width, 193.34, 'lanes-stacking-align-0333 item[3].w');
      expectClose(r.height, 50, 'lanes-stacking-align-0333 item[3].h');
    }
    {
      const r = rectOf(itemRefs[4]);
      expectClose(r.x - container.x, 0, 'lanes-stacking-align-0333 item[4].x');
      expectClose(r.y - container.y, 60, 'lanes-stacking-align-0333 item[4].y');
      expectClose(r.width, 193.33, 'lanes-stacking-align-0333 item[4].w');
      expectClose(r.height, 20, 'lanes-stacking-align-0333 item[4].h');
    }
  });
  it("lanes-stacking-align-0334: align-items:end on lanes ending at different depths", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid-lanes","width":600,"gridTemplateColumns":"1fr 1fr 1fr","gap":10,"flowTolerance":0,"alignItems":"end"}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":40}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":60}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":50}}
          />
          <View
            collapsable={false}
            ref={itemRefs[4]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":20}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'lanes-stacking-align-0334 container.width');
    expectClose(container.height, 90, 'lanes-stacking-align-0334 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'lanes-stacking-align-0334 item[0].x');
      expectClose(r.y - container.y, 0, 'lanes-stacking-align-0334 item[0].y');
      expectClose(r.width, 193.33, 'lanes-stacking-align-0334 item[0].w');
      expectClose(r.height, 40, 'lanes-stacking-align-0334 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 203.33, 'lanes-stacking-align-0334 item[1].x');
      expectClose(r.y - container.y, 30, 'lanes-stacking-align-0334 item[1].y');
      expectClose(r.width, 193.33, 'lanes-stacking-align-0334 item[1].w');
      expectClose(r.height, 60, 'lanes-stacking-align-0334 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 406.66, 'lanes-stacking-align-0334 item[2].x');
      expectClose(r.y - container.y, 0, 'lanes-stacking-align-0334 item[2].y');
      expectClose(r.width, 193.34, 'lanes-stacking-align-0334 item[2].w');
      expectClose(r.height, 30, 'lanes-stacking-align-0334 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 406.66, 'lanes-stacking-align-0334 item[3].x');
      expectClose(r.y - container.y, 40, 'lanes-stacking-align-0334 item[3].y');
      expectClose(r.width, 193.34, 'lanes-stacking-align-0334 item[3].w');
      expectClose(r.height, 50, 'lanes-stacking-align-0334 item[3].h');
    }
    {
      const r = rectOf(itemRefs[4]);
      expectClose(r.x - container.x, 0, 'lanes-stacking-align-0334 item[4].x');
      expectClose(r.y - container.y, 70, 'lanes-stacking-align-0334 item[4].y');
      expectClose(r.width, 193.33, 'lanes-stacking-align-0334 item[4].w');
      expectClose(r.height, 20, 'lanes-stacking-align-0334 item[4].h');
    }
  });
});

describe("lanes-stacking-align-self", () => {
  it("lanes-stacking-align-self-0335: align-self:end on one item only", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid-lanes","width":600,"gridTemplateColumns":"1fr 1fr 1fr","gap":10,"flowTolerance":0}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":40,"alignSelf":"end"}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":60}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":50}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'lanes-stacking-align-self-0335 container.width');
    expectClose(container.height, 90, 'lanes-stacking-align-self-0335 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'lanes-stacking-align-self-0335 item[0].x');
      expectClose(r.y - container.y, 50, 'lanes-stacking-align-self-0335 item[0].y');
      expectClose(r.width, 193.33, 'lanes-stacking-align-self-0335 item[0].w');
      expectClose(r.height, 40, 'lanes-stacking-align-self-0335 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 203.33, 'lanes-stacking-align-self-0335 item[1].x');
      expectClose(r.y - container.y, 0, 'lanes-stacking-align-self-0335 item[1].y');
      expectClose(r.width, 193.33, 'lanes-stacking-align-self-0335 item[1].w');
      expectClose(r.height, 60, 'lanes-stacking-align-self-0335 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 406.66, 'lanes-stacking-align-self-0335 item[2].x');
      expectClose(r.y - container.y, 0, 'lanes-stacking-align-self-0335 item[2].y');
      expectClose(r.width, 193.34, 'lanes-stacking-align-self-0335 item[2].w');
      expectClose(r.height, 30, 'lanes-stacking-align-self-0335 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 406.66, 'lanes-stacking-align-self-0335 item[3].x');
      expectClose(r.y - container.y, 40, 'lanes-stacking-align-self-0335 item[3].y');
      expectClose(r.width, 193.34, 'lanes-stacking-align-self-0335 item[3].w');
      expectClose(r.height, 50, 'lanes-stacking-align-self-0335 item[3].h');
    }
  });
  it("lanes-stacking-align-self-0336: align-self:center on one item only", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid-lanes","width":600,"gridTemplateColumns":"1fr 1fr 1fr","gap":10,"flowTolerance":0}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":40,"alignSelf":"center"}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":60}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":50}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'lanes-stacking-align-self-0336 container.width');
    expectClose(container.height, 90, 'lanes-stacking-align-self-0336 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'lanes-stacking-align-self-0336 item[0].x');
      expectClose(r.y - container.y, 25, 'lanes-stacking-align-self-0336 item[0].y');
      expectClose(r.width, 193.33, 'lanes-stacking-align-self-0336 item[0].w');
      expectClose(r.height, 40, 'lanes-stacking-align-self-0336 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 203.33, 'lanes-stacking-align-self-0336 item[1].x');
      expectClose(r.y - container.y, 0, 'lanes-stacking-align-self-0336 item[1].y');
      expectClose(r.width, 193.33, 'lanes-stacking-align-self-0336 item[1].w');
      expectClose(r.height, 60, 'lanes-stacking-align-self-0336 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 406.66, 'lanes-stacking-align-self-0336 item[2].x');
      expectClose(r.y - container.y, 0, 'lanes-stacking-align-self-0336 item[2].y');
      expectClose(r.width, 193.34, 'lanes-stacking-align-self-0336 item[2].w');
      expectClose(r.height, 30, 'lanes-stacking-align-self-0336 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 406.66, 'lanes-stacking-align-self-0336 item[3].x');
      expectClose(r.y - container.y, 40, 'lanes-stacking-align-self-0336 item[3].y');
      expectClose(r.width, 193.34, 'lanes-stacking-align-self-0336 item[3].w');
      expectClose(r.height, 50, 'lanes-stacking-align-self-0336 item[3].h');
    }
  });
});

describe("lanes-stacking-align-span", () => {
  it("lanes-stacking-align-span-0337: align-items:start with a spanning item leaving a void", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid-lanes","width":600,"gridTemplateColumns":"1fr 1fr 1fr","gap":10,"flowTolerance":0,"alignItems":"start"}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":40}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":60}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":25,"gridColumnEnd":"span 3"}}
          />
          <View
            collapsable={false}
            ref={itemRefs[4]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":20}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'lanes-stacking-align-span-0337 container.width');
    expectClose(container.height, 125, 'lanes-stacking-align-span-0337 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'lanes-stacking-align-span-0337 item[0].x');
      expectClose(r.y - container.y, 0, 'lanes-stacking-align-span-0337 item[0].y');
      expectClose(r.width, 193.33, 'lanes-stacking-align-span-0337 item[0].w');
      expectClose(r.height, 40, 'lanes-stacking-align-span-0337 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 203.33, 'lanes-stacking-align-span-0337 item[1].x');
      expectClose(r.y - container.y, 0, 'lanes-stacking-align-span-0337 item[1].y');
      expectClose(r.width, 193.33, 'lanes-stacking-align-span-0337 item[1].w');
      expectClose(r.height, 60, 'lanes-stacking-align-span-0337 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 406.66, 'lanes-stacking-align-span-0337 item[2].x');
      expectClose(r.y - container.y, 0, 'lanes-stacking-align-span-0337 item[2].y');
      expectClose(r.width, 193.34, 'lanes-stacking-align-span-0337 item[2].w');
      expectClose(r.height, 30, 'lanes-stacking-align-span-0337 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 0, 'lanes-stacking-align-span-0337 item[3].x');
      expectClose(r.y - container.y, 70, 'lanes-stacking-align-span-0337 item[3].y');
      expectClose(r.width, 600, 'lanes-stacking-align-span-0337 item[3].w');
      expectClose(r.height, 25, 'lanes-stacking-align-span-0337 item[3].h');
    }
    {
      const r = rectOf(itemRefs[4]);
      expectClose(r.x - container.x, 0, 'lanes-stacking-align-span-0337 item[4].x');
      expectClose(r.y - container.y, 105, 'lanes-stacking-align-span-0337 item[4].y');
      expectClose(r.width, 193.33, 'lanes-stacking-align-span-0337 item[4].w');
      expectClose(r.height, 20, 'lanes-stacking-align-span-0337 item[4].h');
    }
  });
  it("lanes-stacking-align-span-0338: align-items:end with a spanning item leaving a void", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid-lanes","width":600,"gridTemplateColumns":"1fr 1fr 1fr","gap":10,"flowTolerance":0,"alignItems":"end"}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":40}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":60}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":25,"gridColumnEnd":"span 3"}}
          />
          <View
            collapsable={false}
            ref={itemRefs[4]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":20}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'lanes-stacking-align-span-0338 container.width');
    expectClose(container.height, 125, 'lanes-stacking-align-span-0338 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'lanes-stacking-align-span-0338 item[0].x');
      expectClose(r.y - container.y, 20, 'lanes-stacking-align-span-0338 item[0].y');
      expectClose(r.width, 193.33, 'lanes-stacking-align-span-0338 item[0].w');
      expectClose(r.height, 40, 'lanes-stacking-align-span-0338 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 203.33, 'lanes-stacking-align-span-0338 item[1].x');
      expectClose(r.y - container.y, 0, 'lanes-stacking-align-span-0338 item[1].y');
      expectClose(r.width, 193.33, 'lanes-stacking-align-span-0338 item[1].w');
      expectClose(r.height, 60, 'lanes-stacking-align-span-0338 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 406.66, 'lanes-stacking-align-span-0338 item[2].x');
      expectClose(r.y - container.y, 30, 'lanes-stacking-align-span-0338 item[2].y');
      expectClose(r.width, 193.34, 'lanes-stacking-align-span-0338 item[2].w');
      expectClose(r.height, 30, 'lanes-stacking-align-span-0338 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 0, 'lanes-stacking-align-span-0338 item[3].x');
      expectClose(r.y - container.y, 70, 'lanes-stacking-align-span-0338 item[3].y');
      expectClose(r.width, 600, 'lanes-stacking-align-span-0338 item[3].w');
      expectClose(r.height, 25, 'lanes-stacking-align-span-0338 item[3].h');
    }
    {
      const r = rectOf(itemRefs[4]);
      expectClose(r.x - container.x, 0, 'lanes-stacking-align-span-0338 item[4].x');
      expectClose(r.y - container.y, 105, 'lanes-stacking-align-span-0338 item[4].y');
      expectClose(r.width, 193.33, 'lanes-stacking-align-span-0338 item[4].w');
      expectClose(r.height, 20, 'lanes-stacking-align-span-0338 item[4].h');
    }
  });
});

describe("lanes-stacking-align-stretch-auto", () => {
  it("lanes-stacking-align-stretch-auto-0339: align-items:stretch on auto-height items in lanes", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid-lanes","width":600,"gridTemplateColumns":"1fr 1fr 1fr","gap":10,"flowTolerance":0,"alignItems":"stretch"}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{}}>
            <View style={{height: 40}} />
          </View>
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{}}>
            <View style={{height: 60}} />
          </View>
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{}}>
            <View style={{height: 30}} />
          </View>
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{}}>
            <View style={{height: 50}} />
          </View>
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'lanes-stacking-align-stretch-auto-0339 container.width');
    expectClose(container.height, 90, 'lanes-stacking-align-stretch-auto-0339 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'lanes-stacking-align-stretch-auto-0339 item[0].x');
      expectClose(r.y - container.y, 0, 'lanes-stacking-align-stretch-auto-0339 item[0].y');
      expectClose(r.width, 193.33, 'lanes-stacking-align-stretch-auto-0339 item[0].w');
      expectClose(r.height, 40, 'lanes-stacking-align-stretch-auto-0339 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 203.33, 'lanes-stacking-align-stretch-auto-0339 item[1].x');
      expectClose(r.y - container.y, 0, 'lanes-stacking-align-stretch-auto-0339 item[1].y');
      expectClose(r.width, 193.33, 'lanes-stacking-align-stretch-auto-0339 item[1].w');
      expectClose(r.height, 60, 'lanes-stacking-align-stretch-auto-0339 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 406.66, 'lanes-stacking-align-stretch-auto-0339 item[2].x');
      expectClose(r.y - container.y, 0, 'lanes-stacking-align-stretch-auto-0339 item[2].y');
      expectClose(r.width, 193.34, 'lanes-stacking-align-stretch-auto-0339 item[2].w');
      expectClose(r.height, 30, 'lanes-stacking-align-stretch-auto-0339 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 406.66, 'lanes-stacking-align-stretch-auto-0339 item[3].x');
      expectClose(r.y - container.y, 40, 'lanes-stacking-align-stretch-auto-0339 item[3].y');
      expectClose(r.width, 193.34, 'lanes-stacking-align-stretch-auto-0339 item[3].w');
      expectClose(r.height, 50, 'lanes-stacking-align-stretch-auto-0339 item[3].h');
    }
  });
});

describe("lanes-brick-stacking-align", () => {
  it("lanes-brick-stacking-align-0340: justify-items:start in a brick layout", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid-lanes","width":600,"gridTemplateRows":"100px 100px","gap":10,"flowTolerance":0,"justifyItems":"start"}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":40}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":60}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":30}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'lanes-brick-stacking-align-0340 container.width');
    expectClose(container.height, 210, 'lanes-brick-stacking-align-0340 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'lanes-brick-stacking-align-0340 item[0].x');
      expectClose(r.y - container.y, 0, 'lanes-brick-stacking-align-0340 item[0].y');
      expectClose(r.width, 40, 'lanes-brick-stacking-align-0340 item[0].w');
      expectClose(r.height, 100, 'lanes-brick-stacking-align-0340 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 0, 'lanes-brick-stacking-align-0340 item[1].x');
      expectClose(r.y - container.y, 110, 'lanes-brick-stacking-align-0340 item[1].y');
      expectClose(r.width, 60, 'lanes-brick-stacking-align-0340 item[1].w');
      expectClose(r.height, 100, 'lanes-brick-stacking-align-0340 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 50, 'lanes-brick-stacking-align-0340 item[2].x');
      expectClose(r.y - container.y, 0, 'lanes-brick-stacking-align-0340 item[2].y');
      expectClose(r.width, 30, 'lanes-brick-stacking-align-0340 item[2].w');
      expectClose(r.height, 100, 'lanes-brick-stacking-align-0340 item[2].h');
    }
  });
  it("lanes-brick-stacking-align-0341: justify-items:end in a brick layout", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid-lanes","width":600,"gridTemplateRows":"100px 100px","gap":10,"flowTolerance":0,"justifyItems":"end"}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":40}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":60}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":30}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'lanes-brick-stacking-align-0341 container.width');
    expectClose(container.height, 210, 'lanes-brick-stacking-align-0341 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'lanes-brick-stacking-align-0341 item[0].x');
      expectClose(r.y - container.y, 0, 'lanes-brick-stacking-align-0341 item[0].y');
      expectClose(r.width, 40, 'lanes-brick-stacking-align-0341 item[0].w');
      expectClose(r.height, 100, 'lanes-brick-stacking-align-0341 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 20, 'lanes-brick-stacking-align-0341 item[1].x');
      expectClose(r.y - container.y, 110, 'lanes-brick-stacking-align-0341 item[1].y');
      expectClose(r.width, 60, 'lanes-brick-stacking-align-0341 item[1].w');
      expectClose(r.height, 100, 'lanes-brick-stacking-align-0341 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 50, 'lanes-brick-stacking-align-0341 item[2].x');
      expectClose(r.y - container.y, 0, 'lanes-brick-stacking-align-0341 item[2].y');
      expectClose(r.width, 30, 'lanes-brick-stacking-align-0341 item[2].w');
      expectClose(r.height, 100, 'lanes-brick-stacking-align-0341 item[2].h');
    }
  });
});

describe("lanes-container-min-max", () => {
  it("lanes-container-min-max-0342: maxHeight: 60 on a lanes container", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid-lanes","width":600,"gridTemplateColumns":"1fr 1fr 1fr","gap":10,"flowTolerance":0,"maxHeight":60}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":40}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":60}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":50}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'lanes-container-min-max-0342 container.width');
    expectClose(container.height, 60, 'lanes-container-min-max-0342 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'lanes-container-min-max-0342 item[0].x');
      expectClose(r.y - container.y, 0, 'lanes-container-min-max-0342 item[0].y');
      expectClose(r.width, 193.33, 'lanes-container-min-max-0342 item[0].w');
      expectClose(r.height, 40, 'lanes-container-min-max-0342 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 203.33, 'lanes-container-min-max-0342 item[1].x');
      expectClose(r.y - container.y, 0, 'lanes-container-min-max-0342 item[1].y');
      expectClose(r.width, 193.33, 'lanes-container-min-max-0342 item[1].w');
      expectClose(r.height, 60, 'lanes-container-min-max-0342 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 406.66, 'lanes-container-min-max-0342 item[2].x');
      expectClose(r.y - container.y, 0, 'lanes-container-min-max-0342 item[2].y');
      expectClose(r.width, 193.34, 'lanes-container-min-max-0342 item[2].w');
      expectClose(r.height, 30, 'lanes-container-min-max-0342 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 406.66, 'lanes-container-min-max-0342 item[3].x');
      expectClose(r.y - container.y, 40, 'lanes-container-min-max-0342 item[3].y');
      expectClose(r.width, 193.34, 'lanes-container-min-max-0342 item[3].w');
      expectClose(r.height, 50, 'lanes-container-min-max-0342 item[3].h');
    }
  });
  it("lanes-container-min-max-0343: minHeight: 200 on a lanes container", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid-lanes","width":600,"gridTemplateColumns":"1fr 1fr 1fr","gap":10,"flowTolerance":0,"minHeight":200}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":40}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":60}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":50}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'lanes-container-min-max-0343 container.width');
    expectClose(container.height, 200, 'lanes-container-min-max-0343 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'lanes-container-min-max-0343 item[0].x');
      expectClose(r.y - container.y, 0, 'lanes-container-min-max-0343 item[0].y');
      expectClose(r.width, 193.33, 'lanes-container-min-max-0343 item[0].w');
      expectClose(r.height, 40, 'lanes-container-min-max-0343 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 203.33, 'lanes-container-min-max-0343 item[1].x');
      expectClose(r.y - container.y, 0, 'lanes-container-min-max-0343 item[1].y');
      expectClose(r.width, 193.33, 'lanes-container-min-max-0343 item[1].w');
      expectClose(r.height, 60, 'lanes-container-min-max-0343 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 406.66, 'lanes-container-min-max-0343 item[2].x');
      expectClose(r.y - container.y, 0, 'lanes-container-min-max-0343 item[2].y');
      expectClose(r.width, 193.34, 'lanes-container-min-max-0343 item[2].w');
      expectClose(r.height, 30, 'lanes-container-min-max-0343 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 406.66, 'lanes-container-min-max-0343 item[3].x');
      expectClose(r.y - container.y, 40, 'lanes-container-min-max-0343 item[3].y');
      expectClose(r.width, 193.34, 'lanes-container-min-max-0343 item[3].w');
      expectClose(r.height, 50, 'lanes-container-min-max-0343 item[3].h');
    }
  });
  it("lanes-container-min-max-0344: maxWidth: 400 on a lanes container", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid-lanes","width":600,"gridTemplateColumns":"1fr 1fr 1fr","gap":10,"flowTolerance":0,"maxWidth":400}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":40}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":60}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":50}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 400, 'lanes-container-min-max-0344 container.width');
    expectClose(container.height, 90, 'lanes-container-min-max-0344 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'lanes-container-min-max-0344 item[0].x');
      expectClose(r.y - container.y, 0, 'lanes-container-min-max-0344 item[0].y');
      expectClose(r.width, 126.66, 'lanes-container-min-max-0344 item[0].w');
      expectClose(r.height, 40, 'lanes-container-min-max-0344 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 136.66, 'lanes-container-min-max-0344 item[1].x');
      expectClose(r.y - container.y, 0, 'lanes-container-min-max-0344 item[1].y');
      expectClose(r.width, 126.67, 'lanes-container-min-max-0344 item[1].w');
      expectClose(r.height, 60, 'lanes-container-min-max-0344 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 273.33, 'lanes-container-min-max-0344 item[2].x');
      expectClose(r.y - container.y, 0, 'lanes-container-min-max-0344 item[2].y');
      expectClose(r.width, 126.67, 'lanes-container-min-max-0344 item[2].w');
      expectClose(r.height, 30, 'lanes-container-min-max-0344 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 273.33, 'lanes-container-min-max-0344 item[3].x');
      expectClose(r.y - container.y, 40, 'lanes-container-min-max-0344 item[3].y');
      expectClose(r.width, 126.67, 'lanes-container-min-max-0344 item[3].w');
      expectClose(r.height, 50, 'lanes-container-min-max-0344 item[3].h');
    }
  });
  it("lanes-container-min-max-0345: minWidth: 800 on a lanes container", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid-lanes","width":600,"gridTemplateColumns":"1fr 1fr 1fr","gap":10,"flowTolerance":0,"minWidth":800}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":40}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":60}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":50}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 800, 'lanes-container-min-max-0345 container.width');
    expectClose(container.height, 90, 'lanes-container-min-max-0345 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'lanes-container-min-max-0345 item[0].x');
      expectClose(r.y - container.y, 0, 'lanes-container-min-max-0345 item[0].y');
      expectClose(r.width, 260, 'lanes-container-min-max-0345 item[0].w');
      expectClose(r.height, 40, 'lanes-container-min-max-0345 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 270, 'lanes-container-min-max-0345 item[1].x');
      expectClose(r.y - container.y, 0, 'lanes-container-min-max-0345 item[1].y');
      expectClose(r.width, 260, 'lanes-container-min-max-0345 item[1].w');
      expectClose(r.height, 60, 'lanes-container-min-max-0345 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 540, 'lanes-container-min-max-0345 item[2].x');
      expectClose(r.y - container.y, 0, 'lanes-container-min-max-0345 item[2].y');
      expectClose(r.width, 260, 'lanes-container-min-max-0345 item[2].w');
      expectClose(r.height, 30, 'lanes-container-min-max-0345 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 540, 'lanes-container-min-max-0345 item[3].x');
      expectClose(r.y - container.y, 40, 'lanes-container-min-max-0345 item[3].y');
      expectClose(r.width, 260, 'lanes-container-min-max-0345 item[3].w');
      expectClose(r.height, 50, 'lanes-container-min-max-0345 item[3].h');
    }
  });
});

describe("lanes-absolute", () => {
  it("lanes-absolute-0346: an absolutely-positioned child at 20,30 is out of flow", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid-lanes","width":600,"gridTemplateColumns":"1fr 1fr 1fr","gap":10,"flowTolerance":0}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":40}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":60}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":50,"height":25,"position":"absolute","top":20,"left":30}}
          />
          <View
            collapsable={false}
            ref={itemRefs[4]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":50}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'lanes-absolute-0346 container.width');
    expectClose(container.height, 90, 'lanes-absolute-0346 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'lanes-absolute-0346 item[0].x');
      expectClose(r.y - container.y, 0, 'lanes-absolute-0346 item[0].y');
      expectClose(r.width, 193.33, 'lanes-absolute-0346 item[0].w');
      expectClose(r.height, 40, 'lanes-absolute-0346 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 203.33, 'lanes-absolute-0346 item[1].x');
      expectClose(r.y - container.y, 0, 'lanes-absolute-0346 item[1].y');
      expectClose(r.width, 193.33, 'lanes-absolute-0346 item[1].w');
      expectClose(r.height, 60, 'lanes-absolute-0346 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 406.66, 'lanes-absolute-0346 item[2].x');
      expectClose(r.y - container.y, 0, 'lanes-absolute-0346 item[2].y');
      expectClose(r.width, 193.34, 'lanes-absolute-0346 item[2].w');
      expectClose(r.height, 30, 'lanes-absolute-0346 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 30, 'lanes-absolute-0346 item[3].x');
      expectClose(r.y - container.y, 20, 'lanes-absolute-0346 item[3].y');
      expectClose(r.width, 50, 'lanes-absolute-0346 item[3].w');
      expectClose(r.height, 25, 'lanes-absolute-0346 item[3].h');
    }
    {
      const r = rectOf(itemRefs[4]);
      expectClose(r.x - container.x, 406.66, 'lanes-absolute-0346 item[4].x');
      expectClose(r.y - container.y, 40, 'lanes-absolute-0346 item[4].y');
      expectClose(r.width, 193.34, 'lanes-absolute-0346 item[4].w');
      expectClose(r.height, 50, 'lanes-absolute-0346 item[4].h');
    }
  });
  it("lanes-absolute-0347: an absolutely-positioned child at 0,0 is out of flow", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid-lanes","width":600,"gridTemplateColumns":"1fr 1fr 1fr","gap":10,"flowTolerance":0}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":40}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":60}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"width":50,"height":25,"position":"absolute","top":0,"left":0}}
          />
          <View
            collapsable={false}
            ref={itemRefs[4]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":50}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'lanes-absolute-0347 container.width');
    expectClose(container.height, 90, 'lanes-absolute-0347 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'lanes-absolute-0347 item[0].x');
      expectClose(r.y - container.y, 0, 'lanes-absolute-0347 item[0].y');
      expectClose(r.width, 193.33, 'lanes-absolute-0347 item[0].w');
      expectClose(r.height, 40, 'lanes-absolute-0347 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 203.33, 'lanes-absolute-0347 item[1].x');
      expectClose(r.y - container.y, 0, 'lanes-absolute-0347 item[1].y');
      expectClose(r.width, 193.33, 'lanes-absolute-0347 item[1].w');
      expectClose(r.height, 60, 'lanes-absolute-0347 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.x - container.x, 406.66, 'lanes-absolute-0347 item[2].x');
      expectClose(r.y - container.y, 0, 'lanes-absolute-0347 item[2].y');
      expectClose(r.width, 193.34, 'lanes-absolute-0347 item[2].w');
      expectClose(r.height, 30, 'lanes-absolute-0347 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 0, 'lanes-absolute-0347 item[3].x');
      expectClose(r.y - container.y, 0, 'lanes-absolute-0347 item[3].y');
      expectClose(r.width, 50, 'lanes-absolute-0347 item[3].w');
      expectClose(r.height, 25, 'lanes-absolute-0347 item[3].h');
    }
    {
      const r = rectOf(itemRefs[4]);
      expectClose(r.x - container.x, 406.66, 'lanes-absolute-0347 item[4].x');
      expectClose(r.y - container.y, 40, 'lanes-absolute-0347 item[4].y');
      expectClose(r.width, 193.34, 'lanes-absolute-0347 item[4].w');
      expectClose(r.height, 50, 'lanes-absolute-0347 item[4].h');
    }
  });
});

describe("lanes-display-none", () => {
  it("lanes-display-none-0348: a display:none child takes no lane", () => {
    const containerRef = createRef<HostInstance>();
    const itemRefs = [createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>(), createRef<HostInstance>()];
    const root = Fantom.createRoot({viewportWidth: VIEWPORT_WIDTH});
    Fantom.runTask(() => {
      root.render(
        <View
          collapsable={false}
          ref={containerRef}
          /* $FlowExpectedError[incompatible-type] grid style keys */
          style={{"display":"grid-lanes","width":600,"gridTemplateColumns":"1fr 1fr 1fr","gap":10,"flowTolerance":0}}>
          <View
            collapsable={false}
            ref={itemRefs[0]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":40}}
          />
          <View
            collapsable={false}
            ref={itemRefs[1]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":60}}
          />
          <View
            collapsable={false}
            ref={itemRefs[2]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":30,"display":"none"}}
          />
          <View
            collapsable={false}
            ref={itemRefs[3]}
            /* $FlowExpectedError[incompatible-type] grid style keys */
            style={{"height":50}}
          />
        </View>,
      );
    });
    const container = rectOf(containerRef);
    expectClose(container.width, 600, 'lanes-display-none-0348 container.width');
    expectClose(container.height, 60, 'lanes-display-none-0348 container.height');
    {
      const r = rectOf(itemRefs[0]);
      expectClose(r.x - container.x, 0, 'lanes-display-none-0348 item[0].x');
      expectClose(r.y - container.y, 0, 'lanes-display-none-0348 item[0].y');
      expectClose(r.width, 193.33, 'lanes-display-none-0348 item[0].w');
      expectClose(r.height, 40, 'lanes-display-none-0348 item[0].h');
    }
    {
      const r = rectOf(itemRefs[1]);
      expectClose(r.x - container.x, 203.33, 'lanes-display-none-0348 item[1].x');
      expectClose(r.y - container.y, 0, 'lanes-display-none-0348 item[1].y');
      expectClose(r.width, 193.33, 'lanes-display-none-0348 item[1].w');
      expectClose(r.height, 60, 'lanes-display-none-0348 item[1].h');
    }
    {
      const r = rectOf(itemRefs[2]);
      expectClose(r.width, 0, 'lanes-display-none-0348 item[2].w');
      expectClose(r.height, 0, 'lanes-display-none-0348 item[2].h');
    }
    {
      const r = rectOf(itemRefs[3]);
      expectClose(r.x - container.x, 406.66, 'lanes-display-none-0348 item[3].x');
      expectClose(r.y - container.y, 0, 'lanes-display-none-0348 item[3].y');
      expectClose(r.width, 193.34, 'lanes-display-none-0348 item[3].w');
      expectClose(r.height, 50, 'lanes-display-none-0348 item[3].h');
    }
  });
});

